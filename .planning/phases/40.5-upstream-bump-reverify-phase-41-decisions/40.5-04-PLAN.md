---
phase: 40.5
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md
  - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md
autonomous: false
requirements: ["UPGRADE-10"]
estimated_minutes: 120
gap_closure: false
must_haves:
  truths:
    - "Every Phase 41 CONTEXT.md decision (D-01..D-18 + A-01..A-06 = 24 rows) has a status entry in 40.5-DECISION-VERIFY.md: unchanged | drifted | amended"
    - "Each row's evidence cell cites the specific upstream artifact (file:line, function, API) that was checked OR records 'fork-only — no upstream surface to drift'"
    - "Specific named verification targets per ROADMAP.md criterion #4 are recorded: D-09 vs upstream PR #2499; 999.1 hypothesis vs upstream PR #2487"
    - "Per RESEARCH.md §3: ALL 24 rows are fork-only (no upstream-cited rows). The D-02 priority head is empty — verification load is per-row but light"
    - "Per D-04: interactive Claude in plan/execute (no subagent verifier batch). User in the loop on verification thinking"
    - "Per D-06: every A-07+ amendment to Phase 41 CONTEXT.md is reviewed by user before commit"
    - "Per D-17: any MATERIAL drift halts the wave for amendment review, then resumes"
    - "External-action/library refresh checks performed: gitleaks-action@v2, osv-scanner-action@v2, harden-runner@v2, hyperfine, bun:test test.skip.if API"
    - "999.1 conclusion recorded: per RESEARCH.md §6 upstream #2487 does NOT close the gap, so 999.1 stays backlog"
  artifacts:
    - path: ".planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md"
      provides: "24-row decision matrix per D-11 (ID | Cited target | Upstream commit/SHA | Status | Evidence | Amendment ref)"
      contains: "D-09"
      min_lines: 60
    - path: ".planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md"
      provides: "Possibly amended in-place (per D-03 + D-06) IF any drift was found; A-07+ entries appended to Amendments Log"
      contains: "Amendments Log"
  key_links:
    - from: "40.5-DECISION-VERIFY.md"
      to: "40.5-VERIFICATION.md (Wave 5)"
      via: "Wave 5 cites this file as criterion #4 evidence"
      pattern: "40\\.5-DECISION-VERIFY\\.md"
    - from: "40.5-DECISION-VERIFY.md (per-row Amendment ref column)"
      to: "41-CONTEXT.md Amendments Log entries"
      via: "A-07+ chronological entries (D-03)"
      pattern: "A-0[7-9]|A-[1-9][0-9]"
---

<objective>
Wave 4 of Phase 40.5: Re-verify all 24 Phase 41 CONTEXT.md rows (D-01..D-18 + A-01..A-06)
against the fresh v1.38.5 upstream tree. Per RESEARCH.md §3, all 24 rows are fork-only or
external-action/library — the D-02 "upstream-cited rows first" priority head is empty.
Per D-23 + RESEARCH.md §7 the realistic estimate is ~120 minutes (2 hours) total.

Per D-04 verification owner is interactive Claude in plan/execute, NOT a subagent
verifier batch — the user wants to be in the loop on verification thinking. Per D-05
drift threshold is "cited symbol/API/line behavior changed" (whitespace/comment-only
deltas near a citation are unchanged in this sense). Per D-06 user signs off each
amendment before commit. Per D-17 MATERIAL drift halts the wave for amendment review.

Specific named verification targets per ROADMAP.md criterion #4:
- **D-09 (Phase 41) vs upstream PR #2499** (Windows stdio chunked-mode hang fix, merged
  upstream 2026-04-21, in v1.38.4+). Per RESEARCH.md §2: surfaces are different — Phase 41
  D-09 targets `tests/**` subprocess timeouts (fork-only files); #2499 targets upstream
  `plan-phase.md` workflow's invocation under Windows. Conclusion: D-09 stays valid.
- **999.1 hypothesis vs upstream PR #2487** (wave-dependencies, merged 2026-04-20, in
  v1.38.4+). Per RESEARCH.md §6: #2487 surfaces wave dependencies + cross-cutting
  must_haves.truths constraints — orthogonal to wave-level file-write collision detection.
  The 999.1 hypothesis remains LIVE. 999.1 stays backlog per D-18 disposition (b).

Per per-row sizing strategy (interactive-friendly without sacrificing per-row evidence):
group rows into 5 task chunks of related decisions. Each task is interactive (autonomous:
false) so the user can review each row's status determination + sign off any A-07+
amendment as it surfaces.

Satisfies success criterion #4 of Phase 40.5: Phase 41 CONTEXT.md re-verified against
fresh upstream; each of the 18 decisions + 6 amendments confirmed unchanged OR amended
in-place with the Amendments Log updated.

Output: 40.5-DECISION-VERIFY.md (per D-11), Phase 41 CONTEXT.md A-07+ entries (per D-03,
only if drift found), no .js production-code changes (Wave 4 is verification, not
implementation).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CONTEXT.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-RESEARCH.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VALIDATION.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-03-SUMMARY.md
@.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md
</context>

<tasks>

<task id="04-01" type="auto">
  <name>Task 04-01: Initialize 40.5-DECISION-VERIFY.md scaffold (24-row matrix shell)</name>
  <files>.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md</files>
  <read_first>
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CONTEXT.md (D-11 — column shape; D-04 — interactive ownership; D-05 — drift threshold)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-RESEARCH.md §3 (full citation map for 24 rows; D-02 priority assignment)
    - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md (the verification target — confirm 18 D-XX + 6 A-XX rows exist as captured by RESEARCH.md §3)
  </read_first>
  <action>
    Create `.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md` with the matrix shell per D-11. Columns per D-11: ID | Cited target | Upstream commit/SHA | Status | Evidence | Amendment ref. Status enum: unchanged | drifted | amended.

    Required structure:

    ```
    # Phase 40.5 Decision Verification (Criterion #4 Evidence)

    Wave: 4
    Generated: <YYYY-MM-DD HH:MM>
    Upstream version under test: get-shit-done-cc@1.38.5
    Verification owner: interactive Claude per D-04 (no subagent batch)
    Drift threshold: cited symbol/API/line behavior changed (D-05)
    Amendment sign-off: user reviews each per D-06

    ## Methodology

    Per RESEARCH.md §3, all 24 rows are fork-only or external-action/library.
    D-02 priority head (upstream-cited rows first) is empty. Verification proceeds
    in 5 grouped tasks (Tasks 04-02..04-06) covering:
    - Task 04-02: Phase 41 D-01..D-06 (CI structure + security surface — fork artifacts + external GHA actions)
    - Task 04-03: Phase 41 D-07..D-09 + A-03..A-04 (security scanners + Windows root-cause; named target: D-09 vs #2499)
    - Task 04-04: Phase 41 D-10..D-12 + A-05 (flake collector + escape hatch; external API: bun:test test.skip.if)
    - Task 04-05: Phase 41 D-13..D-18 + A-06 (perf baseline + flip gate; external tool: hyperfine)
    - Task 04-06: 999.1 hypothesis vs #2487 (named target); 999.3 vs upstream regex state at v1.38.5

    ## 24-row matrix

    | ID | Cited target | Upstream commit/SHA | Status | Evidence | Amendment ref |
    |---|---|---|---|---|---|
    | D-01 | .github/workflows/ci.yml:85-100 + scripts/check-overrides.js + scripts/check-boundary.js | (fork-only) | <pending> | <pending> | <pending> |
    | D-02 | .changelog-conflict-check.sh + Phase 42 DOCS-06 ref | (fork-only + future-phase) | <pending> | <pending> | <pending> |
    | D-03 | tests/fixtures/changelog-conflict/* | (fork-only) | <pending> | <pending> | <pending> |
    | D-04 | MAINTENANCE.md + .changelog-conflict-check.sh + .planning/audits/suppressions.json | (fork-only) | <pending> | <pending> | <pending> |
    | D-05 | .planning/audits/suppressions.json + AJV strict | (fork-only) | <pending> | <pending> | <pending> |
    | D-06 | SECURITY.md at repo root | (fork-only) | <pending> | <pending> | <pending> |
    | D-07 | gitleaks-action@v2 + osv-scanner-action@v2 + bun.lock + .gitleaks.toml + .planning/audits/low-sev-findings.jsonl | (fork-only + external action) | <pending> | <pending> | <pending> |
    | D-08 | step-security/harden-runner@v2 + MAINTENANCE.md | (fork-only + external action) | <pending> | <pending> | <pending> |
    | D-09 | tests/helpers/test-timeouts.js + Node ≥18 timeout APIs + tests/sync.test.cjs + tests/hooks.test.js + tests/installer-safety.test.js + tests/installer-exports.test.js | (fork-only + Node API stability + EXPLICIT vs upstream PR #2499) | <pending> | <pending> | <pending> |
    | D-10 | GitHub Issues API + GHA Script step + structured labels | (fork-only) | <pending> | <pending> | <pending> |
    | D-11 | "2 working days hard time budget" (process decision) | (process-only) | <pending> | <pending> | <pending> |
    | D-12 | bun:test test.skip.if + GHA job summary + MAINTENANCE.md Escape-Hatch | (external API + fork doc) | <pending> | <pending> | <pending> |
    | D-13 | scripts/bench.js + scripts/compose.js + bun install --ignore-scripts + .planning/perf/test-timing.json | (fork-only) | <pending> | <pending> | <pending> |
    | D-14 | hyperfine flags + perf-baseline.json + GHA timeout 30 min | (external tool + fork artifact) | <pending> | <pending> | <pending> |
    | D-15 | perf-baseline.json JSON shape + test-timing.json JSON shape | (fork-only) | <pending> | <pending> | <pending> |
    | D-16 | scripts/bench.js + scripts/bench-test-timing.js + config/perf-baseline.schema.json + config/test-timing.schema.json | (fork-only) | <pending> | <pending> | <pending> |
    | D-17 | workflow_dispatch workflow 10x-validation.yml + GHA primitive | (fork-only) | <pending> | <pending> | <pending> |
    | D-18 | tests/check-overrides.test.js + tests/check-overrides-integration.test.js + node scripts/check-overrides.js | (fork-only) | <pending> | <pending> | <pending> |
    | A-01 -> D-02 | Same as D-02 + markdownlint-cli2@0.22.0 | (fork-only + external lib) | <pending> | <pending> | <pending> |
    | A-02 -> D-04 | Same as D-04 (scope-partial, quality-full framing) | (fork-only) | <pending> | <pending> | <pending> |
    | A-03 -> D-07 | Same as D-07 (reverses scanner-level pre-filtering) | (fork-only + external action) | <pending> | <pending> | <pending> |
    | A-04 -> D-09 | Same as D-09 (softens REL-02 wording, adds triangulation) | (fork-only + Node API + EXPLICIT vs #2499) | <pending> | <pending> | <pending> |
    | A-05 -> D-10 | Same as D-10 (adds dedup key + label taxonomy + auto-close + scope cap) | (fork-only + GitHub Issues) | <pending> | <pending> | <pending> |
    | A-06 -> D-13/D-15/D-16 | Same as D-13/D-15/D-16 (splits perf-baseline.json from test-timing.json) | (fork-only) | <pending> | <pending> | <pending> |

    ## External-action/library refresh checks (per RESEARCH.md §3 summary table)

    (Populated as part of Tasks 04-02..04-05; named here for visibility.)

    | Action / Library | Currently cited version | Upstream/registry latest | Refresh status |
    |---|---|---|---|
    | gitleaks-action | v2 | <pending> | <pending> |
    | osv-scanner-action | v2 | <pending> | <pending> |
    | step-security/harden-runner | v2 | <pending> | <pending> |
    | hyperfine | (no version pinned in 41-CONTEXT.md) | v1.18.0 per RESEARCH.md §3 | <pending> |
    | bun:test test.skip.if API | (assumed stable) | <pending> | <pending> |
    | markdownlint-cli2 | 0.22.0 | <pending> | <pending> |

    ## Named-target verification (criterion #4 explicit)

    ### Target 1: Phase 41 D-09 vs upstream PR #2499 (Windows stdio chunked-mode)
    (Populated in Task 04-03)

    ### Target 2: 999.1 hypothesis vs upstream PR #2487 (wave-dependencies)
    (Populated in Task 04-06)

    ### Target 3: 999.3 vs upstream workflows/plan-phase.md UI-gate regex at v1.38.5
    (Populated in Task 04-06)

    ## Wave 4 conclusion

    (Populated in Task 04-07 — atomic commit task)
    ```

    Do NOT begin row-by-row verification in this task — that's Tasks 04-02..04-06. This task creates the shell only.
  </action>
  <acceptance_criteria>
    - test -f .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md returns success
    - File contains a row for every D-01..D-18 (18 rows; check via `grep -cE '^\| D-(0[1-9]|1[0-8])' 40.5-DECISION-VERIFY.md` returns 18)
    - File contains a row for every A-01..A-06 (6 rows; check via `grep -cE '^\| A-0[1-6]' 40.5-DECISION-VERIFY.md` returns 6)
    - File contains the literal string `1.38.5`
    - File contains all 6 sections: Methodology, 24-row matrix, External-action/library refresh checks, Named-target verification, Wave 4 conclusion (placeholder)
    - File contains the column header line `| ID | Cited target | Upstream commit/SHA | Status | Evidence | Amendment ref |`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md &amp;&amp; test "$(grep -cE '^\| D-' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md)" = "18" &amp;&amp; test "$(grep -cE '^\| A-' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md)" = "6"</automated>
  </verify>
  <done>
    Matrix shell file exists with all 24 rows in pending state, external-action refresh table empty, named-target sections placeholdered. Tasks 04-02..04-06 will fill rows interactively.
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="04-02" type="checkpoint:human-verify" gate="blocking">
  <name>Task 04-02: Re-verify Phase 41 D-01..D-06 + A-01..A-02 (CI structure + security surface)</name>
  <what-built>
    40.5-DECISION-VERIFY.md scaffold from Task 04-01 with all 24 rows in pending state.
  </what-built>
  <how-to-verify>
    Interactive per D-04. Walk through each row in this task chunk with the user. For each row, follow this verification protocol:

    1. Read the row's `Cited target` from RESEARCH.md §3 (or directly from 41-CONTEXT.md text). For each cited file/symbol/API:
       - If fork-only file (e.g., `scripts/check-overrides.js`, `.planning/audits/suppressions.json`): confirm it does NOT exist in upstream (`tar -tzf node_modules/get-shit-done-cc -- <path>` empty, or `test ! -f node_modules/get-shit-done-cc/<rel>`). Cited target is fork-only → no upstream surface to drift → status `unchanged` with evidence "fork-only — no upstream surface to drift; <file> verified absent from upstream v1.38.5".
       - If external action/library (e.g., `gitleaks-action@v2`, `markdownlint-cli2@0.22.0`): consult the action's GitHub releases (or registry) for current version. If `v2` is still the recommended major and exists → status `unchanged` with evidence "external action gitleaks-action@v2 still recommended at <date>; latest release v2.X.Y on <date>". If the action has been deprecated/replaced → status `drifted`, propose A-07+ amendment to update the citation.
       - If process-only (D-11): no upstream surface → status `unchanged` with evidence "process decision — no upstream surface".
    2. For each row, ask the user: "Do you accept the status determination? (Y to accept; n to discuss)"
    3. On user accept, update that row in 40.5-DECISION-VERIFY.md: Status column → `unchanged` (or `drifted` / `amended` per finding); Evidence column → the exact verification command run + result; Upstream commit/SHA column → "(fork-only)" or "(external: <action-name>@<latest-tag>)" or "(N/A — process)"; Amendment ref → "—" (or "A-07" if amendment created).
    4. On any drift requiring amendment: per D-17 halt the wave. Per D-03 + D-06: append A-07 (or A-08 etc., next sequential) entry to Phase 41 CONTEXT.md Amendments Log; amend the original D-XX paragraph in-place; user signs off; record `Amendment ref` column in 40.5-DECISION-VERIFY.md row. Do NOT commit yet (final commit is Task 04-07).

    **Rows to verify in this task:**
    - **D-01**: ci.yml:85-100 + check-overrides.js + check-boundary.js — all fork-only files. Verify each absent from upstream tarball.
    - **D-02**: .changelog-conflict-check.sh (fork, doesn't exist yet — Phase 41 creates it) + Phase 42 DOCS-06 ref. Forward-reference, no upstream surface.
    - **D-03**: tests/fixtures/changelog-conflict/* (fork, doesn't exist yet). Forward-reference.
    - **D-04**: MAINTENANCE.md (fork doc, doesn't exist yet) + .changelog-conflict-check.sh + .planning/audits/suppressions.json. All fork-only / future.
    - **D-05**: .planning/audits/suppressions.json + AJV strict precedent (Phase 39). Confirm Phase 39 schema pattern still valid (no upstream change to AJV that affects strict mode).
    - **D-06**: SECURITY.md at repo root. Fork-only.
    - **A-01 -> D-02**: Adds markdownlint-cli2@0.22.0 (Phase 42 DOCS-06 dep). External library refresh check: confirm 0.22.0 still released; check if newer recommended.
    - **A-02 -> D-04**: scope-partial, quality-full framing — process clarification, no upstream surface.

    **Pass criterion for this task:** all 8 rows above have status `unchanged` (or amended with user-signed-off A-07+ entry); `markdownlint-cli2@0.22.0` external-action refresh row populated; user has signed off on each row's status determination.
  </how-to-verify>
  <resume-signal>
    Type "rows D-01 to D-06 + A-01 A-02 verified" to advance to Task 04-03. If material drift found requiring amendment that user prefers to defer: type "halt: <reason>" and surface for plan revision.
  </resume-signal>
  <files></files>
  <action>Interactive checkpoint task — execution detail is in the &lt;how-to-verify&gt; block above. The action is to walk the user through the verification steps interactively per D-04 and capture results.</action>
  <verify>
    <automated>manual</automated>
  </verify>
  <done>Resume-signal phrase received from user (see &lt;resume-signal&gt; above).</done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="04-03" type="checkpoint:human-verify" gate="blocking">
  <name>Task 04-03: Re-verify Phase 41 D-07..D-09 + A-03..A-04 (security scanners + Windows root-cause; D-09 vs #2499 named target)</name>
  <what-built>
    Task 04-02 complete: D-01..D-06 + A-01..A-02 rows verified.
  </what-built>
  <how-to-verify>
    Same per-row protocol as Task 04-02. Rows in this chunk include external-action refresh checks AND the FIRST NAMED VERIFICATION TARGET per criterion #4.

    **Rows to verify in this task:**
    - **D-07**: gitleaks-action@v2 + osv-scanner-action@v2 + bun.lock + .gitleaks.toml + .planning/audits/low-sev-findings.jsonl. External action refresh: confirm v2 still recommended major for both; confirm bun.lock format unchanged in 1.38.5 (it's the lockfile format the FORK uses, so trivially unchanged — but verify no upstream guidance change).
    - **D-08**: step-security/harden-runner@v2 + MAINTENANCE.md. External action refresh: confirm v2 still recommended.
    - **D-09 (NAMED TARGET — special handling per criterion #4)**:
      - Cited targets: `tests/helpers/test-timeouts.js` (fork-only — verify with `tar -tzf node_modules/get-shit-done-cc | grep test-timeouts` returns empty per RESEARCH.md §8 Q2), Node ≥18 timeout APIs, fork test files (`tests/sync.test.cjs`, `tests/hooks.test.js`, `tests/installer-safety.test.js`, `tests/installer-exports.test.js`).
      - **EXPLICIT D-09 vs upstream PR #2499 verification (per ROADMAP.md criterion #4):**
        - Confirm #2499 is in v1.38.5 (per RESEARCH.md §2: tagged in v1.38.4, present in v1.38.5).
        - Read `node_modules/get-shit-done-cc/get-shit-done/workflows/plan-phase.md` for the chunked-mode + filesystem-fallback addition (RESEARCH.md §2 says +171 lines added to this file).
        - Verify the surface-difference conclusion from RESEARCH.md §2: Phase 41 D-09 targets fork `tests/**` subprocess timeouts; #2499 targets upstream plan-phase.md workflow's planner-agent invocation under Windows. Different surfaces, different code paths, different concerns.
        - Confirm: D-09 STAYS VALID. Status `unchanged`. Evidence: cite RESEARCH.md §2 + the line counts + the surface-difference reasoning.
        - Populate "Named-target verification — Target 1" subsection in 40.5-DECISION-VERIFY.md with the explicit reasoning.
    - **A-03 -> D-07**: reverses scanner-level pre-filtering. Process refinement; no upstream surface to drift. Same status as D-07.
    - **A-04 -> D-09 (NAMED TARGET — same as D-09)**: softens REL-02 wording + adds triangulation requirement. Status follows D-09's status.

    **Constraint per D-04:** Even though many of these rows are fast verifications, do NOT bundle them silently. Walk through each with the user. The user wants to see the verification thinking, not just the verdict.

    **Pass criterion for this task:** D-07, D-08, D-09, A-03, A-04 all have status determined and user-signed-off. Named-target Target 1 (D-09 vs #2499) section populated with explicit reasoning. External-action refresh table updated for gitleaks-action@v2, osv-scanner-action@v2, step-security/harden-runner@v2.
  </how-to-verify>
  <resume-signal>
    Type "rows D-07 to D-09 + A-03 A-04 verified" once each row's status is recorded and Named-target Target 1 section is populated. Type "halt: <reason>" if drift requires amendment user wants to defer.
  </resume-signal>
  <files></files>
  <action>Interactive checkpoint task — execution detail is in the &lt;how-to-verify&gt; block above. The action is to walk the user through the verification steps interactively per D-04 and capture results.</action>
  <verify>
    <automated>manual</automated>
  </verify>
  <done>Resume-signal phrase received from user (see &lt;resume-signal&gt; above).</done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="04-04" type="checkpoint:human-verify" gate="blocking">
  <name>Task 04-04: Re-verify Phase 41 D-10..D-12 + A-05 (flake collector + escape hatch)</name>
  <what-built>
    Task 04-03 complete: D-07..D-09 + A-03..A-04 rows verified; Named-target Target 1 populated.
  </what-built>
  <how-to-verify>
    Same per-row protocol. Rows in this chunk are mostly fork-only; one external API check (bun:test test.skip.if).

    **Rows to verify in this task:**
    - **D-10**: GitHub Issues API + GHA Script step + structured labels. GitHub-side surfaces (Issues API stable; GHA Script step stable). No upstream-code surface. Status `unchanged`.
    - **D-11**: "2 working days hard time budget" — process decision, no surface to drift. Status `unchanged`.
    - **D-12**: bun:test `test.skip.if` API + GHA job summary + MAINTENANCE.md Escape-Hatch table.
      - **External API refresh:** Confirm `test.skip.if` is still part of the bun:test public API in the bun version the project uses (check `package.json` bun engine if specified, or use `bun --version` on local; cross-check bun-runtime release notes for any breaking changes to test.skip.if).
      - If still present: status `unchanged`. If renamed/deprecated: status `drifted`, propose A-07+ amendment.
    - **A-05 -> D-10**: adds dedup key + label taxonomy + auto-close + scope cap. Process/design refinement on the same GitHub Issues collector. Same status as D-10.

    **Pass criterion for this task:** D-10, D-11, D-12, A-05 all have status determined; bun:test API row in External-action refresh table populated.
  </how-to-verify>
  <resume-signal>
    Type "rows D-10 to D-12 + A-05 verified" once status recorded. Type "halt: <reason>" on amendment-deferral.
  </resume-signal>
  <files></files>
  <action>Interactive checkpoint task — execution detail is in the &lt;how-to-verify&gt; block above. The action is to walk the user through the verification steps interactively per D-04 and capture results.</action>
  <verify>
    <automated>manual</automated>
  </verify>
  <done>Resume-signal phrase received from user (see &lt;resume-signal&gt; above).</done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="04-05" type="checkpoint:human-verify" gate="blocking">
  <name>Task 04-05: Re-verify Phase 41 D-13..D-18 + A-06 (perf baseline + flip gate)</name>
  <what-built>
    Task 04-04 complete: D-10..D-12 + A-05 rows verified.
  </what-built>
  <how-to-verify>
    Same per-row protocol. Rows in this chunk are fork-only; two external tools (hyperfine, bun:test).

    **Rows to verify in this task:**
    - **D-13**: scripts/bench.js (fork, doesn't exist yet) + scripts/compose.js + bun install --ignore-scripts + .planning/perf/test-timing.json. Forward-reference + bun feature stable. Status `unchanged`.
    - **D-14**: hyperfine flags (`--warmup`, `--runs`, `--export-json`) + perf-baseline.json + GHA timeout 30 min.
      - **External tool refresh:** Confirm hyperfine v1.18.0 is still current. Verify `--warmup`, `--runs`, `--export-json` flags still exist (these are stable hyperfine flags since early versions; very unlikely to drift).
      - Status `unchanged`.
    - **D-15**: perf-baseline.json JSON shape + test-timing.json JSON shape (both fork artifacts). Status `unchanged`.
    - **D-16**: scripts/bench.js + scripts/bench-test-timing.js + config/perf-baseline.schema.json + config/test-timing.schema.json (all fork-only, all forward-referenced). Status `unchanged`.
    - **D-17**: workflow_dispatch workflow `10x-validation.yml` (fork doc, doesn't exist yet) + GHA primitive. workflow_dispatch is a stable GHA primitive. Status `unchanged`.
    - **D-18**: tests/check-overrides.test.js (fork, exists) + tests/check-overrides-integration.test.js (fork, doesn't exist yet) + node scripts/check-overrides.js (exists). Per Phase 40.5's Wave 2 work, scripts/check-overrides.js exit-code semantics are now confirmed working post-bump. Status `unchanged`.
    - **A-06 -> D-13/D-15/D-16**: Splits perf-baseline.json from test-timing.json. Design refinement; no upstream surface. Same status as D-13/D-15/D-16.

    **Pass criterion for this task:** All 7 rows above have status determined. Hyperfine row in External-action refresh table populated.
  </how-to-verify>
  <resume-signal>
    Type "rows D-13 to D-18 + A-06 verified" once status recorded. Type "halt: <reason>" on amendment-deferral.
  </resume-signal>
  <files></files>
  <action>Interactive checkpoint task — execution detail is in the &lt;how-to-verify&gt; block above. The action is to walk the user through the verification steps interactively per D-04 and capture results.</action>
  <verify>
    <automated>manual</automated>
  </verify>
  <done>Resume-signal phrase received from user (see &lt;resume-signal&gt; above).</done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="04-06" type="checkpoint:human-verify" gate="blocking">
  <name>Task 04-06: Named verification targets — 999.1 vs upstream #2487; 999.3 vs upstream regex at v1.38.5</name>
  <what-built>
    Tasks 04-02..04-05 complete: all 24 D-XX/A-XX rows have a status.
  </what-built>
  <how-to-verify>
    Two named targets per ROADMAP.md criterion #4 (one already covered in Task 04-03 as Target 1; the other two completed here as Targets 2 and 3).

    **Target 2: 999.1 hypothesis vs upstream PR #2487 (wave-dependencies)**

    Per RESEARCH.md §6 the conclusion is that #2487 surfaces wave-dependency annotations + cross-cutting `must_haves.truths` constraints — orthogonal to wave-level file-write collision detection. The 999.1 hypothesis (plans in the same wave editing the same file create guaranteed merge conflicts on parallel execution) remains LIVE and unaddressed by upstream.

    Verification steps:
    1. Confirm #2487 is in v1.38.5 (per RESEARCH.md §2: tagged in v1.38.4, present in v1.38.5).
    2. Read `node_modules/get-shit-done-cc/agents/gsd-plan-checker.md` directly. Per RESEARCH.md §6 line references:
       - Line 444: cross-references Wave 0 task to test file (about Wave 0 task structure, not wave collisions).
       - Line ~486: "For each shared data path, check if one plan's transformation conflicts with another's" — data-transformation conflict detection (semantic), NOT file-write collision detection.
       - Line ~497: WARNING for potential conflicts; BLOCKER if incompatible transforms with no preservation mechanism.
       - Line ~761: `grep "files_modified:"` — uses files_modified to read plan frontmatter, but doesn't check wave-overlap.
    3. Confirm: #2487 does NOT close the 999.1 hypothesis. Wave-level write collisions to the same file path remain undetected by upstream.
    4. Per D-18 (40.5 methodology decision) disposition: (b) MET → 999.1 stays backlog with its existing 3 promotion paths (Path A upstream PR / Path B PROCESS-08 / Path C decimal-insert into Phase 42) intact.
    5. Populate "Named-target verification — Target 2" subsection in 40.5-DECISION-VERIFY.md with this conclusion.

    **Target 3: 999.3 vs upstream workflows/plan-phase.md UI-gate regex at v1.38.5**

    Per RESEARCH.md §6: at v1.38.2 the regex on line 457 of `workflows/plan-phase.md` lacks word boundaries. Re-confirm at v1.38.5 (line number may have shifted due to other commits in 1.38.2..1.38.5).

    Verification steps:
    1. `grep -n -E "UI\|interface\|frontend\|component\|layout\|page\|screen\|view\|form\|dashboard\|widget" node_modules/get-shit-done-cc/get-shit-done/workflows/plan-phase.md`
       - If pattern found WITHOUT `\b...\b` word boundaries: bug still present in v1.38.5. Status: bug confirmed; per D-20 file upstream issue + PR in Wave 5.
       - If pattern found WITH `\b...\b` word boundaries: bug already fixed by upstream between v1.38.2 and v1.38.5. Status: NOT-A-GAP; per D-20 close 999.3 with the fix commit SHA.
       - If pattern not found at all (regex restructured): inspect surrounding context; determine whether the gate has been replaced by something else; record outcome.
    2. Populate "Named-target verification — Target 3" subsection in 40.5-DECISION-VERIFY.md with verbatim grep output + line number + bug status (still present / fixed / restructured) + Wave 5 disposition (file PR / close NOT-A-GAP / re-evaluate).

    **Pass criterion for this task:** Targets 2 and 3 sections in 40.5-DECISION-VERIFY.md fully populated with verified evidence (line numbers, grep output, conclusion). Target 1 already populated in Task 04-03.
  </how-to-verify>
  <resume-signal>
    Type "named targets verified" once Target 2 (999.1 vs #2487) and Target 3 (999.3 vs upstream regex at v1.38.5) sections are populated.
  </resume-signal>
  <files></files>
  <action>Interactive checkpoint task — execution detail is in the &lt;how-to-verify&gt; block above. The action is to walk the user through the verification steps interactively per D-04 and capture results.</action>
  <verify>
    <automated>manual</automated>
  </verify>
  <done>Resume-signal phrase received from user (see &lt;resume-signal&gt; above).</done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="04-07" type="auto">
  <name>Task 04-07: Wave 4 conclusion + atomic commit (DECISION-VERIFY.md + any 41-CONTEXT.md A-07+ amendments)</name>
  <files>
    .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md
    .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md
  </files>
  <read_first>
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md (post-Tasks-04-02..04-06 state — all 24 rows + Targets 1, 2, 3 populated)
    - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md (only modified if Wave 4 surfaced drift requiring A-07+ amendments)
    - git status (confirm what files have uncommitted changes)
  </read_first>
  <action>
    1. Append the `Wave 4 conclusion` paragraph to 40.5-DECISION-VERIFY.md. State explicitly:
       - How many rows are `unchanged`, `drifted`, `amended` (counts).
       - Whether ANY material drift surfaced (per D-17 the wave halts on material drift; if we got here without halting, drift was either zero or amended).
       - Number of A-07+ entries appended to Phase 41 CONTEXT.md (zero if no drift).
       - Confirmation that the D-21 plan-phase-41 trigger gate #2 ("Zero MATERIAL drift in the 24-decision matrix") is satisfied — note that A-07+ amendments themselves are not "MATERIAL drift" in the gate sense; per D-21 gate #2 wording, "any number of A-07+ amendments OK — they integrate into Phase 41 CONTEXT.md by 40.5 close".
       - Named-target conclusions (one line each):
         - Target 1: D-09 vs #2499 — unchanged, surfaces are different.
         - Target 2: 999.1 vs #2487 — 999.1 stays backlog, #2487 doesn't close gap.
         - Target 3: 999.3 vs upstream regex at v1.38.5 — bug status (still present / fixed / restructured) + Wave 5 disposition.

    2. Atomic commit. Files:
       - 40.5-DECISION-VERIFY.md (always)
       - 41-CONTEXT.md (only if A-07+ amendments were appended)

       ```bash
       # If 41-CONTEXT.md modified:
       git add .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md \
               .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md

       # If only DECISION-VERIFY.md modified:
       git add .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md

       git commit -m "$(cat <<'EOF'
docs(40.5): re-verify Phase 41 CONTEXT.md decisions against v1.38.5

Phase 40.5 Wave 4 — 24-decision matrix (UPGRADE-10, criterion #4).

Verified all 18 decisions (D-01..D-18) and 6 amendments (A-01..A-06)
in Phase 41 CONTEXT.md against the v1.38.5 upstream tree.

Status counts: <U> unchanged, <D> drifted, <A> amended.
A-07+ entries appended to 41-CONTEXT.md Amendments Log: <count>.

Named-target conclusions per criterion #4:
- D-09 vs upstream PR #2499 (Windows stdio chunked-mode): unchanged.
  Surfaces are different — D-09 targets fork tests/** subprocess
  timeouts; #2499 targets upstream plan-phase.md workflow agent
  invocation under Windows.
- 999.1 vs upstream PR #2487 (wave-dependencies): #2487 surfaces
  wave-dep annotations and shared must_haves.truths but does NOT
  add wave-level file-write collision detection. 999.1 stays
  backlog with existing 3 promotion paths intact (per D-18(b)).
- 999.3 vs upstream regex at v1.38.5: <bug status>. Wave 5 disposition: <action>.

Verification: interactive Claude per D-04 (no subagent batch).
Drift threshold: cited symbol/API/line behavior changed (D-05).
Per RESEARCH.md §3, all 24 rows are fork-only or external-action.
The D-02 priority head (upstream-cited rows first) was empty.

Evidence: 40.5-DECISION-VERIFY.md (cited by Wave 5 VERIFICATION.md per D-11).

Refs: UPGRADE-10
EOF
)"
       ```

    No `--no-verify`. No AI attribution.
  </action>
  <acceptance_criteria>
    - grep -c 'Wave 4 conclusion' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md returns >= 1
    - All 24 row Status cells are NON-pending: `grep -cE '\| (unchanged|drifted|amended) \|' 40.5-DECISION-VERIFY.md` returns 24 (or check `grep -cE '\| <pending> \|' 40.5-DECISION-VERIFY.md` returns 0)
    - Named-target sections all populated: `grep -cE '^### Target [123]' 40.5-DECISION-VERIFY.md` returns 3
    - git log -1 --format='%s' | grep -E '^docs\(40\.5\): re-verify Phase 41 CONTEXT.md decisions' matches
    - git log -1 --stat shows either 1 file (40.5-DECISION-VERIFY.md only, zero drift case) OR 2 files (DECISION-VERIFY.md + 41-CONTEXT.md, drift case)
    - git log -1 --format='%B' | grep -ci 'co-authored-by\|generated with claude' returns 0
    - git status --porcelain returns empty (clean working tree)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Wave 4 conclusion' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md &amp;&amp; test "$(grep -cE '&lt;pending&gt;' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md)" = "0" &amp;&amp; test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>
    Wave 4 commit landed. All 24 rows have non-pending status. Named-target sections populated. 41-CONTEXT.md amended if drift surfaced (else unchanged). Wave 5 can proceed.
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

</tasks>

<verification>
Wave 4 success criteria (bound to Phase 40.5 success criterion #4):

1. 40.5-DECISION-VERIFY.md has all 24 rows in non-pending status
2. Named-target sections (Target 1: D-09 vs #2499; Target 2: 999.1 vs #2487; Target 3: 999.3 vs regex) all populated
3. External-action refresh table covers gitleaks-action@v2, osv-scanner-action@v2, harden-runner@v2, hyperfine, bun:test test.skip.if, markdownlint-cli2@0.22.0
4. If any drift was found: A-07+ entries appended to 41-CONTEXT.md Amendments Log; corresponding D-XX original paragraphs amended in-place; user signed off each amendment per D-06
5. Wave 4 commit is atomic (1 or 2 files, conventional message, no AI attribution)
6. D-21 gate #2 satisfied: "Zero MATERIAL drift in the 24-decision matrix (any number of A-07+ amendments OK)"

Verification commands:

```bash
grep -cE '\| (unchanged|drifted|amended) \|' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md  # expect 24
grep -cE '^### Target [123]' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md  # expect 3
git log -1 --stat
```
</verification>

<success_criteria>
- [ ] 40.5-DECISION-VERIFY.md created with full 24-row matrix
- [ ] Each row has Status, Evidence, Amendment ref columns populated
- [ ] Named-target Target 1 (D-09 vs #2499) — surfaces-are-different conclusion documented
- [ ] Named-target Target 2 (999.1 vs #2487) — gap unaddressed; 999.1 stays backlog conclusion documented
- [ ] Named-target Target 3 (999.3 regex at v1.38.5) — bug status verified + Wave 5 disposition recorded
- [ ] External-action/library refresh table fully populated
- [ ] Phase 41 CONTEXT.md A-07+ entries appended (if drift) AND original D-XX content amended in-place (per D-03 + D-06)
- [ ] User signed off each A-07+ amendment per D-06 BEFORE commit
- [ ] Wave 4 commit landed atomically
- [ ] Wave 5 (40.5-05-PLAN.md) can proceed
</success_criteria>

<output>
After completion, create `.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-04-SUMMARY.md` with:

- Wave 4 commit SHA
- Status counts: U unchanged / D drifted / A amended
- A-07+ amendment count (zero or list of A-XX entries with their amended-D-XX targets)
- Named-target conclusions (one line each, mirroring commit message)
- Wave 5 forward-signal: 999.3 disposition (file PR or close NOT-A-GAP) for Wave 5 task planning
</output>
</content>
</invoke>