---
phase: 43
plan: 11
type: execute
wave: 11
depends_on: ["10"]
status: blocked
blocker: 43-11-BLOCKER.md
requirements: ["UPGRADE-05", "UPGRADE-07", "UPGRADE-08", "UPGRADE-09", "SHIP-03"]
files_modified:
  - overrides/**/*.REASON.md
  - overlay/hooks/*.REASON.md
  - hooks/index.js
  - CHANGELOG.md
  - dist/**
  - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-11-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "override snapshots are refreshed only after authority pin moved"
    - "worker classification is re-evaluated against the bumped target"
    - "Phase 43 gates run against the bumped target"
    - "temporary reports are removed or explicitly committed as evidence"
  artifacts:
    - "overrides/**/*.REASON.md"
    - "CHANGELOG.md"
    - "dist/bom.json"
  key_links:
    - "UPGRADE-05"
    - "UPGRADE-07"
    - "UPGRADE-08"
    - "UPGRADE-09"
    - "SHIP-03"
---

<objective>
Refresh override evidence after the bump, reclassify the worker if applicable,
and run Phase 43 gates against the bumped target.
</objective>

<context>
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-10-SUMMARY.md
@scripts/check-overrides.js
@scripts/generate-override-churn.js
@scripts/run-compat-matrix.js
@scripts/generate-sbom.js
@CHANGELOG.md
</context>

<tasks>

<task id="11-01" type="auto">
  <name>Refresh override snapshots after bump</name>
  <files>overrides/**/*.REASON.md; overlay/hooks/*.REASON.md; hooks/index.js</files>
  <read_first>
    - scripts/check-overrides.js
    - overrides/hooks/gsd-check-update.js.REASON.md
    - overrides/hooks/gsd-statusline.js.REASON.md
    - overlay/hooks/gsd-check-update-worker.js.REASON.md
    - hooks/index.js
  </read_first>
  <action>
    After dependency bump and `bun install`, run `node scripts/check-overrides.js`. Review every stale/orphaned override. Update REASON `Upstream snapshot` version/hash values only after reviewing new target source. Update `Semantic SHA-256` values for `.js` overrides. Remove Plan 06/07 temporary forward-port caveats where the active snapshot now genuinely matches the new target.
    If `overlay/hooks/gsd-check-update-worker.js` exists and the target upstream package contains `hooks/gsd-check-update-worker.js`, move it to `overrides/hooks/gsd-check-update-worker.js`, register kind `override`, and add target-version snapshot REASON. If target still lacks a counterpart, keep kind `overlay` and document why.
  </action>
  <acceptance_criteria>
    - `node scripts/check-overrides.js` exits 0.
    - `.js` override REASON files contain `Semantic SHA-256`.
    - `rg -n "Version: 1.6.1" overrides/**/*.REASON.md` finds updated snapshots for touched overrides if target is `1.6.1`.
    - `rg -n "gsd-check-update-worker.js.*kind.*override|gsd-check-update-worker.js.*kind.*overlay|kind.*override.*gsd-check-update-worker.js|kind.*overlay.*gsd-check-update-worker.js" hooks/index.js` proves worker classification was reviewed after bump.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-overrides.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="11-02" type="auto">
  <name>Generate bump churn and SBOM</name>
  <files>CHANGELOG.md; dist/**</files>
  <read_first>
    - scripts/generate-override-churn.js
    - scripts/generate-sbom.js
    - CHANGELOG.md
  </read_first>
  <action>
    Run `node scripts/generate-override-churn.js --write` to update the CHANGELOG override-churn section under Unreleased. Run `bun run dist` so `dist/bom.json` is generated from the bumped package state. Do not edit published release sections.
  </action>
  <acceptance_criteria>
    - `CHANGELOG.md` contains `### Override Churn`.
    - `bash .changelog-conflict-check.sh --self-test` exits 0.
    - `bun run dist` exits 0.
    - `Test-Path dist\\bom.json` is true.
  </acceptance_criteria>
  <verify>
    <automated>bash .changelog-conflict-check.sh --self-test</automated>
    <automated>bun run dist</automated>
    <automated>Test-Path dist\\bom.json</automated>
  </verify>
  <done>false</done>
</task>

<task id="11-03" type="auto">
  <name>Run Phase 43 gates against bump</name>
  <files>.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-11-SUMMARY.md</files>
  <read_first>
    - package.json
    - scripts/verify-upgrade.js
    - scripts/run-compat-matrix.js
    - scripts/generate-sbom.js
    - .github/workflows/upgrade-verifier.yml
    - .github/workflows/compat-matrix.yml
  </read_first>
  <action>
    Run:
    - `bun run compose`.
    - `bun run build`.
    - `bun run sbom`.
    - `bun run finalize-dist`.
    - `bun run verify-upgrade --from 1.5.0 --to <target> --json --report upgrade-report.json` when Verdaccio is available locally; if unavailable, record CI upgrade-verifier as authoritative and do not fake success.
    - `node scripts/run-compat-matrix.js --manifest .planning/vetted-upstream-versions.json --json --report compat-matrix-report.json`.
    Remove local reports after recording evidence unless intentionally committed.
  </action>
  <acceptance_criteria>
    - `bun run dist` exits 0.
    - `node scripts/run-compat-matrix.js --manifest .planning/vetted-upstream-versions.json --json --report compat-matrix-report.json` completes according to blocking policy.
    - temporary `upgrade-report.json` and `compat-matrix-report.json` are removed or listed as committed evidence in summary.
  </acceptance_criteria>
  <verify>
    <automated>bun run dist</automated>
    <automated>node scripts/run-compat-matrix.js --manifest .planning/vetted-upstream-versions.json --json --report compat-matrix-report.json</automated>
  </verify>
  <done>false</done>
</task>

<task id="11-04" type="auto">
  <name>Record post-bump gate summary</name>
  <files>.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-11-SUMMARY.md</files>
  <read_first>
    - CHANGELOG.md
    - dist/bom.json
    - .planning/vetted-upstream-versions.json
  </read_first>
  <action>
    Create `43-11-SUMMARY.md` listing snapshot updates, worker classification decision, churn output, SBOM output, local gate results, CI-deferred Verdaccio evidence if applicable, and report cleanup status.
  </action>
  <acceptance_criteria>
    - Summary contains `snapshot`, `worker classification`, `Override Churn`, `dist/bom.json`, `compat-matrix`, `upgrade-report`, and `UPGRADE-05`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "snapshot|worker classification|Override Churn|dist/bom.json|compat-matrix|upgrade-report|UPGRADE-05" .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-11-SUMMARY.md</automated>
  </verify>
  <done>false</done>
</task>

</tasks>

<threat_model>
Post-bump cleanup can hide unresolved override drift. Mitigate by running the
blocking override gate after the pin moves, recording worker classification, and
requiring temporary evidence files to be removed or explicitly committed.
</threat_model>

<verification>
Run:
- `node scripts/check-overrides.js`
- `bun run dist`
- `node scripts/run-compat-matrix.js --manifest .planning/vetted-upstream-versions.json --json --report compat-matrix-report.json`
- `bash .changelog-conflict-check.sh --self-test`
</verification>
