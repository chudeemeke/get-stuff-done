---
phase: 34-testing-ci-enforcement
plan: 04
type: execute
wave: 2
depends_on: [34-02]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [CI-02, CI-03, CI-04]

must_haves:
  truths:
    - "CI runs fork tests with coverage on macOS, Linux, and Windows"
    - "CI runs upstream compat tests on macOS, Linux, and Windows"
    - "CI runs check-boundary.js and check-overrides.js as fast enforcement checks"
    - "All four CI checks must pass for the build to be green"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "4-check CI matrix across 3 OSes"
      min_lines: 60
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-boundary.js"
      via: "node scripts/check-boundary.js step"
      pattern: "check-boundary"
    - from: ".github/workflows/ci.yml"
      to: "scripts/run-upstream-compat.js"
      via: "node scripts/run-upstream-compat.js step"
      pattern: "run-upstream-compat"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-overrides.js"
      via: "node scripts/check-overrides.js step"
      pattern: "check-overrides"
---

<objective>
Update ci.yml with the full 4-check matrix: fork tests, upstream compat, boundary check, and override check across macOS, Linux, and Windows.

Purpose: CI-02 (check-overrides in CI), CI-03 (cross-platform matrix), CI-04 (all 4 checks pass). This plan depends on Plan 02 which creates check-boundary.js and run-upstream-compat.js. The existing ci.yml already has a 3-OS matrix for fork tests and a parity check job -- this extends it with 2 new jobs.

Output: Updated ci.yml with all enforcement checks running on every push/PR
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-testing-ci-enforcement/34-RESEARCH.md

<interfaces>
Current ci.yml structure (48 lines):
```yaml
jobs:
  lint:           # ubuntu-latest, bun install, bun run lint
  test:           # matrix [ubuntu, macos, windows], bun test --coverage
  parity:         # ubuntu-latest, node scripts/check-parity.js
```

New scripts to integrate (created by Plan 02):
- scripts/check-boundary.js: `node scripts/check-boundary.js` -- exits 0 clean, 1 violation
- scripts/run-upstream-compat.js: `node scripts/run-upstream-compat.js` -- needs dist/ (bun run compose first)
- scripts/check-overrides.js: `node scripts/check-overrides.js` -- already exists and tested, just not in CI yet
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ci.yml with 4-check enforcement matrix</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Read the current .github/workflows/ci.yml and extend it with new jobs. Keep existing lint, test, and parity jobs intact.

Add two new jobs:

1. **upstream-compat** job (cross-platform, needs compose first):
   - Matrix: ubuntu-latest, macos-latest, windows-latest
   - fail-fast: false
   - Steps: checkout, setup-bun, cache bun deps, bun install --frozen-lockfile, bun run compose, node scripts/run-upstream-compat.js
   - Runs on all 3 OSes because symlink/junction behavior differs per platform

2. **boundary-override-check** job (single OS, fast):
   - Runs on ubuntu-latest only (pure filesystem scan, platform-independent)
   - Steps: checkout, setup-bun, bun install --frozen-lockfile, node scripts/check-boundary.js, node scripts/check-overrides.js
   - Two named steps for boundary check and override check so failures are clear

Do NOT add `needs:` dependencies between new jobs and existing jobs -- all should run in parallel for faster CI feedback. The research (Pitfall 5) recommends parallel jobs over sequential steps.

Keep the existing parity job (check-parity.js) -- it's already lean and serves a different purpose (source/installed parity).

The result is 5 total jobs: lint, test (3 OS), parity, upstream-compat (3 OS), boundary-override-check.

Commit: `ci(34-04): add upstream compat, boundary, and override checks to CI matrix`
  </action>
  <verify>
    <automated>grep -c "runs-on:" .github/workflows/ci.yml</automated>
  </verify>
  <done>ci.yml has 5 jobs. Fork tests run on 3 OSes with coverage. Upstream compat runs on 3 OSes. Boundary and override checks run on ubuntu. All jobs run in parallel (no needs: constraints between independent jobs).</done>
</task>

</tasks>

<verification>
- ci.yml parses as valid YAML (no syntax errors)
- 5 jobs defined: lint, test, upstream-compat, parity, boundary-override-check
- upstream-compat uses 3-OS matrix with compose step before compat runner
- boundary-override-check runs check-boundary.js and check-overrides.js
- Existing lint, test, and parity jobs unchanged
</verification>

<success_criteria>
- CI workflow has all 4 enforcement checks: fork tests, upstream compat, boundary, override
- Cross-platform matrix covers macOS, Linux, Windows for test-heavy jobs
- Fast checks (boundary, override) run on single OS for efficiency
- All jobs can run in parallel (no artificial sequential dependencies)
</success_criteria>

<output>
After completion, create `.planning/phases/34-testing-ci-enforcement/34-04-SUMMARY.md`
</output>
