---
phase: 08-upstream-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/sync/sync-manifest.json
  - .planning/phases/08-upstream-sync/commit-analysis.md
autonomous: true

must_haves:
  truths:
    - "Sync manifest exists with fork point, target tag, and complete pending commit list"
    - "Upstream-sync branch exists and is checked out"
    - "Dry-run analysis shows all 72 commits categorized by type and release"
    - "Merge commits identified and marked for skip"
    - "Protected files list defined (branding, Phase 7 security tooling)"
  artifacts:
    - path: ".planning/sync/sync-manifest.json"
      provides: "Persistent sync state tracking"
      contains: "forkPoint"
    - path: ".planning/phases/08-upstream-sync/commit-analysis.md"
      provides: "Categorized commit list with predicted conflicts"
  key_links:
    - from: "sync-manifest.json"
      to: "git merge-base output"
      via: "fork point detection"
      pattern: "3d2a960"
    - from: "commit-analysis.md"
      to: "git log output"
      via: "commit categorization"
      pattern: "v1\\.18\\.0"
---

<objective>
Set up sync infrastructure, create the upstream-sync branch, and produce a complete dry-run analysis of all upstream commits to cherry-pick.

Purpose: Before cherry-picking 72 upstream commits, we need persistent state tracking (sync manifest), a dedicated branch, and a categorized analysis of what we are about to apply. This prevents blind cherry-picking and enables session-resumable sync.

Output: sync-manifest.json initialized, upstream-sync branch created, commit-analysis.md with categorized dry-run preview
</objective>

<execution_context>
@C:\Users\Destiny\.claude/get-stuff-done/workflows/execute-plan.md
@C:\Users\Destiny\.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-upstream-sync/08-CONTEXT.md
@.planning/phases/08-upstream-sync/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize sync manifest and create upstream-sync branch</name>
  <files>.planning/sync/sync-manifest.json</files>
  <action>
1. Fetch latest upstream: `git fetch upstream`

2. Detect fork point using `git merge-base HEAD upstream/main` (expected: 3d2a960)

3. Verify target tag exists: `git rev-parse v1.18.0` must succeed

4. Create the upstream-sync branch from current main: `git checkout -b upstream-sync`

5. Initialize .planning/sync/sync-manifest.json with this structure (use atomic write pattern: write to .tmp then rename):
```json
{
  "forkPoint": "<detected fork point full SHA>",
  "targetTag": "v1.18.0",
  "upstreamRemote": "upstream",
  "upstreamBranch": "upstream/main",
  "syncBranch": "upstream-sync",
  "startedAt": "<ISO timestamp>",
  "status": "initialized",
  "applied": [],
  "skipped": [],
  "pending": [],
  "conflicted": null,
  "snapshots": [],
  "protectedPaths": [
    "eslint.config.js",
    "src/validation/",
    "get-stuff-done/",
    "assets/gsd-logo-*",
    "config/default-config.json",
    "src/config/ConfigLoader.js",
    "src/theme/"
  ],
  "stats": {
    "totalCommits": 0,
    "mergeCommitsSkipped": 0,
    "applied": 0,
    "conflicts": 0,
    "eslintFixes": 0,
    "brandingCorrections": 0
  }
}
```

6. Populate the `pending` array by running:
   `git log --oneline --reverse 3d2a960..v1.18.0 --no-merges`
   Each entry: `{ "sha": "<full sha>", "shortSha": "<7 char>", "message": "<commit message>", "status": "pending" }`
   Get full SHAs with: `git log --format="%H %s" --reverse 3d2a960..v1.18.0 --no-merges`

7. Also identify merge commits (to document as skipped):
   `git log --oneline --reverse 3d2a960..v1.18.0 --merges`
   Add to `skipped` array with reason "merge commit"

8. Update `stats.totalCommits` with pending count and `stats.mergeCommitsSkipped` with merge count

9. Commit: `git add .planning/sync/sync-manifest.json && git commit --author="Chude <chude@emeke.org>" -m "chore(08-sync): initialize sync manifest and upstream-sync branch"`
  </action>
  <verify>
- `git branch --show-current` returns "upstream-sync"
- `.planning/sync/sync-manifest.json` exists and is valid JSON
- `node -e "const m = require('./.planning/sync/sync-manifest.json'); console.log('pending:', m.pending.length, 'skipped:', m.skipped.length)"` shows expected counts
- Fork point in manifest matches `git merge-base` output
  </verify>
  <done>Sync manifest initialized with fork point 3d2a960, target v1.18.0, all pending commits listed, merge commits identified as skipped, upstream-sync branch created and checked out</done>
</task>

<task type="auto">
  <name>Task 2: Produce dry-run commit analysis</name>
  <files>.planning/phases/08-upstream-sync/commit-analysis.md</files>
  <action>
Analyze every pending commit and produce a comprehensive dry-run document.

1. For each pending commit in the manifest, gather:
   - Files changed: `git diff-tree --no-commit-id --name-only -r <sha>`
   - Change stats: `git show --stat <sha> --format=""`
   - Whether it's a merge commit: check parent count via `git rev-list --parents -1 <sha>`
   - Whether it touches protected paths (check against manifest's protectedPaths)

2. Group commits by upstream release version. The release boundaries are version bump commits:
   - v1.9.14: 197800e through d58f2b5
   - v1.10.0: beca9fa (version bump)
   - v1.10.1: 5660b6f through 80d6799
   - v1.11.0: 5ee22e6 through d8840c4
   - v1.11.1: b5ca9a2
   - v1.11.2: 8d2651d through 7c42763
   - v1.11.3: cc3c6ac through ddc736e
   - v1.12.0: 01ae939 through e02b37d
   - v1.12.1: 246d542 through e92e64c
   - v1.13.0: 3f5ab10 through 64373a8
   - v1.14.0: 6c53737 through ecba990
   - v1.15.0: fa81821 through 9ad7903
   - v1.15.1/v1.16.0: 1c6a35f through ea0204b
   - v1.17.0: ca03a06 through 06399ec
   - v1.18.0: 767bef6 through 9adb09f

3. Categorize each commit as:
   - `feature` (feat: prefix)
   - `bugfix` (fix: prefix)
   - `refactor` (refactor: prefix)
   - `docs` (docs: prefix, version bumps, changelog updates)
   - `chore` (chore: prefix, reverts)

4. Flag predicted conflicts: commits that modify files the fork has also modified. Check by running:
   `git diff --name-only 3d2a960..HEAD` (fork's changes)
   Cross-reference with each commit's file list.

5. Flag commits that touch the `get-shit-done/` directory (will need branding path adjustment).

6. Write `.planning/phases/08-upstream-sync/commit-analysis.md` with:
   - Summary statistics (total commits, by type, predicted conflicts, protected file touches)
   - Per-release-version sections with commit tables
   - Each commit row: SHA (short), message, type, files changed count, conflict risk (none/low/high), touches protected paths (yes/no)
   - A "Batching Recommendations" section: which trivial commits (docs, version bumps, changelogs) can be batched vs which need individual review
   - A "Known Concerns" section flagging: the reverted GSD Memory feature (af7a057 then cc3c6ac), the gsd-tools.js introduction (large file), the get-shit-done directory rename implications

7. Commit: `git add .planning/phases/08-upstream-sync/commit-analysis.md && git commit --author="Chude <chude@emeke.org>" -m "docs(08-sync): add dry-run commit analysis"`
  </action>
  <verify>
- `.planning/phases/08-upstream-sync/commit-analysis.md` exists
- Document contains per-release sections covering all 72 non-merge commits
- Conflict predictions are present (at least the known high-conflict files: bin/install.js, hooks/*, package.json)
- Batching recommendations section exists
  </verify>
  <done>Complete dry-run analysis document with all 72 commits categorized by release, type, conflict risk, and protected path impact. Batching recommendations provided for trivial commits. Known concerns documented.</done>
</task>

</tasks>

<verification>
- [ ] upstream-sync branch exists: `git branch | grep upstream-sync`
- [ ] sync-manifest.json is valid and populated: `node -e "const m=require('./.planning/sync/sync-manifest.json'); console.log(m.pending.length, 'pending,', m.skipped.length, 'skipped')"`
- [ ] commit-analysis.md covers all releases from v1.9.14 through v1.18.0
- [ ] No files outside .planning/ modified (this is setup only)
</verification>

<success_criteria>
- Sync manifest initialized with complete commit inventory
- upstream-sync branch created from main
- Dry-run analysis categorizes all 72 commits with conflict predictions
- Session-resumable state tracking in place
</success_criteria>

<output>
After completion, create `.planning/phases/08-upstream-sync/08-01-SUMMARY.md`
</output>
