## 1. VERDICT

**proceed-with-corrections**

Proceed only into corrective GSD replanning and local implementation. Stop Plan 11R, Plan 11D, further hosted execution, and any formal checkpoint claim.

The phase architecture remains recoverable; a whole-phase redesign is unnecessary. The current `11R → 11D` edge is not executable truthfully, however, and a green rerun would still lack a valid exact-execution-subject proof.

## 2. CRITICAL FINDINGS

1. **[FACT — Critical] The hosted contract does not prove that jobs executed the claimed commit.** The runs are associated with PR head `2c9ba…`, but `actions/checkout` executed synthetic merge commit `e2139…`. The collector checks `workflow_run.head_sha` against local/PR head, not the checked-out SHA inside each job. Therefore a future envelope could record `checkedCommit: 2c9ba…` while the executed bytes were a different commit. Repair [the collector](https://github.com/chudeemeke/get-stuff-done/blob/2c9ba08745cf3bc13cec42b0c05feb2ae5f02233/scripts/verify-hosted-ci.js#L530-L578) and workflow checkout/attestation before accepting any passed envelope.

2. **[FACT — Critical] Plan 11R has reached an inaccessible failure branch.** Task 11R-02 requires a passed envelope; Task 11R-03 contains the graph-correction escape path but is reachable only after that envelope and the later checkpoint. The actual failure requires graph correction before either. Plan 11D meanwhile depends on completed 11R and another exact-head cycle. Closing 11R as a “failure checkpoint” would contradict its declared must-haves; expanding it to edit source would violate its write scope. See [43-11R-PLAN.md](https://github.com/chudeemeke/get-stuff-done/blob/2c9ba08745cf3bc13cec42b0c05feb2ae5f02233/.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-11R-PLAN.md) and [43-11D-PLAN.md](https://github.com/chudeemeke/get-stuff-done/blob/2c9ba08745cf3bc13cec42b0c05feb2ae5f02233/.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-11D-PLAN.md).

3. **[FACT — Critical] The direct CycloneDX dependency is release-blocking.** `@cyclonedx/cyclonedx-npm@4.2.1` is an exact direct dev dependency and both audit authorities reject `GHSA-v75r-vx73-82pj` as HIGH. The audit output identifies `6.0.0` as the remediation version. This requires a tested major upgrade, not suppression. See [package.json](https://github.com/chudeemeke/get-stuff-done/blob/2c9ba08745cf3bc13cec42b0c05feb2ae5f02233/package.json) and [CI run 29367264687](https://github.com/chudeemeke/get-stuff-done/actions/runs/29367264687).

4. **[FACT — High] Public-mutation debt is substantially larger than the packet states.** The latest run added one bot comment to each issue `#5`–`#11`, but those issues now each contain 38 `Observed again` comments: **266 total**. The PR diagnostic workflow carries `issues: write` and mutates issues automatically after local triage. This is not merely one missed disclosure; it is a non-idempotent automation design. See [CI lines 84–185](https://github.com/chudeemeke/get-stuff-done/blob/2c9ba08745cf3bc13cec42b0c05feb2ae5f02233/.github/workflows/ci.yml#L84-L185), [issue #5](https://github.com/chudeemeke/get-stuff-done/issues/5), and [issue #11](https://github.com/chudeemeke/get-stuff-done/issues/11).

5. **[FACT — High] Toolchain and action identities are not reproducible inputs.** The five workflows select `bun-version: latest`; Node is generally major-only; actions use movable major tags; Verdaccio uses movable image tag `:6`; performance installers obtain floating Hyperfine packages. Workflow-file digests do not make those external tags immutable. Pin Bun and performance tooling exactly, pin actions by full SHA, and pin the registry image by version plus digest.

6. **[FACT — High] Performance authority cannot distinguish product regression from environment variance.** The baseline is for upstream `1.5.0`; current code uses `1.6.1`. Merged metadata discards actual per-platform versions as `"per-platform"`. Current jobs use three samples and a different Bun version, while `check-perf.js` applies only a raw mean ratio. The reported failures are real policy failures, but they are not yet proof of product regression. See [perf-baseline.json](https://github.com/chudeemeke/get-stuff-done/blob/2c9ba08745cf3bc13cec42b0c05feb2ae5f02233/perf-baseline.json) and [check-perf.js](https://github.com/chudeemeke/get-stuff-done/blob/2c9ba08745cf3bc13cec42b0c05feb2ae5f02233/scripts/check-perf.js).

7. **[FACT — High] Cross-platform failures have three different owners.**

   - The Windows executable path uses host `path.join` even though it is constructing a Windows target path. On POSIX this creates mixed separators. Use `path.win32.join`; this is a small product-boundary defect, not merely an assertion typo.
   - The `/var` versus `/private/var` and 8.3 versus long-path receipt failures are test-oracle defects. Production intentionally canonicalizes with `realpathSync.native` for containment; tests must assert canonical identity.
   - The DACL test fails during fixture preparation because `Get-Acl` cannot autoload its module. Explicitly import `Microsoft.PowerShell.Security` or use the .NET ACL API. No product DACL behavior was exercised by that failure.

8. **[FACT — High] Upgrade verification is incomplete, not invalid in concept.** Packing passed; publication failed with `ENEEDAUTH`. The verifier’s isolated `.npmrc` contains only the registry URL. Its report sanitizer also preserves raw child stdout/stderr, so adding authentication without redaction would create a new leakage risk. See [upgrade-verifier.yml](https://github.com/chudeemeke/get-stuff-done/blob/2c9ba08745cf3bc13cec42b0c05feb2ae5f02233/.github/workflows/upgrade-verifier.yml) and [verify-upgrade.js](https://github.com/chudeemeke/get-stuff-done/blob/2c9ba08745cf3bc13cec42b0c05feb2ae5f02233/scripts/verify-upgrade.js).

9. **[FACT — Medium] “Compat Matrix passed” is true for this run but weak as a continuing contract.** This run actually reported 315/315 for each vetted version. However, the workflow deliberately converts any matrix command failure to exit zero. A future envelope could therefore call the workflow passed while its report contains blocking pinned-version drift. Historical rows may remain informational, but the active pinned row must fail closed.

10. **[INFERENCE — Medium] The Star History failures are third-party availability failures, not demonstrated project link rot.** All failures were HTTP 500 from the decorative upstream chart API after retries. One run cannot establish persistence. Blanket exclusion would hide real decay; keeping a volatile decorative endpoint in the blocking deterministic gate also misclassifies availability.

## 3. CORRECTED GSD GRAPH

Plan identifiers should be allocated by the GSD planner; the names below are semantic boundaries.

| Boundary | Depends on | Ownership and exit |
|---|---|---|
| **Failure Intake** | 43-11N | Verify and index the immutable failed-run evidence, run IDs, hosted head, collector `run_failed`, and absence of a passed envelope. End with 11R explicitly blocked—not completed. |
| **Hosted Subject + Toolchain Authority** | Failure Intake | Exact PR-head checkout, per-job subject-attestation step, collector validation of that step, pinned Bun/Node/actions, and pinned external images/tools. Also make the active compatibility row fail closed. |
| **Supply-Chain Remediation** | Subject Authority | CycloneDX 6 upgrade, lock regeneration, CLI compatibility, reproducible validated SBOM, audit/OSV green. No suppression. |
| **Platform Correctness** | Subject Authority | `path.win32` construction, canonical-path assertions, explicit ACL fixture dependency, and three-OS regression tests. |
| **Authenticated Upgrade Verification** | Subject Authority | Ephemeral registry identity, scoped temporary npmrc, output redaction, cleanup, and genuine pack/publish/install/bump/reinstall/smoke coverage. |
| **Performance Authority** | Subject Authority | Paired same-run reference/candidate benchmark design, complete environment provenance, variance policy, fixture tests, and no new baseline acceptance. |
| **Deterministic Docs Policy** | Performance Authority | Blocking internal/first-party validation and separately classified third-party monitoring. |
| **Diagnostic/Mutation Separation** | Docs Policy | Read-only PR CI, routing-proposal artifacts, and a separate explicitly authorized idempotent mutation workflow. |
| **Local Integration Gate** | All corrective plans | Frozen install; full functional/native/repository/N=3 suites; audit and OSV; SBOM; authenticated local Verdaccio verification; workflow lint; collector negative tests; docs policy; performance-policy fixtures; ≥95% in each metric for every new or changed correction source. |
| **Corrected Hosted Gate** | Local Integration Gate + user authorization | Publish one exact correction head, run the five workflows, require every expected job and subject-attestation step, require the pinned compatibility row, and create one immutable passed envelope or fail closed. |
| **Genuine Fable Gate** | Committed corrected hosted envelope + safe invocation authorization | Consume only the committed envelope. Any requested source/graph correction creates another corrective plan and loops back; it does not mutate the graph inside the checkpoint. |
| **43-11D** | Genuine Fable Gate | Preserve its reviewed source/Jest ownership. Change only its predecessor edge. Its planned second finalized-head hosted cycle remains separately approval-gated. |

Plan 11R truthfully ends after Task 11R-01 completed and Task 11R-02 executed unsuccessfully. It has no passed envelope, no completed Task 11R-03, no valid summary, and no satisfied downstream dependency. If GSD lacks a terminal blocked/superseded status, leave it `in_progress` with a durable blocker; do not manufacture completion.

## 4. REMEDIATION DIRECTION

- **Security dependency:** Exact-pin CycloneDX 6, regenerate both lock authorities, and RED-test executable discovery, arguments, reproducible output, schema validation, and failure propagation. Run both audit authorities afterward.

- **Platform/path behavior:** Use target-platform path semantics for Windows executables. Preserve canonical containment behavior and correct its assertions. Make ACL setup deterministic by explicitly loading its dependency.

- **Deterministic toolchain:** Add one version manifest consumed by local tooling and workflows. Reject `latest`, mutable action tags, and unpinned container/tool installers through a contract test. Record exact versions and image/action SHAs in hosted evidence.

- **Performance policy:** Replace cross-run absolute comparison with paired reference/candidate measurements on the same runner, preferably randomized/interleaved with at least ten measured pairs after warmup. Persist sample arrays, reference/candidate commits, workload and lock digests, runner image, CPU/architecture, Node/Bun/Hyperfine versions, and statistical result. Rebaseline only after this evidence and an explicit performance-budget decision.

- **Verdaccio authentication:** Bootstrap a disposable least-privilege user against a digest-pinned local registry. Pass auth through process environment into the verifier’s isolated npmrc; never through command arguments or committed configuration. Redact token/password/auth-line patterns from reports and logs, and delete all credential material with the verifier temp root.

- **Docs links:** Keep internal links, anchors, install commands, and project-owned endpoints blocking. Classify decorative third-party resources separately, with bounded retries and a scheduled read-only report. Persistent 404/410 or repeated multi-run failure becomes owned link-rot work; a single 5xx is recorded availability degradation, not silently accepted.

- **Issue mutations:** Set diagnostic workflows to `contents: read`. Emit normalized advisory/flake proposals as artifacts. A separate manual or approved scheduled workflow may obtain `issues: write`, enforce an idempotency key such as advisory/package/version, update one bot-owned record instead of appending every run, and provide dry-run output before mutation.

## 5. USER CONSULTATION BOUNDARY

**Project lead may act autonomously:**

- Replan the local GSD graph and update local read models.
- Implement the corrective plans with RED-GREEN tests.
- Pin dependencies/toolchains and run local deterministic gates.
- Use a disposable local Verdaccio instance and ephemeral credentials.
- Produce read-only performance, docs, security, and issue-routing reports.

**Prior user approval is required for:**

- Any push or PR-head update.
- Any Actions run or rerun.
- The genuine Fable invocation if it consumes shared quota/session capacity.
- Issue creation, commenting, editing, labeling, or deletion—including cleanup of the 266 existing comments.
- Accepting a performance regression, selecting a new SLO, or rebaselining to current observations.
- Credential/session changes, branch-protection changes, merge, release, or publication.

**Notification is required after autonomous action for:**

- Exact graph/dependency changes and any displaced downstream edge.
- CycloneDX major-upgrade compatibility findings.
- Any product behavior change versus test-only correction.
- Performance evidence that exceeds the predeclared policy.
- Further public-automation debt or security-relevant output discovered during implementation.

## 6. REJECTED ALTERNATIVES

- **Shape A as written:** Reject closing 11R as completed failure observation; it contradicts its success contract. Retain only its truthful blocked history and add a separate failure-intake node.
- **Shape B:** Reject expanding 11R to own all corrections; its prerequisites, scope, and evidence purpose have failed.
- **Shape C:** Reject pulling 11D/11K and other downstream plans forward wholesale. Their reviewed ownership and dependencies would be contaminated. New blocker-specific plans should feed them.
- **Another performance waiver:** Reject; it adds no knowledge and launders expired authority.
- **Audit suppression or severity downgrade:** Reject; this is a fixable direct dependency.
- **Anonymous Verdaccio publication or pack-only verification:** Reject; either weakens security or removes the realism the verifier exists to prove.
- **Weakening canonical path containment:** Reject; correct the alias-sensitive tests instead.
- **Blanket exclusion of external links:** Reject; it hides persistent rot.
- **Keeping `issues: write` with throttling:** Reject; rate limiting does not establish authority or idempotency.
- **Immediate hosted rerun:** Reject until the subject contract, toolchain, known failures, and local integration gate are corrected.

## 7. CONFIDENCE AND OPEN EVIDENCE

**High-confidence primary facts:**

- PR `#23` is open and draft at hosted head `2c9ba…`; the five run conclusions and 9/18 CI failure count are current.
- The job logs establish every cited failure and the synthetic merge checkout.
- The collector validates run metadata but not executed checkout identity.
- The security dependency, mutable toolchain inputs, expired baseline, registry-only npmrc, workflow permissions, and 266 comments are directly verified.
- No passed hosted envelope can be inferred from these failed runs.

**High-confidence classification:**

- Canonical receipt failures are test-oracle defects.
- DACL failure is test-harness/runner setup.
- Mixed Windows executable separators expose target-path construction coupling.
- The current performance evidence proves policy failure but not product regression.

**Evidence still required:**

- A compatibility spike against CycloneDX 6 before claiming the major upgrade is behavior-preserving.
- A container integration spike to select the safest non-interactive Verdaccio 6 identity bootstrap.
- Paired same-run performance measurements before regression acceptance or rebaseline.
- At least one later observation to classify the Star History 500 as transient or persistent.
- User decisions on performance budget and public comment cleanup.
- A passed corrected hosted envelope and subsequent genuine checkpoint before Plan 11D.

The unpushed subject commit `f2d50b…` and its local failure JSON could not be independently read: GitHub does not contain that commit, and the local shell was denied by the read-only Windows sandbox. Conclusions unique to that local commit therefore remain packet assertions; hosted commit, workflow, run, issue, implementation, and plan facts above were independently verified.