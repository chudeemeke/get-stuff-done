---
phase: 43
plan: 06
type: execute
wave: 6
depends_on: ["43-05"]
status: pending
requirements: ["UPGRADE-07"]
files_modified:
  - overrides/hooks/gsd-check-update.js
  - overlay/hooks/gsd-check-update-worker.js
  - overlay/hooks/gsd-check-update-worker.js.REASON.md
  - overrides/hooks/gsd-check-update.js.REASON.md
  - hooks/index.js
  - tests/hooks.test.js
  - tests/hooks-manifest.test.js
  - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-06-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "check-update preserves fork package identity @chude/get-stuff-done"
    - "check-update preserves role routing based on gsd.role"
    - "check-update includes isNewer, detectConfigDir, stale hook detection, and shared cache"
    - "check-update preserves exact 4h maintainer and 7d consumer throttle behavior"
  artifacts:
    - "overrides/hooks/gsd-check-update.js"
    - "overlay/hooks/gsd-check-update-worker.js"
    - "tests/hooks.test.js"
  key_links:
    - "UPGRADE-07"
---

<objective>
Reconcile the check-update hook with Open GSD 1.6.1 improvements while keeping
the current 1.5.0 snapshot honest until the dogfood bump moves the pin.
</objective>

<context>
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-CONTEXT.md
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-RESEARCH.md
@overrides/hooks/gsd-check-update.js
@overrides/hooks/gsd-check-update.js.REASON.md
@hooks/index.js
</context>

<tasks>

<task id="06-01" type="auto" tdd="true">
  <name>Extend check-update tests first</name>
  <files>tests/hooks.test.js; tests/hooks-manifest.test.js</files>
  <read_first>
    - tests/hooks.test.js
    - tests/hooks-manifest.test.js
    - overrides/hooks/gsd-check-update.js
    - hooks/index.js
  </read_first>
  <action>
    Add failing tests before implementation:
    - check-update source contains `isNewer`.
    - check-update source contains `detectConfigDir`.
    - check-update detects stale installed hook files before reporting no update.
    - check-update uses shared cache directory `.cache/gsd`.
    - check-update preserves `@chude/get-stuff-done`.
    - check-update preserves `gsd.role`.
    - check-update preserves both `4h` maintainer throttle and `7d` consumer throttle.
    - hook manifest includes `gsd-check-update-worker.js` only if the worker file is added.
  </action>
  <acceptance_criteria>
    - `bun test tests/hooks.test.js tests/hooks-manifest.test.js` fails before implementation and exits 0 after implementation.
    - Tests contain `isNewer`, `detectConfigDir`, `stale`, `.cache`, `4h`, and `7d`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/hooks.test.js tests/hooks-manifest.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="06-02" type="auto">
  <name>Reconcile check-update hook behavior</name>
  <files>overrides/hooks/gsd-check-update.js; hooks/index.js</files>
  <read_first>
    - tests/hooks.test.js
    - tests/hooks-manifest.test.js
    - overrides/hooks/gsd-check-update.js
    - overrides/hooks/gsd-check-update.js.REASON.md
    - hooks/index.js
  </read_first>
  <action>
    Forward-port Open GSD 1.6.1 check-update behavior:
    - add or preserve `isNewer`.
    - add `detectConfigDir(baseDir)`.
    - add stale installed hook detection.
    - use shared cache directory `$HOME/.cache/gsd`.
    - preserve fork package check for `@chude/get-stuff-done`.
    - preserve role routing based on `gsd.role`.
    - preserve exact `4h` maintainer throttle and `7d` consumer network throttle.
    - preserve commit classification behavior already covered in tests.
  </action>
  <acceptance_criteria>
    - `bun test tests/hooks.test.js tests/hooks-manifest.test.js` exits 0.
    - `rg -n "isNewer|detectConfigDir|stale|\\.cache.*gsd|@chude/get-stuff-done|gsd.role|4h|7d" overrides/hooks/gsd-check-update.js tests/hooks.test.js` finds matches.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/hooks.test.js tests/hooks-manifest.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="06-03" type="auto">
  <name>Add worker as overlay when needed</name>
  <files>overlay/hooks/gsd-check-update-worker.js; overlay/hooks/gsd-check-update-worker.js.REASON.md; hooks/index.js</files>
  <read_first>
    - hooks/index.js
    - scripts/finalize-dist.js
    - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-RESEARCH.md
  </read_first>
  <action>
    If preserving the upstream split needs a worker before the active pin moves, add `overlay/hooks/gsd-check-update-worker.js` and create `overlay/hooks/gsd-check-update-worker.js.REASON.md`. Register it in `hooks/index.js` as kind `overlay`, not `override`, unless active `@opengsd/gsd-core@1.5.0` contains the same path. The REASON file must state it is forward-ported from 1.6.1 and will be reclassified by Plan 11 after the authority pin moves.
  </action>
  <acceptance_criteria>
    - If worker exists, `rg -n "gsd-check-update-worker.js.*overlay|kind.*overlay" hooks/index.js` finds a match.
    - If worker exists, `overlay/hooks/gsd-check-update-worker.js.REASON.md` contains `Forward-ported 1.6.1 behavior` and `Plan 11`.
    - `node scripts/check-overrides.js` exits 0 against active 1.5.0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-overrides.js</automated>
    <automated>bun test tests/hooks-manifest.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="06-04" type="auto">
  <name>Update check-update reason and summary</name>
  <files>overrides/hooks/gsd-check-update.js.REASON.md; .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-06-SUMMARY.md</files>
  <read_first>
    - overrides/hooks/gsd-check-update.js.REASON.md
    - overrides/hooks/gsd-check-update.js
    - tests/hooks.test.js
  </read_first>
  <action>
    Keep the main REASON `Upstream snapshot` version/hash tied to active `1.5.0`. Add `Forward-ported 1.6.1 behavior` listing `isNewer`, `detectConfigDir`, stale hook detection, and shared cache. Add `Plan 11 refreshes snapshot after authority pin moves`. Create `43-06-SUMMARY.md` with adopted improvements, preserved fork behavior, current-pin snapshot handling, and tests run.
  </action>
  <acceptance_criteria>
    - `rg -n "Forward-ported 1.6.1 behavior|Plan 11 refreshes snapshot|Version: 1.5.0|isNewer|detectConfigDir|stale hook|shared cache" overrides/hooks/gsd-check-update.js.REASON.md .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-06-SUMMARY.md` finds matches.
    - `node scripts/check-overrides.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-overrides.js</automated>
  </verify>
  <done>false</done>
</task>

</tasks>

<threat_model>
Hooks run in user sessions. Mitigate regressions by preserving package identity,
role routing, stale-hook detection, and exact throttle behavior under tests while
keeping snapshot hashes honest until the active pin changes.
</threat_model>

<verification>
Run:
- `bun test tests/hooks.test.js tests/hooks-manifest.test.js`
- `node scripts/check-overrides.js`
</verification>
