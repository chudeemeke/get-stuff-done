---
phase: 42
plan: 01
type: execute
wave: 1
depends_on: []
status: pending
requirements: ["PERF-03", "PERF-04", "PERF-05"]
files_modified:
  - scripts/check-perf.js
  - tests/check-perf.test.js
  - config/perf-baseline.schema.json
  - perf-baseline.json
  - package.json
  - .github/workflows/ci.yml
  - tests/ci-workflow.test.js
  - .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-01-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "scripts/check-perf.js compares current perf metrics to perf-baseline.json per platform"
    - "warning threshold is 1.10x baseline mean"
    - "failure threshold is 1.25x baseline mean"
    - "acceptedRegressions entries require reason, reviewer, reviewedDate, and ticket"
    - "acceptedRegressions must target platform plus metric unless they explicitly declare reviewed global scope"
    - "perf-budget CI uses macos-15, not macos-latest"
---

<objective>
Add performance budget comparison and CI enforcement against the Phase 41 baseline.
</objective>

<context>
@.planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-CONTEXT.md
@.planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-RESEARCH.md
@perf-baseline.json
@scripts/bench.js
@.github/workflows/ci.yml
</context>

<tasks>

<task id="01-01" type="auto" tdd="true">
  <name>Write perf comparator tests first</name>
  <files>tests/check-perf.test.js; config/perf-baseline.schema.json</files>
  <read_first>
    - tests/bench.test.js
    - tests/perf-baseline-schema.test.js
    - config/perf-baseline.schema.json
    - perf-baseline.json
  </read_first>
  <action>
    Create `tests/check-perf.test.js` before implementation. Cover these exact cases:
    - compose ratio `1.09` exits pass with no warning.
    - compose ratio exactly `1.10` exits pass with no warning because warnings are for ratios greater than the threshold.
    - compose ratio `1.11` exits pass and emits a GitHub warning annotation containing `::warning`.
    - compose ratio exactly `1.25` exits pass or warning only, with no hard failure, because failures are for ratios greater than the threshold.
    - compose ratio `1.26` exits fail and emits a GitHub error annotation containing `::error`.
    - install ratio `1.26` is also considered a hard failure.
    - missing platform in baseline fails with an actionable message containing `Missing baseline for platform`.
    - an accepted regression with required fields and matching `platform` plus `metric` permits the hard failure.
    - an accepted regression without `platform` plus `metric` is ignored unless it explicitly declares reviewed global scope.
    - an accepted regression with reviewed global scope permits all covered metrics only when all required metadata is present.
    - an accepted regression missing `reason`, `reviewer`, `reviewedDate`, or `ticket` is schema-invalid.

    Extend `config/perf-baseline.schema.json` only as needed for matching fields. Keep the original required fields on every accepted regression, and model reviewed global scope explicitly rather than treating missing target fields as a wildcard.
  </action>
  <acceptance_criteria>
    - `bun test tests/check-perf.test.js tests/perf-baseline-schema.test.js` exits non-zero before implementation and exits 0 after implementation.
    - `tests/check-perf.test.js` contains `1.10`, `1.25`, `::warning`, `::error`, and a global-scope accepted regression case.
    - `config/perf-baseline.schema.json` still requires `reason`, `reviewer`, `reviewedDate`, and `ticket` for accepted regressions.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/check-perf.test.js tests/perf-baseline-schema.test.js</automated>
  </verify>
</task>

<task id="01-02" type="auto">
  <name>Implement check-perf comparator</name>
  <files>scripts/check-perf.js; package.json</files>
  <read_first>
    - scripts/bench.js
    - tests/check-perf.test.js
    - perf-baseline.json
  </read_first>
  <action>
    Implement `scripts/check-perf.js` as a pure comparator plus CLI:
    - CLI options: `--baseline <file>`, `--current <file>`, `--platform <linux|macos|windows>`, `--warn-ratio <number>`, `--fail-ratio <number>`.
    - defaults: `--warn-ratio 1.10`, `--fail-ratio 1.25`.
    - read current one-platform artifact in the same shape produced by `scripts/bench.js --platform`.
    - compare `install.mean_ms` and `compose.mean_ms` against matching baseline metrics.
    - write human-readable summary lines with platform, metric, baseline mean, current mean, and ratio.
    - emit `::warning title=Perf budget warning::...` for warning ratios.
    - emit `::error title=Perf budget failure::...` and exit 1 for hard failures.
    - permit a hard failure only when `acceptedRegressions[]` has required fields and matches the failing platform plus metric, or explicitly declares reviewed global scope for all platforms and metrics.

    Add package script `"perf:check": "node scripts/check-perf.js"`.
  </action>
  <acceptance_criteria>
    - `node scripts/check-perf.js --help` exits 0 and documents all CLI options.
    - `bun test tests/check-perf.test.js` exits 0.
    - `package.json` contains `"perf:check": "node scripts/check-perf.js"`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-perf.js --help</automated>
    <automated>bun test tests/check-perf.test.js</automated>
  </verify>
</task>

<task id="01-03" type="auto">
  <name>Wire perf-budget CI job</name>
  <files>.github/workflows/ci.yml; tests/ci-workflow.test.js</files>
  <read_first>
    - .github/workflows/ci.yml
    - .github/workflows/perf-baseline.yml
    - tests/ci-workflow.test.js
    - scripts/check-perf.js
  </read_first>
  <action>
    Add a blocking `perf-budget` job to `.github/workflows/ci.yml`:
    - matrix include linux/ubuntu-latest, macos/macos-15, windows/windows-latest.
    - setup bun.
    - install dependencies with `bun install --frozen-lockfile --ignore-scripts`.
    - install hyperfine using the same OS-specific strategy as `perf-baseline.yml`.
    - run `node scripts/bench.js --platform ${{ matrix.platform }} --runs 3 --warmup 1 --out perf-current.json`.
    - run `node scripts/check-perf.js --baseline perf-baseline.json --current perf-current.json --platform ${{ matrix.platform }} --warn-ratio 1.10 --fail-ratio 1.25`.

    Extend `tests/ci-workflow.test.js` to assert:
    - `perf-budget:` exists.
    - workflow contains `node scripts/check-perf.js`.
    - workflow contains `--warn-ratio 1.10` and `--fail-ratio 1.25`.
    - workflow contains `macos-15` and does not contain `macos-latest`.
  </action>
  <acceptance_criteria>
    - `bun test tests/ci-workflow.test.js` exits 0.
    - `bash scripts/lint-workflows.sh` exits 0.
    - `rg -n "perf-budget|check-perf.js|--warn-ratio 1.10|--fail-ratio 1.25" .github/workflows/ci.yml tests/ci-workflow.test.js` finds matches.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/ci-workflow.test.js</automated>
    <automated>bash scripts/lint-workflows.sh</automated>
  </verify>
</task>

<task id="01-04" type="auto">
  <name>Record perf budget summary</name>
  <files>.planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-VALIDATION.md
  </read_first>
  <action>
    Create `42-01-SUMMARY.md` listing implemented thresholds, CI job name, tests run, and any accepted regression schema changes.
  </action>
  <acceptance_criteria>
    - `42-01-SUMMARY.md` contains `1.10`, `1.25`, `perf-budget`, and `acceptedRegressions`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "1.10|1.25|perf-budget|acceptedRegressions" .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-01-SUMMARY.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
Perf data crosses from CI runner output into merge policy. Mitigate tampering and false positives with schema validation, explicit platform matching, coarse thresholds, and required accepted-regression review metadata.
</threat_model>

<verification>
Run:
- `node scripts/check-perf.js --help`
- `bun test tests/check-perf.test.js tests/perf-baseline-schema.test.js tests/ci-workflow.test.js`
- `bash scripts/lint-workflows.sh`
</verification>
