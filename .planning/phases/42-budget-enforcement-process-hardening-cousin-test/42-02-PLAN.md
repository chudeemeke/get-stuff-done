---
phase: 42
plan: 02
type: execute
wave: 1
depends_on: []
status: pending
requirements: ["SHIP-07"]
files_modified:
  - bin/gsd.js
  - scripts/lib/package-provenance.js
  - tests/package-launcher-v3.test.js
  - package.json
  - .planning/upstream-authority.json
  - .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-02-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "gsd --version exits without launching claude"
    - "gsd --version --json prints package, version, upstream package, upstream version, and overlay manifest sha256"
    - "runtime package provenance reads active upstream identity from the authority helper instead of hardcoded package literals"
    - "new packaged helper files are included in package.json files"
---

<objective>
Create the non-interactive runtime package provenance surface required by cousin-test CI.
</objective>

<context>
@.planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-CONTEXT.md
@bin/gsd.js
@scripts/compose.js
@scripts/lib/upstream-source.js
@.planning/upstream-authority.json
@tests/package-launcher-v3.test.js
@package.json
</context>

<tasks>

<task id="02-01" type="auto" tdd="true">
  <name>Write launcher provenance tests first</name>
  <files>tests/package-launcher-v3.test.js</files>
  <read_first>
    - tests/package-launcher-v3.test.js
    - bin/gsd.js
    - package.json
    - scripts/lib/upstream-source.js
    - .planning/upstream-authority.json
  </read_first>
  <action>
    Add tests before implementation:
    - `node bin/gsd.js --version` exits 0 and stdout contains package name plus version read from root `package.json`, and upstream package read through the upstream-authority helper.
    - `node bin/gsd.js --version --json` exits 0 and parses as JSON.
    - JSON contains keys `packageName`, `version`, `upstreamPackage`, `upstreamVersion`, and `overlayManifestSha256`.
    - JSON `packageName` and `version` equal root `package.json` values.
    - JSON `upstreamPackage` and `upstreamVersion` equal active authority values from `scripts/lib/upstream-source.js` or its packaged fallback.
    - `overlayManifestSha256` matches `/^[a-f0-9]{64}$/` after `bun run compose`.
    - `--version` path does not print `Project state:` and does not attempt to launch `claude`.
  </action>
  <acceptance_criteria>
    - `bun test tests/package-launcher-v3.test.js` fails before implementation and exits 0 after implementation.
    - The test file contains `overlayManifestSha256` and `--version --json`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/package-launcher-v3.test.js</automated>
  </verify>
</task>

<task id="02-02" type="auto">
  <name>Implement package provenance helper and version mode</name>
  <files>scripts/lib/package-provenance.js; bin/gsd.js; package.json</files>
  <read_first>
    - bin/gsd.js
    - scripts/lib/upstream-source.js
    - .planning/upstream-authority.json
    - scripts/compose.js
    - package.json
  </read_first>
  <action>
    Add `scripts/lib/package-provenance.js` with exported functions:
    - `readPackageProvenance(rootDir)`.
    - `hashOverlayManifest(rootDir)`.

    `readPackageProvenance(rootDir)` returns:
    - `packageName` from root `package.json`.
    - `version` from root `package.json`.
    - `upstreamPackage` from the active upstream authority contract via `scripts/lib/upstream-source.js` or its packaged fallback.
    - `upstreamVersion` from `dist/.install-meta.json.upstream_version` when present, falling back to the active upstream authority version.
    - `overlayManifestSha256` as SHA-256 of `dist/.overlay-manifest.json`.
    - Do not hardcode active upstream package names or current fork versions in the helper or tests; compare against the authority contract and `package.json`.

    Update `bin/gsd.js`:
    - if args include `--version`, print provenance and exit 0 before `ensureConfig()`, `displayBanner()`, or `launchClaude()`.
    - if args include both `--version` and `--json`, print JSON only.
    - otherwise preserve current launcher behavior.

    Update `package.json.files` to include `scripts/lib/package-provenance.js` if needed by the packaged launcher.
  </action>
  <acceptance_criteria>
    - `node bin/gsd.js --version --json` exits 0 after `bun run compose`.
    - `node -e "JSON.parse(require('child_process').execFileSync(process.execPath,['bin/gsd.js','--version','--json'],{encoding:'utf8'}))"` exits 0.
    - `bun test tests/package-launcher-v3.test.js` exits 0.
    - `package.json.files` contains `scripts/lib/package-provenance.js`.
  </acceptance_criteria>
  <verify>
    <automated>bun run compose</automated>
    <automated>node bin/gsd.js --version --json</automated>
    <automated>bun test tests/package-launcher-v3.test.js</automated>
  </verify>
</task>

<task id="02-03" type="auto">
  <name>Record launcher provenance summary</name>
  <files>.planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-VALIDATION.md
  </read_first>
  <action>
    Create `42-02-SUMMARY.md` with the exact `gsd --version --json` keys, tests run, upstream authority source used, and whether `overlayManifestSha256` was computed from `dist/.overlay-manifest.json`.
  </action>
  <acceptance_criteria>
    - `42-02-SUMMARY.md` contains `overlayManifestSha256`, `upstreamPackage`, `upstreamVersion`, and `--version --json`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "overlayManifestSha256|upstreamPackage|upstreamVersion|--version --json" .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-02-SUMMARY.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
Runtime package provenance output is a consumer trust surface. Mitigate false provenance by deriving values from packaged files and the upstream-authority contract, and by failing tests when the manifest hash is absent or malformed.
</threat_model>

<verification>
Run:
- `bun run compose`
- `node bin/gsd.js --version`
- `node bin/gsd.js --version --json`
- `bun test tests/package-launcher-v3.test.js`
</verification>
