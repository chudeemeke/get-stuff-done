---
phase: 40.5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
autonomous: true
requirements: ["UPGRADE-10"]
estimated_minutes: 45
gap_closure: false
must_haves:
  truths:
    - "package.json pins get-shit-done-cc@1.38.5 (literal string, no other version)"
    - "bun.lock regenerated against the new pin (no frozen-lockfile mismatch)"
    - "bun test exits 0 on Linux runner (CI green)"
    - "bun test exits 0 on macOS runner (CI green)"
    - "Both file changes (package.json + bun.lock) committed atomically in a single commit"
    - "CI run on the post-bump commit exits green for Linux + macOS jobs"
  artifacts:
    - path: "package.json"
      provides: "Upstream pin pointing to get-shit-done-cc@1.38.5"
      contains: '"get-shit-done-cc": "1.38.5"'
    - path: "bun.lock"
      provides: "Lockfile reconciled against the new pin"
      min_lines: 1
  key_links:
    - from: "package.json"
      to: "node_modules/get-shit-done-cc"
      via: "bun install resolution"
      pattern: '"get-shit-done-cc":\s*"1\.38\.5"'
    - from: "bun.lock"
      to: "package.json"
      via: "lockfile dependency resolution"
      pattern: "get-shit-done-cc"
---

<objective>
Wave 1 of Phase 40.5: Bump the upstream `get-shit-done-cc` pin from 1.34.2 to 1.38.5
(per CONTEXT.md amendment A-04 — exercising criterion #1's "or latest stable at execution
time" clause; resolves the incoherence of verifying D-09 vs upstream PR #2499 against an
upstream that doesn't yet contain #2499). Regenerate `bun.lock`. Verify the fork test
suite exits 0 on Linux and macOS. **Windows flake is explicitly out of scope** — REL-01/02
is Phase 41's concern per ROADMAP.md goal statement.

Satisfies success criterion #1 of Phase 40.5: `package.json` pins `get-shit-done-cc@1.38.5`;
`bun install` succeeds; fork test suite passes on Linux + macOS.

Purpose: Establish the fresh upstream tree that downstream waves (2-5) verify against.
No drift detection, no decision re-verification, no scope creep — pin bump + lockfile
regen + green test sweep only.

Output: Atomic commit landing `package.json` + `bun.lock`, CI run confirming Linux + macOS
green. Provides the v1.38.5 baseline for Wave 2's SHA drift detection.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CONTEXT.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-RESEARCH.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VALIDATION.md
@package.json
</context>

<tasks>

<task id="01-01" type="auto">
  <name>Task 01-01: Update package.json pin from 1.34.2 to 1.38.5</name>
  <files>package.json</files>
  <read_first>
    - package.json (current state, full file — confirm line 53 reads `"get-shit-done-cc": "1.34.2",`)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CONTEXT.md (confirm A-04 sets target to 1.38.5; confirm criterion #1 wording)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-RESEARCH.md §1 (confirms tag exists, npm view dist-tags.latest = 1.38.5)
  </read_first>
  <action>
    Edit package.json: change the line `"get-shit-done-cc": "1.34.2",` (line 53 at plan-time) to `"get-shit-done-cc": "1.38.5",`. Exact-version pin; no caret, no tilde, no range syntax (per locked PROJECT.md decision: AF-8 forbids semver range for get-shit-done-cc). No other lines change. Trailing comma preserved.

    Implements UPGRADE-10 per Phase 40.5 success criterion #1. Implements 40.5 D-23 Wave 1 sizing (mechanical bump).

    Do NOT run bun install in this task — that is Task 01-02 (separation lets the lockfile-regen step have its own acceptance criterion).
  </action>
  <acceptance_criteria>
    - `grep -c '"get-shit-done-cc": "1\.38\.5"' package.json` returns `1`
    - `grep -c '"get-shit-done-cc": "1\.34\.2"' package.json` returns `0`
    - `git diff --shortstat package.json` reports exactly `1 file changed, 1 insertion(+), 1 deletion(-)`
    - No other dependency lines modified: `git diff package.json | grep -E '^[+-]' | grep -v '^[+-]{3}' | wc -l` returns `2` (one + line, one - line)
  </acceptance_criteria>
  <verify>
    <automated>grep -E '"get-shit-done-cc": "1\.38\.5"' package.json &amp;&amp; ! grep -E '"get-shit-done-cc": "1\.34\.2"' package.json</automated>
  </verify>
  <done>
    package.json line for get-shit-done-cc reads `"get-shit-done-cc": "1.38.5",`. No other changes in package.json. File staged but not yet committed (commit happens in Task 01-04 after lockfile + tests are confirmed).
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="01-02" type="auto">
  <name>Task 01-02: Regenerate bun.lock against new pin</name>
  <files>bun.lock</files>
  <read_first>
    - bun.lock (current state — confirm get-shit-done-cc@1.34.2 entry present pre-bump)
    - package.json (post-Task-01-01 state — confirm pin reads 1.38.5)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-RESEARCH.md §8 Q4 (explicit task per planner — bun.lock regen is NOT auto)
  </read_first>
  <action>
    Run `bun install` (WITHOUT `--frozen-lockfile`) from the repo root to regenerate bun.lock against the new package.json pin. This is the Q4 explicit-task answer from RESEARCH.md §8: bun.lock regen is a Wave 1 task, not an auto-step.

    Do NOT use `bun install --frozen-lockfile` — that fails until bun.lock is regenerated against the new pin (the whole point of this task).
    Do NOT run `bun update` — that bumps unrelated dependencies which is out of scope.

    Expected outcome: bun.lock file is updated. node_modules/get-shit-done-cc/package.json now reports version 1.38.5.

    If bun install fails (network, registry error), HALT and surface to user — do not retry endlessly.
  </action>
  <acceptance_criteria>
    - `bun install` exit code is 0 (bash: `bun install; echo "EXIT=$?"` → contains `EXIT=0`)
    - `cat node_modules/get-shit-done-cc/package.json | grep -E '"version":' | head -1` returns a line containing `"1.38.5"`
    - `git diff --stat bun.lock` shows bun.lock modified (any line count, but file is non-empty after change: `wc -l bun.lock` returns >0)
    - `git status --porcelain` shows ONLY `package.json` and `bun.lock` as modified — no other files changed by bun install (excluding node_modules which is gitignored)
  </acceptance_criteria>
  <verify>
    <automated>bun install &amp;&amp; grep -q '"1.38.5"' node_modules/get-shit-done-cc/package.json &amp;&amp; test -s bun.lock</automated>
  </verify>
  <done>
    bun.lock updated; node_modules/get-shit-done-cc resolves to v1.38.5. No untracked files outside node_modules/. Both package.json and bun.lock are staged for the atomic commit in Task 01-04.
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="01-03" type="auto">
  <name>Task 01-03: Run bun test locally to confirm green pre-commit</name>
  <files></files>
  <read_first>
    - package.json (confirm test script if any — the `scripts.test` field; if absent, `bun test` is the bun:test default behavior per VALIDATION.md)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VALIDATION.md (Sampling Rate — `bun test` is the post-task-commit sampler)
  </read_first>
  <action>
    Run `bun test` from the repo root. This is the local smoke-check that the post-bump tree compiles, runs, and passes the fork test suite on the executor's platform (whichever Linux/macOS/WSL2 platform is currently running).

    **Windows is explicitly out of scope** for Wave 1 acceptance per ROADMAP.md goal statement: "Windows flake is Phase 41's concern per REL-01/02, not a blocker here." If running on Windows native, this step is informational; the binding green checks come from CI on Linux + macOS runners (Task 01-05).

    Expected outcome: bun test exits 0. If it fails, HALT and surface to the user with the failure output. Do not attempt fixes — failures here are signal that the upstream bump introduced a real regression that needs investigation, NOT something to paper over.

    If on Windows and tests fail with the known subprocess timeout flakiness pattern (per project memory `project_windows_test_flakiness.md`), record the failure pattern but DO NOT block the wave — proceed to commit + CI; the binding gate is CI Linux+macOS.
  </action>
  <acceptance_criteria>
    - `bun test` exit code is 0 on Linux/macOS/WSL2 (the executor's platform)
    - OR (if executor is on Windows native) the test output is captured for the Wave 1 commit message and the failure pattern is documented as a known-Windows-flake (cross-reference: project_windows_test_flakiness.md). Linux+macOS CI run in 01-05 is the binding criterion.
    - No production-code regressions: if a test fails for a reason OTHER than known Windows subprocess flakiness, HALT and surface
  </acceptance_criteria>
  <verify>
    <automated>bun test</automated>
  </verify>
  <done>
    Local `bun test` reports green on the executor's non-Windows platform, OR (on Windows) the executor has documented the test result and is proceeding to commit + CI knowing the binding gate is Linux+macOS CI.
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="01-04" type="auto">
  <name>Task 01-04: Atomic commit of package.json + bun.lock</name>
  <files>package.json, bun.lock</files>
  <read_first>
    - git status output (confirm only package.json + bun.lock modified outside node_modules/)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CONTEXT.md (re-confirm UPGRADE-10 wording for commit message)
  </read_first>
  <action>
    Stage and commit both files atomically:

    ```bash
    git add package.json bun.lock
    git commit -m "$(cat <<'EOF'
chore(deps): bump get-shit-done-cc 1.34.2 -> 1.38.5

Phase 40.5 Wave 1 — pre-Phase-41 currency bump (UPGRADE-10).
Resolves bump target per CONTEXT.md amendment A-04: latest stable
at execution time, contains PRs #2487 (wave-dependencies, v1.38.4)
and #2499 (Windows stdio chunked-mode, v1.38.4) named as
verification targets in success criterion #4.

Diff size: 394 commits / 674 files vs 1.34.2 (per RESEARCH.md §1).
Wave 2 will SHA-refresh the 2 existing overrides
(overrides/hooks/gsd-check-update.js, overrides/hooks/gsd-statusline.js).
Wave 3 audits compose collisions (zero new path-level collisions
predicted). Wave 4 re-verifies all 24 Phase 41 CONTEXT.md decisions.
Wave 5 synthesises VERIFICATION.md and dispositions backlog 999.1
and 999.3.

Refs: UPGRADE-10
EOF
)"
    ```

    No `--no-verify`. If pre-commit hooks exist and fail, surface the failure to the user — do NOT bypass. Author convention from project: `Chude <chude@emeke.org>` (set via project git config or commit-time `--author` flag if not the default — confirm with `git config user.email` first; do not modify project git config).

    Do NOT include AI attribution lines (`Co-Authored-By: Claude`, "Generated with Claude Code"). Project memory rules-git-commits forbids AI attribution.
  </action>
  <acceptance_criteria>
    - `git log -1 --format='%s'` returns the literal string `chore(deps): bump get-shit-done-cc 1.34.2 -> 1.38.5`
    - `git log -1 --format='%an %ae'` returns `Chude chude@emeke.org` (or whatever the project default is — verify pre-commit; do NOT silently change git config)
    - `git log -1 --stat` shows exactly 2 files changed: package.json + bun.lock
    - `git log -1 --format='%B' | grep -c -i 'co-authored-by\|generated with claude\|claude code'` returns `0` (no AI attribution leaked into commit body)
    - `git status --porcelain` returns empty (working tree clean post-commit)
  </acceptance_criteria>
  <verify>
    <automated>git log -1 --format='%s' | grep -E '^chore\(deps\): bump get-shit-done-cc 1\.34\.2 -&gt; 1\.38\.5$' &amp;&amp; test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>
    A single atomic commit on the current branch landing package.json + bun.lock with the conventional-commits formatted message. Working tree clean.
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="01-05" type="checkpoint:human-verify" gate="blocking">
  <name>Task 01-05: Push and verify CI green on Linux + macOS</name>
  <what-built>
    Atomic commit landing get-shit-done-cc@1.38.5 pin + regenerated bun.lock. Local `bun test` green on the executor's platform.
  </what-built>
  <how-to-verify>
    1. Push the commit: `git push` (or `git push origin <current-branch>`).
    2. Identify the CI run associated with the push: `gh run list --branch $(git rev-parse --abbrev-ref HEAD) --limit 5` — find the run whose `headSha` matches `git rev-parse HEAD`.
    3. Wait for the run to complete: `gh run watch <run-id>` (or poll `gh run view <run-id>`).
    4. Confirm the Linux job is GREEN: in `gh run view <run-id>`, the `test` job (or whichever job runs `bun test` on `ubuntu-latest`) has status `completed` and conclusion `success`.
    5. Confirm the macOS job is GREEN: the equivalent job on `macos-latest` (or `macos-13` / whatever the matrix is per .github/workflows/ci.yml) has conclusion `success`.
    6. Windows: PASS or FAIL is informational for Wave 1 (per ROADMAP.md goal statement) — record the result but do NOT gate on it.

    **Pass:** Linux + macOS jobs both `success`.
    **Fail:** Either Linux or macOS job `failure` → HALT Phase 40.5 and surface failure logs.
  </how-to-verify>
  <resume-signal>
    Type "ci green" to advance to Wave 2. If CI failed: type "halt" and provide failure logs; planner will revise based on actual failure mode.
  </resume-signal>
  <files></files>
  <action>Interactive checkpoint task — execution detail is in the &lt;how-to-verify&gt; block above. The action is to walk the user through the verification steps interactively per D-04 and capture results.</action>
  <verify>
    <automated>manual</automated>
  </verify>
  <done>Resume-signal phrase received from user (see &lt;resume-signal&gt; above).</done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

</tasks>

<verification>
Wave 1 success criteria (bound to Phase 40.5 success criterion #1):

1. `grep '"get-shit-done-cc": "1.38.5"' package.json` matches exactly one line
2. `bun.lock` updated and committed
3. `bun install --frozen-lockfile` would succeed against the committed lockfile (binding test: CI runs this on Linux+macOS)
4. CI run on the bump commit reports `success` for both Linux and macOS test jobs

Verification commands (executor or human runs after task chain):

```bash
# Local
grep -E '"get-shit-done-cc": "1\.38\.5"' package.json
bun install --frozen-lockfile
bun test

# CI (binding)
gh run list --branch $(git rev-parse --abbrev-ref HEAD) --limit 1
gh run view <id> --json jobs --jq '.jobs[] | select(.name == "test") | {name, conclusion}'
```
</verification>

<success_criteria>
- [ ] package.json line 53 (or equivalent) reads `"get-shit-done-cc": "1.38.5",` exactly
- [ ] bun.lock regenerated and committed atomically with package.json
- [ ] CI run on the bump commit: Linux job `success`, macOS job `success`
- [ ] Windows job result recorded (informational; not a gate)
- [ ] Wave 2 (40.5-02-PLAN.md) can proceed against the v1.38.5 tree on disk
</success_criteria>

<output>
After completion, create `.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-01-SUMMARY.md` with:

- The exact commit SHA of the bump
- CI run URL + per-platform conclusion
- Local `bun test` result (platform + exit code)
- node_modules/get-shit-done-cc/package.json reported version (must be 1.38.5)
- Any anomalies observed (e.g., bun install printing warnings — informational only)
</output>
</content>
</invoke>
