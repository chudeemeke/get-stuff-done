---
phase: 32-fork-code-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - overlay/src/platform/detect.js
  - overlay/src/platform/paths.js
  - overlay/src/platform/terminal.js
  - overlay/src/theme/index.js
  - overlay/src/theme/Style.js
  - overlay/src/theme/themes.js
  - overlay/src/theme/tokens.js
  - overlay/src/validation/index.js
  - tests/platform.test.js
  - tests/platform-internal.test.js
  - tests/theme.test.js
  - tests/validation.test.js
autonomous: true
requirements:
  - PORT-02
  - PORT-03
  - PORT-04
  - PORT-09

must_haves:
  truths:
    - "platform/ modules are importable from overlay/src/platform/"
    - "theme/ modules are importable from overlay/src/theme/"
    - "validation/ module is importable from overlay/src/validation/"
    - "All existing tests pass with updated import paths (only paths change, not assertions)"
  artifacts:
    - path: "overlay/src/platform/detect.js"
      provides: "OS/shell/environment detection"
      min_lines: 250
    - path: "overlay/src/platform/paths.js"
      provides: "Cross-platform path utilities"
      min_lines: 100
    - path: "overlay/src/platform/terminal.js"
      provides: "Terminal capability detection"
      min_lines: 230
    - path: "overlay/src/theme/Style.js"
      provides: "Fluent ANSI style builder"
      min_lines: 120
    - path: "overlay/src/theme/themes.js"
      provides: "Theme definitions"
      min_lines: 130
    - path: "overlay/src/theme/tokens.js"
      provides: "Design tokens"
      min_lines: 80
    - path: "overlay/src/theme/index.js"
      provides: "Theme entry point"
      min_lines: 35
    - path: "overlay/src/validation/index.js"
      provides: "Git-ops input validation"
      min_lines: 180
  key_links:
    - from: "tests/platform.test.js"
      to: "overlay/src/platform/detect.js"
      via: "require('../overlay/src/platform/detect')"
      pattern: "require.*overlay/src/platform"
    - from: "tests/theme.test.js"
      to: "overlay/src/theme/Style.js"
      via: "require('../overlay/src/theme/Style')"
      pattern: "require.*overlay/src/theme"
    - from: "tests/validation.test.js"
      to: "overlay/src/validation/index.js"
      via: "require('../overlay/src/validation')"
      pattern: "require.*overlay/src/validation"
---

<objective>
Port leaf modules (platform, theme, validation) from src/ to overlay/src/ and update all test imports.

Purpose: These 8 source files are pure fork code with zero fork-internal dependencies. Moving them first establishes the overlay/src/ directory structure that config/ and hooks depend on. All tests must pass after port with only import path changes.
Output: overlay/src/platform/, overlay/src/theme/, overlay/src/validation/ directories populated; 4 test files updated with new import paths.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-fork-code-port/32-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy leaf source modules to overlay/src/</name>
  <files>
    overlay/src/platform/detect.js
    overlay/src/platform/paths.js
    overlay/src/platform/terminal.js
    overlay/src/theme/index.js
    overlay/src/theme/Style.js
    overlay/src/theme/themes.js
    overlay/src/theme/tokens.js
    overlay/src/validation/index.js
  </files>
  <action>
    Copy 8 source files from their current locations to overlay/src/ preserving directory structure:

    - `src/platform/detect.js` -> `overlay/src/platform/detect.js`
    - `src/platform/paths.js` -> `overlay/src/platform/paths.js`
    - `src/platform/terminal.js` -> `overlay/src/platform/terminal.js`
    - `src/theme/index.js` -> `overlay/src/theme/index.js`
    - `src/theme/Style.js` -> `overlay/src/theme/Style.js`
    - `src/theme/themes.js` -> `overlay/src/theme/themes.js`
    - `src/theme/tokens.js` -> `overlay/src/theme/tokens.js`
    - `src/validation/index.js` -> `overlay/src/validation/index.js`

    Create the directories first: `overlay/src/platform/`, `overlay/src/theme/`, `overlay/src/validation/`.

    IMPORTANT: These are COPIES, not moves. The original files at src/ stay in place for now (they will be deleted in Phase 35 when the overlay branch merges to main).

    After copying, verify NO import path changes are needed inside these files:
    - platform/ files use only Node.js built-ins (`os`, `path`, `child_process`) and npm packages (`pathe`). No changes needed.
    - theme/ files import from each other using relative paths (`./Style`, `./themes`, `./tokens`). Since the entire directory moves together, relative paths stay the same. No changes needed.
    - validation/ uses only `path` built-in. No changes needed.
  </action>
  <verify>
    <automated>cd C:/Projects/get-stuff-done && node -e "const p = require('./overlay/src/platform/detect'); console.log(typeof p.detectOS)" && node -e "const t = require('./overlay/src/theme/Style'); console.log(typeof t.Style)" && node -e "const v = require('./overlay/src/validation'); console.log(typeof v.validateGitSHA)"</automated>
  </verify>
  <done>All 8 files exist in overlay/src/ and are importable without errors. Each module's exports are accessible.</done>
</task>

<task type="auto">
  <name>Task 2: Update test import paths from src/ to overlay/src/</name>
  <files>
    tests/platform.test.js
    tests/platform-internal.test.js
    tests/theme.test.js
    tests/validation.test.js
  </files>
  <action>
    Update require paths in 4 test files. Only import paths change -- NO assertion changes, NO test logic changes.

    **tests/platform.test.js** (1,749 lines):
    - `require('../src/platform/detect')` -> `require('../overlay/src/platform/detect')`
    - `require('../src/platform/paths')` -> `require('../overlay/src/platform/paths')`
    - `require('../src/platform/terminal')` -> `require('../overlay/src/platform/terminal')`

    **tests/platform-internal.test.js** (1,089 lines):
    - `require('../src/platform/detect')` -> `require('../overlay/src/platform/detect')`
    - `require('../src/platform/terminal')` -> `require('../overlay/src/platform/terminal')`
    - `require('../src/platform/paths')` -> `require('../overlay/src/platform/paths')`

    **tests/theme.test.js** (569 lines):
    - `require('../src/theme/Style')` -> `require('../overlay/src/theme/Style')`
    - `require('../src/theme/themes')` -> `require('../overlay/src/theme/themes')`
    - `require('../src/theme/tokens')` -> `require('../overlay/src/theme/tokens')`

    **tests/validation.test.js** (735 lines):
    - `require('../src/validation')` -> `require('../overlay/src/validation')`

    Use find-and-replace per file. Search for `../src/platform/`, `../src/theme/`, `../src/validation` and replace with `../overlay/src/platform/`, `../overlay/src/theme/`, `../overlay/src/validation`.

    WARNING: Do NOT change test framework imports (`bun:test` stays as-is). Do NOT change assertion logic. Do NOT change any require paths that point to `get-stuff-done/bin/lib/` (those are upstream paths, not fork source paths).
  </action>
  <verify>
    <automated>cd C:/Projects/get-stuff-done && bun test tests/platform.test.js tests/platform-internal.test.js tests/theme.test.js tests/validation.test.js</automated>
  </verify>
  <done>All 4 test files pass with updated import paths. Zero assertion failures. Only require paths changed.</done>
</task>

</tasks>

<verification>
- All 8 source files exist under overlay/src/ and are importable
- All 4 test suites pass: `bun test tests/platform.test.js tests/platform-internal.test.js tests/theme.test.js tests/validation.test.js`
- No changes to test assertions (only import paths)
</verification>

<success_criteria>
PORT-02: platform/ modules exist at overlay/src/platform/ and tests pass
PORT-03: theme/ modules exist at overlay/src/theme/ and tests pass
PORT-04: validation/ module exists at overlay/src/validation/ and tests pass
PORT-09 (partial): platform, theme, and validation tests pass with updated imports
</success_criteria>

<output>
After completion, create `.planning/phases/32-fork-code-port/32-01-SUMMARY.md`
</output>
