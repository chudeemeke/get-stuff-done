# Plan 11AG Correction Advisory Review

**Reviewer:** `gpt-5.6-sol`
**Reasoning effort:** `xhigh`
**Reviewed HEAD:** `970d11f1466a57e217535d8bf76ac61f05bdfbff`
**Mode:** read-only, bounded correction review
**Authority:** advisory; Fable remains the architecture and closure authority

## Verdict

`ACCEPT_WITH_FOLLOW_UPS`

The reviewer independently confirmed the exact HEAD, clean worktree, clean
`git diff --check`, and the stated 18-commit review range. All prior blocking
and high-severity findings are closed. One prior medium test-evidence finding is
reduced but remains partially open.

## Prior Finding Disposition

| Finding | Disposition | Evidence |
| --- | --- | --- |
| Exact aggregate arithmetic | Closed | `BigInt` totals and rational cross-multiplication drive status. Numeric means and ratios remain diagnostic caches. The aggregate one-nanosecond boundary fixture fails correctly. |
| Caller-asserted runner image | Closed | The adapter observes an OS fingerprint, records the expectation separately, compares them, and re-observes before and after every sample. The domain independently enforces equality. |
| Historical comparison could block | Closed | Historical measured verdicts emit warnings and return success. Only paired failure can produce a performance-failure exit. Malformed input may still fail as an operational validation error. |
| Warmup schedule wrapping | Closed | Capture and validation independently build warmup schedules sized to `warmupRuns`; the odd measured-pair and longer-warmup fixture passes. |
| Production adapter and drift tests | Open, reduced | Production spawn wiring and post-measure identity, control, and subject drift are covered. Dirty production resolution, nonzero production preparation, and explicit preparation-before-Hyperfine ordering remain unproved. |
| Dispersion absent | Closed | `pairRatioMad` is required by schema, recomputed from raw pairs, and covered by whole-summary disagreement rejection. |

## Boundary Probe

The reviewer performed a read-only arithmetic probe:

- exact `1.10` produces `pass`;
- one aggregate nanosecond above `1.10` produces `warn`;
- exact `1.25` produces `warn`;
- one aggregate nanosecond above `1.25` produces `fail`.

The displayed numeric ratio may remain rounded to `1.10` or `1.25` in the
above-boundary cases. This confirms that the numeric ratio is diagnostic and
the exact rational comparison is authoritative.

## New Findings

No new blocking, high, or medium defects were found.

### Low: paired help example does not match the default observer

The option requires the exact observed runner fingerprint, but the help example
supplies `windows-2025` while the default observer emits
`platform:release:version`.

**Required correction:** document how to derive the exact local fingerprint or
use an example that represents the required format.

## Verification-Claim Assessment

The reviewer did not rerun the supplied Bun, coverage, lint, audit, dist, or
gitleaks commands. Static inspection confirmed:

- exactly 37 focused test declarations across the three stated files;
- exactly 59 `.test.js` files selected by the full Bun runner;
- focused coverage is explicitly limited to `scripts/lib/paired-perf.js`;
- the final dist tree contains 743 files, consistent with 740 composed files
  plus two compose metadata files and `bom.json`;
- branding metadata records 124 rules, and bundled hooks and the finalized
  CycloneDX SBOM are present;
- no inspected evidence contradicts the remaining verification claims.

## Phase Disposition

- **11AG:** corrected blocking contract is acceptable with the bounded adapter-
  test and help follow-ups above.
- **11AH:** hosted observation source, trusted checkout and harness, run
  identity, artifact origin, and upload binding remain correctly deferred.
- **11AI:** the real integrated Hyperfine receipt remains correctly deferred;
  its absence is not an 11AG defect.

## Recommendation To Fable

Do not sustain the prior rejection. Record `ACCEPT_WITH_FOLLOW_UPS` at
`970d11f1`. Require only the bounded adapter-negative test patch and help
correction before unconditional 11AG acceptance. Do not require Plan 11AH
workflow work or the Plan 11AI real receipt for Plan 11AG acceptance.
