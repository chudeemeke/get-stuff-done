---
phase: 41
plan: 07
type: execute
wave: 4
depends_on: ["06"]
status: pending
requirements: ["REL-01", "REL-03"]
files_modified:
  - .github/workflows/10x-validation.yml
  - .github/workflows/flake-issue-maintenance.yml
  - MAINTENANCE.md
  - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-07-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "A workflow_dispatch 10x validation workflow runs the full suite ten times per platform with zero retries"
    - "The 10x workflow is a post-registration default-branch gate, not a pre-merge proof path for a brand-new workflow file"
    - "D-11's 2-working-day timebox is encoded as Phase 41 closure discipline"
    - "Remaining flakes after the timebox move to REL-03 with issue, deadline, reviewer, CI summary, and MAINTENANCE row"
    - "Remaining flakes after the timebox also require the in-test REL-03 skip wrapper shape"
    - "Stale flake issues auto-close after 30 days without a hit"
  artifacts:
    - path: ".github/workflows/10x-validation.yml"
      provides: "REL-01 gated post-execute validation"
      contains: "workflow_dispatch"
    - path: ".github/workflows/flake-issue-maintenance.yml"
      provides: "Weekly flake issue auto-close workflow"
      contains: "flake-report"
    - path: "MAINTENANCE.md"
      provides: "Escape-Hatch Decisions Log"
      contains: "Escape-Hatch Decisions Log"
  key_links:
    - from: ".github/workflows/10x-validation.yml"
      to: "Phase 41 closure rule"
      via: "manual post-registration validation gate"
      pattern: "10x validation run"
    - from: ".github/workflows/flake-issue-maintenance.yml"
      to: "GitHub flake issues"
      via: "scheduled stale issue closure"
      pattern: "flake-report"
    - from: "MAINTENANCE.md"
      to: "REL-03 escape-hatch discipline"
      via: "issue/deadline/reviewer table"
      pattern: "Escape-Hatch Decisions Log"
---

<objective>
Create the REL-01 validation gate and D-11/REL-03 closure discipline for Phase 41.

Purpose: Phase 41 is not complete merely because local tests pass. It closes only
after the 10x workflow proves ten clean full-suite runs per platform, or after the
D-11 timebox forces any residual flakes into the visible REL-03 escape hatch.
</objective>

<context>
@.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md
@.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-RESEARCH.md
@.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-VALIDATION.md
@.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-06-SUMMARY.md
@MAINTENANCE.md
</context>

<tasks>

<task id="07-01" type="auto">
  <name>Create 10x validation workflow</name>
  <files>.github/workflows/10x-validation.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md
  </read_first>
  <action>
    Create `.github/workflows/10x-validation.yml`:
    - `workflow_dispatch` only.
    - include a comment that this workflow must exist on the default branch
      before it can be manually dispatched.
    - matrix over `ubuntu-latest`, `macos-latest`, and `windows-latest`.
    - setup bun with cache.
    - `bun install --frozen-lockfile --ignore-scripts`.
    - `bun run compose`.
    - run the full suite ten times sequentially with zero retries:
      each iteration prints `10x validation run N/10` and runs
      `bun test --coverage`.
    - fail on the first failing iteration.
    - append a summary line stating that Phase 41 remains open unless all 30
      matrix iterations are green.

    Encode the first-run path in `41-07-SUMMARY.md`:
    1. Merge or otherwise register `.github/workflows/10x-validation.yml` on the
       default branch.
    2. Run `gh workflow run 10x-validation.yml --ref <phase-41-ref-or-default>`.
    3. If all Linux, macOS, and Windows matrix runs pass 10/10 with zero retries,
       Phase 41 may close.
    4. If any platform fails, Phase 41 reopens into REL-02/REL-03 triage before
       Phase 42 may start; apply D-11's 2-working-day root-cause timebox and move
       any remaining flake to REL-03 with issue, deadline, reviewer, CI summary,
       and MAINTENANCE.md row.
  </action>
  <acceptance_criteria>
    - `.github/workflows/10x-validation.yml` contains `workflow_dispatch`, `ubuntu-latest`, `macos-latest`, `windows-latest`, and `10x validation run`.
    - `.github/workflows/10x-validation.yml` contains no `pull_request`, no `push`, and no retry action.
    - `41-07-SUMMARY.md` contains `gh workflow run 10x-validation.yml --ref` and `Phase 41 reopens`.
    - `bash scripts/lint-workflows.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/lint-workflows.sh</automated>
    <automated>rg -n "workflow_dispatch|ubuntu-latest|macos-latest|windows-latest|10x validation run" .github/workflows/10x-validation.yml</automated>
  </verify>
  <done>
    Done when `10x-validation.yml` is workflow_dispatch-only, contains all three
    runner labels and `10x validation run`, summary records the `--ref` dispatch
    path plus reopen rule, and workflow lint exits 0.
  </done>
</task>

<task id="07-02" type="auto">
  <name>Create flake issue maintenance workflow</name>
  <files>.github/workflows/flake-issue-maintenance.yml</files>
  <read_first>
    - scripts/flake-triage.js
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Create `.github/workflows/flake-issue-maintenance.yml`:
    - scheduled weekly and `workflow_dispatch`.
    - uses `actions/github-script` to close open `flake-report` issues whose
      last hit comment is older than 30 days.
    - closure comment includes last-hit date, close date, and total hits.
    - does not close issues carrying `rel-03-candidate` unless the
      MAINTENANCE.md Escape-Hatch Decisions Log has no active row for the same
      test path.
  </action>
  <acceptance_criteria>
    - `.github/workflows/flake-issue-maintenance.yml` contains `flake-report`, `30`, `last-hit`, `total-hits`, and `rel-03-candidate`.
    - `bash scripts/lint-workflows.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/lint-workflows.sh</automated>
    <automated>rg -n "flake-report|30|last-hit|total-hits|rel-03-candidate" .github/workflows/flake-issue-maintenance.yml</automated>
  </verify>
  <done>
    Done when the maintenance workflow contains the flake-report closure policy,
    30-day threshold, hit metadata, rel-03-candidate guard, and workflow lint exits 0.
  </done>
</task>

<task id="07-03" type="auto">
  <name>Encode D-11 timebox and REL-03 maintenance log</name>
  <files>MAINTENANCE.md; scripts/flake-triage.js</files>
  <read_first>
    - MAINTENANCE.md
    - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md
  </read_first>
  <action>
    Ensure `MAINTENANCE.md` Escape-Hatch Decisions Log has the required columns:
    `ID | test-path | platform | issue | deadline | reviewer | status`.

    Add text immediately above or below the table stating:
    `Phase 41 Windows root-cause work is timeboxed to 2 working days. After that
    timebox, any remaining flake must move to REL-03 with an issue link, concrete
    deadline, reviewer, CI job-summary visibility, and this table row before
    Phase 41 can close.`

    If no REL-03 skip is active, write a row stating `No active REL-03 skips` in
    the status column and keep issue/deadline as `N/A`. If any skip is introduced,
    it must include an issue URL, concrete deadline date, and matching local
    in-test skip wrapper validated by
    `node scripts/flake-triage.js --validate-rel03-wrappers`.
  </action>
  <acceptance_criteria>
    - `rg -n "Escape-Hatch Decisions Log|2 working days|No active REL-03 skips|REL-03-" MAINTENANCE.md` finds the log, the D-11 timebox language, and either the no-active row or active skip ids.
    - No REL-03 row has an empty issue, deadline, reviewer, or status field.
    - `node scripts/flake-triage.js --validate-rel03-wrappers` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Escape-Hatch Decisions Log|2 working days|No active REL-03 skips|REL-03-" MAINTENANCE.md</automated>
    <automated>node scripts/flake-triage.js --validate-rel03-wrappers</automated>
  </verify>
  <done>
    Done when `MAINTENANCE.md` contains the Escape-Hatch Decisions Log, D-11
    `2 working days` language, and either a no-active row or fully populated
    REL-03 rows whose in-test skip wrappers validate.
  </done>
</task>

<task id="07-04" type="auto">
  <name>Record closure-gate summary</name>
  <files>.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-07-SUMMARY.md</files>
  <read_first>
    - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-VALIDATION.md
  </read_first>
  <action>
    Create `41-07-SUMMARY.md` with:
    - workflow lint result.
    - whether any REL-03 skip exists.
    - D-11 timebox disposition.
    - instructions to trigger `gh workflow run 10x-validation.yml --ref
      <phase-41-ref-or-default>` after the workflow file is registered on the
      default branch.
    - the Phase 41 reopen rule if the 10x workflow fails.
  </action>
  <acceptance_criteria>
    - `41-07-SUMMARY.md` exists and names `10x-validation.yml`.
    - `41-07-SUMMARY.md` contains `D-11`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-07-SUMMARY.md'; const s=fs.readFileSync(p,'utf8'); if(!s.includes('10x-validation.yml') || !s.includes('D-11')) process.exit(1)"</automated>
  </verify>
  <done>
    Done when `41-07-SUMMARY.md` exists and names both `10x-validation.yml` and
    `D-11`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow dispatch to phase closure | Manual 10x workflow result gates Phase 41 closure. |
| Flake issue comments to maintenance workflow | GitHub issue history drives automatic issue closure. |
| Escape-hatch table to project truth | REL-03 rows become durable justification for skipped tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07-01 | Denial of Service | 10x validation | accept | Workflow is manual dispatch only and intentionally runs ten full suites per platform as the REL-01 gate. |
| T-41-07-02 | Repudiation | REL-03 skips | mitigate | Require issue link, deadline, reviewer, CI summary, and MAINTENANCE.md row. |
| T-41-07-03 | Tampering | automatic flake issue closure | mitigate | Do not close rel-03-candidate issues when an active maintenance row still exists. |
</threat_model>

<verification>
Run:
- `bash scripts/lint-workflows.sh`
- `rg -n "Escape-Hatch Decisions Log|2 working days|No active REL-03 skips|REL-03-" MAINTENANCE.md`

Expected: workflow lint exits 0 and the D-11/REL-03 closure discipline is
visible in `MAINTENANCE.md`. Phase 41 closure still requires
`gh workflow run 10x-validation.yml --ref <phase-41-ref-or-default>` to pass on
Linux, macOS, and Windows after the workflow file is registered on the default
branch.
</verification>

<success_criteria>
Plan 07 succeeds when REL-01 has a concrete 10x validation workflow and REL-03
cannot be used silently after the D-11 timebox.
</success_criteria>

<output>
After completion, create `.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-07-SUMMARY.md`.
</output>
