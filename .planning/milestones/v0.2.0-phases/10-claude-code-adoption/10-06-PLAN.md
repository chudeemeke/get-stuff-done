---
phase: 10-claude-code-adoption
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Installer copies teams/ directory from project source to installed location during install"
    - "Installer copies teams/ directory in link mode (symlink) and copy mode"
    - "Stale backup files are removed from version control"
  artifacts:
    - path: "bin/install.js"
      provides: "teams/ directory install logic"
      contains: "teams"
  key_links:
    - from: "bin/install.js teams copy logic"
      to: "get-stuff-done/teams/ (project source)"
      via: "copyWithPathReplacement or createSymlink"
      pattern: "teams"
---

<objective>
Update installer to handle teams/ directory and clean up stale backup files.

Purpose: The installer (bin/install.js) copies agents, commands, workflows, and hooks from project source to the installed location, but has no logic for the new teams/ directory added in Phase 10. Without this, npm installs would not include team templates. Additionally, 3 stale .bak files need removal from version control.

Output: Updated bin/install.js with teams/ copy logic, 3 backup files deleted
</objective>

<execution_context>
@C:\Users\Destiny\.claude/get-stuff-done/workflows/execute-plan.md
@C:\Users\Destiny\.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@bin/install.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add teams/ directory copy logic to installer</name>
  <files>bin/install.js</files>
  <action>
    Add teams/ directory handling to the install() function in bin/install.js. The teams/ directory lives inside get-stuff-done/ (i.e., get-stuff-done/teams/) in the project source. Since the installer already copies the entire get-stuff-done/ directory via copyWithPathReplacement (line ~1258-1272), AND the link mode symlinks the entire get-stuff-done/ directory (line ~1261-1264), the teams/ subdirectory SHOULD already be included automatically.

    VERIFY THIS FIRST by reading the install logic carefully:
    - In copy mode: copyWithPathReplacement(skillSrc, skillDest, ...) copies recursively from src/get-stuff-done/ to target/get-stuff-done/. If teams/ exists inside src/get-stuff-done/, it will be copied.
    - In link mode: createSymlink(skillSrc, skillDest, true) symlinks the entire get-stuff-done/ directory. teams/ inside would be accessible via symlink.

    If the existing logic already handles teams/ (because it copies the entire get-stuff-done/ directory recursively), then:
    1. Add a verification step after the get-stuff-done copy/link that confirms teams/ was included
    2. Add teams/ count to the install output (e.g., "Installed get-stuff-done (including N team templates)")

    If the existing logic does NOT handle teams/ (e.g., it only copies specific subdirectories), then:
    Add explicit teams/ copy logic following the same pattern as other directory copies:
    - In copy mode: use copyWithPathReplacement for get-stuff-done/teams/
    - In link mode: teams/ comes along with the get-stuff-done/ symlink

    Also update the uninstall() function: the existing uninstall removes get-stuff-done/ recursively (line ~926-929), which will include teams/. Verify this is the case and add no extra logic if so.

    ALSO: Add teams/ to the verifyInstalled check after get-stuff-done installation. After the existing verify of get-stuff-done/ directory, add a check for the teams/ subdirectory:
    ```javascript
    // Verify teams directory was included
    const teamsDest = path.join(skillDest, 'teams');
    if (fs.existsSync(teamsDest)) {
      const teamCount = fs.readdirSync(teamsDest).filter(f => f.endsWith('.md')).length;
      if (teamCount > 0) {
        console.log(`  ${green}[checkmark]${reset} Installed ${teamCount} team templates`);
      }
    }
    ```
    Use the actual checkmark character that matches the existing codebase (the green checkmark symbol used throughout install.js).
  </action>
  <verify>
    1. Read bin/install.js and confirm teams verification code exists
    2. Run: node bin/install.js --help (should not crash, confirms no syntax errors)
    3. Grep for "team" in bin/install.js (should find the new verification logic)
  </verify>
  <done>
    bin/install.js includes verification output for teams/ directory installation. The existing recursive copy of get-stuff-done/ handles teams/ automatically; we added user-visible confirmation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete stale backup files</name>
  <files>
    assets/.backup/gsd-logo-2000.svg.bak
    assets/.backup/terminal.svg.bak
    commands/gsd/new-project.md.bak
  </files>
  <action>
    Delete 3 stale backup files that should not be in version control:
    1. assets/.backup/gsd-logo-2000.svg.bak
    2. assets/.backup/terminal.svg.bak
    3. commands/gsd/new-project.md.bak

    Use git rm for tracked files, or plain delete + git add for untracked files.

    After deletion, check if assets/.backup/ directory still contains non-.bak files. If it only has the conversion script and original assets (no .bak files remaining), leave it. If the directory is now empty or only contains .bak files, do NOT delete the directory -- it still has convert-all.js and actual asset files.
  </action>
  <verify>
    1. Confirm files are gone: ls assets/.backup/*.bak 2>/dev/null (expect no output)
    2. Confirm command backup gone: ls commands/gsd/new-project.md.bak 2>/dev/null (expect no output)
    3. Confirm assets/.backup/ still has its real files: ls assets/.backup/ (should show convert-all.js, PNGs, SVGs but no .bak files)
  </verify>
  <done>
    3 stale .bak files deleted. assets/.backup/ directory retains its actual asset files and conversion script.
  </done>
</task>

</tasks>

<verification>
1. bin/install.js has no syntax errors (node bin/install.js --help exits cleanly)
2. teams/ verification output is present in installer
3. All 3 .bak files removed
4. Uninstall function still removes teams/ (via recursive get-stuff-done/ removal)
</verification>

<success_criteria>
- Installer outputs team template count during installation
- 3 backup files deleted from repository
- No installer regressions (--help still works)
</success_criteria>

<output>
After completion, create `.planning/phases/10-claude-code-adoption/10-06-SUMMARY.md`
</output>
