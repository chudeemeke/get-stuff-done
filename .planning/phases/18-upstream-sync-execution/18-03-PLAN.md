---
phase: 18-upstream-sync-execution
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - .planning/sync/sync-manifest.json
  - .planning/phases/18-upstream-sync-execution/approach-comparison.md
  - "(all files modified by Batch 9-11 cherry-picks)"
autonomous: true

must_haves:
  truths:
    - "All 12 Batch 9 commits (v1.19.2..v1.20.0) are cherry-picked with conflicts resolved"
    - "All 25 Batch 10-11 commits (v1.20.0..v1.20.5) are cherry-picked with conflicts resolved"
    - "The Codex add+revert pair (db1d003/87c3873 + e820263/d55998b) cancels cleanly"
    - "Auto-advance pipeline features from upstream are adopted"
    - "Requirements tracking chain improvements from upstream are adopted"
    - "Fork branding is correct after all three batches"
    - "Full test suite passes after each batch"
  artifacts:
    - path: ".planning/sync/sync-manifest.json"
      provides: "Updated sync state through v1.20.5"
      contains: "v1.20.5"
  key_links:
    - from: "get-stuff-done/bin/gsd-tools.cjs"
      to: "auto-advance pipeline commands"
      via: "new gsd-tools commands added by upstream"
      pattern: "auto.advance\|cmdStateAdvance"
---

<objective>
Cherry-pick Batches 9-11 (v1.19.2..v1.20.5, 49 commits total). These batches add auto-advance pipeline, requirements tracking, and subagent discovery -- medium-risk feature additions with predictable conflict patterns.

Purpose: These three batches are grouped together because they share a consistent risk profile (MEDIUM) and conflict pattern (mostly Mechanical branding + some Semantic for new features). The Codex commits in Batch 11 are an add+revert pair that largely cancels. After this plan, the fork is current through v1.20.5 (the last tagged release), setting up the critical Batch 12 module split.

Output: 49 commits applied, fork current through v1.20.5, tests passing, approach comparison updated
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-upstream-sync-execution/18-RESEARCH.md
@.planning/phases/18-upstream-sync-execution/18-CONTEXT.md
@.planning/sync/branding-map.json
@.planning/phases/18-upstream-sync-execution/18-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick Batch 9 (v1.19.2..v1.20.0, 12 commits)</name>
  <files>(all files modified by cherry-picks), .planning/sync/sync-manifest.json</files>
  <action>
Cherry-pick all 12 commits from v1.19.2..v1.20.0 using the established `--no-commit` review workflow.

**Batch 9 overview (v1.19.2..v1.20.0) -- Auto-Advance batch:**
- Auto-advance flag chains phase execution across milestones
- `/gsd:health` command (new)
- `commit_docs` fix in complete-milestone
- `.gitkeep` in phase directories
- `quick --full` mode
- Various workflow improvements

Risk: MEDIUM -- new features that add to existing files, few conflicts expected beyond branding.

**Workflow per commit:**
1. `git cherry-pick --no-commit <hash>`
2. Resolve conflicts per 4-step triage model
3. Check for protected file deletions: `git diff --staged --diff-filter=D`
4. Commit: `git commit -m "cherry-pick(sync): <original message> (<short-hash>)"`

**After all 12 commits:**
1. Branding pass:
   ```bash
   grep -r "get-shit-done\|glittercowboy\|get-shit-done-cc" agents/ commands/ get-stuff-done/ hooks/ bin/ --include="*.js" --include="*.md" --include="*.json" --include="*.cjs" | grep -v "node_modules\|\.planning\|\.upstream\|upstream-sync\|upstream\.md\|PENDING"
   ```
2. Fix any upstream branding, commit if needed
3. Run `bun test` -- all tests must pass
4. Update sync manifest with 12 applied entries
5. Commit: `docs(18): update sync state after Batch 9`
  </action>
  <verify>
- `bun test` passes with 563+ tests, 0 failures
- No upstream branding in tracked files
- Sync manifest has 12 new entries
  </verify>
  <done>All 12 Batch 9 commits applied, auto-advance features adopted, branding clean, tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Cherry-pick Batches 10-11 (v1.20.0..v1.20.5, 25 commits)</name>
  <files>(all files modified by cherry-picks), .planning/sync/sync-manifest.json, .planning/phases/18-upstream-sync-execution/approach-comparison.md</files>
  <action>
Cherry-pick all 25 commits from v1.20.0..v1.20.5 using the established workflow. These span three minor versions (v1.20.1, v1.20.2, v1.20.3) plus the v1.20.4-v1.20.5 range.

**Batch 10 (v1.20.0..v1.20.3, 11 commits):**
- Auto-advance persistence (v1.20.1)
- Requirements tracking chain improvements (v1.20.2) -- strip brackets from requirement IDs, add requirements field to plans and summaries
- Requirements verification loop (v1.20.3) -- 3-source cross-reference, Gemini shell escaping

Risk: MEDIUM -- mostly additive changes to gsd-tools.cjs. Branding conflicts expected.

**Batch 11 (v1.20.3..v1.20.5, 14 commits):**
- executor ROADMAP/REQUIREMENTS updates (v1.20.4)
- Context-proxy orchestration flow
- Subagent CLAUDE.md/skills discovery (#671, #672)
- **Codex commits**: `db1d003` (add Codex prompt install) and `87c3873` (add Codex Task conversion), immediately reverted by `e820263` and `d55998b`. Cherry-pick all four in order -- the add+revert pair cancels out.
- Inline Task() syntax fix (#217)

Risk: MEDIUM -- the Codex add+revert pair cancels. The subagent discovery changes are additive.

**For the Codex add+revert pair:**
Cherry-pick in exact order: `87c3873` (add), `db1d003` (add), `e820263` (revert), `d55998b` (revert). The net effect is zero change -- these four commits cancel. If conflicts arise on the reverts, simply ensure the final state matches pre-Codex state.

**After all 25 commits:**
1. Branding pass (same grep command as previous tasks)
2. Verify no Codex artifacts remain (the reverts should have cleaned them):
   ```bash
   grep -rn "codex" agents/ commands/ get-stuff-done/ bin/ --include="*.md" --include="*.js" --include="*.cjs" --include="*.json" -i | grep -v "node_modules\|\.planning\|\.upstream\|CHANGELOG\|approach-comparison"
   ```
   Should return 0 or only legitimate references (not Codex-specific install logic).
3. Run `bun test` -- all tests must pass
4. Update sync manifest with 25 applied entries
5. Update approach comparison with any divergences found (requirements tracking chain is worth documenting if fork had different approach)
6. Commit: `docs(18): update sync state after Batches 10-11 (current through v1.20.5)`
  </action>
  <verify>
- `bun test` passes with 563+ tests, 0 failures
- No upstream branding in tracked files
- No stale Codex artifacts
- Sync manifest has 25 new entries (total ~83 with previous batches)
- `git log --oneline v1.20.5 | head -1` exists as a known tag in the cherry-pick history
  </verify>
  <done>All 25 Batch 10-11 commits applied, fork current through v1.20.5, Codex add+revert cancelled cleanly, requirements tracking adopted, branding clean, tests passing</done>
</task>

</tasks>

<verification>
- Batches 9-11 fully applied (49 commits total this plan)
- Fork codebase is current through v1.20.5 (last tagged upstream release)
- Auto-advance, requirements tracking, subagent discovery features adopted
- No upstream branding leaked through
- Test suite passes after each batch
- Sync manifest up to date
</verification>

<success_criteria>
- sync/v1.20.5 branch has 49 more cherry-pick commits from this plan
- bun test: 563+ pass, 0 fail
- sync-manifest.json: ~83 total applied entries (30 + 28 + 12 + 25, accounting for skipped merge commits)
- No Codex artifacts remain (reverts cleaned up)
- Zero upstream branding in tracked files
</success_criteria>

<output>
After completion, create `.planning/phases/18-upstream-sync-execution/18-03-SUMMARY.md`
</output>
