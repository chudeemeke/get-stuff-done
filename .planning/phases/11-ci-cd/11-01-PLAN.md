---
phase: 11-ci-cd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tests/helpers/mock-fs.js
  - tests/helpers/mock-process.js
  - tests/helpers/mock-child-process.js
  - tests/helpers/index.js
  - tests/fixtures/config/valid-config.json
  - tests/fixtures/config/invalid-config.json
  - tests/fixtures/config/minimal-config.json
  - tests/fixtures/planning/ROADMAP.md
  - tests/fixtures/planning/phases/01-foundation/01-01-SUMMARY.md
  - tests/gsd-tools.test.js
  - get-stuff-done/bin/gsd-tools.test.js
autonomous: true

must_haves:
  truths:
    - "bun test runs and discovers tests in the tests/ directory"
    - "bun test --coverage produces coverage metrics for statements, branches, functions, and lines"
    - "Test helper utilities provide consistent mocking for fs, process, and child_process across all test files"
    - "Existing gsd-tools.test.js passes under bun test runner after migration from Node.js test runner"
    - "Shared fixtures provide reusable test data without duplication across test files"
  artifacts:
    - path: "package.json"
      provides: "test and test:coverage scripts using bun test"
      contains: "bun test"
    - path: "tests/helpers/index.js"
      provides: "Aggregated test helper exports"
      contains: "mock"
    - path: "tests/helpers/mock-fs.js"
      provides: "Filesystem mocking utilities for temp dirs, fake config files"
      min_lines: 30
    - path: "tests/helpers/mock-process.js"
      provides: "Process env/platform mocking with cleanup"
      min_lines: 20
    - path: "tests/helpers/mock-child-process.js"
      provides: "Child process mock for execSync/execFileSync/spawn"
      min_lines: 30
    - path: "tests/gsd-tools.test.js"
      provides: "Migrated gsd-tools tests using bun:test APIs"
      contains: "describe"
    - path: "tests/fixtures/config/valid-config.json"
      provides: "Valid GSD config for test assertions"
      contains: "version"
  key_links:
    - from: "package.json"
      to: "tests/"
      via: "bun test script"
      pattern: "bun test"
    - from: "tests/helpers/index.js"
      to: "tests/helpers/mock-fs.js"
      via: "re-export"
      pattern: "require.*mock-fs"
    - from: "tests/gsd-tools.test.js"
      to: "get-stuff-done/bin/gsd-tools.js"
      via: "TOOLS_PATH constant"
      pattern: "gsd-tools\\.js"
---

<objective>
Set up bun test infrastructure with shared helpers, fixtures, and migrate the existing gsd-tools test suite from Node.js test runner to bun:test.

Purpose: Foundation for all subsequent test plans. Without test infrastructure and working helpers, Plans 02-04 cannot write tests. Migrating the existing 599-line test file proves bun test works with the codebase.

Output: Working test runner, shared mocking utilities, fixture data, and migrated gsd-tools tests passing under bun.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ci-cd/11-CONTEXT.md
@package.json
@get-stuff-done/bin/gsd-tools.test.js
@src/config/ConfigSchema.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure bun test and create shared test helpers</name>
  <files>
    package.json
    tests/helpers/mock-fs.js
    tests/helpers/mock-process.js
    tests/helpers/mock-child-process.js
    tests/helpers/index.js
    tests/fixtures/config/valid-config.json
    tests/fixtures/config/invalid-config.json
    tests/fixtures/config/minimal-config.json
    tests/fixtures/planning/ROADMAP.md
    tests/fixtures/planning/phases/01-foundation/01-01-SUMMARY.md
  </files>
  <action>
    1. Update package.json to add test scripts:
       - "test": "bun test"
       - "test:coverage": "bun test --coverage"
       Do NOT add bun as a devDependency (it is the runtime, not a package).

    2. Create tests/helpers/mock-fs.js:
       - createTempDir(): Creates os.tmpdir()-based temp directory, returns path and cleanup function
       - createTempFile(dir, name, content): Writes a file into a temp dir
       - createMockPlanningDir(dir): Scaffolds a .planning/ structure with phases/ subdirectory
       - createMockConfig(dir, overrides): Writes a valid config.json with optional overrides
       - All functions return cleanup functions for use in afterEach

    3. Create tests/helpers/mock-process.js:
       - mockEnv(overrides): Replaces process.env keys, returns restore function
       - mockPlatform(platform): Replaces process.platform, returns restore function
       - mockCwd(dir): Replaces process.cwd, returns restore function
       - Each mock function MUST return a restore function. Use afterEach to call restore.
       - IMPORTANT: process.platform is read-only in some runtimes. Use Object.defineProperty with configurable:true.

    4. Create tests/helpers/mock-child-process.js:
       - MockExecSync class: Records calls, returns configurable responses, can throw configurable errors
       - mockExecSync(module, responses): Patches a module's execSync, returns restore function
       - Responses keyed by command substring match (e.g., "git log" -> returns specific output)
       - Include a default "command not mocked" error for unmocked commands

    5. Create tests/helpers/index.js:
       - Re-exports all helpers from mock-fs, mock-process, mock-child-process
       - Single import point: const { createTempDir, mockEnv, ... } = require('./helpers')

    6. Create test fixtures:
       - tests/fixtures/config/valid-config.json: Full valid GSD config matching ConfigSchema (version:1, all sections)
       - tests/fixtures/config/invalid-config.json: Config with version:99 and unknown fields (for validation error testing)
       - tests/fixtures/config/minimal-config.json: Just { "version": 1 } (for defaults testing)
       - tests/fixtures/planning/ROADMAP.md: Minimal roadmap with 2 phases (for gsd-tools testing)
       - tests/fixtures/planning/phases/01-foundation/01-01-SUMMARY.md: Minimal SUMMARY with YAML frontmatter (for history-digest testing)

    All helpers use CommonJS (require/module.exports) matching the project convention.
  </action>
  <verify>
    Run: bun test --help (confirms bun test is available)
    Run: node -e "const h = require('./tests/helpers'); console.log(Object.keys(h))" (confirms exports work)
    Check package.json has "test" and "test:coverage" scripts.
  </verify>
  <done>
    bun test command is configured. Test helpers provide createTempDir, mockEnv, mockPlatform, MockExecSync. Fixtures directory has valid/invalid/minimal configs and planning structure. All helpers are importable from tests/helpers/index.js.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate gsd-tools.test.js from Node.js test runner to bun:test</name>
  <files>
    tests/gsd-tools.test.js
    get-stuff-done/bin/gsd-tools.test.js
  </files>
  <action>
    1. Copy get-stuff-done/bin/gsd-tools.test.js to tests/gsd-tools.test.js

    2. Migrate the test file from Node.js test runner to bun:test:
       - Replace: const { test, describe, beforeEach, afterEach } = require('node:test')
         With: const { test, describe, beforeEach, afterEach, expect } = require('bun:test')
       - Replace all assert.ok(value, message) with expect(value).toBeTruthy()
       - Replace all assert.strictEqual(a, b) with expect(a).toBe(b)
       - Replace all assert.deepStrictEqual(a, b) with expect(a).toEqual(b)
       - Replace assert.ok(result.success, ...) with expect(result.success).toBe(true)
       - Remove: const assert = require('node:assert')
       - Keep the runGsdTools helper function, createTempProject, and cleanup functions as-is (they use fs/path/execSync directly, which is fine for CLI integration tests)

    3. Update the TOOLS_PATH constant to point to the correct relative location:
       - Old: path.join(__dirname, 'gsd-tools.js')
       - New: path.join(__dirname, '..', 'get-stuff-done', 'bin', 'gsd-tools.js')

    4. Delete the old test file: get-stuff-done/bin/gsd-tools.test.js
       (Tests now live in tests/ directory per CONTEXT.md decision)

    5. Verify all 19 existing tests pass under bun test. The tests are:
       - 6 history-digest tests
       - 5 phases list tests
       - 5 roadmap get-phase tests
       - 3 phase next-decimal tests

    Do NOT rewrite test logic or add new tests. This is a pure migration.
  </action>
  <verify>
    Run: bun test tests/gsd-tools.test.js
    Expected: All 19 tests pass with 0 failures.
    Verify old file deleted: ls get-stuff-done/bin/gsd-tools.test.js should fail.
  </verify>
  <done>
    All 19 existing gsd-tools tests pass under bun:test. Test file relocated to tests/gsd-tools.test.js. Old file removed. No test logic changed -- pure runner migration.
  </done>
</task>

</tasks>

<verification>
- bun test discovers and runs tests in tests/ directory
- bun test tests/gsd-tools.test.js passes all 19 tests
- Test helpers are importable: node -e "require('./tests/helpers')"
- Fixture files exist and are valid JSON where applicable
- package.json contains "test" and "test:coverage" scripts
</verification>

<success_criteria>
- bun test passes with 19/19 tests
- Test infrastructure (helpers, fixtures) ready for Plans 02-04
- No Node.js test runner references remain in active test files
</success_criteria>

<output>
After completion, create `.planning/phases/11-ci-cd/11-01-SUMMARY.md`
</output>
