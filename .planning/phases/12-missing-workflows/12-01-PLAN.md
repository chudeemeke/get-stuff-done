---
phase: 12-missing-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-stuff-done/workflows/add-phase.md
  - get-stuff-done/workflows/add-todo.md
  - get-stuff-done/workflows/audit-milestone.md
  - get-stuff-done/workflows/check-todos.md
  - get-stuff-done/workflows/help.md
  - get-stuff-done/workflows/insert-phase.md
  - get-stuff-done/workflows/new-milestone.md
  - get-stuff-done/workflows/new-project.md
  - get-stuff-done/workflows/pause-work.md
  - get-stuff-done/workflows/plan-milestone-gaps.md
  - get-stuff-done/workflows/plan-phase.md
  - get-stuff-done/workflows/progress.md
  - get-stuff-done/workflows/quick.md
  - get-stuff-done/workflows/remove-phase.md
  - get-stuff-done/workflows/set-profile.md
  - get-stuff-done/workflows/settings.md
autonomous: true

must_haves:
  truths:
    - "All 16 missing workflow files exist in get-stuff-done/workflows/"
    - "Each workflow matches its corresponding command's @ reference"
    - "Full test suite passes with no regressions"
  artifacts:
    - path: "get-stuff-done/workflows/add-phase.md"
      provides: "Workflow for adding integer phase to end of roadmap"
    - path: "get-stuff-done/workflows/add-todo.md"
      provides: "Workflow for capturing ideas/tasks as structured todos"
    - path: "get-stuff-done/workflows/audit-milestone.md"
      provides: "Workflow for verifying milestone definition of done"
    - path: "get-stuff-done/workflows/check-todos.md"
      provides: "Workflow for listing pending todos, select and route"
    - path: "get-stuff-done/workflows/help.md"
      provides: "Workflow for outputting complete GSD command reference"
    - path: "get-stuff-done/workflows/insert-phase.md"
      provides: "Workflow for inserting decimal phase for urgent work"
    - path: "get-stuff-done/workflows/new-milestone.md"
      provides: "Workflow for starting new milestone cycle"
    - path: "get-stuff-done/workflows/new-project.md"
      provides: "Workflow for initializing new project with deep context"
    - path: "get-stuff-done/workflows/pause-work.md"
      provides: "Workflow for creating handoff file preserving work state"
    - path: "get-stuff-done/workflows/plan-milestone-gaps.md"
      provides: "Workflow for creating phases to close audit gaps"
    - path: "get-stuff-done/workflows/plan-phase.md"
      provides: "Workflow for creating PLAN.md with research and verification"
    - path: "get-stuff-done/workflows/progress.md"
      provides: "Workflow for checking progress, summarize, route to next action"
    - path: "get-stuff-done/workflows/quick.md"
      provides: "Workflow for executing quick task with GSD guarantees"
    - path: "get-stuff-done/workflows/remove-phase.md"
      provides: "Workflow for removing future phase and renumbering"
    - path: "get-stuff-done/workflows/set-profile.md"
      provides: "Workflow for switching model profile"
    - path: "get-stuff-done/workflows/settings.md"
      provides: "Workflow for configuring GSD workflow toggles"
---

<objective>
Create all 16 missing workflow files referenced by GSD commands via `@~/.claude/get-stuff-done/workflows/*.md`.

Purpose: Commands contain inline instructions as fallback, but the `@` references to workflow files silently fail because the files don't exist. This means the detailed orchestration logic is never loaded. Creating these files makes all 25 commands fully functional.

Output: 16 new workflow files in `get-stuff-done/workflows/`.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-missing-workflows/SCOPE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create simple-tier workflow files (7 files)</name>
  <files>
    get-stuff-done/workflows/help.md
    get-stuff-done/workflows/set-profile.md
    get-stuff-done/workflows/settings.md
    get-stuff-done/workflows/progress.md
    get-stuff-done/workflows/add-phase.md
    get-stuff-done/workflows/remove-phase.md
    get-stuff-done/workflows/insert-phase.md
  </files>
  <action>
Create workflow files for commands with thin orchestration needs. For each:
1. Read the corresponding command file in commands/gsd/
2. Extract the inline instructions and @ reference path
3. Create workflow file following patterns from existing workflows (execute-phase.md, verify-work.md)
4. Include steps, gates, and error handling appropriate to complexity

Simple workflows: help, set-profile, settings, progress, add-phase, remove-phase, insert-phase.
  </action>
  <verify>
Confirm all 7 files exist and contain valid workflow structure.

```bash
ls -la get-stuff-done/workflows/{help,set-profile,settings,progress,add-phase,remove-phase,insert-phase}.md
```
  </verify>
  <done>
All 7 simple-tier workflow files created with appropriate orchestration logic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create medium-tier workflow files (4 files)</name>
  <files>
    get-stuff-done/workflows/add-todo.md
    get-stuff-done/workflows/check-todos.md
    get-stuff-done/workflows/pause-work.md
    get-stuff-done/workflows/quick.md
  </files>
  <action>
Create workflow files for commands with moderate orchestration needs. For each:
1. Read the corresponding command file in commands/gsd/
2. Extract the inline instructions and @ reference path
3. Create workflow file with multi-step orchestration, state management, and routing logic

Medium workflows: add-todo, check-todos, pause-work, quick.
  </action>
  <verify>
Confirm all 4 files exist and contain valid workflow structure.

```bash
ls -la get-stuff-done/workflows/{add-todo,check-todos,pause-work,quick}.md
```
  </verify>
  <done>
All 4 medium-tier workflow files created with orchestration and routing logic.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create complex-tier workflow files (5 files)</name>
  <files>
    get-stuff-done/workflows/plan-phase.md
    get-stuff-done/workflows/new-project.md
    get-stuff-done/workflows/new-milestone.md
    get-stuff-done/workflows/audit-milestone.md
    get-stuff-done/workflows/plan-milestone-gaps.md
  </files>
  <action>
Create workflow files for commands with multi-step orchestration and subagent spawning. For each:
1. Read the corresponding command file in commands/gsd/
2. Extract the inline instructions and @ reference path
3. Create workflow file with full orchestration: subagent delegation, verification loops, state gates

Complex workflows: plan-phase, new-project, new-milestone, audit-milestone, plan-milestone-gaps.
  </action>
  <verify>
Confirm all 5 files exist and contain valid workflow structure.

```bash
ls -la get-stuff-done/workflows/{plan-phase,new-project,new-milestone,audit-milestone,plan-milestone-gaps}.md
```
  </verify>
  <done>
All 5 complex-tier workflow files created with subagent orchestration and verification loops.
  </done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:

```bash
bun test
```

Verify all 16 new workflow files exist:

```bash
for f in add-phase add-todo audit-milestone check-todos help insert-phase new-milestone new-project pause-work plan-milestone-gaps plan-phase progress quick remove-phase set-profile settings; do
  test -f "get-stuff-done/workflows/${f}.md" && echo "OK: ${f}.md" || echo "MISSING: ${f}.md"
done
```
</verification>

<success_criteria>
Phase complete when:
- [ ] All 16 workflow files exist in get-stuff-done/workflows/
- [ ] Each workflow matches its command's @ reference path
- [ ] Each workflow follows established patterns from existing workflows
- [ ] Full test suite passes (355 tests, 0 failures)
- [ ] Released as v2.1.3 and published to npm
</success_criteria>

<output>
After completion, create `.planning/phases/12-missing-workflows/12-01-SUMMARY.md`
</output>
