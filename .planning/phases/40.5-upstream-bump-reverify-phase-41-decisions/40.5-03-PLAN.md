---
phase: 40.5
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md
autonomous: true
requirements: ["UPGRADE-10"]
estimated_minutes: 40
gap_closure: false
must_haves:
  truths:
    - "bun run compose exits 0 with no COMP-08 collision errors against the v1.38.5 tree"
    - "Every overlay/* path is cross-checked against node_modules/get-shit-done-cc paths in v1.38.5"
    - "RESEARCH.md §1 prediction (zero new path-level collisions) is verified empirically and documented"
    - "PR #1859 review.md audit confirms no fork-side review.md exists (no overlay/workflows/review.md, no overrides/workflows/review.md), and composed dist/.../review.md matches upstream's merged 2026-04-10 version"
    - "40.5-COLLISIONS.md cites criterion #3 evidence per D-13"
    - "If predictions hold (zero collisions), the non-event is documented EXPLICITLY — no synthesis of collisions that aren't there"
  artifacts:
    - path: ".planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md"
      provides: "Per-overlay collision + #1859 audit + keep/move/remove decision (D-13)"
      contains: "#1859"
      min_lines: 30
  key_links:
    - from: "40.5-COLLISIONS.md"
      to: "40.5-VERIFICATION.md (Wave 5)"
      via: "Wave 5 cites this file as criterion #3 evidence"
      pattern: "40\\.5-COLLISIONS\\.md"
    - from: "40.5-COLLISIONS.md #1859 section"
      to: "node_modules/get-shit-done-cc/get-shit-done/workflows/review.md"
      via: "diff confirmation that fork composes upstream verbatim"
      pattern: "review\\.md"
---

<objective>
Wave 3 of Phase 40.5: Compose collision audit against v1.38.5. Per RESEARCH.md §1
(definitive cross-check of every fork `overlay/*` path against the v1.38.2 npm tarball
— equivalent applies to v1.38.5 as no significant overlay-vs-upstream path additions in
1.38.2..1.38.5 per RESEARCH.md §3 / §5), the prediction is **ZERO new path-level
collisions**. This wave is a CONFIRMATION step, not a reconciliation.

Per D-09 (40.5 methodology, distinct from Phase 41 D-09): handle PR #1859 (review.md
per-CLI model selection, merged upstream 2026-04-10, present from v1.35.0 through v1.38.5).
Per RESEARCH.md §5 the fork has NO `overlay/workflows/review.md` and NO
`overrides/workflows/review.md`. Audit reduces to "confirm no fork divergence exists."

Per D-08: default action on overlay/upstream collision is `compare semantics, then decide`
— equivalent → remove overlay; meaningfully divergent → move to overrides/ with REASON.md
+ SHA snapshot. **If RESEARCH.md prediction holds (zero collisions), the audit documents
the non-event explicitly — do NOT synthesize collisions that aren't there.**

Satisfies success criterion #3 of Phase 40.5: compose-collision audit complete; any files
upstream now ships natively which we had as overlays are either moved to `overrides/` (with
REASON.md + SHA snapshot) or removed if no longer needed.

Purpose: Empirical confirmation via `bun run compose` exit 0 (COMP-08 detector built into
compose.js) plus explicit overlay-path cross-check vs v1.38.5 tarball plus #1859
non-event audit. Document the all-clear (or surface any new collision RESEARCH.md missed).

Output: 40.5-COLLISIONS.md (per D-13). Cited inline by Wave 5 VERIFICATION.md as
criterion #3 evidence.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CONTEXT.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-RESEARCH.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VALIDATION.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-02-SUMMARY.md
@scripts/compose.js
</context>

<tasks>

<task id="03-01" type="auto">
  <name>Task 03-01: Run bun run compose against v1.38.5; capture COMP-08 result</name>
  <files>.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md</files>
  <read_first>
    - scripts/compose.js (full file — confirm COMP-08 collision detection logic; understand exit-code semantics)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CONTEXT.md (D-08, D-09, D-13)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-RESEARCH.md §1 (the path-level matrix predicting zero collisions)
  </read_first>
  <action>
    Run `bun run compose` from the repo root with the v1.38.5 tree on disk (post-Wave-1 + post-Wave-2 state). Capture full stdout + stderr + exit code.

    Per RESEARCH.md §1 prediction: exit code 0 (no COMP-08 collision errors). If exit code is non-zero with a COMP-08 message, that contradicts RESEARCH.md and represents a real new collision — HALT and surface to user; the planner expected zero new collisions, so a non-zero exit needs investigation before proceeding.

    Initialize `.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md` per D-13 with this structure (placeholder sections will be filled in Tasks 03-02/03-03/03-04):

    ```
    # Phase 40.5 Collisions Audit (Criterion #3 Evidence)

    Wave: 3
    Generated: <YYYY-MM-DD HH:MM>
    Upstream version under test: get-shit-done-cc@1.38.5
    bun run compose exit code: <0 or 1>

    ## bun run compose output (verbatim)

    <full stdout + stderr from `bun run compose`, quoted block>

    ## Static path cross-check (overlay/* vs v1.38.5 npm tarball)

    (Populated in Task 03-02)

    ## PR #1859 review.md audit

    (Populated in Task 03-03)

    ## Disposition summary

    (Populated in Task 03-04)
    ```
  </action>
  <acceptance_criteria>
    - test -f .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md returns success
    - File contains the literal string `bun run compose exit code:`
    - File contains the literal string `1.38.5`
    - File contains four section headers: `bun run compose output`, `Static path cross-check`, `PR #1859 review.md audit`, `Disposition summary`
    - bun run compose exit code is captured in the file (either 0 = expected per RESEARCH.md §1, or non-zero with HALT signaled to user)
  </acceptance_criteria>
  <verify>
    <automated>bun run compose; echo "EXIT=$?"; test -f .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md &amp;&amp; grep -q '1.38.5' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md</automated>
  </verify>
  <done>
    40.5-COLLISIONS.md exists with header + verbatim compose output + 3 placeholder sections for static cross-check, #1859 audit, and disposition summary.
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="03-02" type="auto">
  <name>Task 03-02: Static path cross-check overlay/* vs v1.38.5 tarball</name>
  <files>.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md</files>
  <read_first>
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-RESEARCH.md §1 (the path-level matrix and "Note for the planner" about overlay/get-shit-done symlink)
    - overlay/ directory listing (find overlay -type f)
    - node_modules/get-shit-done-cc/ directory listing
  </read_first>
  <action>
    Cross-check every fork `overlay/*` path (excluding the symlink `overlay/get-shit-done` per RESEARCH.md §1 note) against the v1.38.5 upstream tree at `node_modules/get-shit-done-cc/`.

    Procedure:

    1. Enumerate fork overlay files:
       `find overlay -type f \( -name '*.md' -o -name '*.cjs' -o -name '*.js' -o -name '*.json' -o -name '*.sh' \) | grep -v '^overlay/get-shit-done/' | sort > /tmp/overlay-files.txt`
    2. For each overlay path, derive the corresponding upstream path the compose pipeline targets. Per scripts/compose.js (read it for the exact mapping logic — typical pattern: `overlay/<rel>` -> `dist/get-shit-done/<rel>` or `dist/.claude/<rel>` depending on type). Two checks per overlay file:
       - (a) Does upstream v1.38.5 ship a file at the SAME composed-output path? (test against `node_modules/get-shit-done-cc/<derived-path>`)
       - (b) If yes, is it a path-level COLLISION (overlay would shadow upstream's same-path file) or is it ADDITIVE (overlay adds, upstream has different file at different path)?
    3. Build the cross-check table in 40.5-COLLISIONS.md under the `Static path cross-check` section. Columns: Overlay file | Derived upstream path | Upstream v1.38.5 ships? | Collision?. One row per overlay file. Symlink `overlay/get-shit-done` excluded with explicit note per RESEARCH.md §1.
    4. Final line in section: `Result: <N> collisions found across <M> overlay files inspected.`
    5. Per RESEARCH.md §1 the expected outcome is N=0. If you find N >= 1, HALT and surface — that's a real new collision RESEARCH.md missed.
    6. If N=0, document the non-event explicitly per must_have: a `Result: ZERO new path-level collisions` subsection confirming RESEARCH.md §1 prediction.
  </action>
  <acceptance_criteria>
    - grep -c 'Static path cross-check' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md returns >= 1
    - The cross-check table has at least 20 rows (per RESEARCH.md §1 the overlay file count is ~21)
    - File contains the literal string `Result:` followed by either `ZERO new path-level collisions` (expected) or `<N> collisions found` with N >= 1 (which HALTS the task and surfaces)
    - The symlink `overlay/get-shit-done` is explicitly excluded with a note (per RESEARCH.md §1)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Static path cross-check' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md &amp;&amp; grep -q 'Result:' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md</automated>
  </verify>
  <done>
    Static path cross-check section is populated. Either zero collisions documented as a non-event (expected) or specific collisions enumerated for follow-up via D-08 (compare semantics, then decide).
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="03-03" type="auto">
  <name>Task 03-03: PR #1859 review.md audit (confirm no fork divergence)</name>
  <files>.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md</files>
  <read_first>
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-RESEARCH.md §5 (the audit-as-confirmation framing; verified non-event)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CONTEXT.md (D-09 — methodology audit + remove if equivalent)
  </read_first>
  <action>
    Confirm RESEARCH.md §5: PR #1859 (review.md per-CLI model selection, merged upstream 2026-04-10 in v1.35.0+, present in v1.38.5). The fork has no fork-side review.md to reconcile.

    Verification commands:
    1. `find overlay -name 'review*.md' 2>/dev/null` — Expected: empty output.
    2. `find overrides -name 'review*.md' 2>/dev/null` — Expected: empty output.
    3. `ls dist/get-shit-done/workflows/review.md 2>/dev/null` and `ls node_modules/get-shit-done-cc/get-shit-done/workflows/review.md 2>/dev/null`, then `diff dist/get-shit-done/workflows/review.md node_modules/get-shit-done-cc/get-shit-done/workflows/review.md` — Expected: diff returns empty (composition is verbatim per RESEARCH.md §5). If dist/ doesn't exist or doesn't contain workflows/review.md, surface to user.

    4. Append the `PR #1859 review.md audit` section to 40.5-COLLISIONS.md per RESEARCH.md §5 with the verified results inline (commands run + outputs). Conclude with `NO FORK DIVERGENCE` and explain that the fork composes upstream's review.md verbatim into dist/, that PR #1859's per-CLI model selection feature lands in the fork automatically through the compose pipeline, and that no action is required per D-09.
  </action>
  <acceptance_criteria>
    - grep -c 'PR #1859' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md returns >= 1
    - find overlay -name 'review*.md' 2>/dev/null | wc -l returns 0 (fork has no overlay review.md — confirmed in 40.5-COLLISIONS.md)
    - find overrides -name 'review*.md' 2>/dev/null | wc -l returns 0 (fork has no override review.md)
    - File contains the literal string `NO FORK DIVERGENCE` (or equivalent verified conclusion)
    - If `dist/get-shit-done/workflows/review.md` exists, the diff vs node_modules/get-shit-done-cc version is empty (verbatim composition confirmed)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'PR #1859' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md &amp;&amp; test "$(find overlay overrides -name 'review*.md' 2&gt;/dev/null | wc -l)" = "0"</automated>
  </verify>
  <done>
    PR #1859 audit section populated with the verified non-event conclusion. Fork has no fork-side review.md, dist composes upstream verbatim, no action required.
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="03-04" type="auto">
  <name>Task 03-04: Disposition summary + Wave 3 atomic commit</name>
  <files>.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md</files>
  <read_first>
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md (post-Tasks-03-01/02/03 state)
  </read_first>
  <action>
    1. Append `Disposition summary` section to 40.5-COLLISIONS.md with table columns Source | Finding | Disposition. Three rows: bun run compose (COMP-08), Static path cross-check (Task 03-02), PR #1859 review.md audit (Task 03-03). Each row records the finding + disposition from prior tasks.

    2. Append a `Wave 3 conclusion` paragraph: state explicitly whether new collisions were found. If zero (expected per RESEARCH.md §1) say so explicitly — bump is path-level clean, no overlay-to-override promotions needed, no overlay removals needed. If non-zero, enumerate per-file decisions per D-08.

    3. Atomic commit:

       ```bash
       git add .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md
       git commit -m "$(cat <<'EOF'
docs(40.5): record compose-collision audit for v1.38.5

Phase 40.5 Wave 3 — compose-collision audit (UPGRADE-10, criterion #3).

bun run compose exit: <code>. Static path cross-check across all <M>
overlay files vs v1.38.5 tree: <N> collisions. PR #1859 review.md audit:
no fork-side divergence (fork composes upstream verbatim).

Confirms RESEARCH.md §1 prediction (zero new path-level collisions in
1.34.2 -> 1.38.5 bump).

Evidence: 40.5-COLLISIONS.md (cited by Wave 5 VERIFICATION.md per D-13).

Refs: UPGRADE-10
EOF
)"
       ```

    No `--no-verify`. No AI attribution.
  </action>
  <acceptance_criteria>
    - grep -c 'Disposition summary' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md returns >= 1
    - grep -c 'Wave 3 conclusion' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md returns >= 1
    - git log -1 --format='%s' | grep -E '^docs\(40\.5\): record compose-collision audit' matches
    - git log -1 --stat shows exactly 1 file: 40.5-COLLISIONS.md
    - git log -1 --format='%B' | grep -ci 'co-authored-by\|generated with claude' returns 0
    - git status --porcelain returns empty (clean working tree)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Disposition summary' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md &amp;&amp; grep -q 'Wave 3 conclusion' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md &amp;&amp; test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>
    Wave 3 commit landed atomically (1 file: 40.5-COLLISIONS.md). All 4 sections populated. Working tree clean.
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

</tasks>

<verification>
Wave 3 success criteria (bound to Phase 40.5 success criterion #3):

1. `bun run compose` exits 0 against the v1.38.5 tree (no COMP-08 collisions)
2. 40.5-COLLISIONS.md exists with all 4 sections populated (compose output, static cross-check, PR #1859 audit, disposition summary)
3. PR #1859 audit confirms NO FORK DIVERGENCE (no fork-side review.md, dist composes verbatim)
4. RESEARCH.md §1 zero-collisions prediction confirmed empirically (or surfaced as miss if non-zero)

Verification commands:

```bash
bun run compose; echo "EXIT=$?"
grep -c '^## ' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md  # expect >= 4
find overlay overrides -name 'review*.md' 2>/dev/null  # expect empty
git log -1 --stat
```
</verification>

<success_criteria>
- [ ] bun run compose exits 0 (no COMP-08 collision errors)
- [ ] Every overlay/* path is cross-checked against v1.38.5 paths and result documented
- [ ] PR #1859 audit conclusion: NO FORK DIVERGENCE (verified)
- [ ] 40.5-COLLISIONS.md has all 4 expected sections
- [ ] Wave 3 commit landed atomically (1 file)
- [ ] If zero collisions found (expected per RESEARCH.md §1): non-event explicitly documented
- [ ] Wave 4 (40.5-04-PLAN.md) can proceed
</success_criteria>

<output>
After completion, create `.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-03-SUMMARY.md` with:

- Wave 3 commit SHA
- bun run compose exit code (must be 0)
- Static path cross-check result (N collisions across M overlay files; expected N=0, M~21)
- PR #1859 audit conclusion (verbatim line: NO FORK DIVERGENCE — fork composes upstream's review.md verbatim)
- Any anomalies vs RESEARCH.md §1 prediction
</output>
