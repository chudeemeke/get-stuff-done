---
phase: 18-upstream-sync-execution
plan: 04
type: execute
wave: 4
depends_on: ["18-03"]
files_modified:
  - get-stuff-done/bin/gsd-tools.cjs
  - get-stuff-done/bin/lib/core.cjs
  - get-stuff-done/bin/lib/state.cjs
  - get-stuff-done/bin/lib/phase.cjs
  - get-stuff-done/bin/lib/roadmap.cjs
  - get-stuff-done/bin/lib/verify.cjs
  - get-stuff-done/bin/lib/config.cjs
  - get-stuff-done/bin/lib/template.cjs
  - get-stuff-done/bin/lib/milestone.cjs
  - get-stuff-done/bin/lib/commands.cjs
  - get-stuff-done/bin/lib/init.cjs
  - get-stuff-done/bin/lib/frontmatter.cjs
  - hooks/gsd-statusline.js
  - .planning/sync/sync-manifest.json
  - .planning/phases/18-upstream-sync-execution/approach-comparison.md
autonomous: true

must_haves:
  truths:
    - "All 11 Batch 12 commits (v1.20.5..upstream/main HEAD) are cherry-picked"
    - "gsd-tools.cjs is now a thin router (~553 lines) importing from 11 lib/*.cjs modules"
    - "The 11 domain modules exist under get-stuff-done/bin/lib/ with .cjs extension"
    - "Fork's validation import (validateGitSHA, validateBranchName, etc.) is preserved in the router or core.cjs"
    - "Fork's statusline (172 lines, 4-stage indicator) is preserved AND bridge file write is added for context-monitor hook"
    - "bun test runs both fork .test.js files and upstream .test.cjs files (or .test.cjs files are configured to run separately)"
    - "Full test suite passes"
  artifacts:
    - path: "get-stuff-done/bin/gsd-tools.cjs"
      provides: "Thin CLI router dispatching to 11 domain modules"
      min_lines: 500
      contains: "require('./lib/core.cjs')"
    - path: "get-stuff-done/bin/lib/core.cjs"
      provides: "Shared utilities, constants, git helpers"
      min_lines: 350
    - path: "get-stuff-done/bin/lib/state.cjs"
      provides: "STATE.md operations + progression engine"
      min_lines: 450
    - path: "get-stuff-done/bin/lib/phase.cjs"
      provides: "Phase CRUD, query, lifecycle"
      min_lines: 800
    - path: "get-stuff-done/bin/lib/verify.cjs"
      provides: "Verification suite + consistency validation"
      min_lines: 700
    - path: "get-stuff-done/bin/lib/init.cjs"
      provides: "Compound init commands"
      min_lines: 600
    - path: "get-stuff-done/bin/lib/commands.cjs"
      provides: "Standalone utility commands"
      min_lines: 500
    - path: "hooks/gsd-statusline.js"
      provides: "Fork's 4-stage context indicator with bridge file write"
      min_lines: 172
  key_links:
    - from: "get-stuff-done/bin/gsd-tools.cjs"
      to: "get-stuff-done/bin/lib/*.cjs"
      via: "require('./lib/<module>.cjs')"
      pattern: "require\\('./lib/"
    - from: "get-stuff-done/bin/gsd-tools.cjs"
      to: "../../src/validation"
      via: "fork validation import"
      pattern: "require\\('\\.\\..*validation"
    - from: "hooks/gsd-statusline.js"
      to: "bridge file for context-monitor"
      via: "fs.writeFileSync bridge JSON"
      pattern: "claude-ctx-.*\\.json"
---

<objective>
Cherry-pick Batch 12 (v1.20.5..upstream/main HEAD, 11 commits) -- the critical module split that transforms gsd-tools.cjs from a 5324-line monolith into a thin router + 11 domain modules. This is the highest-risk batch of the entire sync.

Purpose: This batch delivers the primary architectural goal of Phase 18 (SYNC-II-02): adopting upstream's modular gsd-tools architecture. The monolithic gsd-tools.cjs (~4953 lines after Batch 8 growth) becomes a ~553-line router dispatching to 11 CJS modules under bin/lib/. The fork's validation import must be preserved in the router. The fork's statusline must be augmented with bridge file write for the new context-monitor hook. After this batch, the fork has the modular architecture.

Output: 11 modules under get-stuff-done/bin/lib/, thin router, fork customizations preserved, tests passing
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-upstream-sync-execution/18-RESEARCH.md
@.planning/phases/18-upstream-sync-execution/18-CONTEXT.md
@.planning/sync/branding-map.json
@.planning/phases/18-upstream-sync-execution/18-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick Batch 12 module split commits</name>
  <files>get-stuff-done/bin/gsd-tools.cjs, get-stuff-done/bin/lib/*.cjs, hooks/gsd-statusline.js, hooks/gsd-context-monitor.js</files>
  <action>
Cherry-pick all 11 commits from v1.20.5..upstream/main HEAD. The critical commits are:

1. `fa2e156` -- Split gsd-tools.test.cjs into 7 domain test files (creates tests/*.test.cjs + tests/helpers.cjs)
2. `c67ab75` -- Split gsd-tools.cjs into 11 domain modules under bin/lib/ (THE module split)
3. `ebfc17a` -- Merge commit for the split PR (skip if merge commit)
4. `e0f9c73` -- Nyquist validation layer
5. `e3dda45` -- discuss-phase option highlighting
6. `2c0db8e` -- Gemini TOML bug fix
7. `7542d36` -- Context window monitor hook (new gsd-context-monitor.js)
8. `131f24b` -- Auto-advance chain fix
9. Plus merge commits (skip)

**Pre-cherry-pick preparation:**
Before starting, note the current state of fork's gsd-tools.cjs:
```bash
wc -l get-stuff-done/bin/gsd-tools.cjs
grep "validateGitSHA\|validateBranchName\|validateConfigPath\|validateTagName\|validateRemoteURL" get-stuff-done/bin/gsd-tools.cjs
```
Save the validation import line for re-insertion after the split.

**Cherry-pick workflow:**
For each non-merge commit:
1. `git cherry-pick --no-commit <hash>`
2. The module split commit (`c67ab75`) will be VERY conflicted because:
   - Upstream splits `get-shit-done/bin/gsd-tools.cjs` (5324 lines) into 12 files
   - Fork has `get-stuff-done/bin/gsd-tools.cjs` (~4953+ lines)
   - Git may not auto-detect the split
3. If the module split cannot be cherry-picked cleanly:
   - Abort: `git cherry-pick --abort`
   - Manually apply the module split:
     a. Copy upstream's lib/ modules: `git show upstream/main:get-shit-done/bin/lib/<module>.cjs > get-stuff-done/bin/lib/<module>.cjs` for each of the 11 modules
     b. Copy upstream's thin router: `git show upstream/main:get-shit-done/bin/gsd-tools.cjs > get-stuff-done/bin/gsd-tools.cjs`
     c. Apply branding to all 12 files (replace `get-shit-done` with `get-stuff-done` in require paths and strings)
     d. Re-insert fork's validation import into gsd-tools.cjs (router) near the other require statements
     e. Stage and commit: `git commit -m "cherry-pick(sync): split gsd-tools.cjs into 11 domain modules (c67ab75)"`
4. For the test split commit (`fa2e156`):
   - New test files (`tests/*.test.cjs`) will be created under the upstream directory name
   - Accept these files, apply branding pass
   - These use `node --test` runner (upstream convention). Fork's bun test may or may not pick them up -- we verify in Task 2
5. For the context-monitor hook (`7542d36`):
   - New file `hooks/gsd-context-monitor.js` -- accept as-is
   - This hook reads a bridge file that the statusline writes
   - After this commit, the fork's statusline must be augmented (see below)

**CRITICAL: Fork validation import preservation:**
After the module split, `get-stuff-done/bin/gsd-tools.cjs` must contain:
```javascript
const { validateGitSHA, validateBranchName, validateConfigPath, validateTagName, validateRemoteURL } = require('../../src/validation');
```
This import should go near the top of the router, after the other require statements. The validation functions are used in git operations throughout the codebase.

If the validation functions are called from within specific modules (not just the router), the import should go in the appropriate module instead (likely `core.cjs` where `execGit` is defined). Check where `validateGitSHA` etc. are called and place the import accordingly.

**CRITICAL: Statusline bridge file write:**
After the context-monitor hook commit, augment `hooks/gsd-statusline.js` to write the bridge file. Add the following AFTER the existing statusline logic computes the `remaining` percentage and `session` ID (adapt variable names to match fork's code):

```javascript
// Bridge file for context-monitor hook
if (session) {
  try {
    const bridgePath = path.join(os.tmpdir(), `claude-ctx-${session}.json`);
    const bridgeData = JSON.stringify({
      session_id: session,
      remaining_percentage: remaining,
      used_pct: used,
      timestamp: Math.floor(Date.now() / 1000)
    });
    fs.writeFileSync(bridgePath, bridgeData);
  } catch (e) { /* Silent fail */ }
}
```

This preserves the fork's 4-stage indicator while adding the bridge file that `gsd-context-monitor.js` reads.

Commit the statusline bridge addition: `feat(sync): add context-monitor bridge file write to statusline`

**After all commits:**
1. Branding pass on ALL new files:
   ```bash
   grep -r "get-shit-done\|glittercowboy\|get-shit-done-cc" get-stuff-done/bin/lib/ get-stuff-done/bin/gsd-tools.cjs hooks/ agents/ commands/ --include="*.js" --include="*.cjs" --include="*.md" --include="*.json" | grep -v "node_modules\|\.planning\|\.upstream"
   ```
2. Fix any upstream branding in lib/*.cjs modules
3. Commit branding fixes if needed
4. Update sync manifest with all Batch 12 entries
5. Update approach comparison with the module split details
  </action>
  <verify>
- `ls get-stuff-done/bin/lib/*.cjs | wc -l` returns 11
- `wc -l get-stuff-done/bin/gsd-tools.cjs` shows ~500-600 lines (thin router, not monolith)
- `grep "require('./lib/" get-stuff-done/bin/gsd-tools.cjs | wc -l` returns 11 (one per module)
- `grep "validateGitSHA\|validateBranchName" get-stuff-done/bin/gsd-tools.cjs get-stuff-done/bin/lib/core.cjs` confirms validation import exists
- `grep "claude-ctx-" hooks/gsd-statusline.js` confirms bridge file write added
- `ls hooks/gsd-context-monitor.js` confirms new hook exists
- No upstream branding in lib/*.cjs or gsd-tools.cjs
  </verify>
  <done>Module split applied: 11 domain modules under get-stuff-done/bin/lib/, thin router, fork validation import preserved, statusline bridge file added, context-monitor hook adopted</done>
</task>

<task type="auto">
  <name>Task 2: Verify test suite compatibility and fix .test.cjs integration</name>
  <files>package.json (potentially), bunfig.toml (potentially)</files>
  <action>
After the module split, verify the full test suite works.

**Step 1: Run bun test and observe:**
```bash
bun test
```

**Possible outcomes:**

A) **bun runs all tests (both .test.js and .test.cjs):** Ideal. Verify all pass.

B) **bun runs .test.js but ignores .test.cjs:** The upstream CJS tests are not running. This is acceptable for now -- the fork's 563+ tests cover the same code paths. Document in approach comparison.

C) **bun attempts .test.cjs and fails:** The upstream tests use `node:test` assertions (`describe`, `it`, `assert`). If bun cannot run these:
   - Option 1: Configure bun to only run `*.test.js` files:
     Add to `package.json` scripts or create `bunfig.toml`:
     ```toml
     [test]
     root = "."
     preload = []
     include = ["tests/**/*.test.js"]
     ```
   - Option 2: Run upstream tests separately with node:
     ```bash
     node --test tests/*.test.cjs
     ```
   - Prefer Option 1 (keep bun as primary runner) with Option 2 as a secondary check

**Step 2: Verify fork tests pass:**
```bash
bun test
```
Must show 563+ tests passing, 0 failures.

**Step 3: If upstream .test.cjs files exist, verify they run (with node):**
```bash
node --test tests/*.test.cjs 2>/dev/null
```
If these pass, document in approach comparison. If they fail (likely due to path differences -- they reference `get-shit-done/`), apply branding to the test files and retry.

**Step 4: Coverage check (final validation):**
```bash
bun test --coverage 2>&1 | tail -20
```
Check if coverage is above 90%. If below 90%, that is acceptable mid-sync (CONTEXT.md says 95%+ enforced at end only). Document current coverage level.

**Step 5: Verify gsd-tools CLI works:**
```bash
node get-stuff-done/bin/gsd-tools.cjs --help 2>&1 | head -5
node get-stuff-done/bin/gsd-tools.cjs current-timestamp
```
Both should work without errors, confirming the modular architecture functions correctly.

**Step 6: Commit any configuration changes:**
If bunfig.toml was created or package.json test pattern was updated, commit:
`fix(sync): configure test runner for modular architecture compatibility`

**Step 7: Update sync manifest and approach comparison:**
- Mark Batch 12 as complete in manifest
- Add module split entry to approach comparison documenting:
  - Fork's monolith (1947 lines pre-sync) vs upstream's modular split (553 + 4771 across 11 modules)
  - Fork's validation import preservation strategy
  - Test runner compatibility decision (bun for fork tests, node for upstream CJS tests)
- Commit: `docs(18): complete sync state through upstream/main HEAD`
  </action>
  <verify>
- `bun test` passes with 563+ tests, 0 failures
- `node get-stuff-done/bin/gsd-tools.cjs current-timestamp` returns a timestamp without errors
- `node get-stuff-done/bin/gsd-tools.cjs --help` outputs usage information
- Coverage is documented (even if below 95% -- that is enforced at plan 05 only)
  </verify>
  <done>Test suite verified: fork tests pass under bun, gsd-tools CLI functional with modular architecture, upstream CJS test compatibility documented, coverage level recorded</done>
</task>

</tasks>

<verification>
- Batch 12 fully applied (11 commits)
- 11 domain modules exist under get-stuff-done/bin/lib/
- gsd-tools.cjs is thin router (~553 lines)
- Fork validation import preserved
- Statusline bridge file write added
- Test suite passes
- gsd-tools CLI functional
</verification>

<success_criteria>
- get-stuff-done/bin/lib/ contains 11 .cjs files (core, state, phase, roadmap, verify, config, template, milestone, commands, init, frontmatter)
- get-stuff-done/bin/gsd-tools.cjs is ~500-600 lines (router, not monolith)
- Fork validation import exists in router or core.cjs
- hooks/gsd-statusline.js has bridge file write AND 4-stage indicator
- bun test: 563+ pass, 0 fail
- node get-stuff-done/bin/gsd-tools.cjs current-timestamp works
- Sync manifest complete through upstream/main HEAD
</success_criteria>

<output>
After completion, create `.planning/phases/18-upstream-sync-execution/18-04-SUMMARY.md`
</output>
