---
phase: 34-testing-ci-enforcement
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/sync.test.cjs
  - overlay/lib/sync.cjs
autonomous: true
requirements: [TEST-01, TEST-04]

must_haves:
  truths:
    - "sync.cjs functions are exercisable from source tree via a test shim that resolves the dist-relative import"
    - "sync.cjs achieves 95%+ coverage at functions and lines when tested via the shim"
    - "Existing sync.test.cjs assertions pass without modification to test logic"
  artifacts:
    - path: "tests/sync.test.cjs"
      provides: "Comprehensive sync.cjs tests running from source tree"
      min_lines: 1500
  key_links:
    - from: "tests/sync.test.cjs"
      to: "overlay/lib/sync.cjs"
      via: "require with symlink shim resolving ../get-shit-done/bin/lib/core.cjs"
      pattern: "require.*sync\\.cjs"
---

<objective>
Close the largest coverage gap: overlay/lib/sync.cjs (1,420 lines at ~5% coverage).

Purpose: sync.cjs is a massive drag on overall metrics -- bringing it from ~5% to 95%+ is the single highest-impact coverage action. The existing tests/sync.test.cjs has 1,583 lines of comprehensive tests (48 tests, 289 assertions) that already pass against the upstream version. The blocker is a dist-relative import path: sync.cjs requires `../get-shit-done/bin/lib/core.cjs` which only resolves from the composed dist/ layout, not the source tree.

Output: sync.test.cjs running against sync.cjs from source tree with symlink shim, 95%+ coverage on sync.cjs
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-testing-ci-enforcement/34-RESEARCH.md

<interfaces>
From overlay/lib/sync.cjs line 7:
```javascript
const { execGit } = require('../get-shit-done/bin/lib/core.cjs');
```
This import path resolves from dist/ layout:
  dist/overlay/lib/sync.cjs -> dist/get-shit-done/bin/lib/core.cjs

From source tree:
  overlay/lib/sync.cjs -> get-shit-done/bin/lib/core.cjs (DOES NOT EXIST in source)

BUT: get-stuff-done/bin/lib/core.cjs DOES exist in source (the renamed upstream copy).
The fix: create a symlink/junction `get-shit-done` -> `get-stuff-done` at project root during test setup.
This makes `../get-shit-done/bin/lib/core.cjs` resolve to the existing `get-stuff-done/bin/lib/core.cjs`.

From tests/sync.test.cjs (existing):
- 48 tests across 68 suites, 289 assertions
- Tests the fork's sync module functions: runSupplyChainChecks, generateSyncPlan, etc.
- 3 assertions reference fork-specific package names (@chude/get-stuff-done, chudeemeke/get-stuff-done)
- These 3 assertions may need minor adjustment for the source tree context
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED -- verify sync.test.cjs fails without shim and create test infrastructure</name>
  <files>tests/sync.test.cjs</files>
  <action>
First, verify the current failure mode: run `node --test tests/sync.test.cjs` and confirm it fails with "Cannot find module '../get-shit-done/bin/lib/core.cjs'" (documenting the baseline).

Then create a test setup approach:
1. At the top of sync.test.cjs (before any require of sync.cjs), add a beforeAll or top-level setup block that creates a symlink/junction at `{PROJECT_ROOT}/get-shit-done` pointing to `{PROJECT_ROOT}/get-stuff-done`. On Windows use 'junction' type, on Unix use 'dir'.
2. Add a corresponding afterAll/cleanup that removes the symlink after tests complete.
3. Use fs.existsSync to check if the symlink already exists (idempotent -- don't error if already present).
4. If symlink creation fails (permissions), skip the entire test file with a clear message.

The symlink approach was recommended by the Phase 34 research (Open Question 1, Option A) as the simplest fix requiring zero code changes to sync.cjs.

Run the tests again -- they should now LOAD sync.cjs successfully. Some tests may still fail due to other environmental dependencies (e.g., git operations, network calls). That is expected at this stage.

Do NOT modify sync.cjs itself. The import path targeting dist/ is by design (it runs from installed dist/ in production).

Commit: `test(34-01): add symlink shim for sync.cjs source-tree testing`
  </action>
  <verify>
    <automated>node --test tests/sync.test.cjs 2>&1 | tail -20</automated>
  </verify>
  <done>sync.test.cjs loads sync.cjs successfully via symlink shim. Tests that depend only on core.cjs imports now pass. Any remaining failures are from environmental dependencies (git, network), not the import path.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN -- ensure comprehensive sync.cjs test coverage reaches 95%+</name>
  <files>tests/sync.test.cjs, overlay/lib/sync.cjs</files>
  <action>
With the symlink shim in place from Task 1, assess what coverage sync.cjs achieves with the existing tests.

Run `node --test tests/sync.test.cjs` and note which tests pass/fail. For tests that fail due to environmental dependencies (git repo state, network calls, filesystem expectations), add appropriate mocking or skip annotations.

Key areas of sync.cjs that likely need coverage work:
- runSupplyChainChecks (6-vector scanner) -- may need mock git repo setup
- generateSyncPlan -- likely needs mock package.json and git history
- CLI entry point block (if require.main === module)
- Error handling paths (malformed input, missing files, network failures)

For any functions that cannot be tested from the source tree (truly require composed dist/), document why and add appropriate skip annotations. These will be covered by the upstream compat runner (Plan 02, TEST-03).

Do NOT add meaningless tests for coverage theater. Each test must verify actual behavior.

If coverage reaches 95%+ on sync.cjs functions and lines with existing tests + shim alone, commit. If gaps remain, write targeted tests for uncovered functions/branches.

Note: sync.test.cjs uses node:test (not bun:test). It is excluded from bun test via bunfig.toml. For coverage measurement, either:
- Use `node --test --experimental-test-coverage tests/sync.test.cjs` (Node 22 built-in)
- Or rely on the upstream compat runner (Plan 02) for sync.cjs coverage measurement

Commit: `test(34-01): close sync.cjs coverage gaps with symlink shim`
  </action>
  <verify>
    <automated>node --test tests/sync.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <done>sync.test.cjs passes with majority of tests green. sync.cjs coverage at functions and lines is measurably improved from the ~5% baseline. Any skipped tests are documented with reason.</done>
</task>

</tasks>

<verification>
- `node --test tests/sync.test.cjs` runs without the "Cannot find module" error
- Symlink shim is created/cleaned up automatically by test infrastructure
- Coverage on sync.cjs is substantially improved from ~5% baseline
- No modifications to sync.cjs production code (import paths unchanged)
</verification>

<success_criteria>
- sync.test.cjs loads and executes from source tree via symlink shim
- sync.cjs coverage improved substantially (targeting 95%+ at functions and lines)
- Tests follow TDD discipline (failing baseline documented, then fixed)
- Symlink cleanup is idempotent and cross-platform (junction on Windows)
</success_criteria>

<output>
After completion, create `.planning/phases/34-testing-ci-enforcement/34-01-SUMMARY.md`
</output>
