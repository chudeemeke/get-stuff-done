---
phase: 40.6
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .planning/upstream-authority.json
  - .planning/phases/40.6-upstream-authority-migration/40.6-MIGRATION-SPIKE.md
  - .planning/phases/40.6-upstream-authority-migration/40.6-02-SUMMARY.md
autonomous: true
requirements: ["UPGRADE-11"]
estimated_minutes: 120
gap_closure: false
must_haves:
  truths:
    - "Open GSD package layout inspected in an isolated temp/scratch directory"
    - "No global install or global GSD patching performed"
    - "npm package integrity/provenance metadata recorded before trusting layout"
    - "Current legacy package layout inspected from installed/pinned dependency for evidence-based comparison"
    - "Scratch workspace is outside the repo or cleaned/ignored before summary"
    - "Source root and bin entries verified from extracted package, not only npm metadata"
    - "Manifest updated only with evidence-backed layout fields"
  artifacts:
    - path: ".planning/phases/40.6-upstream-authority-migration/40.6-MIGRATION-SPIKE.md"
      provides: "Package layout and compatibility spike"
      min_lines: 80
---

<objective>
Inspect `@opengsd/gsd-core@1.5.0` in isolation and record the concrete package layout
needed for compose, override, boundary, and preview-update migration.
</objective>

<context>
@.planning/phases/40.6-upstream-authority-migration/40.6-CONTEXT.md
@.planning/phases/40.6-upstream-authority-migration/40.6-RESEARCH.md
@.planning/phases/40.6-upstream-authority-migration/40.6-VALIDATION.md
@.planning/upstream-authority.json
</context>

<tasks>

<task id="02-01" type="auto">
  <name>Create isolated package spike workspace</name>
  <files>.planning/phases/40.6-upstream-authority-migration/40.6-MIGRATION-SPIKE.md</files>
  <action>
    Create a scratch directory under the OS temp directory or `.planning/tmp/40.6-opengsd-spike`
    if project-local scratch is easier to audit. Do not commit scratch contents.
    Prefer the OS temp directory. If `.planning/tmp` is used, verify it is ignored or
    remove it before the wave summary.

    Capture the commands used. Preferred:

    ```powershell
    npm pack @opengsd/gsd-core@1.5.0 --pack-destination <scratch>
    npm install --prefix <scratch>/install-root @opengsd/gsd-core@1.5.0 --ignore-scripts
    ```

    If `npm pack` is enough to inspect the layout, avoid running package scripts.
  </action>
</task>

<task id="02-01b" type="auto">
  <name>Capture package integrity and provenance metadata</name>
  <files>.planning/phases/40.6-upstream-authority-migration/40.6-MIGRATION-SPIKE.md</files>
  <action>
    Before trusting the package layout, run and record:

    ```powershell
    npm view @opengsd/gsd-core@1.5.0 dist.integrity dist.shasum dist.tarball license scripts gitHead maintainers --json
    gh release view v1.5.0 -R open-gsd/gsd-core --json tagName,publishedAt,targetCommitish,url
    ```

    If GitHub release metadata does not map cleanly to npm metadata, record that as a
    risk for Plan 04; do not silently assume provenance.
  </action>
</task>

<task id="02-02" type="auto">
  <name>Record package metadata and file tree</name>
  <files>.planning/phases/40.6-upstream-authority-migration/40.6-MIGRATION-SPIKE.md</files>
  <action>
    Extract and record:
    - package name/version.
    - package `bin`.
    - package `files` or effective tarball root.
    - top-level directory tree.
    - path to installer.
    - path to `gsd-tools`.
    - path to workflows, commands, agents, hooks, memory, and config surfaces.
    - whether a `gsd-sdk` bin exists.
    - whether package lifecycle scripts exist and what they are.

    Include exact commands and summarized output.
  </action>
</task>

<task id="02-02b" type="auto">
  <name>Inspect current legacy package layout for comparison baseline</name>
  <files>.planning/phases/40.6-upstream-authority-migration/40.6-MIGRATION-SPIKE.md</files>
  <action>
    Inspect the currently installed/pinned legacy package, not just remembered layout:
    - `node_modules/get-shit-done-cc/package.json`
    - legacy bin map
    - legacy source root
    - legacy workflow/hook/agent locations used by current scripts

    If `node_modules` is missing or stale, run the repo's safe install command first or
    document the blocker. Do not use `get-shit-done-cc@latest`.
  </action>
</task>

<task id="02-03" type="auto">
  <name>Compare old and new layout assumptions</name>
  <files>.planning/phases/40.6-upstream-authority-migration/40.6-MIGRATION-SPIKE.md</files>
  <action>
    Compare the old assumptions from current scripts against Open GSD:
    - old package dir: `node_modules/get-shit-done-cc`
    - old source root: direct package root / `get-shit-done` subdir depending on path
    - old installer path.
    - old workflow path.
    - old hook path.

    Produce a table:
    `surface | old path | Open GSD path | migration impact | test needed`.
  </action>
</task>

<task id="02-04" type="auto">
  <name>Update manifest with evidence-backed layout fields</name>
  <files>.planning/upstream-authority.json</files>
  <action>
    Update `.planning/upstream-authority.json` with:
    - `active.sourceRoot`
    - `active.bin`
    - any additional path fields needed by Plan 03.

    Any new field must be added to `.planning/upstream-authority.schema.json` in the
    same wave. Do not invent fields that are not needed by tooling. Keep the manifest
    small.
  </action>
</task>

<task id="02-05" type="auto">
  <name>Write spike summary</name>
  <files>.planning/phases/40.6-upstream-authority-migration/40.6-02-SUMMARY.md</files>
  <action>
    Write a summary with:
    - scratch path used.
    - cleanup/ignore status for scratch path.
    - commands run.
    - integrity/provenance evidence.
    - package layout decision.
    - Plan 03 implementation notes.
    - any blocker or incompatibility.
  </action>
</task>

</tasks>

<success>
Plan 02 succeeds when implementation can point to a documented Open GSD source root and
bin layout, with no global install mutation.
</success>
