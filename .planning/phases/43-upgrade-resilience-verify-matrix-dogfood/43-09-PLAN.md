---
phase: 43
plan: 09
type: execute
wave: 9
depends_on: ["08"]
status: pending
requirements: ["SHIP-03"]
files_modified:
  - scripts/generate-sbom.js
  - scripts/compose.js
  - scripts/finalize-dist.js
  - tests/compose.test.js
  - tests/ci-workflow.test.js
  - package.json
  - bun.lock
  - .github/workflows/ci.yml
  - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-09-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "SBOM tool is pinned to @cyclonedx/cyclonedx-npm@4.2.1 per SHIP-03"
    - "dist/bom.json is generated after compose and before finalize-dist"
    - "package tarball includes dist/bom.json"
    - "CI uploads or preserves SBOM as release/package evidence"
  artifacts:
    - "scripts/generate-sbom.js"
    - "dist/bom.json"
    - "package.json"
  key_links:
    - "SHIP-03"
---

<objective>
Generate CycloneDX SBOM output in the dist pipeline and make the package artifact
contain `dist/bom.json`.
</objective>

<context>
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-CONTEXT.md
@scripts/compose.js
@scripts/finalize-dist.js
@package.json
@.github/workflows/ci.yml
</context>

<tasks>

<task id="09-01" type="auto" tdd="true">
  <name>Write SBOM pipeline tests first</name>
  <files>tests/compose.test.js; tests/ci-workflow.test.js</files>
  <read_first>
    - tests/compose.test.js
    - tests/ci-workflow.test.js
    - scripts/compose.js
    - scripts/finalize-dist.js
    - package.json
  </read_first>
  <action>
    Add failing tests before implementation:
    - package scripts include `sbom`.
    - package devDependency pins `@cyclonedx/cyclonedx-npm` to `4.2.1`.
    - `bun run dist` creates `dist/bom.json`.
    - package `files` includes `dist/bom.json` or `dist/**`.
    - CI references `bun run sbom` and `dist/bom.json`.
  </action>
  <acceptance_criteria>
    - `bun test tests/compose.test.js tests/ci-workflow.test.js` fails before implementation and exits 0 after implementation.
    - Tests contain `@cyclonedx/cyclonedx-npm`, `4.2.1`, `dist/bom.json`, and `bun run sbom`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/compose.test.js tests/ci-workflow.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="09-02" type="auto">
  <name>Implement SBOM generator and dist integration</name>
  <files>scripts/generate-sbom.js; scripts/compose.js; scripts/finalize-dist.js; package.json; bun.lock</files>
  <read_first>
    - tests/compose.test.js
    - scripts/compose.js
    - scripts/finalize-dist.js
    - package.json
  </read_first>
  <action>
    Add devDependency `@cyclonedx/cyclonedx-npm@4.2.1`. Create `scripts/generate-sbom.js` that runs CycloneDX with `--output-format JSON --output-file dist/bom.json` after `dist` exists. Add package script `"sbom": "node scripts/generate-sbom.js"`. Integrate the dist lifecycle so SBOM generation occurs after compose and before finalize-dist/package verification.
  </action>
  <acceptance_criteria>
    - `bun install --frozen-lockfile --ignore-scripts` exits 0 after lockfile update.
    - `bun run sbom` exits 0 after compose and creates `dist/bom.json`.
    - `Test-Path dist\\bom.json` is true.
    - `rg -n "@cyclonedx/cyclonedx-npm|4.2.1|dist/bom.json|\\\"sbom\\\"" package.json scripts/generate-sbom.js scripts/compose.js scripts/finalize-dist.js` finds matches.
  </acceptance_criteria>
  <verify>
    <automated>bun install --frozen-lockfile --ignore-scripts</automated>
    <automated>bun run dist</automated>
    <automated>Test-Path dist\\bom.json</automated>
  </verify>
  <done>false</done>
</task>

<task id="09-03" type="auto">
  <name>Wire SBOM CI evidence</name>
  <files>.github/workflows/ci.yml; tests/ci-workflow.test.js</files>
  <read_first>
    - .github/workflows/ci.yml
    - tests/ci-workflow.test.js
    - scripts/generate-sbom.js
  </read_first>
  <action>
    Update CI so the relevant package/dist job runs `bun run sbom` or `bun run dist` with SBOM generation, verifies `dist/bom.json`, and uploads `dist/bom.json` with `actions/upload-artifact@v7`.
  </action>
  <acceptance_criteria>
    - `bun test tests/ci-workflow.test.js` exits 0.
    - `bash scripts/lint-workflows.sh` exits 0.
    - `rg -n "bun run sbom|dist/bom.json|actions/upload-artifact@v7" .github/workflows/ci.yml tests/ci-workflow.test.js` finds matches.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/ci-workflow.test.js</automated>
    <automated>bash scripts/lint-workflows.sh</automated>
  </verify>
  <done>false</done>
</task>

<task id="09-04" type="auto">
  <name>Record SBOM summary</name>
  <files>.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-09-SUMMARY.md</files>
  <read_first>
    - scripts/generate-sbom.js
    - package.json
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Create `43-09-SUMMARY.md` listing the pinned SBOM tool, lifecycle order, package inclusion proof, CI artifact path, and tests run.
  </action>
  <acceptance_criteria>
    - Summary contains `@cyclonedx/cyclonedx-npm@4.2.1`, `dist/bom.json`, `after compose`, `before finalize-dist`, and `SHIP-03`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "@cyclonedx/cyclonedx-npm@4.2.1|dist/bom.json|after compose|before finalize-dist|SHIP-03" .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-09-SUMMARY.md</automated>
  </verify>
  <done>false</done>
</task>

</tasks>

<threat_model>
SBOM generation can become theater if the file is not in the package artifact.
Mitigate with dist lifecycle tests, package files checks, and CI artifact proof.
</threat_model>

<verification>
Run:
- `bun run dist`
- `Test-Path dist\\bom.json`
- `bun test tests/compose.test.js tests/ci-workflow.test.js`
- `bash scripts/lint-workflows.sh`
</verification>
