node_modules/
.DS_Store
TO-DOS.md
CLAUDE.md
/research.claude/
commands.html

# Cross-project issue inbox: active files are local WIP; terminal records
# under archived/ and rejected/ are tracked for audit.
docs/inbox/*.md
!docs/inbox/README.md
!docs/inbox/archived/
!docs/inbox/archived/**
!docs/inbox/rejected/
!docs/inbox/rejected/**

# Local test installs
.claude/

# Build artifacts (committed to npm, not git)
hooks/dist/
get-stuff-done/bin/dist/
dist/

# Test symlink shim (created by sync.test.cjs, cleaned up on exit)
overlay/get-shit-done

# Animation assets
animation/
*.gif

# Internal planning documents
reports/
RAILROAD_ARCHITECTURE.md
analysis/
docs/GSD-MASTER-ARCHITECTURE.md
docs/GSD-RUST-IMPLEMENTATION-GUIDE.md
docs/GSD-SYSTEM-SPECIFICATION.md
gaps.md
improve.md
philosophy.md
.planning/upstream-outreach-drafts.md

# Exact-head hosted verdict receipts are local read models. Tracking a receipt
# would change the commit SHA that it certifies.
.planning/evidence/phase43-hosted-verdict.json

# Secret-leakage proactive ignores (Phase 40.5 Wave 1.7-A, A-11)
# Defense in depth: also gated by gitleaks step in CI + GitHub-native
# secret scanning + push protection (post-public-flip). These patterns
# catch common-named secret files before they reach the staging area.
*.env
*.pem
*.key
id_rsa
id_ed25519
credentials*
secrets.json
