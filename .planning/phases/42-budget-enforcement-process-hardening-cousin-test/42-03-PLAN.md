---
phase: 42
plan: 03
type: execute
wave: 2
depends_on: ["02"]
status: pending
requirements: ["SHIP-07", "DOCS-04"]
files_modified:
  - scripts/cousin-smoke.js
  - tests/cousin-smoke.test.js
  - .github/workflows/cousin-install.yml
  - tests/ci-workflow.test.js
  - INSTALL.md
  - .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-03-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "cousin install uses ubuntu-latest, macos-15, and windows-latest"
    - "cousin install covers Node 20 and Node 22"
    - "cousin install covers npm, pnpm, and bun"
    - "installer side effects are isolated under runner temp paths"
    - "INSTALL.md documents public install plus optional read-only token path"
---

<objective>
Add cold-install cousin CI and installation documentation using the non-interactive provenance surface from Plan 02.
</objective>

<context>
@.planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-CONTEXT.md
@bin/gsd.js
@bin/install.js
@package.json
@.github/workflows/ci.yml
</context>

<tasks>

<task id="03-01" type="auto" tdd="true">
  <name>Write cousin smoke helper tests first</name>
  <files>tests/cousin-smoke.test.js; scripts/cousin-smoke.js</files>
  <read_first>
    - tests/helpers/subprocess-with-timeout.js
    - bin/install.js
    - package.json
  </read_first>
  <action>
    Create tests for a new `scripts/cousin-smoke.js` helper before implementation:
    - package managers accepted: `npm`, `pnpm`, `bun`.
    - invalid package manager exits non-zero with `Unsupported package manager`.
    - helper builds an environment with `HOME`, `USERPROFILE`, `CLAUDE_CONFIG_DIR`, and `GSD_CI_SMOKE_DIR` under a supplied temp root.
    - npm mode creates a temp project, runs `npm init -y`, installs the package spec with `npm install`, and runs the installed local `gsd` bin with `--version --json`.
    - pnpm mode creates a temp project, uses Corepack-provided pnpm, installs with `pnpm add`, and runs the installed local `gsd` bin with `--version --json`.
    - bun mode creates a temp project, installs with `bun add`, and runs the installed local `gsd` bin with `--version --json`.
    - smoke command resolves the installed local bin under the temp project rather than relying on a global `gsd`.
    - PR mode can use a packed tarball spec.
    - published mode can use `@chude/get-stuff-done@latest`.
    - optional `NODE_AUTH_TOKEN` writes only to a temp-project `.npmrc` and is redacted from command output.
    - no command prints raw token values.
  </action>
  <acceptance_criteria>
    - `bun test tests/cousin-smoke.test.js` fails before implementation and exits 0 after implementation.
    - `tests/cousin-smoke.test.js` contains `CLAUDE_CONFIG_DIR`, `--version --json`, `npm`, `pnpm`, and `bun`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/cousin-smoke.test.js</automated>
  </verify>
</task>

<task id="03-02" type="auto">
  <name>Implement cousin smoke helper</name>
  <files>scripts/cousin-smoke.js</files>
  <read_first>
    - tests/cousin-smoke.test.js
    - bin/install.js
    - package.json
  </read_first>
  <action>
    Implement `scripts/cousin-smoke.js`:
    - CLI options: `--package-manager <npm|pnpm|bun>`, `--package <spec>`, `--temp-root <path>`.
    - create temp project, HOME, USERPROFILE, CLAUDE_CONFIG_DIR, and GSD_CI_SMOKE_DIR under `--temp-root`.
    - write a temp-project `.npmrc` only when `NODE_AUTH_TOKEN` is present; never use the user's global npm config.
    - execute the selected package-manager cold install command: `npm install <spec>`, `pnpm add <spec>`, or `bun add <spec>`.
    - resolve and execute the installed local `gsd` bin from the temp project's `node_modules/.bin` path with `--version --json`.
    - parse JSON and assert `packageName`, `version`, `upstreamPackage`, `upstreamVersion`, and `overlayManifestSha256`.
    - redact any token-like environment values in error output.
  </action>
  <acceptance_criteria>
    - `node scripts/cousin-smoke.js --help` exits 0.
    - `bun test tests/cousin-smoke.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/cousin-smoke.js --help</automated>
    <automated>bun test tests/cousin-smoke.test.js</automated>
  </verify>
</task>

<task id="03-03" type="auto">
  <name>Create cousin install workflow</name>
  <files>.github/workflows/cousin-install.yml; tests/ci-workflow.test.js</files>
  <read_first>
    - .github/workflows/ci.yml
    - .github/workflows/10x-validation.yml
    - scripts/cousin-smoke.js
    - tests/ci-workflow.test.js
  </read_first>
  <action>
    Create `.github/workflows/cousin-install.yml`:
    - triggers: `pull_request`, `workflow_dispatch`, and a weekly schedule.
    - matrix OS: `ubuntu-latest`, `macos-15`, `windows-latest`.
    - matrix Node: `20`, `22`.
    - matrix package manager: `npm`, `pnpm`, `bun`.
    - enable Corepack for pnpm before pnpm jobs and install Bun before bun jobs.
    - set the smoke helper `--temp-root` under `${{ runner.temp }}`.
    - PR mode builds the current branch with `bun run dist`, creates a tarball with `npm pack`, and uses that tarball spec for `scripts/cousin-smoke.js`.
    - scheduled/manual mode may install `@chude/get-stuff-done@latest`.
    - use `NODE_AUTH_TOKEN` only if an optional read-only secret is configured; public install must work without it.
    - never echo token values.

    Extend `tests/ci-workflow.test.js` to assert the workflow has all matrix axes, uses `macos-15`, does not use `macos-latest`, calls `scripts/cousin-smoke.js`, and contains `--version --json`.
  </action>
  <acceptance_criteria>
    - `bun test tests/ci-workflow.test.js` exits 0.
    - `bash scripts/lint-workflows.sh` exits 0.
    - `rg -n "cousin-smoke.js|macos-15|node-version|package-manager|--version --json" .github/workflows/cousin-install.yml tests/ci-workflow.test.js` finds matches.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/ci-workflow.test.js</automated>
    <automated>bash scripts/lint-workflows.sh</automated>
  </verify>
</task>

<task id="03-04" type="auto">
  <name>Write INSTALL.md cousin scenario</name>
  <files>INSTALL.md</files>
  <read_first>
    - README.md
    - QUICKSTART.md
    - package.json
    - .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-CONTEXT.md
  </read_first>
  <action>
    Create `INSTALL.md` with:
    - public install path for `@chude/get-stuff-done@latest`.
    - optional read-only token path for private registry scenarios.
    - PATH setup notes for npm, pnpm, and bun on Linux, macOS, and Windows.
    - runtime package provenance verification command: `gsd --version --json`.
    - expected JSON keys: `packageName`, `version`, `upstreamPackage`, `upstreamVersion`, `overlayManifestSha256`.
    - troubleshooting for lifecycle installer side effects and how CI isolates `CLAUDE_CONFIG_DIR`.
  </action>
  <acceptance_criteria>
    - `INSTALL.md` contains `@chude/get-stuff-done`, `gsd --version --json`, `overlayManifestSha256`, `npm`, `pnpm`, `bun`, and `CLAUDE_CONFIG_DIR`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "@chude/get-stuff-done|gsd --version --json|overlayManifestSha256|npm|pnpm|bun|CLAUDE_CONFIG_DIR" INSTALL.md</automated>
  </verify>
</task>

<task id="03-05" type="auto">
  <name>Record cousin install summary</name>
  <files>.planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-VALIDATION.md
  </read_first>
  <action>
    Create `42-03-SUMMARY.md` listing workflow matrix axes, install source modes, isolation env vars, and tests run.
  </action>
  <acceptance_criteria>
    - `42-03-SUMMARY.md` contains `ubuntu-latest`, `macos-15`, `windows-latest`, `Node 20`, `Node 22`, `npm`, `pnpm`, and `bun`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "ubuntu-latest|macos-15|windows-latest|Node 20|Node 22|npm|pnpm|bun" .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-03-SUMMARY.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
The package lifecycle installer can mutate user config. CI must isolate HOME, USERPROFILE, and CLAUDE_CONFIG_DIR under runner temp paths and must never print tokens.
</threat_model>

<verification>
Run:
- `node scripts/cousin-smoke.js --help`
- `bun test tests/cousin-smoke.test.js tests/ci-workflow.test.js`
- `bash scripts/lint-workflows.sh`
</verification>
