---
phase: 34-testing-ci-enforcement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/compose.test.js
  - tests/preview-update.test.js
  - tests/check-overrides.test.js
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "compose.js CLI entry point and error paths are exercised by integration tests"
    - "preview-update.js exported functions are exercised by unit tests covering all paths"
    - "check-overrides.js CLI entry block and orphan report formatting are tested"
    - "Fork test suite achieves 95%+ at functions and lines individually"
  artifacts:
    - path: "tests/compose.test.js"
      provides: "Extended compose pipeline tests with CLI and error path coverage"
    - path: "tests/preview-update.test.js"
      provides: "Extended preview-update tests with integration path coverage"
    - path: "tests/check-overrides.test.js"
      provides: "Extended check-overrides tests with CLI entry and formatting coverage"
  key_links:
    - from: "tests/compose.test.js"
      to: "scripts/compose.js"
      via: "subprocess execution for CLI entry testing"
      pattern: "execFileSync.*compose\\.js"
    - from: "tests/preview-update.test.js"
      to: "scripts/preview-update.js"
      via: "direct function calls for exported functions"
      pattern: "require.*preview-update"
---

<objective>
Close remaining coverage gaps in three scripts: compose.js (~86% -> 95%+), preview-update.js (~8% -> 95%+), and check-overrides.js (~89% -> 95%+).

Purpose: These three files are the remaining coverage drags after sync.cjs (Plan 01). Research identified specific uncovered regions: compose.js CLI entry block (lines ~1007-1063) and error paths (lines ~340-400), preview-update.js integration paths, check-overrides.js CLI entry block (lines ~327-332) and orphan report formatting (lines ~279-281).

Output: All three scripts at 95%+ coverage per metric, overall fork coverage at 95%+
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-testing-ci-enforcement/34-RESEARCH.md

<interfaces>
From scripts/compose.js (uncovered CLI entry, ~lines 1007-1063):
```javascript
if (require.main === module) {
  const args = process.argv.slice(2);
  // --dry-run, --diff, --verbose flag parsing
  // compose() call with options
  // stdout summary output
}
```
Test approach: subprocess execution via execFileSync('node', ['scripts/compose.js', ...flags])

From scripts/compose.js (uncovered error paths, ~lines 340-400):
```javascript
// resolve() error paths: missing upstream dir, invalid structure
// filter() error paths: invalid features.json
// brand() error paths: invalid branding.json
```
Test approach: unit tests with mock filesystem state triggering each error

From scripts/preview-update.js exports:
```javascript
module.exports = { getVersionDelta, runPreviewScan, getOverrideImpact, generateReport };
```
Currently 27 tests covering unit behavior. Gaps: integration paths where functions call each other and CLI entry block.

From scripts/check-overrides.js (uncovered ~lines 279-281, 327-332):
```javascript
// Orphan report formatting (REASON.md without matching override)
// CLI entry: if (require.main === module) { ... process.exit() }
```
Currently 38 tests. Gaps: orphan REASON.md edge case and CLI entry.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close compose.js coverage gaps -- CLI entry and error paths</name>
  <files>tests/compose.test.js</files>
  <action>
Add integration tests to the existing tests/compose.test.js for compose.js uncovered regions.

**CLI entry block tests** (subprocess execution pattern per research anti-pattern guidance):
1. Test `node scripts/compose.js` (default mode) -- verify it exits 0 and outputs summary
2. Test `node scripts/compose.js --dry-run` -- verify it exits 0 and outputs preview without writing
3. Test `node scripts/compose.js --diff` -- verify it exits 0 and outputs delta report
4. Test `node scripts/compose.js --verbose` -- verify it includes verbose detail
5. Use execFileSync in a new describe('CLI entry') block. Capture stdout. Assert key output patterns (upstream_version, files_written).

**Error path tests** (unit tests with invalid inputs):
1. Test resolve() with missing upstream dir -- expect error mentioning the missing path
2. Test resolve() with invalid upstream structure -- expect structure validation error
3. Test that invalid branding.json triggers schema validation error
4. Test that invalid features.json triggers schema validation error

Use existing test patterns from compose.test.js. Do NOT duplicate existing passing tests -- only add coverage for uncovered lines.

Commit: `test(34-03): close compose.js CLI entry and error path coverage gaps`
  </action>
  <verify>
    <automated>bun test tests/compose.test.js</automated>
  </verify>
  <done>compose.js CLI entry block and error paths exercised by tests. compose.js coverage at 95%+ functions and lines.</done>
</task>

<task type="auto">
  <name>Task 2: Close preview-update.js and check-overrides.js coverage gaps</name>
  <files>tests/preview-update.test.js, tests/check-overrides.test.js</files>
  <action>
**preview-update.js gaps** -- add to existing tests/preview-update.test.js:

1. Test the CLI entry block via subprocess: `node scripts/preview-update.js` -- verify it attempts to run and produces output (may fail gracefully if no network, which is acceptable)
2. Test generateReport() with various input combinations: all-clean, some-warnings, failures
3. Test runPreviewScan() fallback path (when sync.cjs is not loadable) -- verify fallback checks execute
4. Test getOverrideImpact() delegation to checkOverrides -- verify it returns the structured result
5. Test error handling: getVersionDelta() with missing package.json fields

**check-overrides.js gaps** -- add to existing tests/check-overrides.test.js:

1. Test orphan REASON.md detection: create a REASON.md file with no matching override file, verify the report includes an orphan warning
2. Test CLI entry via subprocess: `node scripts/check-overrides.js` -- verify it exits 0 (current repo has zero overrides, so should be clean)
3. Test CLI entry with `--overrides-dir` pointing to a fixture dir with violations -- verify exit 1

Follow the same test patterns already established in each file. Use temp directories for filesystem fixtures.

Commit: `test(34-03): close preview-update.js and check-overrides.js coverage gaps`
  </action>
  <verify>
    <automated>bun test tests/preview-update.test.js tests/check-overrides.test.js</automated>
  </verify>
  <done>preview-update.js at 95%+ functions and lines. check-overrides.js at 95%+ functions and lines. Overall fork coverage at 95%+ per metric.</done>
</task>

</tasks>

<verification>
- `bun test --coverage` shows 95%+ at functions and lines for compose.js, preview-update.js, and check-overrides.js
- No existing tests broken by the additions
- Total fork test count increased by the number of new tests added
- Each test verifies actual behavior (no coverage theater)
</verification>

<success_criteria>
- compose.js coverage: 95%+ at functions and lines (up from ~86%)
- preview-update.js coverage: 95%+ at functions and lines (up from ~8%)
- check-overrides.js coverage: 95%+ at functions and lines (up from ~89%)
- Overall fork coverage (bun test --coverage): 95%+ at functions and lines
- All new tests verify real behavioral expectations
</success_criteria>

<output>
After completion, create `.planning/phases/34-testing-ci-enforcement/34-03-SUMMARY.md`
</output>
