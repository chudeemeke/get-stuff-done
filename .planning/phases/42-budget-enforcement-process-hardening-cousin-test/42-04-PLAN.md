---
phase: 42
plan: 04
type: execute
wave: 1
depends_on: []
status: pending
requirements: ["PROCESS-01", "PROCESS-02", "PROCESS-03", "PROCESS-04", "PROCESS-05", "PROCESS-06", "PROCESS-07"]
files_modified:
  - overlay/memory/oversight-principle-evidence-before-claim.md
  - overlay/agents/gsd-oversight-execution.md
  - overlay/agents/gsd-oversight-verification.md
  - overlay/agents/gsd-oversight-planning.md
  - scripts/verify-oversight-probes.js
  - tests/fixtures/oversight-probes/*.md
  - tests/verify-oversight-probes.test.js
  - .github/workflows/oversight-probes.yml
  - tests/ci-workflow.test.js
  - MAINTENANCE.md
  - .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-04-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "Evidence-before-claim principle exists once"
    - "Execution oversight has EBC-EXEC-POSTMERGE and EBC-EXEC-SUMMARY triggers"
    - "Verification oversight has EBC-VERIFY-CI-BEFORE-MEASURE trigger"
    - "Planning oversight has EBC-PLAN-METRIC-COMPAT trigger"
    - "verify-oversight-probes.js deterministically verifies all trigger IDs"
    - "each trigger references PROCESS-07 graduation criteria"
    - "PROCESS-07 criteria are documented and no trigger is blocking in v1.2.0"
---

<objective>
Consolidate oversight into one evidence-before-claim principle, short trigger references, and deterministic probes.
</objective>

<context>
@.planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-CONTEXT.md
@overlay/agents/gsd-oversight-execution.md
@overlay/agents/gsd-oversight-verification.md
@overlay/agents/gsd-oversight-planning.md
@MAINTENANCE.md
</context>

<tasks>

<task id="04-01" type="auto" tdd="true">
  <name>Write oversight probe tests first</name>
  <files>tests/verify-oversight-probes.test.js</files>
  <read_first>
    - overlay/agents/gsd-oversight-execution.md
    - overlay/agents/gsd-oversight-verification.md
    - overlay/agents/gsd-oversight-planning.md
  </read_first>
  <action>
    Add tests before implementation:
    - required trigger IDs are exactly `EBC-EXEC-POSTMERGE`, `EBC-EXEC-SUMMARY`, `EBC-VERIFY-CI-BEFORE-MEASURE`, and `EBC-PLAN-METRIC-COMPAT`.
    - each trigger references `overlay/memory/oversight-principle-evidence-before-claim.md`.
    - each trigger references the PROCESS-07 graduation criteria in `MAINTENANCE.md`.
    - each trigger is no more than 3 non-empty lines in its trigger block.
    - probe script maps curated synthetic violation fixtures to their expected trigger IDs.
    - probe script includes one evidence-backed abstain fixture that must not match any trigger.
    - probe script fails when a trigger ID is missing from a fixture, an unexpected trigger fires, or an abstain fixture matches.
  </action>
  <acceptance_criteria>
    - `bun test tests/verify-oversight-probes.test.js` fails before implementation and exits 0 after implementation.
    - Test file contains all four trigger IDs.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/verify-oversight-probes.test.js</automated>
  </verify>
</task>

<task id="04-02" type="auto">
  <name>Add principle and short triggers</name>
  <files>overlay/memory/oversight-principle-evidence-before-claim.md; overlay/agents/gsd-oversight-execution.md; overlay/agents/gsd-oversight-verification.md; overlay/agents/gsd-oversight-planning.md</files>
  <read_first>
    - overlay/agents/gsd-oversight-execution.md
    - overlay/agents/gsd-oversight-verification.md
    - overlay/agents/gsd-oversight-planning.md
  </read_first>
  <action>
    Create `overlay/memory/oversight-principle-evidence-before-claim.md` defining the evidence-before-claim principle once.

    Add a compact trigger block to execution oversight:
    - `EBC-EXEC-POSTMERGE`: flag claims that a merge or remote state exists before checking the remote state.
    - `EBC-EXEC-SUMMARY`: flag SUMMARY/verification claims that lack command, run, or file evidence.

    Add a compact trigger block to verification oversight:
    - `EBC-VERIFY-CI-BEFORE-MEASURE`: flag CI gate claims raised before local measurement or workflow evidence exists.

    Add a compact trigger block to planning oversight:
    - `EBC-PLAN-METRIC-COMPAT`: flag test approaches that do not check metric-target compatibility before promising coverage.

    Each trigger must reference the principle doc path, reference the PROCESS-07 graduation criteria in `MAINTENANCE.md`, and stay advisory.
  </action>
  <acceptance_criteria>
    - `rg -n "EBC-EXEC-POSTMERGE|EBC-EXEC-SUMMARY|EBC-VERIFY-CI-BEFORE-MEASURE|EBC-PLAN-METRIC-COMPAT" overlay/agents` finds all four IDs.
    - `rg -n "overlay/memory/oversight-principle-evidence-before-claim.md" overlay/agents overlay/memory/oversight-principle-evidence-before-claim.md` finds principle references.
    - `rg -n "PROCESS-07|graduation" overlay/agents` finds graduation references for all trigger blocks.
    - No trigger text contains `blocks execution` or `blocking in v1.2.0`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "EBC-EXEC-POSTMERGE|EBC-EXEC-SUMMARY|EBC-VERIFY-CI-BEFORE-MEASURE|EBC-PLAN-METRIC-COMPAT" overlay/agents</automated>
  </verify>
</task>

<task id="04-03" type="auto">
  <name>Implement deterministic oversight probes and workflow</name>
  <files>scripts/verify-oversight-probes.js; .github/workflows/oversight-probes.yml; tests/ci-workflow.test.js</files>
  <read_first>
    - tests/verify-oversight-probes.test.js
    - .github/workflows/flake-issue-maintenance.yml
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Implement `scripts/verify-oversight-probes.js`:
    - read the principle doc and three agent files.
    - verify all four trigger IDs exist.
    - verify every trigger references the principle doc path.
    - verify every trigger references PROCESS-07 graduation criteria.
    - run deterministic synthetic fixture checks:
      - `postmerge-claim.md` must match `EBC-EXEC-POSTMERGE`.
      - `summary-without-evidence.md` must match `EBC-EXEC-SUMMARY`.
      - `ci-before-measure.md` must match `EBC-VERIFY-CI-BEFORE-MEASURE`.
      - `metric-incompatible-plan.md` must match `EBC-PLAN-METRIC-COMPAT`.
      - `evidence-backed-summary.md` must abstain with no trigger.
    - report the exact trigger ID and fixture name if any fixture fails.
    - exit 1 on missing trigger, missing principle reference, missing graduation reference, unexpected trigger, or failed fixture.

    Add `.github/workflows/oversight-probes.yml`:
    - scheduled weekly.
    - `workflow_dispatch`.
    - `pull_request` when overlay agent files, principle doc, probe script, or tests change.
    - setup bun and run `node scripts/verify-oversight-probes.js`.

    Extend `tests/ci-workflow.test.js` to assert workflow triggers and command.
  </action>
  <acceptance_criteria>
    - `node scripts/verify-oversight-probes.js` exits 0.
    - `bun test tests/verify-oversight-probes.test.js tests/ci-workflow.test.js` exits 0.
    - `bash scripts/lint-workflows.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-oversight-probes.js</automated>
    <automated>bun test tests/verify-oversight-probes.test.js tests/ci-workflow.test.js</automated>
    <automated>bash scripts/lint-workflows.sh</automated>
  </verify>
</task>

<task id="04-04" type="auto">
  <name>Document PROCESS-07 graduation criteria</name>
  <files>MAINTENANCE.md</files>
  <read_first>
    - MAINTENANCE.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Add a MAINTENANCE.md section for oversight trigger graduation:
    - minimum 20 PRs observed with trigger firing or correctly abstaining.
    - maximum 5% false-positive rate across the observation window.
    - explicit promotion ceremony requires maintainer-authored PR review.
    - 2 weeks clean CI history.
    - MAINTENANCE.md update recording trigger name, promotion date, and observation evidence.
    - no trigger graduates to blocking in v1.2.0.
    - define the anchor or heading text that each advisory trigger must reference.
  </action>
  <acceptance_criteria>
    - `rg -n "20 PRs|5% false-positive|2 weeks clean CI|no trigger graduates to blocking in v1.2.0" MAINTENANCE.md` finds all phrases.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "20 PRs|5% false-positive|2 weeks clean CI|no trigger graduates to blocking in v1.2.0" MAINTENANCE.md</automated>
  </verify>
</task>

<task id="04-05" type="auto">
  <name>Record oversight summary</name>
  <files>.planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-VALIDATION.md
  </read_first>
  <action>
    Create `42-04-SUMMARY.md` listing trigger IDs, probe command output, workflow name, and PROCESS-07 graduation criteria.
  </action>
  <acceptance_criteria>
    - `42-04-SUMMARY.md` contains all four trigger IDs and `PROCESS-07`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "EBC-EXEC-POSTMERGE|EBC-EXEC-SUMMARY|EBC-VERIFY-CI-BEFORE-MEASURE|EBC-PLAN-METRIC-COMPAT|PROCESS-07" .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-04-SUMMARY.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
Oversight triggers can become noisy governance theater. Keep triggers advisory, short, probed, and tied to explicit graduation criteria.
</threat_model>

<verification>
Run:
- `node scripts/verify-oversight-probes.js`
- `bun test tests/verify-oversight-probes.test.js tests/ci-workflow.test.js`
- `bash scripts/lint-workflows.sh`
</verification>
