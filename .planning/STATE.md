---
gsd_state_version: 1.0
milestone: v1.2.0
milestone_name: Ship-Ready Hardening
status: in_progress
stopped_at: Phase 43 Plan 11AG is locally complete; Plan 11AH is next and owns read-only subject-bound hosted workflow governance
last_updated: "2026-07-20T05:08:58.0758261+01:00"
last_activity: 2026-07-20
progress:
  total_phases: 10
  completed_phases: 3
  total_plans: 72
  completed_plans: 44
  percent: 61
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-20)

**Core value:** Get upstream improvements automatically while preserving fork identity and additions
**Current focus:** Phase 43 — upgrade-resilience-verify-matrix-dogfood

## Current Position

Phase: 43 (upgrade-resilience-verify-matrix-dogfood) — IN PROGRESS
Plan: 11AH of 50
Status: Plan 11AG is locally complete. Blocking performance authority now uses a closed paired same-run contract with deterministic AB/BA scheduling, fresh tracked sandboxes, at least ten measured pairs, raw integer nanosecond receipts, exact BigInt ratio classification, recomputed diagnostics, and unchanged 1.10/1.25 budgets. Focused tests pass 39/39 with 100% statements, 98.18% branches, 100% functions, and 100% lines; the broad suite passes 1,581/1,581. Fable independently reran those gates, returned `ACCEPT_WITH_FOLLOW_UPS` for 11AG, and `APPROVE_PLAN_WITH_FOLLOW_UPS` for 11AH. Binding amendments now limit blocking paired authority to same-repository PR heads, treat check-name identity as non-authoritative, require four-checkout authority-file preflight, and require one real local Hyperfine 1.20.0 run in Plan 11AI. The roadmap read model is 44/72 summaries/plans (61%) and Phase 43 is 22/50. Plan 11AH is next. Plan 11R still has no passed hosted envelope. No push, hosted run, issue mutation, merge, release, credential change, or public action occurred.
Last activity: 2026-07-20

**Upstream state:** Active worktree pins Open GSD `@opengsd/gsd-core@1.6.1`. Live npm evidence on 2026-07-19 reports stable `1.7.0`, published on 2026-07-15 after the prior latest-stable check; the active pin is therefore intentionally unchanged but no longer current. Exact tarball inspection proves 1.7.0 fixes issue 2252's `PLAN-REVIEW` suffix case while retaining the broad embedded-`PLAN` fallback, so the fork's wider delimiter-bounded override remains necessary pending reviewed reconciliation. Legacy `get-shit-done-cc` remains deprecation evidence only and must not be used as `latest` authority. Open GSD package layout is not drop-in: no `gsd-sdk` bin in core, source root is package-specific, and compose/override/update tooling routes through the upstream-authority helper.

**Remote authority:** Local `origin` is `chudeemeke/get-stuff-done`, `upstream` is `open-gsd/gsd-core`, and the redirected Glittercowboy lineage is retained only as `legacy-upstream`. GitHub CLI default-repository authority is `chudeemeke/get-stuff-done`. This prevents repository-relative PR/status commands from silently resolving against the retired legacy upstream while preserving explicit historical access.

## Performance Metrics

**Velocity:**

- Total plans completed: 102 (across v0.1.0-v1.2.0 through Phase 42)
- v0.1.0: 12 plans, 1.38 hours
- v0.2.0: 32 plans, 4.45 hours
- v0.3.0: 17 plans, 15.0 hours
- v1.0.0: 21 plans, 4 days (2026-03-28 -> 2026-03-31)
- v1.1.0: 8 plans, 2 days (2026-04-02 -> 2026-04-04)

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
All v1.1.0 decisions archived to .planning/milestones/v1.1.0-ROADMAP.md.

v1.2.0 roadmap decisions:

- 4 phases derived from research-recommended structure (Foundation -> Budget+Process -> Upgrade Resilience -> Ship Polish)
- REL-01 (100% test pass) treated as Phase 41 completion criterion, not aspirational SLO
- SECURITY-04 owned by Phase 41 (audit mode install); block-mode promotion is execution of same requirement, documented as conditional on 2+ weeks clean audit log (Phase 44)
- DOCS-01 (MAINTENANCE.md) placed in Phase 44 with single owner to avoid cross-phase ownership split; sharper acceptance criteria (per-section executable examples + minimum content + CI-extracted example run) added to requirement to prevent slop
- PROCESS-07 graduation criteria locked to specific numbers (N=20 PRs, <=5% FP rate, maintainer-reviewed PR + 2 weeks clean CI + MAINTENANCE.md entry)
- Phase numbering continues integer sequence from v1.1.0 last phase (40) -> v1.2.0 starts at Phase 41
- Phase 40.6 inserted 2026-06-22 after upstream status review found the Phase 40.5 legacy authority assumption invalid. This is not a rewrite and not a GSD-2 migration; it preserves the overlay architecture while changing the authoritative upstream package/repo from legacy TACHES/GSD to Open GSD.
- Phase 40.6 execution migrated active code to exact-pinned `@opengsd/gsd-core@1.5.0`, added the upstream-authority helper/fallback, and changed active `/gsd:upstream` surfaces to `open-gsd/gsd-core`.
- Phase 40.6 completed 2026-06-23. UPGRADE-11 satisfied; package smoke passed from packed tarball; boundary checker remains known structural debt at 41 violations.
- 2026-07-01 GSD self-routing/progress fix: `STATE.md` current phase is authoritative over older partial phase directories for `roadmap analyze` and `init progress`; effective plan counts must include ROADMAP `**Plans:**` / `**Plans**:` declarations so disk-only PLAN counts cannot report false completion.
- 2026-07-01 memory-nexus milestone-scope fix: runtime roadmap parsing must prefer explicit `STATE.md` body milestone text over stale frontmatter, recognize active/in-progress ROADMAP milestone declarations, and filter shared `Phase Details` sections to the current milestone summary list so older milestone detail blocks cannot leak into current progress or next-phase selection.
- 2026-07-03 Phase 42 planning decision: use `macos-15` instead of stale `macos-latest` wording because Phase 41 added a repository invariant forbidding `macos-latest`.
- 2026-07-03 Phase 42 planning decision: add non-interactive `gsd --version --json` provenance before cousin-test CI, because the current launcher otherwise displays a banner and launches `claude`.
- 2026-07-03 Phase 42 planning decision: `@chude/get-stuff-done@3.0.2` is public on npm, so cousin-test default path must not require a secret; optional read-only token support is allowed for private registry scenarios.
- 2026-07-03 Phase 42 plan-review decision: runtime package provenance must read fork version from `package.json` and upstream identity from the upstream-authority helper, not hardcoded literal package/version values.
- 2026-07-03 Phase 42 plan-review decision: docs gates target tracked markdown discovered with `git ls-files` semantics; recursive `**/*.md` globs are not sufficient because ignored generated/dependency copies can pollute the signal.
- 2026-07-03 Phase 42 plan-review decision: perf budget thresholds are strict greater-than boundaries; exact `1.10` does not warn and exact `1.25` does not hard-fail.
- 2026-07-03 Phase 42 Plan 01 decision: `scripts/bench.js` remains measurement-only; `scripts/check-perf.js` owns merge policy, annotations, and accepted-regression handling.
- 2026-07-03 Phase 42 Plan 01 decision: `acceptedRegressions[]` entries must include reviewed metadata plus either `platform` + `metric` or explicit `scope: "global"`; missing targets are not wildcard approvals.
- 2026-07-03 Phase 42 Plan 02 decision: `gsd --version` and `gsd --version --json` exit before config migration, startup banner, or `claude` spawn; runtime package provenance is a non-interactive trust surface for cousin CI.
- 2026-07-03 Phase 42 Plan 02 decision: runtime package provenance derives fork name/version from package.json, upstream package from the upstream-authority helper, upstream version from `dist/.install-meta.json` when available, and overlay manifest identity from the SHA-256 of `dist/.overlay-manifest.json`.
- 2026-07-03 Phase 43 Plan 02 decision: installer rollback is transaction-based around the upstream delegation boundary; it snapshots previous settings, metadata, previous manifests, and current dist overlay paths, then removes only newly copied overlay files and restores prior snapshots on failure.
- 2026-07-03 Phase 43 Plan 02 decision: runtime/install provenance now exposes `forkPackage`, `forkVersion`, `upstreamPackage`, and `upstreamVersion` while preserving `packageName` and `version` as backward-compatible fork aliases for cousin smoke consumers.
- 2026-07-03 Phase 43 Plan 03 decision: `.planning/vetted-upstream-versions.json` is the manifest source of truth for the next compatibility matrix, with exactly `1.5.0`, `1.6.0`, and `1.6.1` as candidate entries. `vettedAt` remains `null` until Plan 04 applies matrix evidence.
- 2026-07-03 Phase 43 Plan 04 decision: compatibility matrix CI is report-only per AF-7 so existing upstream-compat drift does not become a failed PR check; manifest validation and artifact upload remain blocking evidence gates. The matrix report records all three vetted pins as failed, with `1.5.0` classified as the current blocking drift row.
- 2026-07-03 Phase 43 Plan 05 decision: semantic override staleness is limited to parsed `.js` overrides with recorded `Semantic SHA-256`; Acorn parse failures, missing semantic hashes, and non-JS overrides preserve byte-hash blocking behavior. Markdown semantic diff remains deferred to v1.3.0.
- 2026-07-03 Phase 43 Plan 06 decision: check-update adopted the upstream parent/worker split, `detectConfigDir`, shared `$HOME/.cache/gsd` cache, and stale installed hook detection while preserving `@chude/get-stuff-done`, `gsd.role`, maintainer commit classification, and exact 4-hour/7-day throttles. Active `@opengsd/gsd-core@1.5.0` already ships `hooks/gsd-check-update-worker.js`, so the fork worker is an override, not an overlay.
- 2026-07-04 Phase 43 Plan 07 decision: statusline now consumes the same shared package-lineage cache as check-update, parses `active_phase`, `next_action`, `next_phases`, `completed_phases`, `total_phases`, and `percent` from STATE frontmatter, and uses `CLAUDE_CODE_AUTO_COMPACT_WINDOW` for context scaling while preserving fork branding and `gsd.role` update display. `scripts/finalize-dist.js` now finalizes `gsd-check-update-worker.js` with the other bundled hooks.
- 2026-07-04 Phase 43 Plan 08 decision: override churn is generated from filesystem evidence by comparing current override paths across previous and target upstream trees. CHANGELOG writes are constrained to the `override-churn` marker block under `Unreleased`, while real bump execution in Plans 10-11 supplies the actual upstream directory pair.
- 2026-07-04 Phase 43 Plan 09 decision: SBOM generation is a separate `bun run sbom` lifecycle step using pinned `@cyclonedx/cyclonedx-npm@4.2.1`, executed after compose/build and before finalize-dist in `bun run dist`. The wrapper removes Bun's npm user-agent env before spawning CycloneDX and uses `--ignore-npm-errors` because the existing npm tree reports `picomatch` override noise under `npm ls`.
- 2026-07-04 Phase 43 Plan 10 decision: live upstream evidence captured on the 2026-07-03 US session date confirmed `@opengsd/gsd-core@1.6.1` is still latest stable, `1.7.0-rc.2` remains prerelease-only, `open-gsd/gsd-core` is active, and `refs/tags/v1.6.1` exists. Active authority, embedded fallback authority, `package.json`, `bun.lock`, and `node_modules` now use exact stable `1.6.1`; override snapshot refresh remains Plan 11 work.
- 2026-07-11 Phase 43 Plan 11 decision: post-bump override snapshots, worker classification, churn, dist, and SBOM evidence succeeded, and the compatibility harness helper staging, Windows timeout policy, and repo-root-only runtime-test exclusion were repaired. The active `1.6.1` matrix still fails 87 tests after 159 pass. Fail closed: keep `43-11-SUMMARY.md` absent, keep Plans 11-12 incomplete, and add a corrective GSD runtime-compatibility slice before closeout.
- 2026-07-13 Phase 43 corrective-slice decision: treat the matrix as one shared composed-package contract under Open GSD authority, classify every suite and direct internal dependency explicitly, modernize stale success-exit/config/module assumptions, include candidate runtime override coverage, and port only the proven roadmap EOL/format-preservation guarantee through an importer-restricted fork-private adapter. The corrective chain runs before the blocked original Plan 43-11; current authoritative sequencing is recorded in ROADMAP and the canonical dependency graph. No legacy `core.cjs` facade, version-specific suite skip, or silent test exclusion is allowed.
- 2026-07-13 Phase 43 plan-review decision: split umbrella SHIP-03 into Phase 43 SHIP-03A (SBOM generation and tarball inclusion) and Phase 44 SHIP-03B (GitHub release attachment and digest match). Split SHIP-08 production assurance into SHIP-08A, the blocking 95%-per-metric canonical fork-authored gate, and SHIP-08B, the separate blocking shipped-snapshot provenance/drift/delta/N=3 contract. Neither child can stand in for whole-product assurance.
- 2026-07-13 coverage architecture decision: retain Bun as primary functional authority; exact-pin Jest `30.4.2` for unchanged CommonJS parity and c8 `11.0.0` for merged native `NODE_V8_COVERAGE` from Jest plus required Node subprocess/suites. Vitest was rejected after 1,223/1,223 tests paired with 0% measured native CommonJS source. SHIP-08A's planning-time baseline measured 52 canonical fork executable files and counted byte-identical mirrors once; execution must derive and classify the live post-Plan-11L set. SHIP-08B independently blocks on exact provenance, named delta tests, owner/removal triggers, and green N=3 compatibility for every shipped upstream snapshot.
- 2026-07-13 Phase 43 Plan 11A decision: the compatibility contract now classifies all 13 root suites as 11 candidate, 1 repository-only, and 1 retired suite, with fail-closed inventory and authority-boundary validation. Candidate suites execute independently through a marker-owned temp lifecycle; matrix schema v2 carries per-suite evidence, defaults to current-pin blocking, and adds closeout-only `--require-all`. Failed matrix rows retain failure evidence but cannot retain `vettedAt`.
- 2026-07-13 Phase 43 Plan 11A execution evidence: the active Open GSD `1.6.1` run now reports 159 passing and 89 failing assertions across the classified candidate contract. This supersedes the pre-classification 159/87 aggregate for current planning; `43-11-BLOCKER.md` retains that earlier result as historical evidence.
- 2026-07-13 Phase 43 Plan 11B decision: candidate contract suites bind through a pure package-root resolver and explicit package-bound command helper rather than mutating process environment. Repository-only `sync.test.cjs` retains its legacy package default, while matrix subprocesses and direct candidate suites resolve the composed Open GSD package. The shared-process config-plus-sync regression passes 200/200, proving authority is independent of Bun test order.
- 2026-07-13 Phase 43 Plan 11B execution evidence: the exact direct candidate batch passes 243/243, repository-only sync passes 153/153, and the active Open GSD `1.6.1` contract now reports 309 passing and 1 failing assertion. Only the roadmap CRLF/EOL preservation case remains red and is owned by Plan 11C. The transitional gate rejects incomplete, duplicate, contradictory, malformed, or aggregate-inconsistent evidence and passed final independent review.
- 2026-07-14 Phase 43 Plan 11C decision: preserve roadmap bytes through one importer-restricted fork-private adapter, keep the Open GSD global writer unchanged, publish successful closeout evidence atomically, and date vetted pins only from the exact durable report digest. The final shared candidate contract is 11 suites across `1.5.0`, `1.6.0`, and `1.6.1`; active-only phase verification and sync behavior remain blocking repository contracts selected dynamically from the registry.
- 2026-07-14 Phase 43 Plan 11C execution evidence: the standalone active row passes 312/312 and the authoritative `--require-all` matrix passes 936/936 with no failures or skips. Durable report SHA-256 is `6b7bb361b7ef34d82e11be5289ecc3f1f14a8200192ee826fb59ea110d8c387c`; all vetted rows reference it with `status: passed`. Compatibility is resolved, but Plan 11 remains incomplete until SHIP-08A, SHIP-08B, and validator-first closeout pass.
- 2026-07-14 Fable whole-project review decision: retain exact-pin Open GSD plus overlay/override composition; treat Fable as the standing lead developer, architect, and designer for the entire project, covering end state, architecture, sequencing, implementation details, API/UX, integration, release, and maintenance at eight evidence checkpoints plus consequential decision deltas. Fable's reviewed technical and design direction is the default implementation authority. Scoped reviews are checkpoints within that role, not one-plan approvals. Accepted changes add hosted CI before 11D, a machine-readable coverage-feasibility and denominator contract, variance-aware performance disposition, Phase 44 cross-platform reproducibility/scenario evidence, and maintenance-cadence measurement. Fable's INST-04 concern was rejected as stale after 10/10 focused uninstall tests passed and `.planning/PROJECT.md` confirmed Phase 40.6 resolution. Plan 11M is a hosted-CI precondition discovered by the local parity gate, not a substitute for that checkpoint.
- 2026-07-14 Fable checkpoint auth blocker: the 06:52 retry after quota reset used the exact `claude -p --model fable` command with a 118,678-character whole-program delta packet but returned `401 Invalid authentication credentials`; a one-line probe reproduced it. Claude Code 2.1.208 reports a logged-in first-party Max account while `daemon-auth-status.json` reports `auth_required`; Anthropic services were operational. Four shared Claude processes are active, so logout/login, daemon restart, token replacement, and credential mutation are prohibited until the user confirms a safe reauthentication window. The checkpoint remains unavailable, not passed, and no substitute review is accepted.
- 2026-07-14 Plan 11L promotion decision: Open GSD `1.6.1` and `next` both misclassify `*-PLAN-REVIEW.md` through the loose plan-scan fallback. Upstream issue `open-gsd/gsd-core#2252` owns the source defect; Plan 11L adds the minimal shared temporary override, removal trigger, and N=3 revalidation before Plan 11D.
- 2026-07-14 Phase 43 Plan 11L execution evidence: the shared plan-scan override excludes only PLAN-REVIEW derivatives and retains strict, nested, loose-legacy, and summary behavior. Native roadmap tests pass 19/19; repository contracts pass 154/154; the final N=3 matrix passes 945/945 with report SHA-256 `e4b29fab3d07f5fdac1b4c62785ac3a708b7d3bb4f7ca05935c393f39fb84712`. Composed `roadmap analyze` reports the authoritative 46-plan portfolio total, including Phase 43 at 24 and Phase 42 at 5.
- 2026-07-14 Phase 43 Plan 11M trigger: the first ordinary push was correctly blocked when current Bun ignored legacy `bunfig.toml` discovery keys and executed Node-native `.test.cjs` contracts under Bun. Focused native Node authority remains green at 54/54 phase and 19/19 roadmap tests. Plan 11M owns supported Bun discovery configuration, recurrence tests, the negative-path meta-test false positive, and an unbypassed pre-push rerun before hosted CI.
- 2026-07-14 Phase 43 Plan 11M execution evidence: ADR 004 establishes disjoint Bun functional and native Node authorities through one fail-closed adapter and preload capability. The ordinary push hook passed 1,322/1,322 functional tests across 54 files with zero `.test.cjs` headers; native focused contracts passed 73/73, repository compatibility passed 154/154, and local/remote refs matched at `9246673bac922773d3493ba8f430ba46a1ae2c60`. Hosted CI remains the required checkpoint before Plan 11D.
- 2026-07-14 hosted-CI blocker decision: draft PR #23 created all five expected workflow runs at head `64f137a110e985c86b08acb3140bc8b982d34843`, but every job contained zero steps and check-run annotation `86986198397` reported an account billing lock. These rows provide zero hosted evidence. The user owns the off-platform billing action; Plan 11D and every later Phase 43 implementation plan remain blocked until a fresh exact-head run passes.
- 2026-07-14 Fable blocker review decision, superseded in evidence persistence only: accept a machine-checkable exact-head hosted verdict over live GitHub metadata. The initial gitignored-receipt design was rejected by the later independent GSD plan check because isolated-plan worktree cleanup would delete cross-plan evidence. Plan 11N now owns tracked immutable envelopes that certify an ancestor `checkedCommit` and validate governed-digest continuity without self-reference. Plan 11P remains the independent implementation slice before 11N; later product plans remain transitive on 11D.
- 2026-07-14 portable market-ready gate decision: register the authkey handoff as backlog Phase 999.7 under get-stuff-done implementation ownership and conversations orchestration ownership. Activate after v1.2 ships or explicit reprioritization, then run discuss/spec and standing Fable review; do not expand Phase 43/44 release closure into a second multi-project methodology product.
- 2026-07-14 upstream authority reconciliation: renamed the redirected Glittercowboy remote to `legacy-upstream`, assigned `upstream` to `open-gsd/gsd-core`, fetched Open GSD without importing tags, and set the GitHub CLI default to `chudeemeke/get-stuff-done`. Open GSD `next` at `2cbf18642005d235a14a877a24107ba6650cdf7b` still has the W002 diagnostic defect preserved under backlog 999.8. Issue #2252's generated fix branch is byte-identical to `next`, with no fix commit or PR; Plan 11L's override removal trigger remains unmet.
- 2026-07-14 Phase 43 dependency-authority correction: active Open GSD `phase-plan-index` resolves canonical plan IDs, while migrated Phase 43 frontmatter used legacy phase-local IDs. The red read model silently ignored every edge and collapsed all plans into Wave 1. Canonical `43-*` dependencies plus the reviewed plan split now produce an exact 41-plan Waves 1-41 graph with zero wave dependency warnings. This is an execution-order correction, not backlog 999.1's separate same-wave edit-collision hypothesis.
- 2026-07-14 fresh plan-review correction: bare `checkpoint` tasks were non-executable under Open GSD, copied hashes did not prove returned-review integrity, final mutations lacked fresh hosted authority, and Plan 11O violated locked D-22. Plan 11P now owns one TDD checkpoint validator; Fable tasks are automated fail-closed whole-project reviews; Plans 11E/11T/11S separate launcher, runtime, and review ownership; Plan 11Q binds final product closure to fresh hosted CI plus Fable. Plans 12/12B/12C separate automated D-7 preparation, human truth confirmation, Fable entry review, post-correction reruns, and final verification. Health-diagnostic research and upstream evidence move intact to owned backlog 999.8 with an explicit promotion trigger.
- 2026-07-14 fresh re-review correction: output hashing alone could replay an older Fable body, Plan 12B corrections could stale hosted evidence, and PROJECT retained obsolete active results. Plan 11P now specifies a subject/evidence-bound input manifest, nonce, exact captured Fable subprocess receipt, and replay-negative tests for all six Phase 43 callers. Plan 12B republishes the committed post-Fable correction head and recaptures exact-head hosted/CI evidence before 12C. PROJECT now records 315/315 active candidate, 945/945 N=3, separate 154/154 repository authority, and Plan 11K SBOM ownership; Plan 12C rechecks this read model against durable evidence.
- 2026-07-14 second fresh plan-review correction: Plan 11D mixed source ownership, Jest parity, coverage orchestration, feasibility, and review in one oversized plan, while Plan 11J mixed pre-implementation advisory governance with validator source and final measurement. Plans 11D/11W/11X now separate source/Jest, coverage feasibility, and the post-foundation whole-project Fable checkpoint. Plan 11Y owns the pre-validator whole-project Fable checkpoint.
- 2026-07-14 third fresh plan-review correction: Plan 11N's Fable commit would invalidate its only exact-head hosted verdict and its known billing/auth boundary was prose rather than an executable pause. Plan 11N now ends after contract/collector/blocker implementation; non-autonomous Plan 11R owns one blocking human-action gate, first hosted verdict, standing Fable lead decision, tracked disposition, and ordinary GSD summary/metadata finalization. Plan 11D recertifies that fully finalized head before its first source edit. Wildcard-expanded scope also exceeded the real cap in Plans 11F/11G/11H/11J, so 11F/11U, 11G/11V, 11H/11Z, and 11J/11AA/11AB now separate their actual source responsibilities. Plan 12 candidate D-7 evidence uses a dedicated pre-confirmation validator mode. The canonical graph is 41 singleton waves.
- 2026-07-14 Fable lead-authority clarification: Fable is not merely an advisor or critic. Fable is the project's lead developer, architect, and designer; its reviewed technical/design decision and implementation direction are adopted by default. Project orchestration still verifies repository facts and evidence, enforces security and WoW, preserves locked user decisions, and escalates genuine product conflicts to the user.
- 2026-07-14 fourth fresh plan-review correction: hosted verdicts now cross explicit GSD task-commit boundaries as tracked ancestor-certifying envelopes; D-7 uses non-auto-approvable `checkpoint:human-action`; and Plans 11X/11S/11Y record Fable dispositions but halt for a full planner/checker revision and execute-phase restart before any executable-plan correction. The review artifact is `43-GSD-REVIEW.md`, avoiding plan-shaped index noise. Plans 11E, 11J, and 11V split oversized write sets into independently verifiable TDD tasks.
- 2026-07-14 Phase 43 Plan 11P execution evidence: versioned input and receipt schemas now bind exact subject/evidence bytes, nonce, argv `claude -p --model fable`, lead decision, implementation direction, record location, and review digests. The runner rechecks manifest, HEAD, evidence, record, and receipt absence after execution; receipts publish create-only; the offline verifier reconstructs packet and disposition authority. Focused tests pass 28/28. Focused Bun coverage remains below ship authority at 88.37% lines/69.12% functions for the runner and 92.06% lines/93.75% functions for the verifier; Plan 11W owns the blocking four-metric 95% closure. No live Fable call was made because the shared authentication window remains blocked.
- 2026-07-14 Phase 43 Plan 11N execution evidence: immutable tracked hosted envelopes now bind requested PR, exact local/PR/run commit, pull-request event and PR association, all five workflow/job topologies, real steps, and governed source/workflow/contract/policy digests. Pending and strict ancestor verification are offline/read-only; publication is create-only with rollback; the billing-lock shape remains unavailable with zero evidence. Focused/config tests pass 43/43; focused Bun coverage is 97.67% functions and 95.37% lines, while Plan 11W retains statement/branch authority. The exact Fable closeout invocation returned `out of usage credits`, so no fresh Fable approval is claimed and Plan 11R retains the evidence-bearing checkpoint. Implementation commit `dceff38b`, coverage hardening `a8bbb9d3`; no GitHub action occurred.
- 2026-07-19 corrective-review synthesis decision: three independent `xhigh` reviews (Fable, Opus, GPT-5.6-Sol) returned `proceed-with-corrections`. Primary verification confirmed that hosted jobs executed synthetic merge `e2139a78...` while the collector claimed PR head `2c9ba087...`; issues #5-#11 contain 266 accumulated `Observed again` bot comments; PR CI has both OSV and Windows-flake issue-write surfaces; and active compatibility drift can be converted to workflow success. Plan 11R remains blocked without a summary. Plans 11AC-11AI branch from completed 11N for exact-subject/toolchain authority, four bounded remediations, workflow governance, and local integration. Non-autonomous 11AJ alone owns a new user-authorized hosted cycle, formal Fable checkpoint, and eventual truthful 11R supersession closure. The graph is 49 plans across 45 waves; blocked 11R and corrective 11AC share Wave 18 as sibling branches, and downstream implementation ownership is unchanged while shifting by four waves.
- 2026-07-19 continuity correction: local commit `94021639` added source-backed retrospective summaries for executed Phase 40.5 Plans 02/03 and an explicit non-executed supersession summary for Plan 05. GSD now reports Phase 40.5 as 6/6 complete and resumes Phase 43. The active memory-nexus W002 handoff now points to backlog Phase 999.8 under D-22 rather than removed Plan 11O; its inbox lint passes. A remaining conversations control-plane projection is non-blocking and was left untouched because conversations is live.
- 2026-07-19 root-inbox reconciliation: the repository root is a bare control-plane surface and contains three active `docs/inbox/` records that are absent from the locked Phase 43 worktree snapshot; absence here is not closure. Canonical conversations inbox lint and Markdown lint pass for all three. The installer contract re-passes 59/59 with 243 expectations; draft PR #23 remains open at remote head `2c9ba087`, mergeable but blocked by real CI/Upgrade Verifier failures rather than billing, so closure remains merge-gated under Plans 11AC-11AJ. Portable market-ready gates remain triaged to Phase 999.7 after v1.2 or explicit reprioritization, and W002 remains in-progress under Phase 999.8 with the same trigger. The receiver target feed still projects the portable-gate handoff from live conversations; a root-local event records the response without mutating conversations.
- 2026-07-19 Plan 11AC post-Sol WIP checkpoint: a persistent read-only GPT-5.6-Sol `xhigh` review completed after 1,325 seconds with `proceed-with-corrections`, no P0, seven P1, and two P2 findings. RED-GREEN corrections bind attempt-specific GitHub job retrieval and per-subject evidence, reject unsafe run attempts and scalar evidence, close setup-node/setup-bun source inputs, reject shell/runtime injection environments, require exact hosted/toolchain workflow-set equality, and strictly bound `.bun-version`. Focused authority/config tests pass 119/119 with 454 expectations; syntax, diff hygiene, and lint have zero errors. A bounded Node-native seam reuses those verifier suites through `node:test`, exact `expect@30.4.1`, and c8 `9.1.0`; 107/107 Node tests prove per-file four-metric coverage above 95%. Full Jest and c8 11 were removed after proof showed they were unnecessary/incompatible with the deliberate minimatch 3 override. The compatible brace-expansion 1.1.13 override removes the new-path advisory; `bun audit` remains red on eight pre-existing advisories including the Plan 11AD CycloneDX high. The latest broad run executed 1,459 tests (1,458 pass/1 fail); a focused 40-test rerun confirms the sole failure precedes product execution. Bounded diagnosis proves name-based security-module import selects an incompatible PowerShell 7 WindowsApps module, while the exact Windows PowerShell `$PSHOME` manifest succeeds; the unimplemented correction remains dependency-blocked Plan 11AE work. The bundled statusline malformed-input failure did not reproduce across direct, isolated, or full-suite probes and remains conditionally owned by Plan 11I. Direct full-toolchain composition was tested and removed because the mandatory Bash subject-control step makes action-only jobs appear to be Node/Bun runtime jobs under the current conservative classifier. Fable must adjudicate that control-step distinction, checkout/security prelude ordering, momentary versus whole-job subject claims, hosted runtime receipts/runner identity, acceptance/downstream ownership of the bounded coverage seam, and whether the 982-line/1,622-line verifier entry modules require a hexagonal/SRP split. The cold-readable packet is `43-FABLE-PLAN11AC-ADJUDICATION-PACKET-2026-07-19.md`. No Plan 11AC task is complete or committed; no push, hosted run, issue mutation, merge, release, or credential action occurred.
- 2026-07-19 Fable availability recheck: `claude auth status` reports a healthy first-party Max session, reviewer-skill mirrors are byte-identical, and a read-only `fable --effort high` invocation against the Plan 11AC packet returned `session limit` with an exact 09:40 Europe/London reset. No credential mutation or reviewer substitution occurred. Open GSD's canonical progress projection reports 39/71 summaries/plans (55%) and 49 Phase 43 plans; stale 36/71, 51%, and 51-plan persistence values were corrected.
- 2026-07-19 corrective preflight evidence: exact `@cyclonedx/cyclonedx-npm@6.0.0` accepted the current explicit generator arguments, produced a valid CycloneDX 1.6 `get-stuff-done@3.0.2` application BOM, and generated byte-identical output twice. Exact `verdaccio@6.8.0` then ran on an OS-assigned loopback port, registered a crypto-random ephemeral user through its supported HTTP route, and accepted a real authenticated local package publish with no token/password/auth line in captured output or argv. Both evidence records are linked from pending Plans 11AD/11AF; no dependency changed, no task completed, Docker/container authority remains unclaimed, and the server plus owned credential/temp root were destroyed. Subsequent evidence corrected the broader host-config cleanup claim below.
- 2026-07-19 preflight attribution correction and plan-scan hardening: a fresh N=3 run exposed the user-level npm registry pointing to a dead local endpoint. The operator later confirmed manually running `npm set registry http://localhost:4873/` from WSL, so this was operator-created ambient config rather than evidence of a Verdaccio-spike mutation. The shared Windows/WSL userconfig was restored to `https://registry.npmjs.org/` while preserving the unprinted npmjs auth entry; both environments now report npmjs and no local endpoint reference. Plan 11AF retains external npm-config byte invariance as defense in depth. The same run exposed two review documents containing `PLAN11AC` being counted as plans. RED-GREEN hardened the shared override from substring matching to a delimiter-bounded legacy `PLAN` token, preserving current portfolio plan forms. Roadmap tests pass 19/19, repository compatibility 154/154, N=3 compatibility 945/945, and composed progress again reports Phase 43 at 17/49. No hosted or public mutation occurred.
- 2026-07-19 public-repository audit: GitHub and unauthenticated HTTP checks confirmed `chudeemeke/get-stuff-done` was already public, independent, and MIT licensed; no visibility mutation occurred. GitHub secret scanning, push protection, and Dependabot report no open alerts. A 2,660-commit Gitleaks scan over published `origin/*` history returned only two occurrences of one test-fixture filename false positive. Published workflows use standard GitHub-hosted runners; repository cache and artifact snapshots were approximately 1.35 GB and 0.92 MB. Account-level billing attribution was not available without adding the prohibited unapproved `user` token scope. Public planning history contains non-secret absolute user paths and bounded account/authentication diagnostics; forward hygiene is proposed for Plan 11AH, while stale branch-protection reconciliation remains an explicit Plan 11AJ user-gated mutation.
- 2026-07-19 local PR/issue governance implementation: branch `docs/pr-issue-evidence-workflow` is five clean, unpushed commits ahead of `origin/main`. It defines issue and PR evidence templates, the approved label taxonomy, a machine-readable hosted-check contract, forward public-artifact hygiene, and issue-ready records for governance adoption, stale branch protection, missing coverage enforcement, and missing release-plan verification. Focused governance tests pass 12/12 with 182 assertions and prove exact pull-request workflow/job inventory, cataloged issue-form labels, and durable issue links for every missing capability; focused ESLint, changed-document lint, and diff gates pass. Remote labels, issue creation, push, PR, branch-protection reconciliation, signing-key work, merge, and release remain owner-gated and did not occur.
- 2026-07-19 Plan 11AE Windows ACL diagnosis: bounded Windows PowerShell 5.1 probes reproduced the sole broad-suite failure before product execution. `Get-Acl` did not autoload; two security modules were visible, with an incompatible PowerShell 7 WindowsApps module resolving before the Windows PowerShell inbox module. Import by name failed on duplicate `ObjectSecurity` type data, while explicit import of the `$PSHOME` manifest succeeded without changing `PSModulePath`. A disposable-file follow-up protected and read back a real DACL through that manifest and removed its temporary root. Plan 11AE now requires that exact host-owned manifest or an equivalent .NET API, remains dependency-blocked on 11AC, and has no implementation claim.
- 2026-07-19 Plan 11AD audit-authority diagnosis: the repository tracks only `bun.lock`, while `scripts/audit-check.js` hard-requires absent `package-lock.json` for `audit-ci`; the pending plan's second-lock assumption conflicts with Bun-first single-authority discipline. Installed Bun 1.3.5 emits structured audit JSON and exits nonzero on the current eight findings, including the CycloneDX high with a canonical GHSA URL, but bounded `--ignore` probes did not suppress that advisory by slug or numeric ID. Fable D7 must choose a tested Bun-native suppression adapter or an explicitly parity-proven dual-lock contract before Plan 11AD executes. No dependency, lockfile, suppression, or audit policy changed.
- 2026-07-19 upstream stable refresh: npm now reports exact stable `@opengsd/gsd-core@1.7.0`, published 2026-07-15 with registry integrity and git-head evidence, so active pin 1.6.1 is no longer current. Issue #2252 is closed after merged PR 2263, and exact tarball runtime evidence proves 1.7.0 excludes `PLAN-REVIEW` while still counting embedded `PLAN11AC`; the local delimiter-bounded override remains necessary but needs a broader owner. The proposed Fable D8 boundary is a dedicated exact-pin bump, N=3 rotation to 1.6.0/1.6.1/1.7.0, upgrade/override reconciliation, and affected local reruns after Plans 11AD-11AI but before non-autonomous 11AJ. No pin, lockfile, override implementation, package install, public issue/comment, or GitHub state changed; the 2.5 MB registry tarball and probe directory were removed.
- 2026-07-19 authoritative Fable Plan 11AC review: the authenticated read-only `fable --effort high` review completed after 659 seconds with `proceed-with-corrections`. Exact packet, captured response, and authoritative adjudication are preserved as `43-FABLE-PLAN11AC-ADJUDICATION-PACKET-2026-07-19.md`, `43-FABLE-PLAN11AC-WHOLE-PROJECT-REVIEW-2026-07-19.md`, and `43-FABLE-PLAN11AC-AUTHORITATIVE-ADJUDICATION-2026-07-19.md`; the imported authoritative artifact is byte-identical to Fable's external plan at SHA-256 `0245ADEA36CEB48E872D8AB94FF3A97A6106C418C508A2057089BC57CD5D8B9E`. D1-D4 require a closed per-job runtime map, exact control-step exemption, pinned harden-runner/checkout topology, narrowly scoped subject claims, Tier A runner metadata, and Tier B receipts for exactly 21 runtime subjects. D5 accepts the bounded Node/c8 coverage seam. D6 permits at most two hosted-verifier modules plus one toolchain CLI extraction only after D1-D4 pass and only if mechanically pure. D7 makes `bun.lock` the sole audit authority with CycloneDX 6 and a Bun-native advisory adapter. D8 creates combined local Plan 11AK for the exact 1.7.0 bump, N=3 rotation, upgrade/churn proof, and all override reconciliation before Plan 11AJ. The amended graph validates at 50 Phase 43 plans across 46 waves, and roadmap tests pass 19/19. A formal post-hosted Fable checkpoint remains required; no public mutation occurred.
- 2026-07-19 Plan 11AC execution decision: implemented the closed per-job runtime map, byte-exact control-step exemption, optional pinned harden-runner plus closed checkout topology, claim-scoped subject proof, and closed Tier A/Tier B receipt schemas. Retained the exact-pinned `expect` 30.4.1 and c8 9.1.0 Node coverage seam with Bun as functional authority; both verifier files exceed 95% for statements, branches, functions, and lines. D6 extraction was aborted because the named functions cannot move purely without circular imports, factories, or semantic shims; Phase 44 must complete the policy/I/O split before the first post-v1.2 verifier-changing feature. Broad verification remains red only on Plan 11AE's known DACL harness defect, while all audit findings remain Plan 11AD-owned. No hosted or public claim was made.
- 2026-07-19 Plan 11AD execution decision: exact-pinned CycloneDX 6.0.0, removed audit-ci and its forbidden second-lock dependency, and made Bun 1.3.5 JSON the sole package-manager audit authority. The no-shell adapter validates a closed bounded schema, derives IDs only from canonical GitHub advisory URLs, fails closed on command/runtime/status drift, and applies exact visible expiry-bounded suppressions while retaining HIGH/CRITICAL blocking. The generator uses only local executable shims, constrains output to dist, validates CycloneDX 1.6 and package identity, and produced byte-identical real output. Focused and distribution gates pass; the broad suite remains red only on Plan 11AE's independently reproduced PowerShell module/DACL harness failure. No hosted or public claim was made.
- 2026-07-19 Plan 11AE execution decision: target-Windows executable construction uses `path.win32` independently of the host path implementation; receipt containment production code remains unchanged and its tests compare native canonical identities for linked, macOS alias, and Windows short-path cases. The Windows DACL harness explicitly imports the compatible Windows PowerShell inbox security manifest and fails before the probe body when that dependency is unavailable. The broad suite is now 1,505/1,505 and the roadmap module has an enforced per-file c8 gate above 95% in every metric. The user-level registry redirect issued from WSL through Windows npm explains the ambient local endpoint found by preflight, not the hosted verifier's independent missing-authentication `ENEEDAUTH`; the current effective registry is npmjs and no project registry override exists. No hosted or public claim was made.
- 2026-07-19 Plan 11AF execution decision: the upgrade verifier remains a worker behind an explicit external-disposable registry lifecycle. It bootstraps a crypto-random local identity through a bounded HTTP adapter, stores authentication only in an owned npm config, sanitizes every report surface, fingerprints ambient config, and attempts independent cleanup stages on every path. Sol's five fail-open findings became RED tests; the corrective revision removes source `NODE_PATH`, validates exact canonical workspace target identity, binds smoke to the composed-manifest digest, caches baseline absolute non-project tools, isolates npm prepare to static owned husky shims, canonicalizes temp aliases, and rejects reused or identity-mismatched tarballs. A pinned Verdaccio 6.8.0 lifecycle passed all nine real operations with complete cleanup and a source/hash-bound receipt. Fable independently verified those bindings and returned `ACCEPT_WITH_FOLLOW_UPS`, with all ten truths proven. Plan 11AH owns the stale hosted `--from/--to` arguments and immutable Verdaccio image; no hosted or public claim was made.
- 2026-07-20 Plan 11AG execution decision: blocking performance authority is a paired same-run contract over one observed/expected runner and toolchain identity, deterministic AB/BA scheduling, equal independent warmups, at least ten measured pairs, raw positive integer nanosecond receipts, and exact BigInt cross-multiplication at unchanged 1.10/1.25 budgets. Historical baseline/current input is diagnostic only; calendar exceptions, accepted regressions, and threshold overrides cannot reach the paired verdict. Fable independently reproduced 39 focused tests, 100/98.18/100/100 coverage, and the 1,581-test suite, then returned `ACCEPT_WITH_FOLLOW_UPS`. Fable also approved 11AH with binding amendments: same-repository-only blocking paired authority, explicit check-name non-authority, four-checkout authority-file preflight, runner fingerprint/image separation, narrow token allowlisting, and a real Hyperfine 1.20.0 adapter-operability gate in 11AI. No real timing, hosted, regression-acceptance, SLO-change, or public claim was made.
- 2026-07-03 Phase 42 Plan 04 decision: evidence-before-claim oversight is centralized in `overlay/memory/oversight-principle-evidence-before-claim.md`; execution, verification, and planning agents carry only compact advisory triggers tied to PROCESS-07.

### Roadmap Evolution

- Phase 40.5 inserted after Phase 40: Upstream bump 1.34.2 -> 1.38.2 and Phase 41 decision re-verification. (INSERTED 2026-04-22 URGENT). User chose bump-first ordering over plan-now-with-bump-in-Wave-0 to verify Phase 41 decisions against fresh upstream state before planning. UPGRADE-10 requirement added (Phase 40.5); UPGRADE-05 reworded to preserve dogfood-bump timing semantics (Phase 43); backlog 999.3 captured (conditional upstream regex-fix PR filing, only if bug still present on clean 1.38.2).
- Phase 40.6 inserted after Phase 40.5: Upstream authority migration from `get-shit-done-cc` / `gsd-build/get-shit-done` to Open GSD `@opengsd/gsd-core` / `open-gsd/gsd-core`. (INSERTED 2026-06-22). This supersedes Phase 40.5 Wave 5's legacy upstream filing path and blocks Phase 41 until compose/override/update tooling is verified against Open GSD layout.

### Carried Forward Tech Debt

- The legacy `gsd-tools state-snapshot` parser recognizes only the older bold-field STATE template and returns null fields for this repository's current frontmatter/body STATE format. `roadmap analyze` and direct STATE reads remain authoritative until a dedicated TDD fix reconciles the parser and current template; Plan 11AC did not absorb that unrelated state-tooling change.
- 41 boundary violations (structural, informational CI)
- Active Open GSD `1.6.1` now passes 315/315 candidate assertions, and the complete N=3 contract passes 945/945 with durable exact-byte evidence after Plan 11L. Plan 11C's 312/312 and 936/936 evidence, Plan 11B's 309/1 row, Plan 11A's 159/89 baseline, and older 159/87 rows remain history, not current acceptance evidence. Repository-only phase-verification and sync contracts remain separately blocking at 154/154.
- preview-update.js ~5% uncovered I/O paths (documented exception)
- Intermittent Windows subprocess timeout flakiness (advanced in Phase 41 Plans 05-07; 10x validation run `28639808289` passed on Windows, and flake telemetry remains in CI)
- First GitHub Actions run showed `gitleaks/gitleaks-action@v2` needs `GITHUB_TOKEN` for pull_request scans and OSV `@v2` is not a resolvable tag. The repaired PR run `28533068807` passed all CI jobs. Boundary report-only cleanup was remote-verified by run `28534255286`; first-party action/runtime and `macos-latest` annotation cleanup was remote-verified by run `28534944081`. The docs reconciliation run `28535445005` passed but surfaced the remaining `gitleaks/gitleaks-action@v2` Node 20 annotation; `gitleaks/gitleaks-action@v3` cleanup was remote-verified by run `28536087964`.
- Canonical fork-authored coverage remains below the user's 95% per-metric standard. The proven planning-time 52-file baseline is 70.88% statements, 75.48% branches, 74.47% functions, and 70.88% lines; Plan 11D must derive the live set after Plans 11C and 11L add reviewed fork source, so 52 is evidence rather than a hard-coded invariant. Plans 43-11D, 11W, 11K, 11E, 11T, 11F, 11U, 11G, 11V, 11H, 11Z, 11I, 11J, 11AA, and 11AB split SHIP-08A source/Jest, feasibility, toolchain, launcher, runtime, distribution, quality-security, quality-reliability, discovery, execution, hooks/sync, schemas/core, adapters/CI, and final transition into bounded units; Plans 11X, 11S, and 11Y are whole-project review boundaries. SHIP-08B separately covers shipped snapshot assurance. No broad single-task closure, wildcard-compressed scope, or ignore directive is allowed.
- The 2026-07-13 Bun coverage research run passed 1,837 tests and exposed the SBOM integration red. Focused diagnosis proved a Windows case-sensitivity bug: Bunx supplies uppercase npm identity keys while `buildCycloneDxEnv()` deletes lowercase keys from a copied object. Plan 43-11K owns a TDD case-insensitive scrub and full dist regression. All disposable coverage artifacts were removed.
- Phase 41 Plan 04 implemented perf schemas, scripts, package commands, and a `workflow_dispatch` manual capture workflow. Real Linux/macOS/Windows artifacts from run `28638612289` generated `perf-baseline.json` and `.planning/perf/test-timing.json` after the bench script was corrected to use Bun's supported `--cwd` flag instead of unsupported `hyperfine --working-directory`.
- Phase 41 Plan 07 implemented the `workflow_dispatch` 10x validation gate, weekly flake issue maintenance, and D-11/REL-03 maintenance-log discipline. Post-registration run `28639808289` passed on ubuntu-latest, macos-15, and windows-latest.
- Phase 42 planning created five executable plans: 42-01 perf budget enforcement, 42-02 non-interactive runtime package provenance, 42-03 cousin cold-install workflow and INSTALL.md, 42-04 oversight principle/probes, and 42-05 markdown/link docs gates. Planning refresh found `markdownlint-cli2` latest `0.23.0`, `lycheeverse/lychee-action` v2 latest `v2.8.0`, and `@chude/get-stuff-done` npm latest `3.0.2`. External plan review found no blockers; accepted findings were incorporated; second-pass review found no blockers/high/medium findings. Review evidence is recorded in `.planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-PLAN-REVIEW.md`.
- Phase 42 Plan 02 PR #18 first CI run `28645820292` exposed macOS install perf-budget variance: current 176ms vs baseline 134ms, ratio 1.31, but delta 42ms remained within baseline stddev 45ms. Phase 42 Plan 04 PR #19 run `28647664953` recurred at 228ms vs 134ms, ratio 1.70, on macos-15-arm64 runner image `20260623.0190`. Phase 42 Plan 03 PR #20 run `28651494074` recurred on macOS compose at 499ms vs 370ms, ratio 1.35, on runner image `20260630.0202.1` with 96.6ms stddev. `perf-baseline.json` now contains targeted accepted regressions for `macos` + `install`, maxRatio 1.4 for PR #18 and maxRatio 1.8 for PR #19, and `macos` + `compose`, maxRatio 1.5 for PR #20, all expiring 2026-07-10. Replace with rebaseline or variance-aware policy before adding further macOS perf exceptions.
- Phase 42 Plan 04 completed PROCESS-01 through PROCESS-07: four advisory triggers (`EBC-EXEC-POSTMERGE`, `EBC-EXEC-SUMMARY`, `EBC-VERIFY-CI-BEFORE-MEASURE`, `EBC-PLAN-METRIC-COMPAT`) are mechanically probed by `scripts/verify-oversight-probes.js` and `.github/workflows/oversight-probes.yml`.
- Phase 42 Plan 03 completed SHIP-07 and DOCS-04: `scripts/cousin-smoke.js` verifies cold installs through `gsd --version --json`, `.github/workflows/cousin-install.yml` covers ubuntu-latest/macos-15/windows-latest x Node 20/22 x npm/pnpm/bun, and `INSTALL.md` documents public install plus optional read-only token usage.
- Phase 42 Plan 05 completed DOCS-05 and DOCS-06: `scripts/lint-docs.js` runs `markdownlint-cli2@0.23.0` against tracked markdown discovered by `git ls-files`, `.github/workflows/ci.yml` includes a `docs-gates` job using `lycheeverse/lychee-action@v2`, and `lychee.toml` excludes only generated/vendor/dependency paths (`node_modules/`, `dist/`, `.upstream/`, `overlay/get-shit-done/`) plus localhost examples. Local lychee verification passed with 602 total links and 0 errors.
- 2026-07-01 inbox triage fix closed the confirmed authkey `roadmap update-plan-progress` wrong-checkbox/CRLF issue and medesine-rx false-100% progress issue in code and tests. Memory-nexus v5/decimal roadmap drift is partially fixed in code and tests: `init execute-phase 42` now reports `v5.0`, `roadmap analyze` scopes to v5 phases only, and no v4 Phase 30/32.6 leakage remains. Memory-nexus Codex no-frontmatter installer crash is fixed/tested as of 2026-07-03; Phase 43 Plan 02 closed the related installer transaction/preflight/rollback and VERSION mapping clarity items for this repo. Config-schema drift and grouped stale-STATE health diagnostics remain open. Plan 06 completed the remaining real test subprocess migration and added Windows flake telemetry; no active REL-03 skips exist.
- Phase 43 Plan 02 resolved the Plan 01 post-wave `tests/installer-v3.test.js` Windows timeout/hang by finalizing upstream delegation on `exit` and adding rollback-aware post-upstream handling. Hook test timeout debt remains intentionally routed to Phase 43 Plans 06-07.
- Phase 43 Plans 06-07 resolved the coupled check-update/statusline hook reconciliation debt with shared `$HOME/.cache/gsd` cache semantics, package-lineage guards, worker packaging, phase-lifecycle display, and focused hook/compose tests.
- Phase 43 Plan 08 observed that direct `bash .changelog-conflict-check.sh CHANGELOG.md` fails on existing published-release bullets even though the required synthetic `--self-test` passes. Before wiring that script as a full-file gate, clarify whether it is fixture-only or update its awk state handling to ignore normal subsection bullets.
- Phase 43 Plan 09 generated and packaged `dist/bom.json`, and CI uploads it as package evidence. This is SHIP-03A. Literal GitHub release-asset attachment and tarball-digest comparison are SHIP-03B in Phase 44; umbrella SHIP-03 remains incomplete until both children pass.
- The consumed Open GSD `v1.6.1` research clone under the OS temp root was removed on 2026-07-13 only after resolved-path containment, non-reparse-point, clean-worktree, exact-tag, and `open-gsd/gsd-core` remote checks passed. No live project session was touched.
- Phase 43 Plan 10 observed Bun 1.3.5 Windows `bun install --ignore-scripts` crashes while moving from installed `@opengsd/gsd-core@1.5.0` to lockfile target `1.6.1`. Lockfile-only install plus targeted `bun add -d @opengsd/gsd-core@1.6.1 --ignore-scripts` resolved the local install state, and the required frozen install passed afterward.
- Config schema drift remains explicitly owned by backlog 999.2. Phase 43 does not partially migrate `.planning/config.json`: the current fork schema, fork consumers, Open GSD canonical keys, and extension namespace must move atomically, including tested preservation of maintainer routing.
- Open GSD `plan-scan.cjs` derivative misclassification is locally resolved by Plan 11L through a shared temporary override. Stable 1.7.0 and closed issue #2252 own the exact `PLAN-REVIEW` suffix correction, but upstream still counts embedded references such as `PLAN11AC`; the fork's broader delimiter-bounded delta has no new public filing. The current corrected read model reports 71 portfolio plans and 49 Phase 43 plans, with 39 and 17 summaries respectively; no derivative review or diagnosis artifact is counted.
- `roadmap update-plan-progress` preserves semantic counts but rewrites an incomplete five-column progress row's spacing and `-` completion placeholder. Plan 11L closeout corrected the live row manually; backlog 999.6 owns TDD writer fidelity before v1.2.0 closeout or the next roadmap-writer change.
- `_auto_chain_active` schema key (RESOLVED -- fork-specific, fixed in Phase 39 schema)
- Codex `extractFrontmatterField` crash (RESOLVED -- fork-specific, 4 oversight agents had heading before frontmatter)

### Pending Todos

None.

### Blockers/Concerns

BLOCKER [2026-07-20] `hosted-ci-corrective-authority`: GitHub billing and Fable availability are no longer the current blockers, but the 2026-07-14 cycle produced real CI/Upgrade Verifier failures and no passed envelope. Plans 11AC-11AG have locally resolved exact-subject proof, deterministic toolchain inputs, the HIGH CycloneDX advisory, platform ownership, authenticated fail-closed upgrade proof, and paired same-run performance authority. Plans 11AH-11AI must still implement and locally verify read-only same-repository PR performance governance, docs availability classification, active compatibility blocking, exact hosted evidence transport, the real local Hyperfine adapter run, and the integrated local receipt. Plan 11AK then owns D8's exact Open GSD 1.7.0 pin, N=3 rotation, upgrade/churn proof, and override reconciliation. Only after those local gates pass may Plan 11AJ request separate explicit user authorization for a push, hosted run, ruleset/protection mutation, governance PR, bot cleanup, upstream proposal, or formal post-hosted Fable checkpoint. Plan 11D remains blocked until a genuine exact-subject envelope and formal Fable receipt exist.

Plan 43-11 compatibility and plan-denominator correctness are resolved, but the plan remains incomplete on SHIP-08A fork-authored four-metric coverage, SHIP-08B shipped-snapshot assurance, and machine-validatable closeout evidence. `43-11-BLOCKER.md` preserves the pre-classification 159/87 result, Plan 11C's 936/936 replacement, and Plan 11L's current 945/945 evidence. Plan 11 remains blocked until Plans 11J, 11AA, and 11AB implement strict schemas/core, adapters/CI, then measure and transactionally accept durable evidence. Product closure then requires Plan 11Q's fresh exact-subject hosted verdict and Fable lead decision; final phase closure requires Plans 12, 12B, and 12C. Before 11D, Plan 11P provides the shared review validator, Plan 11N provides the collector, blocked Plan 11R preserves the failed first attempt, completed Plans 11AF-11AG plus pending Plans 11AH-11AI restore local corrective authority after completed Plans 11AC-11AE, and Plan 11AJ owns the next human-gated hosted/Fable success path plus truthful 11R closure. Plan 11D recertifies that finalized 11AJ head before its first source edit. Plans 11D and 11W establish source/Jest and coverage-feasibility contracts, and Plan 11X runs the next standing whole-project lead checkpoint before closure plans begin. Plan 11Y runs the pre-validator whole-project checkpoint before 11J/11AA/11AB. The W002 health-diagnostic defect remains backlog 999.8 under locked D-22. Performance enforcement now uses paired same-run authority without renewing calendar exceptions; budget/SLO changes remain user-owned. Fork PRs receive no blocking performance authority until a separately authorized trusted re-run or stronger isolation design exists. Boundary checker still reports 41 known structural root-mirror violations under report-only plus the blocking debt ratchet. Fable's initial whole-project and interim corrective reviews remain authoritative direction; the separate formal post-hosted checkpoint remains unpassed until Plan 11AJ validates it. Old Phase 40.5 Wave 5 remains retired. Root-local inbox handoffs are reconciled terminally when resolved, while memory-nexus/authkey implementation items remain separately owned.

## Session Continuity

Last session: 2026-07-20T05:08:58.0758261+01:00
Resumed: 2026-06-22 -- user approved taking project lead and routing all project work through GSD.
Stopped at: Plan 11AG is locally complete and summarized. Paired same-run performance evidence, exact rational status, raw receipt validation, fresh tracked sample sandboxes, nonblocking historical mode, and unchanged owner-controlled budgets are proven by 39 focused tests, 100/98.18/100/100 coverage, the 1,581-test broad suite, and Fable `ACCEPT_WITH_FOLLOW_UPS`. The roadmap is 44/72 (61%) and Phase 43 is 22/50. Plan 11AH is next with Fable-approved architecture and binding same-repository, check-origin, runner-identity, token, preflight, and later local-Hyperfine amendments. Plan 11R remains blocked; no push, hosted run, public mutation, merge, release, credential change, upstream proposal, or formal post-hosted Fable action is authorized before Plans 11AH-11AI and 11AK pass locally and Plan 11AJ obtains explicit owner gates.
Resume file: .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-11AH-PLAN.md
