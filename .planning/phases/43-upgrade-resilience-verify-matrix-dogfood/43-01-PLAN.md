---
phase: 43
plan: 01
type: execute
wave: 1
depends_on: []
status: pending
requirements: ["UPGRADE-01"]
files_modified:
  - scripts/verify-upgrade.js
  - tests/verify-upgrade.test.js
  - package.json
  - .github/workflows/upgrade-verifier.yml
  - tests/ci-workflow.test.js
  - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-01-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "verify-upgrade uses temp-scoped registry, install target, HOME, USERPROFILE, CLAUDE_CONFIG_DIR, npmrc, and npm cache paths"
    - "verify-upgrade report includes the full D-03 evidence schema"
    - "Linux CI runs the Verdaccio-backed verifier on schedule and upgrade-relevant PR paths"
  artifacts:
    - "scripts/verify-upgrade.js"
    - "tests/verify-upgrade.test.js"
    - ".github/workflows/upgrade-verifier.yml"
  key_links:
    - "UPGRADE-01"
    - "D-03"
---

<objective>
Build the temp-isolated upgrade verifier and Verdaccio workflow with the full
D-03 report schema required for CI, summaries, and maintenance evidence.
</objective>

<context>
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-CONTEXT.md
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-RESEARCH.md
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-VALIDATION.md
@scripts/compose.js
@scripts/cousin-smoke.js
@scripts/lib/upstream-source.js
@.github/workflows/ci.yml
</context>

<tasks>

<task id="01-01" type="auto" tdd="true">
  <name>Write verifier report tests first</name>
  <files>tests/verify-upgrade.test.js</files>
  <read_first>
    - tests/cousin-smoke.test.js
    - tests/run-upstream-compat.test.js
    - scripts/cousin-smoke.js
    - scripts/lib/upstream-source.js
  </read_first>
  <action>
    Create `tests/verify-upgrade.test.js` before implementation. Cover:
    - `node scripts/verify-upgrade.js --help` documents `--from`, `--to`, `--registry-url`, `--json`, `--temp-root`, `--skip-verdaccio-health`, and `--report`.
    - invalid `--from latest` exits non-zero with `exact stable version required`.
    - invalid `--to 1.7.0-rc.2` exits non-zero with `exact stable version required`.
    - report JSON contains `fromVersion`, `toVersion`, `registryUrl`, `packageTarball`, `packedArtifact`, `composeResult`, `reinstallTarget`, `smokeCommands`, `durationMs`, `changedOverrides`, `steps`, `warnings`, and `exitClassification`.
    - injected runner calls use temp-scoped `HOME`, `USERPROFILE`, `CLAUDE_CONFIG_DIR`, `npm_config_userconfig`, `NPM_CONFIG_USERCONFIG`, and `npm_config_cache`.
    - simulated compose and reinstall failures return `compose_failed` and `reinstall_failed`.
  </action>
  <acceptance_criteria>
    - `bun test tests/verify-upgrade.test.js` fails before implementation and exits 0 after implementation.
    - Test file contains `packageTarball`, `packedArtifact`, `composeResult`, `reinstallTarget`, `smokeCommands`, `changedOverrides`, `npm_config_userconfig`, and `compose_failed`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/verify-upgrade.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="01-02" type="auto">
  <name>Implement verify-upgrade orchestration</name>
  <files>scripts/verify-upgrade.js; package.json</files>
  <read_first>
    - tests/verify-upgrade.test.js
    - scripts/cousin-smoke.js
    - scripts/compose.js
    - scripts/lib/upstream-source.js
    - package.json
  </read_first>
  <action>
    Implement `scripts/verify-upgrade.js` as a CommonJS CLI plus exported pure helpers:
    - CLI options: `--from <x.y.z>`, `--to <x.y.z>`, `--registry-url <url>`, `--json`, `--temp-root <path>`, `--skip-verdaccio-health`, `--report <path>`.
    - reject non-stable exact semver for `--from` or `--to`.
    - default registry URL: `http://localhost:4873/`.
    - create temp subdirectories `registry`, `workspace`, `install-target`, `home`, `npm-cache`, and `npmrc`.
    - write a temp npmrc pointing only to the supplied registry.
    - run and record steps `pack-current`, `publish-current`, `install-from`, `bump-upstream`, `compose`, `pack-bumped`, `publish-bumped`, `reinstall-to`, and `smoke-verify`.
    - use argument arrays, never shell string concatenation.
    - emit the full D-03 JSON report to stdout for `--json` and to `--report` when supplied.
    Add package script `"verify-upgrade": "node scripts/verify-upgrade.js"`.
  </action>
  <acceptance_criteria>
    - `node scripts/verify-upgrade.js --help` exits 0.
    - `package.json` contains `"verify-upgrade": "node scripts/verify-upgrade.js"`.
    - `bun test tests/verify-upgrade.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-upgrade.js --help</automated>
    <automated>bun test tests/verify-upgrade.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="01-03" type="auto">
  <name>Wire Linux Verdaccio verifier workflow</name>
  <files>.github/workflows/upgrade-verifier.yml; tests/ci-workflow.test.js</files>
  <read_first>
    - .github/workflows/ci.yml
    - .github/workflows/cousin-install.yml
    - tests/ci-workflow.test.js
    - scripts/verify-upgrade.js
  </read_first>
  <action>
    Create `.github/workflows/upgrade-verifier.yml`:
    - triggers: `workflow_dispatch`, weekly `schedule`, and `pull_request` paths for `.planning/upstream-authority.json`, `package.json`, `bun.lock`, `scripts/compose.js`, `scripts/verify-upgrade.js`, `bin/install.js`, `overlay/**`, and `overrides/**`.
    - runs on `ubuntu-latest`.
    - starts service image `verdaccio/verdaccio:6` on `4873:4873`.
    - installs Bun and Node 22.
    - runs `bun install --frozen-lockfile --ignore-scripts`.
    - runs `bun run verify-upgrade --from 1.5.0 --to 1.6.1 --registry-url http://localhost:4873/ --json --report upgrade-report.json`.
    - uploads `upgrade-report.json` with `actions/upload-artifact@v7`.
    Extend `tests/ci-workflow.test.js` to assert those strings.
  </action>
  <acceptance_criteria>
    - `bun test tests/ci-workflow.test.js` exits 0.
    - `bash scripts/lint-workflows.sh` exits 0.
    - `rg -n "verdaccio/verdaccio:6|4873|verify-upgrade|upgrade-report.json|actions/upload-artifact@v7" .github/workflows/upgrade-verifier.yml tests/ci-workflow.test.js` finds matches.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/ci-workflow.test.js</automated>
    <automated>bash scripts/lint-workflows.sh</automated>
  </verify>
  <done>false</done>
</task>

<task id="01-04" type="auto">
  <name>Record verifier summary</name>
  <files>.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-01-SUMMARY.md</files>
  <read_first>
    - scripts/verify-upgrade.js
    - tests/verify-upgrade.test.js
    - .github/workflows/upgrade-verifier.yml
  </read_first>
  <action>
    Create `43-01-SUMMARY.md` listing verifier steps, D-03 report fields, temp-scope protections, workflow trigger paths, tests run, and any local Verdaccio limitation.
  </action>
  <acceptance_criteria>
    - Summary contains `verify-upgrade`, `Verdaccio`, `packageTarball`, `composeResult`, `changedOverrides`, `upgrade-report.json`, and `UPGRADE-01`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "verify-upgrade|Verdaccio|packageTarball|composeResult|changedOverrides|upgrade-report.json|UPGRADE-01" .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-01-SUMMARY.md</automated>
  </verify>
  <done>false</done>
</task>

</tasks>

<threat_model>
Upgrade verification runs package install and registry operations. Mitigate global
mutation by forcing temp-scoped HOME, USERPROFILE, CLAUDE_CONFIG_DIR, npmrc,
and cache paths; require full report fields so failures cannot be hidden.
</threat_model>

<verification>
Run:
- `node scripts/verify-upgrade.js --help`
- `bun test tests/verify-upgrade.test.js tests/ci-workflow.test.js`
- `bash scripts/lint-workflows.sh`
</verification>
