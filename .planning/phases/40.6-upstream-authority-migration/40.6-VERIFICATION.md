---
phase: 40.6
status: passed
verified_at: 2026-06-23T01:25:00+01:00
requirements: ["UPGRADE-11"]
---

# Phase 40.6 Verification

## Result

Phase 40.6 passes.

The active upstream authority is now Open GSD `@opengsd/gsd-core@1.5.0` /
`open-gsd/gsd-core`. Legacy `get-shit-done-cc` / `gsd-build/get-shit-done`
references remain only as explicit historical, deprecation, fallback, or superseded
evidence.

## Success Criteria

| Criterion | Status | Evidence |
|-----------|--------|----------|
| Authority decision recorded | PASS | `docs/decisions/003-UPSTREAM-AUTHORITY-MIGRATION.md`; `.planning/upstream-authority.json`; `.planning/upstream-authority.schema.json` |
| Open GSD package integrity/layout inspected | PASS | `40.6-MIGRATION-SPIKE.md`; `40.6-02-SUMMARY.md` |
| Active tooling migrated off legacy package | PASS | `scripts/lib/upstream-source.js`; `package.json`; `scripts/compose.js`; `scripts/check-overrides.js`; `scripts/check-boundary.js`; `scripts/preview-update.js`; tests |
| Compose/override/update verified | PASS | `bun run compose`; `node scripts/check-overrides.js`; focused and full tests |
| Boundary state classified | PASS WITH KNOWN DEBT | `node scripts/check-boundary.js` reports 41 structural root-mirror violations; this is pre-existing/informational boundary debt, not a hidden migration failure |
| Package artifact verified | PASS | `npm pack --dry-run --json`; real `npm pack`; scratch consumer `npm install <tarball> --ignore-scripts`; packaged CLI smoke |
| Phase 41 readiness reset | PASS | `.planning/STATE.md`; `.planning/CONTINUE.md`; `.planning/ROADMAP.md`; `.planning/REQUIREMENTS.md` |

## Command Evidence

| Command | Exit | Notes |
|---------|------|-------|
| `bun install --frozen-lockfile --ignore-scripts` | 0 | Checked 234 installs across 310 packages; no changes; scripts intentionally skipped |
| `bun run compose` | 0 | upstream `1.5.0`; overlay `3.0.2`; 644 files written; 118 branding rules applied |
| `bun test tests/sync.test.cjs tests/check-overrides.test.js tests/hooks-manifest.test.js tests/test-config-hygiene.test.js tests/upstream-source.test.js tests/preview-update.test.js tests/preview-update-coverage.test.js` | 0 | 318 pass, 0 fail |
| `bun test tests/package-launcher-v3.test.js` | 0 | 7 pass, 0 fail after package files invariant update |
| `bun test` | 0 | 1666 pass, 0 fail, 2288 expects, 38 files, 554.05s |
| `node scripts/check-overrides.js` | 0 | 2 overrides checked, all fresh |
| authority schema validation via `ajv/dist/2020` | 0 | `authority manifest valid` |
| `git diff --check` | 0 | CRLF/LF warnings only |
| `bun run lint` | 0 | 98 warnings, 0 errors |
| `node scripts/check-boundary.js` | 1 | 41 known structural violations; classified below |

## Package Artifact Smoke

`npm pack --dry-run --json`:

- Package: `@chude/get-stuff-done@3.0.2`
- `entryCount`: 659
- `size`: 2,019,700 bytes
- `unpackedSize`: 7,446,751 bytes
- Includes `bin/preview-update-cli.js`
- Includes `scripts/check-overrides.js`
- Includes `scripts/lib/upstream-source.js`
- Includes `scripts/preview-update.js`
- Does not include `.planning/upstream-authority.json`; packaged runtime uses the embedded Open GSD authority fallback.

Scratch consumer smoke after real `npm pack`:

- `npm install chude-get-stuff-done-3.0.2.tgz --ignore-scripts`: exit 0
- `node node_modules/@chude/get-stuff-done/bin/install.js --help`: exit 0; mentions `@chude/get-stuff-done`
- `node node_modules/@chude/get-stuff-done/bin/preview-update-cli.js`: exit 0; first line `preview-update: Checking for upstream updates...`
- Preview smoke had no `MODULE_NOT_FOUND`, `ENOENT`, or `EINVAL`
- Manual install with `--claude --global --config-dir <scratch>`: exit 0
- Installed `gsd-core`: yes
- Wrote `.install-meta.json`: yes
- Wrote `.overlay-manifest.json`: yes
- Manual uninstall: exit 0; remaining target content `settings.json`

One earlier package-smoke attempt failed because `%TEMP%` reached 0 bytes free during
scratch installs. The generated `gsd-package-consumer-*` scratch directories were
verified under `%TEMP%`, removed, and the smoke was rerun successfully.

## Boundary Classification

`node scripts/check-boundary.js` still exits 1 with 41 violations. The report is
structural: root-level mirrors of upstream-owned paths still exist outside
`overrides/`, `dist/`, or `node_modules/`.

Classes:

- `agents/*.md`: root mirror of upstream agent files.
- `commands/gsd/*.md`: root mirror of upstream command files.
- `assets/*`: root mirror of upstream assets.
- `hooks/*` and `hooks/dist/*`: root mirror of upstream hook files.
- `bin/install.js`, `LICENSE`, `README.md`, `package.json`, `scripts/setup-branch-protection.sh`: root files that also exist in upstream.

This is not caused by the Open GSD migration; it is the same structural boundary
class carried forward from earlier overlay phases, reduced to 41 current hits.
The migration did update `check-boundary` to resolve Open GSD layout correctly.

Disposition: carry as explicit v1.2 structural debt. Do not convert boundary CI to
blocking until the root mirror policy is deliberately redesigned; this preserves the
existing informational CI decision while keeping the debt visible.

## Legacy Reference Audit

Active blockers: zero.

Allowed classes:

- `scripts/lib/upstream-source.js` and `tests/upstream-source.test.js`: explicit legacy
  deprecation/fallback metadata used to document and test the migration contract.
- `docs/decisions/003-UPSTREAM-AUTHORITY-MIGRATION.md`, `40.6-RESEARCH.md`,
  `40.6-MIGRATION-SPIKE.md`, and Phase 40.6 plans/summaries: migration evidence.
- `40.5-05-PLAN.md`: superseded legacy plan text; top-level notice now states that
  command snippets are historical and not live instructions.
- `README.md`, old changelog/methodology/spec docs: original attribution or historical
  documentation.
- `.planning/ROADMAP.md` / `.planning/REQUIREMENTS.md` / `.planning/STATE.md` /
  `.planning/PROJECT.md`: requirement and historical-evidence wording, now paired
  with active Open GSD state.

Active surfaces updated:

- `overlay/commands/gsd/upstream.md` now points at `open-gsd/gsd-core`.
- `overlay/workflows/upstream-sync.md` completion text now names `open-gsd/gsd-core`.
- Generic branding-map fixtures now use `@opengsd/gsd-core` and `open-gsd/gsd-core`.
- Future Phase 43 verify-upgrade wording now references the upstream-authority pin,
  not `get-shit-done-cc`.
- Backlog 999.3 is retired as a legacy filing path unless re-verified against Open GSD.

## Phase 40.5 Wave 5 Disposition

Phase 40.5 Wave 5 is not the next execution path. It is superseded by Phase 40.6.

The old Wave 5 instructions remain as historical plan text only. The plan now states
that no legacy `gsd-build/get-shit-done` issue/PR filing should occur from that file.
Any future upstream filing must be planned against Open GSD and get explicit user
sign-off before filing.

## Global Install Mutation Breach

Plan 04 required: "No global install mutation occurred."

Actual result: Plan 03 ran `bun install` without `--ignore-scripts`, which triggered
the root package lifecycle `node bin/install.js` and mutated the global Claude/GSD
install. This was not repeated during Plan 04. Plan 04 validation used
`bun install --frozen-lockfile --ignore-scripts`, and package behavior was verified
only in scratch consumer directories.

Mitigation:

- Breach is recorded here and in `40.6-03-SUMMARY.md`.
- Later install/package validation used scratch paths only.
- Scratch dirs were cleaned after ENOSPC.
- No further global install mutation was performed.

## Remaining Risks

- Boundary checker remains informational but red with 41 structural violations.
- Lint remains warning-clean only: 98 warnings, 0 errors.
- Root repo outside this worktree has unrelated dirty state and an index lock; this
  phase work was isolated in the `.claude` worktree.

## UPGRADE-11 Verdict

UPGRADE-11 is satisfied for this phase:

- active upstream package is exact-pinned to `@opengsd/gsd-core@1.5.0`;
- authority identity is centralized in a manifest/helper with package-runtime fallback;
- compose, override, boundary, preview-update, installer, package, and tests use the
  Open GSD layout;
- legacy upstream filing paths are explicitly retired or classified.

