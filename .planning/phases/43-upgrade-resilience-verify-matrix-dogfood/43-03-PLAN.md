---
phase: 43
plan: 03
type: execute
wave: 3
depends_on: ["43-02"]
status: pending
requirements: ["UPGRADE-02"]
files_modified:
  - .planning/vetted-upstream-versions.json
  - .planning/vetted-upstream-versions.schema.json
  - scripts/vetted-upstream-versions.js
  - tests/vetted-upstream-versions.test.js
  - package.json
  - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-03-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "vetted upstream manifest contains exactly three stable candidate versions"
    - "current pinned version is blocking"
    - "candidate entries are not marked vetted before matrix evidence exists"
    - "prune-oldest-on-successful-bump behavior is automated and tested"
  artifacts:
    - ".planning/vetted-upstream-versions.json"
    - "scripts/vetted-upstream-versions.js"
    - "tests/vetted-upstream-versions.test.js"
  key_links:
    - "UPGRADE-02"
    - "D-07"
---

<objective>
Create the vetted-upstream candidate manifest and helper without prematurely
marking versions vetted before matrix evidence exists.
</objective>

<context>
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-CONTEXT.md
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-RESEARCH.md
@.planning/upstream-authority.json
@scripts/lib/upstream-source.js
</context>

<tasks>

<task id="03-01" type="auto" tdd="true">
  <name>Write vetted manifest tests first</name>
  <files>tests/vetted-upstream-versions.test.js</files>
  <read_first>
    - .planning/upstream-authority.json
    - tests/upstream-source.test.js
    - scripts/lib/upstream-source.js
  </read_first>
  <action>
    Create `tests/vetted-upstream-versions.test.js` before implementation. Cover:
    - fewer or more than three versions fails with `exactly 3`.
    - `latest`, `next`, and prerelease versions fail with `stable semver`.
    - exactly one entry has `"blocking": true`.
    - blocking entry version equals `.planning/upstream-authority.json` active version.
    - candidate entries may have `"vettedAt": null`.
    - entries with `"vettedAt"` require non-empty `evidence.matrixReport`.
    - `pruneForBump(manifest, "1.7.0")` drops the oldest historical version and keeps exactly three versions.
  </action>
  <acceptance_criteria>
    - `bun test tests/vetted-upstream-versions.test.js` fails before implementation and exits 0 after implementation.
    - Test file contains `exactly 3`, `stable semver`, `blocking`, `vettedAt`, `matrixReport`, and `pruneForBump`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/vetted-upstream-versions.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="03-02" type="auto">
  <name>Add candidate manifest and schema</name>
  <files>.planning/vetted-upstream-versions.json; .planning/vetted-upstream-versions.schema.json</files>
  <read_first>
    - .planning/upstream-authority.json
    - .planning/upstream-authority.schema.json
    - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-CONTEXT.md
  </read_first>
  <action>
    Add `.planning/vetted-upstream-versions.json` with candidates:
    - `1.5.0`, role `current`, blocking `true`, `vettedAt: null`, `evidence: {}`.
    - `1.6.0`, role `historical-candidate`, blocking `false`, `vettedAt: null`, `evidence: {}`.
    - `1.6.1`, role `latest-stable-candidate`, blocking `false`, `vettedAt: null`, `evidence: {}`.
    Add `policy.maxVersions: 3`, `policy.prune: "oldest-on-successful-bump"`, and a note that Plan 04 fills `vettedAt` only after matrix execution.
  </action>
  <acceptance_criteria>
    - `.planning/vetted-upstream-versions.json` contains `1.5.0`, `1.6.0`, `1.6.1`, `"vettedAt": null`, and `"oldest-on-successful-bump"`.
    - `.planning/vetted-upstream-versions.schema.json` contains `maxVersions`, `vettedAt`, and `matrixReport`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "1.5.0|1.6.0|1.6.1|vettedAt|oldest-on-successful-bump" .planning/vetted-upstream-versions.json</automated>
  </verify>
  <done>false</done>
</task>

<task id="03-03" type="auto">
  <name>Implement vetted manifest helper</name>
  <files>scripts/vetted-upstream-versions.js; package.json</files>
  <read_first>
    - tests/vetted-upstream-versions.test.js
    - .planning/vetted-upstream-versions.json
    - .planning/upstream-authority.json
    - scripts/lib/upstream-source.js
  </read_first>
  <action>
    Add `scripts/vetted-upstream-versions.js` with:
    - `loadVettedManifest(filePath)`.
    - `validateVettedManifest(manifest, authority)`.
    - `listMatrixEntries(manifest)`.
    - `applyMatrixEvidence(manifest, report, date)`.
    - `pruneForBump(manifest, newVersion)`.
    - CLI modes `--validate`, `--list-json`, `--apply-matrix-evidence <report>`, and `--prune-for-bump <version>`.
    Add package script `"vetted-upstream:validate": "node scripts/vetted-upstream-versions.js --validate"`.
  </action>
  <acceptance_criteria>
    - `node scripts/vetted-upstream-versions.js --validate` exits 0 with candidate entries.
    - `node scripts/vetted-upstream-versions.js --list-json` emits JSON with exactly three entries.
    - `package.json` contains `"vetted-upstream:validate": "node scripts/vetted-upstream-versions.js --validate"`.
    - `bun test tests/vetted-upstream-versions.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/vetted-upstream-versions.js --validate</automated>
    <automated>node scripts/vetted-upstream-versions.js --list-json</automated>
    <automated>bun test tests/vetted-upstream-versions.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="03-04" type="auto">
  <name>Record manifest summary</name>
  <files>.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-03-SUMMARY.md</files>
  <read_first>
    - .planning/vetted-upstream-versions.json
    - scripts/vetted-upstream-versions.js
  </read_first>
  <action>
    Create `43-03-SUMMARY.md` listing the three candidate versions, blocking policy, why `vettedAt` remains null before Plan 04 matrix evidence, prune behavior, and tests run.
  </action>
  <acceptance_criteria>
    - Summary contains `1.5.0`, `1.6.0`, `1.6.1`, `vettedAt`, `matrix evidence`, and `UPGRADE-02`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "1.5.0|1.6.0|1.6.1|vettedAt|matrix evidence|UPGRADE-02" .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-03-SUMMARY.md</automated>
  </verify>
  <done>false</done>
</task>

</tasks>

<threat_model>
Version matrices can create false confidence if versions are labeled vetted
before evidence exists. Mitigate by separating candidate state from evidence
application and enforcing exact stable pins.
</threat_model>

<verification>
Run:
- `node scripts/vetted-upstream-versions.js --validate`
- `node scripts/vetted-upstream-versions.js --list-json`
- `bun test tests/vetted-upstream-versions.test.js`
</verification>
