---
phase: 43
plan: 10
type: execute
wave: 10
depends_on: ["09"]
status: pending
requirements: ["UPGRADE-05"]
files_modified:
  - .planning/upstream-authority.json
  - .planning/upstream-authority.schema.json
  - scripts/lib/upstream-source.js
  - package.json
  - bun.lock
  - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-10-SUMMARY.md
autonomous: false
must_haves:
  truths:
    - "live upstream target is reverified immediately before bump"
    - "authority pin changes to an exact stable version only"
    - "package dependency and upstream-authority helper agree"
    - "no snapshot hashes are refreshed before the authority pin moves"
  artifacts:
    - ".planning/upstream-authority.json"
    - "scripts/lib/upstream-source.js"
    - "package.json"
  key_links:
    - "UPGRADE-05"
---

<objective>
Reverify the live Open GSD target and bump the active authority/package pin to
the reviewed stable target without touching override snapshots yet.
</objective>

<context>
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-CONTEXT.md
@.planning/vetted-upstream-versions.json
@.planning/upstream-authority.json
@scripts/lib/upstream-source.js
@docs/decisions/003-UPSTREAM-AUTHORITY-MIGRATION.md
</context>

<tasks>

<task id="10-01" type="manual-checkpoint">
  <name>Reverify live upstream target</name>
  <files>.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-10-SUMMARY.md</files>
  <read_first>
    - .planning/upstream-authority.json
    - .planning/vetted-upstream-versions.json
    - docs/decisions/003-UPSTREAM-AUTHORITY-MIGRATION.md
  </read_first>
  <action>
    Re-run live upstream checks immediately before changing authority files:
    - `npm view @opengsd/gsd-core version dist-tags versions --json`.
    - `gh repo view open-gsd/gsd-core --json nameWithOwner,defaultBranchRef,isArchived,pushedAt,updatedAt`.
    - `git ls-remote --tags https://github.com/open-gsd/gsd-core.git`.
    If latest stable is still `1.6.1`, proceed with target `1.6.1`. If latest stable changed, stop and update target only after confirming it is stable, tagged, and compatible with exact-pin policy.
  </action>
  <acceptance_criteria>
    - Summary records command outputs or condensed evidence with date `2026-07-03`.
    - No authority/package files are changed before evidence is recorded.
  </acceptance_criteria>
  <verify>
    <manual>Confirm target stable version and evidence in 43-10-SUMMARY.md before continuing.</manual>
  </verify>
  <done>false</done>
</task>

<task id="10-02" type="auto">
  <name>Bump active upstream authority and package dependency</name>
  <files>.planning/upstream-authority.json; scripts/lib/upstream-source.js; package.json; bun.lock</files>
  <read_first>
    - .planning/upstream-authority.json
    - scripts/lib/upstream-source.js
    - package.json
    - .planning/vetted-upstream-versions.json
  </read_first>
  <action>
    Bump active pin to reviewed stable target:
    - update `.planning/upstream-authority.json` active version from `1.5.0` to target, expected `1.6.1`.
    - update `scripts/lib/upstream-source.js` fallback active version to same target.
    - update `package.json` devDependency `@opengsd/gsd-core` to same exact target.
    - run `bun install` to update `bun.lock`.
    Do not use semver ranges, npm tags, or prerelease versions.
  </action>
  <acceptance_criteria>
    - `rg -n '"version": "1.6.1"|@opengsd/gsd-core.*1.6.1' .planning/upstream-authority.json scripts/lib/upstream-source.js package.json` finds matches if target is `1.6.1`.
    - `bun install --frozen-lockfile --ignore-scripts` exits 0 after lockfile update.
  </acceptance_criteria>
  <verify>
    <automated>bun install --frozen-lockfile --ignore-scripts</automated>
  </verify>
  <done>false</done>
</task>

<task id="10-03" type="auto">
  <name>Prune vetted manifest for bump</name>
  <files>.planning/vetted-upstream-versions.json</files>
  <read_first>
    - .planning/vetted-upstream-versions.json
    - scripts/vetted-upstream-versions.js
    - .planning/upstream-authority.json
  </read_first>
  <action>
    Run `node scripts/vetted-upstream-versions.js --prune-for-bump <target>` and write the resulting three-version manifest. Then run `node scripts/vetted-upstream-versions.js --validate`.
  </action>
  <acceptance_criteria>
    - `.planning/vetted-upstream-versions.json` contains exactly three stable versions.
    - blocking entry equals bumped active upstream version.
    - `node scripts/vetted-upstream-versions.js --validate` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/vetted-upstream-versions.js --validate</automated>
  </verify>
  <done>false</done>
</task>

<task id="10-04" type="auto">
  <name>Record bump summary</name>
  <files>.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-10-SUMMARY.md</files>
  <read_first>
    - .planning/upstream-authority.json
    - package.json
    - .planning/vetted-upstream-versions.json
  </read_first>
  <action>
    Complete `43-10-SUMMARY.md` with live target evidence, authority files changed, exact pin values, lockfile update, vetted manifest prune result, and tests run. State explicitly that override snapshots are not refreshed until Plan 11.
  </action>
  <acceptance_criteria>
    - Summary contains `1.6.1`, `exact stable`, `upstream-authority`, `bun.lock`, `vetted`, `snapshots are not refreshed`, and `UPGRADE-05`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "1.6.1|exact stable|upstream-authority|bun.lock|vetted|snapshots are not refreshed|UPGRADE-05" .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-10-SUMMARY.md</automated>
  </verify>
  <done>false</done>
</task>

</tasks>

<threat_model>
Dogfood bumps can silently consume moving tags or prereleases. Mitigate with
live registry/repo/tag evidence, exact stable pins, and no snapshot refresh until
after the authority pin actually changes.
</threat_model>

<verification>
Run:
- `npm view @opengsd/gsd-core version dist-tags versions --json`
- `node scripts/vetted-upstream-versions.js --validate`
- `bun install --frozen-lockfile --ignore-scripts`
</verification>
