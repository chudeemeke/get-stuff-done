---
phase: 40.6
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - .planning/phases/40.6-upstream-authority-migration/40.6-VERIFICATION.md
  - .planning/phases/40.6-upstream-authority-migration/40.6-README.md
  - .planning/phases/40.6-upstream-authority-migration/40.6-04-SUMMARY.md
  - .planning/STATE.md
  - .planning/CONTINUE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-05-PLAN.md
autonomous: false
requirements: ["UPGRADE-11"]
estimated_minutes: 180
gap_closure: false
must_haves:
  truths:
    - "compose/check-overrides/check-boundary/preview-update verified against Open GSD"
    - "legacy-reference audit distinguishes active failures from historical allowed hits"
    - "package dry-run/pack and scratch consumer install verified"
    - "full bun test and lint/static checks passed unless an environment blocker prevents completion"
    - "Phase 41 readiness reset points at Open GSD authority"
    - "Phase 40.5 Wave 5 disposition is explicit"
    - "No global install mutation occurred"
  artifacts:
    - path: ".planning/phases/40.6-upstream-authority-migration/40.6-VERIFICATION.md"
      provides: "Phase 40.6 verification report"
      min_lines: 60
    - path: ".planning/phases/40.6-upstream-authority-migration/40.6-README.md"
      provides: "Phase directory index"
      min_lines: 15
---

<objective>
Verify the Open GSD authority migration end to end, close Phase 40.6, and reset the
next execution path for Phase 40.5/41.
</objective>

<context>
@.planning/phases/40.6-upstream-authority-migration/40.6-CONTEXT.md
@.planning/phases/40.6-upstream-authority-migration/40.6-VALIDATION.md
@.planning/phases/40.6-upstream-authority-migration/40.6-MIGRATION-SPIKE.md
@.planning/phases/40.6-upstream-authority-migration/40.6-03-SUMMARY.md
@.planning/upstream-authority.json
</context>

<tasks>

<task id="04-01" type="auto">
  <name>Run migration validation commands</name>
  <files>.planning/phases/40.6-upstream-authority-migration/40.6-VERIFICATION.md</files>
  <action>
    Run and record:
    - `bun install --frozen-lockfile` if lockfile was changed and should be frozen.
    - `bun run compose`.
    - `node scripts/check-overrides.js`.
    - `node scripts/check-boundary.js`.
    - focused tests for compose/override/update/helper.
    - `bun run lint`.
    - full `bun test`.
    - `bun run dist` if Plan 03 did not already regenerate and verify dist.

    If any command fails, classify as:
    - migration blocker,
    - pre-existing unrelated failure,
    - expected informational failure with evidence.

    Full `bun test` and lint are required for Phase 40.6 completion. If the environment
    prevents either, record the blocker and do not mark Phase 40.6 complete.
  </action>
</task>

<task id="04-01b" type="auto">
  <name>Verify packed artifact and scratch consumer install</name>
  <files>.planning/phases/40.6-upstream-authority-migration/40.6-VERIFICATION.md</files>
  <action>
    Verify the package artifact, not only source:
    - Run `bun pm pack` or `npm pack --dry-run` to inspect included files.
    - If a tarball is produced, install it into an isolated scratch consumer project with
      `npm install <tarball> --ignore-scripts` or equivalent.
    - Use isolated temp/HOME-style paths where practical and do not mutate the global GSD
      install.
    - Confirm expected `dist`, `bin`, and any required package authority metadata are
      present.
  </action>
</task>

<task id="04-02" type="auto">
  <name>Run active legacy-reference audit</name>
  <files>.planning/phases/40.6-upstream-authority-migration/40.6-VERIFICATION.md</files>
  <action>
    Run:

    ```powershell
    rg -n "get-shit-done-cc|gsd-build/get-shit-done|@opengsd/get-shit-done-redux" package.json bun.lock scripts bin overlay overrides tests dist .github README.md docs .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/CONTINUE.md .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-05-PLAN.md
    ```

    Classify each hit as:
    - active blocker,
    - allowed legacy evidence,
    - historical/archive reference,
    - docs needing updated wording.

    There must be zero active blockers.
  </action>
</task>

<task id="04-02b" type="auto">
  <name>Finalize Phase 40.5 Wave 5 disposition</name>
  <files>.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-05-PLAN.md; .planning/phases/40.6-upstream-authority-migration/40.6-VERIFICATION.md</files>
  <action>
    Amend `40.5-05-PLAN.md` so a future executor cannot follow stale Phase 41 or
    legacy upstream filing instructions. The final disposition must be one of:
    - retired because Phase 40.6 replaces the legacy authority closeout path,
    - amended to run only after Phase 40.6 and only against Open GSD,
    - partially preserved with exact tasks explicitly marked historical.

    Record the chosen disposition in `40.6-VERIFICATION.md`.
  </action>
</task>

<task id="04-03" type="auto">
  <name>Reset Phase 41 readiness</name>
  <files>.planning/STATE.md; .planning/CONTINUE.md; .planning/ROADMAP.md</files>
  <action>
    Update resume surfaces so the next session knows:
    - Phase 40.6 is complete.
    - active authority is Open GSD.
    - Phase 40.5 Wave 5 is either closed, amended, or explicitly no longer needed.
    - Phase 41 planning/execution must use Open GSD evidence.
  </action>
</task>

<task id="04-04" type="auto">
  <name>Create verification and phase index</name>
  <files>.planning/phases/40.6-upstream-authority-migration/40.6-VERIFICATION.md; .planning/phases/40.6-upstream-authority-migration/40.6-README.md</files>
  <action>
    Write:
    - `40.6-VERIFICATION.md` with success-criteria matrix, commands, pass/fail status,
      legacy-reference audit, and unresolved risks.
    - `40.6-README.md` as a short index of all phase artifacts.
  </action>
</task>

<task id="04-05" type="auto">
  <name>Write final wave summary</name>
  <files>.planning/phases/40.6-upstream-authority-migration/40.6-04-SUMMARY.md</files>
  <action>
    Summarize:
    - what changed,
    - verification result,
    - remaining debt,
    - recommended next command.
  </action>
</task>

</tasks>

<success>
Plan 04 succeeds when Phase 40.6 is verifiably complete and the next GSD command is
safe to run without reviving the legacy upstream authority by accident.
</success>
