# Phase 38: Statusline Deployment - Context

**Gathered:** 2026-04-02
**Status:** Ready for planning

<domain>
## Phase Boundary

Deploy the fork's enhanced statusline globally so it works reliably across all projects, not just the GSD project. Fix the composition pipeline gap that prevents the statusline from being included in dist/, wire the global settings, and add timeout safety to both the check-update hook and the statusline itself.

</domain>

<decisions>
## Implementation Decisions

### Composition pipeline
- **D-01:** Move `hooks/gsd-statusline.js` (254 lines, repo root) to `overlay/hooks/gsd-statusline.js`. The composition pipeline walks `overlay/` -- this makes the statusline a proper overlay file, no special cases needed.
- **D-02:** After moving, the statusline will appear in `.overlay-manifest.json` and be included in `dist/`. The installer's overlay file-copy step will deploy it to `~/.claude/hooks/gsd-statusline.js`.
- **D-03:** Also move `hooks/gsd-check-update.js` to `overlay/hooks/` if it's fork-specific (it has maintainer-mode git fetch that upstream doesn't have).

### Global settings wiring
- **D-04:** The overlay's post-install step (after upstream installer completes) patches `~/.claude/settings.json` to add the `statusLine` entry. This is read-modify-write, preserving existing user settings.
- **D-05:** The statusLine setting should be: `{ "type": "command", "command": "node <target>/hooks/gsd-statusline.js" }` where `<target>` is the resolved install directory (same as where overlay files are copied).
- **D-06:** If user already has a custom statusLine setting, preserve it (don't overwrite). Only add if missing.

### Timeout fix
- **D-07:** Reduce `gsd-check-update.js` git fetch timeout from 15s to 3s. On timeout, write `fetch_error: true` to cache (existing behavior) and continue gracefully.
- **D-08:** Add a defensive 3s overall timeout to the statusline script itself (belt-and-suspenders). If the statusline takes >3s for any reason, output a fallback minimal status and exit 0.
- **D-09:** Industry pattern: statusline NEVER does blocking I/O. It reads pre-computed cache. The check-update hook writes the cache asynchronously. This architecture is already correct -- the timeout changes are safety nets.

### Claude's Discretion
- Whether gsd-check-update.js needs to move to overlay/ or is already handled by the composition pipeline
- Exact fallback output format when statusline times out
- Whether to use `setTimeout` or `AbortController` for the statusline timeout

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Statusline code
- `hooks/gsd-statusline.js` -- Fork's enhanced statusline (254 lines). Will be moved to `overlay/hooks/`.
- `hooks/gsd-check-update.js` -- Fork's update checker with maintainer-mode git fetch (line 82: 15s timeout).

### Globally installed versions (for comparison)
- `~/.claude/hooks/gsd-statusline.js` -- Currently upstream's 119-line base (will be replaced)
- `~/.claude/hooks/gsd-check-update.js` -- Currently upstream's version (npm check only, no git fetch)

### Installer
- `bin/install.js` -- Delegation installer. Post-install overlay file copy step needs statusLine settings wiring.

### Composition pipeline
- `scripts/compose.js` -- 5-stage pipeline. `overlay/` directory is the source for fork-specific files.
- `dist/.overlay-manifest.json` -- Lists all overlay files. Currently does NOT include statusline hooks.

### Settings
- `.claude/settings.json` (project-level) -- Has statusLine setting (relative path, only works in GSD project)
- `~/.claude/settings.json` (global) -- Has hooks but NO statusLine setting (the gap)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `bin/install.js` post-install overlay copy step (line ~500+) -- copies overlay files from dist/ to target. Can be extended to also patch settings.json.
- `scripts/compose.js` resolve() stage -- already walks `overlay/` recursively. Moving files to `overlay/hooks/` is sufficient.

### Established Patterns
- Installer does read-modify-write on `settings.json` for hooks registration (upstream pattern). Same approach works for statusLine.
- Overlay manifest is auto-generated by compose pipeline -- no manual entries needed once files are in `overlay/`.

### Integration Points
- `compose.js` resolve() -> picks up `overlay/hooks/*.js`
- `bin/install.js` copyOverlayFiles() -> deploys to `~/.claude/hooks/`
- `bin/install.js` needs new `patchSettingsJson()` for statusLine entry

</code_context>

<specifics>
## Specific Ideas

- The statusline currently uses relative path (`node hooks/gsd-statusline.js`) in project settings, which only works when Claude Code is opened in the GSD project. Global settings need an absolute path to the installed hook location.
- The check-update hook has a maintainer-mode path that does `git fetch upstream` -- this is fork-specific behavior not in upstream. Confirm it belongs in overlay/.

</specifics>

<deferred>
## Deferred Ideas

None -- discussion stayed within phase scope

</deferred>

---

*Phase: 38-statusline-deployment*
*Context gathered: 2026-04-02*
