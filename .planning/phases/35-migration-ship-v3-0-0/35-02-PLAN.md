---
phase: 35-migration-ship-v3-0-0
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - UPGRADING.md
autonomous: false
requirements:
  - MIG-01
  - MIG-02
  - MIG-06

# REVIEWS replan changes (2026-03-30):
# - Item 5: Derive tag commit from `git rev-list -n 1 v2.4.0` instead of hardcoding 681dab8
# - Item 6: Remote tag push is now a human-verify checkpoint (was autonomous:true)
# - autonomous changed to false due to human-verify checkpoint for remote tag push

must_haves:
  truths:
    - "v2.4.0-legacy annotated tag exists on the same commit as v2.4.0"
    - "v2.4.0-legacy tag is pushed to origin"
    - "UPGRADING.md documents what changed, how to install v3.0, and how to rollback to v2.4.0"
    - ".planning/ history is intact (no squashing or rewriting)"
  artifacts:
    - path: "UPGRADING.md"
      provides: "Migration and rollback documentation"
      contains: "bunx @chude/get-stuff-done@2.4.0"
  key_links:
    - from: "UPGRADING.md"
      to: "npm registry"
      via: "bunx install command"
      pattern: "bunx @chude/get-stuff-done@"
---

<objective>
Create the v2.4.0-legacy git tag (derived from v2.4.0 tag, not hardcoded) and write UPGRADING.md with migration/rollback documentation.

Purpose: MIG-01 requires tagging the last pre-overlay commit for rollback reference. MIG-06 requires documented rollback instructions. MIG-02 is already satisfied (overlay structure is on main) but is formally closed here.

Output: Git tag created locally, pushed via human-verified step, UPGRADING.md committed.
</objective>

<execution_context>
@C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-migration-ship-v3-0-0/35-CONTEXT.md
@.planning/phases/35-migration-ship-v3-0-0/35-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v2.4.0-legacy tag and write UPGRADING.md</name>
  <files>UPGRADING.md</files>
  <read_first>
    - .planning/phases/35-migration-ship-v3-0-0/35-CONTEXT.md (D-01 for tag name, D-06 for rollback docs)
    - .planning/phases/35-migration-ship-v3-0-0/35-RESEARCH.md (tagging commands, UPGRADING.md template)
    - package.json (verify current version, verify package name)
  </read_first>
  <action>
    **Git tag (per D-01, addresses review Item 5 -- derive commit instead of hardcoding):**

    1. Derive the target commit from the existing v2.4.0 tag (not hardcoded):
    ```bash
    TARGET_COMMIT=$(git rev-list -n 1 v2.4.0)
    echo "Tagging commit: $TARGET_COMMIT"
    ```

    2. Create annotated tag `v2.4.0-legacy` on the derived commit:
    ```bash
    git tag -a v2.4.0-legacy "$TARGET_COMMIT" -m "Legacy tag: last release before overlay architecture (v3.0.0)"
    ```

    3. Verify the tag was created correctly:
    ```bash
    git show v2.4.0-legacy --format="%H" --no-patch
    git show v2.4.0 --format="%H" --no-patch
    # Both should output the same full commit hash
    ```

    NOTE: Do NOT push the tag to origin yet -- that happens in the checkpoint step below.

    This satisfies MIG-01 (legacy tag) and MIG-02 (overlay is already on main -- tag-and-continue per D-01, no branch merge needed). MIG-03 is satisfied by D-02 (no squashing).

    **Sync-snapshot tag cleanup (Claude's discretion per CONTEXT.md):**

    Leave the 72 sync-snapshot-* tags in place. Per RESEARCH.md recommendation: they cost nothing and document pre-overlay sync history. No cleanup.

    **UPGRADING.md (per D-06):**

    4. Create UPGRADING.md at project root with this content (no emojis, factual tone):

    Title: "Upgrading to v3.0.0"
    Sections:
    - "What Changed" -- overlay architecture explanation, all additions continue working
    - "Install v3.0" -- `bunx @chude/get-stuff-done@3.0.0 --claude --global` with note about auto v2.x cleanup
    - "Rollback to v2.x" -- `bunx @chude/get-stuff-done@2.4.0 --claude --global` with git tag reference
    - "Architecture Overview" -- comparison table (v2.x vs v3.0): upstream integration, package contents, branding, feature control, module overrides

    Use the template from RESEARCH.md as the base.

    5. Commit UPGRADING.md:
    ```bash
    git add UPGRADING.md && git commit -m "docs(35): add UPGRADING.md with v3.0 migration and rollback instructions"
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done" && git show v2.4.0-legacy --format="%H" --no-patch && git show v2.4.0 --format="%H" --no-patch && diff <(git show v2.4.0-legacy --format="%H" --no-patch) <(git show v2.4.0 --format="%H" --no-patch) && grep -c "bunx @chude/get-stuff-done@2.4.0" UPGRADING.md && grep -c "bunx @chude/get-stuff-done@3.0.0" UPGRADING.md</automated>
  </verify>
  <acceptance_criteria>
    - `git show v2.4.0-legacy --format="%H" --no-patch` outputs a full commit hash
    - `git show v2.4.0 --format="%H" --no-patch` outputs the SAME hash (tags point to same commit)
    - `git tag --list v2.4.0-legacy` returns `v2.4.0-legacy`
    - UPGRADING.md exists at project root
    - UPGRADING.md contains `bunx @chude/get-stuff-done@3.0.0 --claude --global`
    - UPGRADING.md contains `bunx @chude/get-stuff-done@2.4.0 --claude --global`
    - UPGRADING.md contains `v2.4.0-legacy`
    - UPGRADING.md contains the architecture comparison table
    - UPGRADING.md does NOT contain emojis
  </acceptance_criteria>
  <done>v2.4.0-legacy tag on correct commit (derived, not hardcoded), UPGRADING.md documents install, rollback, and architecture changes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify and push legacy tag to origin</name>
  <files></files>
  <action>
    Human verifies the local tag and UPGRADING.md, then pushes the tag to origin.

    1. Verify the local tag exists and points to the right commit:
    ```
    git log --oneline v2.4.0-legacy -1
    git log --oneline v2.4.0 -1
    ```
    Both should show the same commit hash and message.

    2. Review UPGRADING.md:
    ```
    cat UPGRADING.md
    ```
    Confirm content looks correct (install command, rollback command, architecture table).

    3. Push the tag to origin:
    ```
    git push origin v2.4.0-legacy
    ```

    4. Verify the push succeeded:
    ```
    git ls-remote --tags origin v2.4.0-legacy
    ```
    Should return a line with the tag reference.

    Resume signal: Type "pushed" after confirming the tag is on origin, or describe any issues.
  </action>
  <verify>
    <automated>cd "C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done" && git ls-remote --tags origin v2.4.0-legacy 2>/dev/null | grep -c "v2.4.0-legacy"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote --tags origin v2.4.0-legacy` returns a non-empty result
    - Tag points to the same commit as v2.4.0
  </acceptance_criteria>
  <done>v2.4.0-legacy tag pushed to origin, verified via git ls-remote</done>
</task>

</tasks>

<verification>
1. `git show v2.4.0-legacy --format="%H" --no-patch` and `git show v2.4.0 --format="%H" --no-patch` output the same hash
2. `git ls-remote --tags origin v2.4.0-legacy` confirms remote push
3. `git log --oneline .planning/ | wc -l` confirms .planning/ history is intact (MIG-03)
4. `grep "bunx @chude/get-stuff-done@2.4.0" UPGRADING.md` returns a match (MIG-06)
5. `cat UPGRADING.md` contains all required sections
</verification>

<success_criteria>
- v2.4.0-legacy annotated tag exists on the same commit as v2.4.0 and is pushed to origin (MIG-01)
- Overlay structure is confirmed on main (MIG-02 -- already satisfied, formally closed)
- UPGRADING.md documents rollback via version pin (MIG-06)
- No history rewriting occurred (MIG-03 per D-02)
</success_criteria>

<output>
After completion, create `.planning/phases/35-migration-ship-v3-0-0/35-02-SUMMARY.md`
</output>
