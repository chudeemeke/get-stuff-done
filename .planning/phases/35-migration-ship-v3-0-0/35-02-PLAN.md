---
phase: 35-migration-ship-v3-0-0
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - UPGRADING.md
autonomous: true
requirements:
  - MIG-01
  - MIG-02
  - MIG-06

must_haves:
  truths:
    - "v2.4.0-legacy annotated tag exists on commit 681dab8"
    - "UPGRADING.md documents what changed, how to install v3.0, and how to rollback to v2.4.0"
    - ".planning/ history is intact (no squashing or rewriting)"
  artifacts:
    - path: "UPGRADING.md"
      provides: "Migration and rollback documentation"
      contains: "bunx @chude/get-stuff-done@2.4.0"
  key_links:
    - from: "UPGRADING.md"
      to: "npm registry"
      via: "bunx install command"
      pattern: "bunx @chude/get-stuff-done@"
---

<objective>
Create the v2.4.0-legacy git tag and write UPGRADING.md with migration/rollback documentation.

Purpose: MIG-01 requires tagging the last pre-overlay commit for rollback reference. MIG-06 requires documented rollback instructions. MIG-02 is already satisfied (overlay structure is on main) but is formally closed here.

Output: Git tag pushed, UPGRADING.md committed.
</objective>

<execution_context>
@C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-migration-ship-v3-0-0/35-CONTEXT.md
@.planning/phases/35-migration-ship-v3-0-0/35-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v2.4.0-legacy tag and write UPGRADING.md</name>
  <files>UPGRADING.md</files>
  <read_first>
    - .planning/phases/35-migration-ship-v3-0-0/35-CONTEXT.md (D-01 for tag name, D-06 for rollback docs)
    - .planning/phases/35-migration-ship-v3-0-0/35-RESEARCH.md (tagging commands, UPGRADING.md template)
    - package.json (verify current version is 2.4.0, verify package name)
  </read_first>
  <action>
    **Git tag (per D-01):**

    1. Create annotated tag `v2.4.0-legacy` on commit `681dab8` (the v2.4.0 release commit):
    ```bash
    git tag -a v2.4.0-legacy 681dab8 -m "Legacy tag: last release before overlay architecture (v3.0.0)"
    ```

    2. Push the tag:
    ```bash
    git push origin v2.4.0-legacy
    ```

    This satisfies MIG-01 (legacy tag) and MIG-02 (overlay is already on main -- tag-and-continue per D-01, no branch merge needed). MIG-03 is satisfied by D-02 (no squashing).

    **Sync-snapshot tag cleanup (Claude's discretion per CONTEXT.md):**

    Leave the 72 sync-snapshot-* tags in place. Per RESEARCH.md recommendation: they cost nothing and document pre-overlay sync history. No cleanup.

    **UPGRADING.md (per D-06):**

    3. Create UPGRADING.md at project root with this content:

    ```markdown
    # Upgrading to v3.0.0

    ## What Changed

    v3.0 replaces the direct-edit fork with an overlay architecture.
    Upstream (get-shit-done-cc) is now consumed as a build-time dependency
    and composed into a self-contained dist/ at publish time.

    All fork-specific additions (platform detection, theming, validation,
    sync tools, hooks) continue to work identically. Internal paths
    (get-shit-done/) are preserved -- only user-visible text is rebranded.

    ## Install v3.0

    ```
    bunx @chude/get-stuff-done@3.0.0 --claude --global
    ```

    If you have a v2.x installation, the installer automatically detects
    and cleans up old files before proceeding. User config (~/.gsd/) and
    project data (.planning/) are not affected.

    ## Rollback to v2.x

    ```
    bunx @chude/get-stuff-done@2.4.0 --claude --global
    ```

    The v2.4.0 release remains available on npm. The `v2.4.0-legacy` git
    tag marks the last commit before the overlay architecture.

    ## Architecture Overview

    | Aspect | v2.x | v3.0 |
    |--------|------|------|
    | Upstream integration | Cherry-pick sync | npm devDependency |
    | Package contents | Source files | Composed dist/ |
    | Branding | Internal path renaming | Surface-only text |
    | Feature control | Direct file edits | features.json exclusion |
    | Module overrides | Direct file replacement | overrides/ with REASON.md |
    ```

    Note: Use proper markdown fenced code blocks for the install/rollback commands (the triple-backtick notation above is escaped for plan readability -- the actual file should use standard markdown code blocks).
  </action>
  <verify>
    <automated>cd "C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done" && git show v2.4.0-legacy --format="%H" --no-patch 2>/dev/null | grep -c "681dab8" && grep -c "bunx @chude/get-stuff-done@2.4.0" UPGRADING.md</automated>
  </verify>
  <acceptance_criteria>
    - `git show v2.4.0-legacy --format="%H" --no-patch` outputs a hash starting with `681dab8`
    - `git tag --list v2.4.0-legacy` returns `v2.4.0-legacy`
    - UPGRADING.md exists at project root
    - UPGRADING.md contains `bunx @chude/get-stuff-done@3.0.0 --claude --global`
    - UPGRADING.md contains `bunx @chude/get-stuff-done@2.4.0 --claude --global`
    - UPGRADING.md contains `v2.4.0-legacy`
    - UPGRADING.md contains the architecture comparison table
    - UPGRADING.md does NOT contain emojis
  </acceptance_criteria>
  <done>v2.4.0-legacy tag on correct commit, UPGRADING.md documents install, rollback, and architecture changes</done>
</task>

</tasks>

<verification>
1. `git show v2.4.0-legacy --format="%H" --no-patch` outputs hash starting with 681dab8
2. `git log --oneline .planning/ | wc -l` confirms .planning/ history is intact (MIG-03)
3. `grep "bunx @chude/get-stuff-done@2.4.0" UPGRADING.md` returns a match (MIG-06)
4. `cat UPGRADING.md` contains all required sections
</verification>

<success_criteria>
- v2.4.0-legacy annotated tag exists on commit 681dab8 and is pushed to origin (MIG-01)
- Overlay structure is confirmed on main (MIG-02 -- already satisfied, formally closed)
- UPGRADING.md documents rollback via version pin (MIG-06)
- No history rewriting occurred (MIG-03 per D-02)
</success_criteria>

<output>
After completion, create `.planning/phases/35-migration-ship-v3-0-0/35-02-SUMMARY.md`
</output>
