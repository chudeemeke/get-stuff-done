---
phase: 34-testing-ci-enforcement
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/check-boundary.js
  - tests/check-boundary.test.js
  - scripts/run-upstream-compat.js
autonomous: true
requirements: [TEST-02, TEST-03, TEST-04, CI-01]

must_haves:
  truths:
    - "check-boundary.js detects upstream files existing in repo outside overrides/ and dist/"
    - "check-boundary.js exits 0 when no violations and exits 1 with violation report when violations found"
    - "Upstream compat runner executes upstream .test.cjs files against composed dist/ output"
    - "Compat runner reports pass/fail count for each upstream test file"
  artifacts:
    - path: "scripts/check-boundary.js"
      provides: "Repo boundary enforcement -- finds vendored upstream files"
      min_lines: 80
    - path: "tests/check-boundary.test.js"
      provides: "Tests for boundary check script"
      min_lines: 60
    - path: "scripts/run-upstream-compat.js"
      provides: "Upstream test runner against composed dist/"
      min_lines: 80
  key_links:
    - from: "scripts/check-boundary.js"
      to: "node_modules/get-shit-done-cc/"
      via: "walkDir comparison against upstream file tree"
      pattern: "get-shit-done-cc"
    - from: "scripts/run-upstream-compat.js"
      to: "dist/get-shit-done/"
      via: "symlink/junction redirection for test path resolution"
      pattern: "symlinkSync"
---

<objective>
Create two new CI enforcement scripts: check-boundary.js (boundary violation detection) and run-upstream-compat.js (upstream test runner against composed dist/).

Purpose: check-boundary.js prevents accidental vendoring of upstream files into the repo (CI-01). run-upstream-compat.js validates that composed output preserves upstream behavioral correctness (TEST-03). TEST-02 was completed during research (assertion categorization: 0.22% package-name, 1.7% path-based, 98.1% behavioral -- PASS on feasibility gate).

Output: Two tested scripts ready for CI integration in Plan 04
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-testing-ci-enforcement/34-RESEARCH.md

<interfaces>
From scripts/check-overrides.js (established pattern for CI scripts):
```javascript
function walkDir(dir, base) {
  const results = [];
  if (!fs.existsSync(dir)) return results;
  for (const entry of fs.readdirSync(dir)) {
    const abs = path.join(dir, entry);
    const rel = base ? `${base}/${entry}` : entry;
    if (fs.statSync(abs).isDirectory()) {
      results.push(...walkDir(abs, rel));
    } else {
      results.push(rel);
    }
  }
  return results;
}

// CLI entry pattern:
if (require.main === module) {
  const result = checkOverrides(opts);
  // ... format and print report ...
  process.exit(result.ok ? 0 : 1);
}

module.exports = { checkOverrides };
```

From tests/helpers.cjs:
```javascript
const TOOLS_PATH = path.join(__dirname, '..', 'get-stuff-done', 'bin', 'gsd-tools.cjs');
// Tests require from get-stuff-done/bin/lib/*.cjs
```

Upstream test files (12 .test.cjs files in tests/):
- All require from `get-stuff-done/bin/lib/*.cjs` or use helpers.cjs TOOLS_PATH
- Compat runner creates symlink: `get-stuff-done/` -> `dist/get-shit-done/` in temp dir
- sync.test.cjs excluded from compat runner (has dist-relative import blocker)

From research assertion categorization:
- 451/452 upstream tests currently pass
- sync.test.cjs is the 1 failing file (dist-relative import)
- 11 remaining files pass with path redirection
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD check-boundary.js -- boundary violation detection</name>
  <files>scripts/check-boundary.js, tests/check-boundary.test.js</files>
  <action>
**RED phase:** Create tests/check-boundary.test.js with failing tests:

1. Test that checkBoundary() returns `{ ok: true, violations: [] }` when no upstream files exist in repo outside allowed dirs
2. Test that a file matching an upstream path triggers a violation
3. Test that files in overrides/ are explicitly allowed (not violations)
4. Test that files in dist/ are explicitly allowed (not violations)
5. Test that files in node_modules/ are explicitly allowed
6. Test that .git/ is excluded from scanning
7. Test the CLI exit code behavior (0 for clean, 1 for violations)

Use a temp directory with mock upstream and repo structures for isolation. Follow the check-overrides.js pattern: export checkBoundary() for testability, CLI entry via require.main check.

Commit RED: `test(34-02): add failing tests for check-boundary.js`

**GREEN phase:** Implement scripts/check-boundary.js:

1. Walk `node_modules/get-shit-done-cc/` to get upstream file set (relative paths)
2. Walk project root, skipping ALLOWED_PREFIXES: `overrides/`, `dist/`, `node_modules/`, `.git/`, `.planning/`, `tests/`, `.github/`
3. For each repo file: check if its relative path exists in the upstream file set
4. Collect violations with file path and guidance
5. Export `checkBoundary(opts)` returning `{ ok, violations }`
6. CLI entry: print violations, exit 1 if any found, exit 0 if clean
7. Accept `--upstream-dir` and `--project-dir` flags for test isolation (same pattern as check-overrides.js)

The script should be ~80-120 lines following the check-overrides.js structure. Use CJS (require/module.exports) consistent with all other scripts/.

Commit GREEN: `feat(34-02): implement check-boundary.js repo boundary enforcement`
  </action>
  <verify>
    <automated>bun test tests/check-boundary.test.js</automated>
  </verify>
  <done>check-boundary.js detects violations (upstream files in repo outside allowed dirs), returns structured result, exits 1 on violation and 0 on clean. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build upstream compatibility runner</name>
  <files>scripts/run-upstream-compat.js</files>
  <action>
Create scripts/run-upstream-compat.js that runs upstream .test.cjs files against composed dist/ output.

Implementation:
1. Verify dist/ exists (error with "Run `bun run compose` first" if missing)
2. Create temp directory with `fs.mkdtempSync`
3. Create symlink/junction: `{tmpDir}/get-stuff-done/` -> `{PROJECT_ROOT}/dist/get-shit-done/`
   - Windows: use `fs.symlinkSync(target, link, 'junction')`
   - Unix: use `fs.symlinkSync(target, link, 'dir')`
4. Copy `tests/helpers.cjs` to `{tmpDir}/tests/helpers.cjs`
5. Copy all `tests/*.test.cjs` files to `{tmpDir}/tests/` EXCEPT `sync.test.cjs` (excluded per research -- dist-relative import blocker, fork-specific code tested via TEST-01)
6. Patch the copied helpers.cjs TOOLS_PATH to use the symlinked path: `path.join(__dirname, '..', 'get-stuff-done', 'bin', 'gsd-tools.cjs')`
7. Run `node --test {tmpDir}/tests/*.test.cjs` via execFileSync, capture output
8. Parse output for pass/fail counts
9. Print summary report: N passed, M failed, K skipped
10. Clean up temp directory in a finally block
11. Exit 0 if all pass, exit 1 if any fail

The runner must NOT modify upstream test files. Path redirection via symlink is the only adaptation.

Accept `--compose-first` flag that runs `bun run compose` before testing (convenience for CI).

This script does NOT need a bun:test wrapper (it is itself a test runner). Verify it works by running `bun run compose && node scripts/run-upstream-compat.js`.

Commit: `feat(34-02): implement upstream compatibility runner against composed dist/`
  </action>
  <verify>
    <automated>node scripts/run-upstream-compat.js 2>&1 | tail -5</automated>
  </verify>
  <done>Upstream compat runner creates temp dir with symlink, copies test files, runs them against dist/, reports pass/fail. 11 test files execute (sync.test.cjs excluded). Exit 0 when all pass.</done>
</task>

</tasks>

<verification>
- `node scripts/check-boundary.js` exits 0 (no boundary violations in current repo)
- `bun test tests/check-boundary.test.js` -- all tests pass
- `bun run compose && node scripts/run-upstream-compat.js` -- upstream tests pass against dist/
- No upstream test files are modified (only copied to temp dir)
- Symlink cleanup happens even on test failure (finally block)
</verification>

<success_criteria>
- check-boundary.js is tested and detects upstream file violations
- run-upstream-compat.js runs 11 upstream test files against composed dist/
- Both scripts follow CJS convention and export functions for programmatic use
- Both scripts have CLI entry points with appropriate exit codes
- TEST-02 assertion categorization confirmed as PASS (completed in research)
</success_criteria>

<output>
After completion, create `.planning/phases/34-testing-ci-enforcement/34-02-SUMMARY.md`
</output>
