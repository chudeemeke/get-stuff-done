---
phase: 41
plan: 01
type: execute
wave: 1
depends_on: []
status: pending
requirements: ["UPGRADE-03", "UPGRADE-06"]
files_modified:
  - .github/workflows/ci.yml
  - tests/check-overrides.test.js
  - tests/check-overrides-integration.test.js
  - .changelog-conflict-check.sh
  - tests/fixtures/changelog-conflict/good-changelog.md
  - tests/fixtures/changelog-conflict/bad-changelog.md
  - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-01-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "Override staleness is a blocking CI job with no continue-on-error on the override-check job"
    - "Boundary checking remains informational and still uses continue-on-error: true"
    - "Missing REASON.md and stale SHA override cases fail automated tests with actionable output"
    - "The changelog conflict script rejects a bullet inside a published release section and accepts the good fixture"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Separate override-check and boundary-check jobs"
      contains: "override-check:"
    - path: ".changelog-conflict-check.sh"
      provides: "Single-pattern changelog conflict guard"
      contains: "--self-test"
    - path: "tests/check-overrides-integration.test.js"
      provides: "Subprocess-level stale override failure proof"
      contains: "--overrides-dir"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-overrides.js"
      via: "blocking override-check job"
      pattern: "node scripts/check-overrides.js"
    - from: ".changelog-conflict-check.sh"
      to: "tests/fixtures/changelog-conflict/bad-changelog.md"
      via: "self-test fixture"
      pattern: "bad-changelog.md"
---

<objective>
Make override staleness a blocking merge gate and add the Phase 41 changelog
conflict guard.

Purpose: UPGRADE-03 and UPGRADE-06 are foundational gates that later upgrade work
depends on. Boundary debt remains visible but non-blocking per the locked
informational-boundary decision.
</objective>

<context>
@.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md
@.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-RESEARCH.md
@.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-VALIDATION.md
@.planning/phases/40.6-upstream-authority-migration/40.6-VERIFICATION.md
@.github/workflows/ci.yml
@scripts/check-overrides.js
@scripts/check-boundary.js
@tests/check-overrides.test.js
</context>

<tasks>

<task id="01-01" type="auto" tdd="true">
  <name>Split boundary and override CI jobs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md
  </read_first>
  <action>
    Implement D-01 exactly. Replace the combined `boundary-override-check` job
    with two sibling ubuntu-latest jobs:
    - `boundary-check`, named `Boundary Check (informational)`, running
      `node scripts/check-boundary.js` with `continue-on-error: true` at job level
      or on the boundary step.
    - `override-check`, named `Override Staleness Check (blocking)`, running
      `node scripts/check-overrides.js` with no `continue-on-error` anywhere in
      that job.

    Preserve the existing ratchet gate behavior as blocking. If the ratchet gate
    stays in `boundary-check`, keep the step blocking even though the boundary
    step itself is informational; do not weaken `scripts/check-debt-ratchet.cjs`.
  </action>
  <acceptance_criteria>
    - `rg -n "override-check:|boundary-check:|node scripts/check-overrides.js|node scripts/check-boundary.js|check-debt-ratchet" .github/workflows/ci.yml` shows both job ids and all three commands.
    - `override-check` has no `continue-on-error` line between its job header and the next top-level job.
    - `boundary-check` retains `continue-on-error: true` for `node scripts/check-boundary.js`.
    - `bash scripts/lint-workflows.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/lint-workflows.sh</automated>
  </verify>
  <done>
    Done when `.github/workflows/ci.yml` contains separate `override-check:` and
    `boundary-check:` jobs, the override job has no `continue-on-error`, the
    boundary job remains informational, and workflow lint exits 0.
  </done>
</task>

<task id="01-02" type="auto" tdd="true">
  <name>Extend override gate tests before relying on CI</name>
  <files>tests/check-overrides.test.js; tests/check-overrides-integration.test.js</files>
  <read_first>
    - tests/check-overrides.test.js
    - scripts/check-overrides.js
    - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md
  </read_first>
  <action>
    Implement D-18 test coverage. Add failing tests first, then production/test
    fixture changes only if needed.

    Extend `tests/check-overrides.test.js` with unit cases for:
    - stale SHA detection exits non-zero and includes the override path plus a
      stale/mismatch message.
    - missing `REASON.md` exits non-zero and includes `REASON.md` in output.
    - a valid override with matching SHA passes.

    Create `tests/check-overrides-integration.test.js`. The integration test must
    create a temporary override directory, write a source fixture and a deliberately
    stale `REASON.md`, invoke `node scripts/check-overrides.js --overrides-dir
    <temp>`, and assert exit code 1 plus actionable stderr/stdout.
  </action>
  <acceptance_criteria>
    - `bun test tests/check-overrides.test.js tests/check-overrides-integration.test.js` exits 0.
    - Integration failure output includes the stale override path and does not depend on network or Open GSD package downloads.
    - No test writes outside a temp directory.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/check-overrides.test.js tests/check-overrides-integration.test.js</automated>
  </verify>
  <done>
    Done when both override test files pass and cover stale SHA, missing
    `REASON.md`, and valid override cases without network or non-temp writes.
  </done>
</task>

<task id="01-03" type="auto" tdd="true">
  <name>Add changelog conflict guard and fixtures</name>
  <files>.changelog-conflict-check.sh; tests/fixtures/changelog-conflict/good-changelog.md; tests/fixtures/changelog-conflict/bad-changelog.md</files>
  <read_first>
    - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md
    - CHANGELOG.md
  </read_first>
  <action>
    Implement D-02 and D-03 exactly. Create `.changelog-conflict-check.sh` using
    a shell state machine with two states: `in_published_section` and
    `in_unreleased_section`. It must flag a `- ` bullet between a concrete
    `## [X.Y.Z]` heading and the next `## [` heading, excluding `## [Unreleased]`.

    Create fixtures:
    - `tests/fixtures/changelog-conflict/good-changelog.md` with an Unreleased
      bullet and a published section with no new bullet.
    - `tests/fixtures/changelog-conflict/bad-changelog.md` with the known entry
      inside a published release section.

    Add `--self-test` mode to run the script against both fixtures. The bad
    fixture must fail and print the offending line number.
  </action>
  <acceptance_criteria>
    - `bash .changelog-conflict-check.sh --self-test` exits 0.
    - `bash .changelog-conflict-check.sh tests/fixtures/changelog-conflict/bad-changelog.md` exits 1 and prints `bad-changelog.md:` with a line number.
    - `bash .changelog-conflict-check.sh tests/fixtures/changelog-conflict/good-changelog.md` exits 0.
    - The script does not attempt to detect a second changelog structural pattern per A-01.
  </acceptance_criteria>
  <verify>
    <automated>bash .changelog-conflict-check.sh --self-test</automated>
  </verify>
  <done>
    Done when the self-test exits 0, the bad fixture exits 1 with a line-numbered
    `bad-changelog.md:` message, and the good fixture exits 0.
  </done>
</task>

<task id="01-04" type="auto">
  <name>Record plan summary</name>
  <files>.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-VALIDATION.md
  </read_first>
  <action>
    Create `41-01-SUMMARY.md` with changed files, test commands, pass/fail status,
    and any blocker. Include the exact statement: `boundary-check remains
    informational; override-check is blocking`.
  </action>
  <acceptance_criteria>
    - `41-01-SUMMARY.md` exists and lists the commands in this plan.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-01-SUMMARY.md'; const s=fs.readFileSync(p,'utf8'); if(!s.includes('boundary-check remains informational; override-check is blocking')) process.exit(1)"</automated>
  </verify>
  <done>
    Done when `41-01-SUMMARY.md` exists, lists the plan commands, and contains
    `boundary-check remains informational; override-check is blocking`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR content to CI | Untrusted repo changes can alter overrides, REASON.md files, CHANGELOG.md, or workflows. |
| Shell script input | The changelog guard reads attacker-controlled markdown content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Tampering | override-check CI job | mitigate | Make `node scripts/check-overrides.js` blocking and test stale/missing metadata cases. |
| T-41-01-02 | Denial of Service | `.changelog-conflict-check.sh` | mitigate | Single-pass awk/sed state machine over local files only; no command execution from file content. |
| T-41-01-03 | Repudiation | CI status split | mitigate | Separate job names make boundary debt and override staleness auditable as distinct statuses. |
</threat_model>

<verification>
Run:
- `bash scripts/lint-workflows.sh`
- `bun test tests/check-overrides.test.js tests/check-overrides-integration.test.js`
- `bash .changelog-conflict-check.sh --self-test`
- `node scripts/check-overrides.js`

Expected: all exit 0 on the clean repo. `node scripts/check-boundary.js` may still
exit 1 with the known 41 structural violations; do not make boundary-check blocking.
</verification>

<success_criteria>
Plan 01 succeeds when a stale override or missing `REASON.md` fails automated tests
and the CI `override-check` job would fail a PR, while boundary checking remains
informational.
</success_criteria>

<output>
After completion, create `.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-01-SUMMARY.md`.
</output>
