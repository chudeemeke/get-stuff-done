---
phase: 04-branding-and-urls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - README.md
  - bin/install.js
  - commands/gsd/update.md
autonomous: true

must_haves:
  truths:
    - "package.json repository points to chudeemeke/get-stuff-done"
    - "README shows private repo clone URL"
    - "Install completion message shows Chude/AI Dev Environment author"
    - ".upstream/ directory remains unchanged"
  artifacts:
    - path: "package.json"
      provides: "Fork identity in npm metadata"
      contains: "chudeemeke/get-stuff-done"
    - path: "README.md"
      provides: "User-facing documentation with fork identity"
      contains: "chudeemeke/get-stuff-done"
    - path: "bin/install.js"
      provides: "Installer with fork author attribution"
      contains: "Chude"
    - path: "commands/gsd/update.md"
      provides: "Update command pointing to fork changelog"
      contains: "chudeemeke/get-stuff-done"
  key_links:
    - from: "package.json"
      to: "GitHub repository"
      via: "repository.url field"
      pattern: "chudeemeke/get-stuff-done"
    - from: "README.md"
      to: "Clone instructions"
      via: "git clone command"
      pattern: "chudeemeke/get-stuff-done"
---

<objective>
Update all identity markers and URLs to point to the private fork chudeemeke/get-stuff-done.

Purpose: Establish fork identity while preserving upstream attribution in .upstream/ directory for future diffing.
Output: All user-facing URLs point to private fork; .upstream/ preserved unchanged.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json identity</name>
  <files>package.json</files>
  <action>
Update package.json to establish fork identity:

1. Change `author` from "TACHES" to "Chude (fork), TACHES (original)"
2. Change `description` to acknowledge fork: "A meta-prompting, context engineering and spec-driven development system for Claude Code. Fork of GSD by TACHES."
3. Change `repository.url` from "git+https://github.com/glittercowboy/get-shit-done.git" to "git+https://github.com/chudeemeke/get-stuff-done.git"
4. Change `homepage` from "https://github.com/glittercowboy/get-shit-done" to "https://github.com/chudeemeke/get-stuff-done"
5. Change `bugs.url` from "https://github.com/glittercowboy/get-shit-done/issues" to "https://github.com/chudeemeke/get-stuff-done/issues"

DO NOT change `name` field - it remains "get-stuff-done" for npm compatibility.
DO NOT change version - that's handled separately.
  </action>
  <verify>
Run: `cat package.json | grep -E "(repository|homepage|bugs|author)" | head -10`
Verify all URLs contain "chudeemeke/get-stuff-done" and author includes "Chude".
  </verify>
  <done>package.json has fork identity: repository/homepage/bugs URLs point to chudeemeke/get-stuff-done, author credits both Chude and TACHES</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md URLs and attribution</name>
  <files>README.md</files>
  <action>
Update README.md to reflect fork identity:

1. Update clone URL in "Getting Started" section:
   - Change `git clone https://github.com/YOUR_USERNAME/get-stuff-done.git` to `git clone https://github.com/chudeemeke/get-stuff-done.git`

2. Update "Development Installation" section:
   - Change `git clone https://github.com/glittercowboy/get-shit-done.git` to `git clone https://github.com/chudeemeke/get-stuff-done.git`

3. Keep the fork note near the top that already exists:
   > **Note**: This is a fork of the original [Get Shit Done](https://github.com/glittercowboy/get-shit-done) by TACHES...

4. Keep the footer attribution that already exists:
   *Forked from [Get Shit Done](https://github.com/glittercowboy/get-shit-done) by TACHES...*

5. REMOVE or update the Star History section (lines 708-715) - it references glittercowboy/get-shit-done. Either remove entirely or comment that it shows upstream stars.

DO NOT change any references in .upstream/ directory.
  </action>
  <verify>
Run: `grep -n "git clone" README.md`
Verify clone URLs point to chudeemeke/get-stuff-done.
Run: `grep -n "glittercowboy" README.md`
Verify remaining glittercowboy references are only in attribution/fork-note sections, not in actionable URLs.
  </verify>
  <done>README clone URLs point to chudeemeke/get-stuff-done; upstream attribution preserved in fork notes and footer</done>
</task>

<task type="auto">
  <name>Task 3: Update install.js banner and update.md changelog link</name>
  <files>bin/install.js, commands/gsd/update.md</files>
  <action>
Update installer banner and update command:

**bin/install.js (line ~103):**
Change the banner description from:
```
  development system for Claude Code (and opencode) by TÂCHES.
```
To:
```
  development system for Claude Code. Fork by Chude.
```

Keep the ASCII art unchanged. Keep the version display unchanged.

**commands/gsd/update.md (line ~157):**
Change the changelog link from:
```
[View full changelog](https://github.com/glittercowboy/get-shit-done/blob/main/CHANGELOG.md)
```
To:
```
[View full changelog](https://github.com/chudeemeke/get-stuff-done/blob/main/CHANGELOG.md)
```

DO NOT modify .upstream/ directory files.
  </action>
  <verify>
Run: `grep -n "by TÂCHES\|by Chude\|Fork by" bin/install.js`
Verify banner shows "Fork by Chude".
Run: `grep "github.com" commands/gsd/update.md`
Verify changelog link points to chudeemeke/get-stuff-done.
  </verify>
  <done>Install banner shows "Fork by Chude"; update command changelog link points to private repo</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **URL audit:**
```bash
# Find remaining glittercowboy references outside .upstream/
grep -r "glittercowboy" --include="*.md" --include="*.js" --include="*.json" . | grep -v ".upstream/" | grep -v ".planning/"
```
Only attribution/fork-note references should remain (README fork note, README footer).

2. **Identity check:**
```bash
cat package.json | jq '.repository.url, .homepage, .author'
```
Should show chudeemeke URLs and Chude author.

3. **Upstream preservation:**
```bash
ls -la .upstream/package.json
```
.upstream/ directory should exist and be unchanged.
</verification>

<success_criteria>
- [ ] package.json repository URL is chudeemeke/get-stuff-done
- [ ] package.json homepage URL is chudeemeke/get-stuff-done
- [ ] package.json bugs URL is chudeemeke/get-stuff-done/issues
- [ ] package.json author includes "Chude"
- [ ] README clone URLs point to chudeemeke/get-stuff-done
- [ ] README preserves upstream attribution in fork notes
- [ ] bin/install.js banner shows "Fork by Chude"
- [ ] commands/gsd/update.md changelog link points to private repo
- [ ] .upstream/ directory remains unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-branding-and-urls/04-01-SUMMARY.md`
</output>
