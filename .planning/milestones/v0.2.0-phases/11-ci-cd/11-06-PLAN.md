---
phase: 11-ci-cd
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/platform.test.js
  - tests/theme.test.js
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All src/ files reach 95%+ functions coverage"
    - "All src/ files reach 95%+ lines coverage"
    - "Mock-based tests cover all environment variable branches"
  artifacts:
    - path: "tests/platform.test.js"
      provides: "Additional mock-based tests for detect.js and terminal.js"
      min_lines: 150
    - path: "tests/theme.test.js"
      provides: "Additional mock-based tests for themes.js and Style.js"
      min_lines: 50
  key_links:
    - from: "tests/platform.test.js"
      to: "src/platform/detect.js clearCache()"
      via: "clearCache() before each mocked detection"
      pattern: "clearPlatformCache\\(\\)"
    - from: "tests/platform.test.js"
      to: "src/platform/terminal.js clearCache()"
      via: "clearTerminalCache() before each mocked detection"
      pattern: "clearTerminalCache\\(\\)"
    - from: "tests/theme.test.js"
      to: "process.env mocking"
      via: "mockEnv() for all supports256Color branches"
      pattern: "mockEnv\\("
---

<objective>
Close coverage gaps in platform and theme modules by adding mock-based tests for all environment-specific code paths.

Purpose: Reach 95%+ coverage on both functions and lines for detect.js (70%→95%), terminal.js (50%→95%), themes.js (85%→95%), and Style.js (86%→95%).

Output: Updated test files with comprehensive environment mocking for all conditional branches.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ci-cd/11-CONTEXT.md

# Source files requiring coverage
@src/platform/detect.js
@src/platform/terminal.js
@src/theme/themes.js
@src/theme/Style.js

# Existing test files to extend
@tests/platform.test.js
@tests/theme.test.js

# Test helpers
@tests/helpers/mock-process.js
@tests/helpers/mock-child-process.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add platform coverage tests (detect.js + terminal.js)</name>
  <files>tests/platform.test.js</files>
  <action>
Add mock-based test cases to close coverage gaps in detect.js and terminal.js.

**For detect.js (70%→95% lines):**

1. Add test group "shell detection - all shell types with mocks" covering:
   - Line 43-52: PowerShell detection (PSModulePath)
   - Line 43-52: PowerShell detection (POWERSHELL_DISTRIBUTION_CHANNEL)
   - Line 54-62: cmd default on Windows (no MSYSTEM, no PSModulePath)
   - Line 68-77: zsh detection (SHELL=/bin/zsh)
   - Line 79-88: bash detection (SHELL=/bin/bash)
   - Line 90-98: unknown shell fallback (SHELL=/bin/unknown-shell)

2. Add test group "environment detection - all WSL branches" covering:
   - Line 119-127: WSL detection via /proc/version (mock fs.readFileSync to include "Microsoft")
   - Line 119-127: WSL detection via /proc/version (mock fs.readFileSync to include "WSL")
   - Line 123-127: WSL detection via WSL_DISTRO_NAME fallback (mock fs.readFileSync to throw, set WSL_DISTRO_NAME)
   - Line 131-132: isGitBash detection (MSYSTEM + EXEPATH containing "git")

3. Add test group "git detection - error handler" covering:
   - Line 183-189: execSync throws for git detection (mock execSync to throw)

**For terminal.js (50%→95% lines):**

1. Add test group "color level detection - all environment branches" covering:
   - Line 34-40: FORCE_COLOR=0 (returns 0)
   - Line 34-40: FORCE_COLOR=1 (returns 1)
   - Line 34-40: FORCE_COLOR=2 (returns 2)
   - Line 48-51: WT_SESSION truecolor detection
   - Line 54-56: COLORTERM=24bit truecolor detection
   - Line 61-63: TERM=dumb (returns 0)
   - Line 66-68: TERM includes "256color" (returns 2)
   - Line 71-78: TERM includes "color" (returns 1)
   - Line 71-78: TERM includes "ansi" (returns 1)
   - Line 71-78: TERM includes "xterm" (returns 1)
   - Line 71-78: TERM includes "screen" (returns 1)
   - Line 80-83: ConEmuTask detection (returns 2)
   - Line 85-91: TERM_PROGRAM=iTerm.app (returns 3)
   - Line 85-91: TERM_PROGRAM=vscode (returns 3)
   - Line 85-91: TERM_PROGRAM=Apple_Terminal (returns 2)
   - Line 85-91: TERM_PROGRAM=Hyper (returns 3)
   - Line 94-96: TERM includes "kitty" (returns 3)
   - Line 94-96: TERM includes "alacritty" (returns 3)

2. Add test group "terminal emulator detection - all branches" covering:
   - Line 110: ConEmuTask detection
   - Line 111-114: All TERM_PROGRAM cases (iTerm.app, vscode, Apple_Terminal, Hyper)
   - Line 117-121: TERM substring detection (kitty, alacritty, xterm, screen, tmux)
   - Line 124-126: Windows Console Host detection (win32 + no WT_SESSION + no MSYSTEM)

3. Add test group "Unicode support detection - all branches" covering:
   - Line 139-141: GSD_UNICODE=1 override
   - Line 139-141: GSD_UNICODE=true override
   - Line 144-147: All truthy branches (WT_SESSION, iTerm.app, vscode, Hyper)
   - Line 149-151: TERM includes "kitty" or "alacritty"
   - Line 154-156: Windows Console Host returns false (win32 + no MSYSTEM + no WT_SESSION)
   - Line 159-160: TERM=linux returns false
   - Line 164-165: non-win32 default returns true
   - Line 168-169: unknown Windows default returns false

**Implementation notes:**
- Use clearPlatformCache() and clearTerminalCache() before EVERY mocked detection call
- Use mockEnv() from tests/helpers/mock-process.js for all environment mocking
- Use mockExecSync() from tests/helpers/mock-child-process.js for git error testing
- For fs.readFileSync mocking, use inline stub: `const originalReadFile = fs.readFileSync; fs.readFileSync = () => "Microsoft WSL"; ... fs.readFileSync = originalReadFile;`
- Group tests by concern (shell detection, environment detection, color level, emulator, Unicode)
- Each test: clearCache() → mockEnv() → detectPlatform()/detectTerminal() → expect() → restore()
</action>
  <verify>
Run coverage and confirm detect.js and terminal.js reach 95%+ on both functions and lines:

```bash
cd "C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done"
bun test --coverage tests/platform.test.js
```

Check output for:
- src/platform/detect.js: functions 100%, lines >=95%
- src/platform/terminal.js: functions 100%, lines >=95%
  </verify>
  <done>
All environment-specific branches in detect.js and terminal.js are covered by mock-based tests. Both files show 95%+ functions and lines coverage. Test output shows no uncovered line ranges.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add theme coverage tests (themes.js + Style.js)</name>
  <files>tests/theme.test.js</files>
  <action>
Add mock-based test cases to close coverage gaps in themes.js and Style.js.

**For themes.js (85%→95% lines):**

1. Add test group "supports256Color() - all branches" covering:
   - Line 21-23: COLORTERM=truecolor (returns true)
   - Line 21-23: COLORTERM=256color (returns true)
   - Line 25-27: TERM includes "256color" (returns true)
   - Line 29-31: WT_SESSION set (returns true)
   - Line 33-35: TERM_PROGRAM=vscode (returns true)
   - Line 37-39: ConEmuTask set (returns true)
   - Line 40: default returns false (no env vars set)

**For Style.js (86%→95% functions):**

1. Add test group "text decorations - missing methods" covering:
   - Line 90: blink() method (create style, call blink(), render(), expect ANSI code \x1b[5m)
   - Line 92: hidden() method (create style, call hidden(), render(), expect ANSI code \x1b[8m)

**Implementation notes:**
- Use mockEnv() from tests/helpers/mock-process.js for supports256Color() tests
- Clear all env vars relevant to color detection before each test to ensure clean state
- For supports256Color tests: set only the target env var, verify return value, restore
- For Style tests: no mocking needed, direct method invocation and output verification
- Each test: mockEnv() if needed → call method → expect() → restore()
</action>
  <verify>
Run coverage and confirm themes.js and Style.js reach 95%+ on both functions and lines:

```bash
cd "C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done"
bun test --coverage tests/theme.test.js
```

Check output for:
- src/theme/themes.js: functions 100%, lines >=95%
- src/theme/Style.js: functions >=95%, lines 100%
  </verify>
  <done>
All conditional branches in themes.js (supports256Color) are covered by mock-based tests. Style.js blink() and hidden() methods are tested. Both files show 95%+ functions and lines coverage. Test output shows no uncovered line ranges.
  </done>
</task>

</tasks>

<verification>
Run full coverage report to confirm all 4 files meet 95%+ threshold:

```bash
cd "C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done"
bun test --coverage
```

Expected output:
- src/platform/detect.js: functions 100%, lines >=95%
- src/platform/terminal.js: functions 100%, lines >=95%
- src/theme/themes.js: functions 100%, lines >=95%
- src/theme/Style.js: functions >=95%, lines 100%
</verification>

<success_criteria>
Gap closure complete when:
- [ ] All platform.test.js additions pass (71 existing + ~40 new = ~111 tests)
- [ ] All theme.test.js additions pass (56 existing + ~9 new = ~65 tests)
- [ ] detect.js functions 100%, lines >=95%
- [ ] terminal.js functions 100%, lines >=95%
- [ ] themes.js functions 100%, lines >=95%
- [ ] Style.js functions >=95%, lines 100%
- [ ] No test failures in full suite
- [ ] Coverage report shows all src/ files >=95% on each metric
</success_criteria>

<output>
After completion, create `.planning/phases/11-ci-cd/11-06-SUMMARY.md`
</output>
