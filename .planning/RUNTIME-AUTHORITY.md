# GSD runtime authority recovery — 2026-09-05

The September5 contract authorizes completing delivery from the vetted skin.
The authority is the exact fork artifact composed from the ratified fixed
Open-GSD1.12.0 endpoint, once its acceptance gates pass. Neither an old global
install nor project-local legacy imports supersede that source.

This records recovered content authority; it is not an installation receipt.
No runtime has been replaced or deleted by this decision.

## Inventory and authored changes

| Surface | Manifest | Current differences | Disposition |
|---|---|---|---|
| Global Claude | Open-GSD1.6.1, 542 tracked files, none missing | Four oversight agents; planner/checker effort | Oversight agents equal current overlay after EOL normalization; reverting only documented effort high to low/xhigh reproduces the original manifest SHA-256 exactly |
| Global Codex | Legacy1.32.0, 229 tracked paths, 57 missing | No changed existing manifest file | Legacy lineage, not a higher Open-GSD release; adapter dispatch needs fresh delivery proof |
| Project Claude | Legacy1.30.0, 218 tracked files, none missing | commands/gsd/upstream.md | Only old glittercowboy URL/objective differs from current overlay command; superseded upstream authority, not unexplained authored behavior |

The effort adjustment remains until the candidate's effective model/effort and
installed effort-sync pass. Global custom rules and non-GSD skills are outside
GSD ownership and must not be overwritten.

## Backup

Every existing manifest file was copied as bytes with its manifest into
`C:/Users/Destiny/.codex/backups/get-stuff-done/2026-09-05-skin-start/` under
`claude-global/`, `codex-global/` and `claude-project/`. Changed-file hashes and
missing-file lists are recorded in
`C:/Users/Destiny/.codex/reports/gsd-skin-2026-09-05/runtime-authored-inventory.json`.
The inbox and all Git branch/tag refs were backed up before recovery edits.

The subsequent GSD-directory inventory found global Claude's extra
`.install-meta.json`, `.overlay-manifest.json`, and `commands/gsd/upstream.md`,
plus project Claude's extra `.install-meta.json`. All were copied into the
same backup. The global upstream command matches the current overlay.
No additional Codex GSD file was found in that scan. Recheck for edits made
after the backup before activation; absence from a manifest is not permission
to discard a file.
Desktop work was classified at HEAD265e3659f0b7477cf8e59bca24433e8904bdf093
on branch main through read-only `remotely run --stdin`:

- 475 tracked changes are CRLF/LF only; no other tracked content changes.
- Six deleted metadata paths have corresponding control-character path
  variants among the untracked files; their normalized bytes match this
  repository's tracked metadata.
- Three untracked recovery documents match the already preserved artifacts.
- Two untracked agent memory files match current overlay memory after EOL
  normalization.
- The two untracked hooks match historical revisions5eaf3de9 (check-update)
  and54fb5841 (statusline), after EOL normalization.

All13 untracked files are accounted for. Nothing was reset, cleaned or pruned.
The old locked worktree registration remains untouched. Before any future
desktop mutation, recheck for edits since this inventory.

Desktop backup: `~/Projects/gsd-skin-backup-2026-09-05-KKfilu/` contains all
Git refs in repository.bundle, tracked.patch, dirty-files.tar.gz, status and
worktree inventory plus SHA-256 receipts. External local evidence is retained
under the September5 report directory as desktop-dirty-classification.json,
desktop-normalized-comparison.json, desktop-backup-receipt.json and
desktop-backup-verification.json. This is preservation evidence, not authority
to discard unexplained future work.

## Activation gates

1. Exact source/artifact provenance and green candidate tests.
2. Isolated Windows Claude/Codex and supported Linux installation receipts.
3. Every advertised skill/import resolves; installed effort-sync works.
4. Same intended source generation with runtime-native adapters.
5. Local changes conflict visibly; failed/interrupted activation recovers;
   rollback and no-op reinstall pass.
6. Only then activate from the vetted artifact and record installed hashes,
   source revision, adapter version, backup and rollback receipt.

Issue54 remains open until the broken resume routes are verified fixed in a
fresh session. These findings remove a repeated abstract which-copy question;
they do not pre-approve overwriting newly discovered competing authored files.
