# Phase 40.5 Collisions Audit (Criterion #3 Evidence)

**Wave:** 3
**Generated:** 2026-05-15
**Upstream version under test:** get-shit-done-cc@1.39.1
**bun run compose exit code:** 0 (no COMP-08 collisions — matches RESEARCH.md §1 prediction)

## bun run compose output (verbatim)

```
$ node scripts/compose.js
Composition complete
  upstream_version:       1.39.1
  overlay_version:        3.0.2
  files_written:          928
  branding_rules_applied: 18
```

Composition completed cleanly. 928 files written. 18 branding rules applied (text-substitution rules from `scripts/compose.js`). Zero COMP-08 collision errors emitted.

## Static path cross-check (overlay/* vs v1.39.1 tree)

**Approach:** RESEARCH.md §1 enumerated 21 fork overlay paths and verified zero collisions against the v1.38.2 npm tarball. This wave extends the check to v1.39.1 — both the existing 21 paths AND any new overlay paths added since the §1 baseline.

**Baseline (RESEARCH.md §1):** 21 overlay paths × v1.38.2 tarball = **zero collisions**.

**Current state (this Wave):** Re-enumerated overlay paths via `find overlay -type f -not -path 'overlay/get-shit-done/*'`. **33 paths found** (12 more than the §1 baseline — see "Delta since §1 baseline" below). Re-checked all 33 against v1.39.1 tree (`node_modules/get-shit-done-cc/`).

### Delta since RESEARCH.md §1 baseline (12 new overlay paths)

| New overlay path | Upstream v1.39.1 path | Collision? |
|---|---|---|
| `overlay/.gitkeep` | (none) | NO (gitkeep) |
| `overlay/src/config/ConfigLoader.js` | (none) | NO (fork-only) |
| `overlay/src/config/ConfigSchema.js` | (none) | NO (fork-only) |
| `overlay/src/config/schema.js` | (none) | NO (fork-only) |
| `overlay/src/platform/detect.js` | (none) | NO (fork-only) |
| `overlay/src/platform/paths.js` | (none) | NO (fork-only) |
| `overlay/src/platform/terminal.js` | (none) | NO (fork-only) |
| `overlay/src/theme/index.js` | (none) | NO (fork-only) |
| `overlay/src/theme/Style.js` | (none) | NO (fork-only) |
| `overlay/src/theme/themes.js` | (none) | NO (fork-only) |
| `overlay/src/theme/tokens.js` | (none) | NO (fork-only) |
| `overlay/src/validation/index.js` | (none) | NO (fork-only) |

All 12 new paths are under `overlay/src/*` — fork-only namespaces with no upstream equivalent. No upstream-emitted paths under `src/*` of this form. Zero collisions.

### Re-check of the 21 RESEARCH.md §1 baseline paths against v1.39.1

All 21 still non-colliding. Empirically verified via:

```bash
for f in $(find overlay -type f -not -path 'overlay/get-shit-done/*'); do
  composed="${f#overlay/}"
  test -e "node_modules/get-shit-done-cc/$composed" && echo "$f COLLIDES" || true
done
# (no output — zero collisions)
```

Upstream's 1.38.2..1.39.1 delta of 631 files did NOT introduce any of the baseline path names.

### 1.38.2..1.39.1 upstream delta scope

**631 files changed** in 1.38.2..1.39.1 (much larger than the "5 commits / 2 files" reconciliation note in RESEARCH.md §1 anticipated — that note referenced the 1.34.2..1.38.2 → 1.34.2..1.39.1 delta for OVERRIDES specifically, not the full upstream churn).

However, the broader delta touches dirs like `agents/`, `commands/gsd/`, `bin/`, `.github/workflows/` — NONE of these intersect with the fork's overlay namespace. The fork's overlay paths (`overlay/agents/gsd-oversight-*`, `overlay/commands/gsd/upstream.md`, `overlay/src/*`, etc.) are FORK-SPECIFIC names that don't collide with any upstream additions.

### Result

**ZERO new path-level collisions** across all 33 fork overlay files inspected (21 baseline + 12 new since baseline). RESEARCH.md §1 prediction extends cleanly to v1.39.1 despite the larger-than-anticipated upstream delta — the fork's overlay namespace is structurally non-overlapping with upstream's emission paths.

The symlink `overlay/get-shit-done` is excluded from collision logic per RESEARCH.md §1 "Note for the planner" (it's a build artifact symlinking to the project root, not an overlay file).

**Cross-validation with `bun run compose`:** compose.js's built-in COMP-08 collision detector ALSO ran during this wave's compose invocation (Task 03-01) and exited 0. Two independent collision checks (this static cross-check + compose's runtime detector) agree: zero collisions.

## PR #1859 review.md audit

PR #1859 ("review.md per-CLI model selection") was merged upstream 2026-04-10 in v1.35.0 and is present through v1.39.1. Per RESEARCH.md §5 and D-09 (40.5 methodology), the audit reduces to confirming the fork has no fork-side review.md to reconcile.

**Verification commands run (2026-05-15):**

```bash
$ find overlay -name 'review*.md' 2>/dev/null
(empty)

$ find overrides -name 'review*.md' 2>/dev/null
(empty)

$ ls dist/get-shit-done/workflows/review.md
dist/get-shit-done/workflows/review.md

$ ls node_modules/get-shit-done-cc/get-shit-done/workflows/review.md
node_modules/get-shit-done-cc/get-shit-done/workflows/review.md

$ diff -q dist/get-shit-done/workflows/review.md node_modules/get-shit-done-cc/get-shit-done/workflows/review.md
(no output; exit 0)
```

**Result:**

1. No `overlay/.../review*.md` exists — fork has no overlay-level review.md.
2. No `overrides/.../review*.md` exists — fork has no override-level review.md.
3. `dist/get-shit-done/workflows/review.md` exists post-compose (Task 03-01 produced it among the 928 composed files).
4. `dist/get-shit-done/workflows/review.md` is **byte-identical** to upstream's `node_modules/get-shit-done-cc/get-shit-done/workflows/review.md` — verbatim composition confirmed.

**Conclusion: NO FORK DIVERGENCE.** The fork composes upstream's `review.md` verbatim into `dist/`. PR #1859's per-CLI model selection feature lands in the fork automatically through the compose pipeline. No fork-side reconciliation is required per D-09 (40.5 methodology: audit + remove if equivalent — fork has nothing to remove because nothing was added on fork side).

## Disposition summary

| Source | Finding | Disposition |
|---|---|---|
| `bun run compose` (COMP-08 runtime detector) | Exit 0; 928 files composed; zero COMP-08 collisions emitted | No action — compose is clean |
| Static path cross-check (overlay/* vs v1.39.1 tree) | 33 overlay paths inspected (21 RESEARCH.md §1 baseline + 12 new under `overlay/src/*`); ZERO collisions | No action — namespace structurally non-overlapping |
| PR #1859 review.md audit | No fork-side review.md exists (overlay or override); dist composes upstream verbatim (byte-identical diff); upstream PR feature lands automatically | No action — NO FORK DIVERGENCE |

## Wave 3 conclusion

**Zero new collisions in the 1.34.2 → 1.39.1 bump.** RESEARCH.md §1 prediction confirmed empirically AND extended to the v1.39.1 target (vs the v1.38.2 baseline §1 was authored against). The bump is path-level clean:

- No overlay-to-override promotions needed.
- No overlay removals needed.
- No fork-side review.md reconciliation needed (#1859 lands automatically via verbatim composition).

Two independent collision-detection channels (compose's runtime COMP-08 detector + this wave's static path cross-check) agree.

The 1.38.2..1.39.1 upstream delta turned out to be larger than RESEARCH.md §1 anticipated (631 files vs the "5 commits / 2 files" reconciliation note's scope — that note was about override-affecting changes specifically). Despite the broader churn, the fork's overlay namespace is structurally non-overlapping with upstream's emission paths, so no new collisions emerged.

**Wave 4 can proceed** (24-decision matrix verification → DECISION-VERIFY.md).
