---
phase: 43
plan: 04
type: execute
wave: 4
depends_on: ["43-03"]
status: pending
requirements: ["UPGRADE-04"]
files_modified:
  - scripts/run-compat-matrix.js
  - scripts/run-upstream-compat-ci.js
  - tests/run-upstream-compat-ci.test.js
  - .github/workflows/compat-matrix.yml
  - tests/ci-workflow.test.js
  - .planning/vetted-upstream-versions.json
  - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-04-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "compat matrix expands only from .planning/vetted-upstream-versions.json"
    - "blocking entry controls exit code"
    - "historical entries are informational"
    - "vettedAt is filled only after matrix evidence exists"
  artifacts:
    - "scripts/run-compat-matrix.js"
    - ".github/workflows/compat-matrix.yml"
    - ".planning/vetted-upstream-versions.json"
  key_links:
    - "UPGRADE-04"
    - "D-07"
---

<objective>
Run the vetted upstream matrix, apply evidence back to the manifest, and wire
the matrix into CI without marking candidates vetted early.
</objective>

<context>
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-CONTEXT.md
@.planning/vetted-upstream-versions.json
@scripts/vetted-upstream-versions.js
@scripts/run-upstream-compat-ci.js
@scripts/run-upstream-compat.js
@.github/workflows/ci.yml
</context>

<tasks>

<task id="04-01" type="auto" tdd="true">
  <name>Write compat matrix runner tests first</name>
  <files>tests/run-upstream-compat-ci.test.js</files>
  <read_first>
    - tests/run-upstream-compat-ci.test.js
    - scripts/run-upstream-compat-ci.js
    - scripts/run-upstream-compat.js
    - scripts/vetted-upstream-versions.js
  </read_first>
  <action>
    Extend tests before implementation:
    - matrix invokes the compatibility runner once per manifest entry.
    - blocking entry failure controls final exit code.
    - historical failures are reported as `informational` and do not force final non-zero exit.
    - JSON report contains `version`, `blocking`, `ok`, `exitCode`, `durationMs`, and `classification` for every entry.
    - invalid manifest exits non-zero before any matrix entry runs.
  </action>
  <acceptance_criteria>
    - `bun test tests/run-upstream-compat-ci.test.js` fails before implementation and exits 0 after implementation.
    - Test file contains `informational`, `blocking`, and `classification`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/run-upstream-compat-ci.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="04-02" type="auto">
  <name>Implement compat matrix runner</name>
  <files>scripts/run-compat-matrix.js; scripts/run-upstream-compat-ci.js; package.json</files>
  <read_first>
    - tests/run-upstream-compat-ci.test.js
    - scripts/run-upstream-compat-ci.js
    - scripts/run-upstream-compat.js
    - scripts/vetted-upstream-versions.js
    - .planning/vetted-upstream-versions.json
  </read_first>
  <action>
    Add `scripts/run-compat-matrix.js`:
    - CLI options: `--manifest <path>`, `--json`, `--report <path>`, `--compose-first`, and `--version <x.y.z>`.
    - load entries from `.planning/vetted-upstream-versions.json`.
    - isolate each `@opengsd/gsd-core` version run in a temp dependency override or temp project path.
    - preserve existing `run-upstream-compat-ci.js` informational behavior for non-matrix jobs.
    - final exit code is non-zero only if manifest validation fails or a blocking entry fails.
    - write `compat-matrix-report.json` when `--report` is supplied.
    Add package script `"compat:matrix": "node scripts/run-compat-matrix.js"`.
  </action>
  <acceptance_criteria>
    - `node scripts/run-compat-matrix.js --help` exits 0.
    - `package.json` contains `"compat:matrix": "node scripts/run-compat-matrix.js"`.
    - `bun test tests/run-upstream-compat-ci.test.js tests/vetted-upstream-versions.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-compat-matrix.js --help</automated>
    <automated>bun test tests/run-upstream-compat-ci.test.js tests/vetted-upstream-versions.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="04-03" type="auto">
  <name>Apply matrix evidence to manifest</name>
  <files>.planning/vetted-upstream-versions.json</files>
  <read_first>
    - scripts/run-compat-matrix.js
    - scripts/vetted-upstream-versions.js
    - .planning/vetted-upstream-versions.json
  </read_first>
  <action>
    Run the matrix locally or with injected fixture output. Then apply evidence:
    - write `compat-matrix-report.json`.
    - run `node scripts/vetted-upstream-versions.js --apply-matrix-evidence compat-matrix-report.json`.
    - set `vettedAt: "2026-07-03"` only for entries present in the matrix report.
    - set each entry `evidence.matrixReport` to the recorded report path or summary object.
    - remove `compat-matrix-report.json` after extracting evidence unless intentionally committed as evidence.
  </action>
  <acceptance_criteria>
    - `node scripts/vetted-upstream-versions.js --validate` exits 0 after `vettedAt` is populated.
    - `.planning/vetted-upstream-versions.json` contains `matrixReport` and no longer contains `"vettedAt": null`.
    - no untracked `compat-matrix-report.json` remains unless listed in `43-04-SUMMARY.md` as committed evidence.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/vetted-upstream-versions.js --validate</automated>
    <automated>git status --short compat-matrix-report.json</automated>
  </verify>
  <done>false</done>
</task>

<task id="04-04" type="auto">
  <name>Wire compatibility matrix CI and summary</name>
  <files>.github/workflows/compat-matrix.yml; tests/ci-workflow.test.js; .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-04-SUMMARY.md</files>
  <read_first>
    - .github/workflows/ci.yml
    - .github/workflows/upgrade-verifier.yml
    - tests/ci-workflow.test.js
    - scripts/run-compat-matrix.js
  </read_first>
  <action>
    Create `.github/workflows/compat-matrix.yml` with `pull_request`, `workflow_dispatch`, and weekly `schedule` triggers. Include path filters for `.planning/vetted-upstream-versions.json`, `.planning/upstream-authority.json`, `package.json`, `bun.lock`, `scripts/run-compat-matrix.js`, `scripts/run-upstream-compat*.js`, `overlay/**`, and `overrides/**`. Run `node scripts/vetted-upstream-versions.js --validate`, then `node scripts/run-compat-matrix.js --manifest .planning/vetted-upstream-versions.json --json --report compat-matrix-report.json`, and upload the report with `actions/upload-artifact@v7`.
    Create `43-04-SUMMARY.md` listing the matrix policy, evidence application, workflow name, and tests run.
  </action>
  <acceptance_criteria>
    - `bun test tests/ci-workflow.test.js` exits 0.
    - `bash scripts/lint-workflows.sh` exits 0.
    - `rg -n "compat-matrix|vetted-upstream-versions|run-compat-matrix|compat-matrix-report.json|actions/upload-artifact@v7" .github/workflows/compat-matrix.yml tests/ci-workflow.test.js .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-04-SUMMARY.md` finds matches.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/ci-workflow.test.js</automated>
    <automated>bash scripts/lint-workflows.sh</automated>
  </verify>
  <done>false</done>
</task>

</tasks>

<threat_model>
Matrix results influence upgrade confidence. Mitigate false confidence by
blocking only on the current pin, labeling historical versions informational,
and applying `vettedAt` only after report evidence exists.
</threat_model>

<verification>
Run:
- `node scripts/vetted-upstream-versions.js --validate`
- `node scripts/run-compat-matrix.js --help`
- `bun test tests/run-upstream-compat-ci.test.js tests/vetted-upstream-versions.test.js tests/ci-workflow.test.js`
- `bash scripts/lint-workflows.sh`
</verification>
