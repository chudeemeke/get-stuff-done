---
phase: 43
plan: 07
type: execute
wave: 7
depends_on: ["06"]
status: pending
requirements: ["UPGRADE-07"]
files_modified:
  - overrides/hooks/gsd-statusline.js
  - overrides/hooks/gsd-statusline.js.REASON.md
  - scripts/finalize-dist.js
  - tests/hooks.test.js
  - tests/compose.test.js
  - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-07-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "statusline parses Phase 43 frontmatter fields from STATE"
    - "statusline uses CLAUDE_CODE_AUTO_COMPACT_WINDOW"
    - "hook packaging includes check-update, optional worker, and statusline"
    - "statusline snapshot remains tied to active 1.5.0 until Plan 11"
  artifacts:
    - "overrides/hooks/gsd-statusline.js"
    - "scripts/finalize-dist.js"
    - "dist/hooks"
  key_links:
    - "UPGRADE-07"
---

<objective>
Reconcile the statusline hook and hook packaging with the same current-pin
honesty used for check-update.
</objective>

<context>
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-CONTEXT.md
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-RESEARCH.md
@overrides/hooks/gsd-statusline.js
@overrides/hooks/gsd-statusline.js.REASON.md
@scripts/finalize-dist.js
</context>

<tasks>

<task id="07-01" type="auto" tdd="true">
  <name>Extend statusline and packaging tests first</name>
  <files>tests/hooks.test.js; tests/compose.test.js</files>
  <read_first>
    - tests/hooks.test.js
    - tests/compose.test.js
    - overrides/hooks/gsd-statusline.js
    - scripts/finalize-dist.js
  </read_first>
  <action>
    Add failing tests before implementation:
    - statusline parses `active_phase`, `next_action`, `next_phases`, `completed_phases`, `total_phases`, and `percent` from STATE frontmatter.
    - statusline uses `CLAUDE_CODE_AUTO_COMPACT_WINDOW`.
    - statusline preserves fork role display from `gsd.role`.
    - packaged `dist/hooks/` contains `gsd-check-update.js`, `gsd-statusline.js`, and `gsd-check-update-worker.js` if the worker exists.
  </action>
  <acceptance_criteria>
    - `bun test tests/hooks.test.js tests/compose.test.js` fails before implementation and exits 0 after implementation.
    - Tests contain `active_phase`, `next_action`, `next_phases`, `CLAUDE_CODE_AUTO_COMPACT_WINDOW`, and `gsd-check-update-worker`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/hooks.test.js tests/compose.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="07-02" type="auto">
  <name>Reconcile statusline behavior</name>
  <files>overrides/hooks/gsd-statusline.js</files>
  <read_first>
    - tests/hooks.test.js
    - overrides/hooks/gsd-statusline.js
    - overrides/hooks/gsd-statusline.js.REASON.md
    - .planning/STATE.md
  </read_first>
  <action>
    Forward-port Open GSD 1.6.1 statusline improvements:
    - parse `active_phase`, `next_action`, `next_phases`, `completed_phases`, `total_phases`, and `percent`.
    - use `CLAUDE_CODE_AUTO_COMPACT_WINDOW` to compute autocompact buffer.
    - preserve fork-specific role display from `gsd.role`.
    - preserve fork branding and current todo behavior.
    - add deterministic fallback when frontmatter fields are absent.
  </action>
  <acceptance_criteria>
    - `bun test tests/hooks.test.js` exits 0.
    - `rg -n "active_phase|next_action|next_phases|CLAUDE_CODE_AUTO_COMPACT_WINDOW|gsd.role" overrides/hooks/gsd-statusline.js tests/hooks.test.js` finds matches.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/hooks.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="07-03" type="auto">
  <name>Prove hook packaging</name>
  <files>scripts/finalize-dist.js; tests/compose.test.js</files>
  <read_first>
    - tests/compose.test.js
    - scripts/compose.js
    - scripts/finalize-dist.js
    - hooks/index.js
  </read_first>
  <action>
    Extend compose/finalize packaging so generated `dist/hooks/` contains `gsd-check-update.js`, `gsd-statusline.js`, and `gsd-check-update-worker.js` when the worker exists. Run `bun run dist` and verify generated hook files exist.
  </action>
  <acceptance_criteria>
    - `bun test tests/compose.test.js tests/hooks.test.js tests/hooks-manifest.test.js` exits 0.
    - `bun run dist` exits 0.
    - `Test-Path dist\\hooks\\gsd-check-update.js` is true.
    - `Test-Path dist\\hooks\\gsd-statusline.js` is true.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/compose.test.js tests/hooks.test.js tests/hooks-manifest.test.js</automated>
    <automated>bun run dist</automated>
  </verify>
  <done>false</done>
</task>

<task id="07-04" type="auto">
  <name>Update statusline reason and summary</name>
  <files>overrides/hooks/gsd-statusline.js.REASON.md; .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-07-SUMMARY.md</files>
  <read_first>
    - overrides/hooks/gsd-statusline.js.REASON.md
    - overrides/hooks/gsd-statusline.js
    - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-06-SUMMARY.md
  </read_first>
  <action>
    Keep statusline REASON `Upstream snapshot` version/hash tied to active `1.5.0`. Add `Forward-ported 1.6.1 behavior` listing frontmatter fields and `CLAUDE_CODE_AUTO_COMPACT_WINDOW`. Add `Plan 11 refreshes snapshot after authority pin moves`. Create `43-07-SUMMARY.md` with adopted improvements, packaging proof, and tests run.
  </action>
  <acceptance_criteria>
    - `rg -n "Forward-ported 1.6.1 behavior|Plan 11 refreshes snapshot|Version: 1.5.0|CLAUDE_CODE_AUTO_COMPACT_WINDOW|active_phase|UPGRADE-07" overrides/hooks/gsd-statusline.js.REASON.md .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-07-SUMMARY.md` finds matches.
    - `node scripts/check-overrides.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-overrides.js</automated>
  </verify>
  <done>false</done>
</task>

</tasks>

<threat_model>
Statusline defects are user-visible and hook packaging errors break installed
runtime behavior. Mitigate with focused hook tests, dist packaging proof, and
snapshot honesty until the active upstream pin changes.
</threat_model>

<verification>
Run:
- `bun test tests/hooks.test.js tests/hooks-manifest.test.js tests/compose.test.js`
- `bun run dist`
- `node scripts/check-overrides.js`
</verification>
