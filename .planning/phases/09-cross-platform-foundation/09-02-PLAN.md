---
phase: 09-cross-platform-foundation
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - bin/gsd.js
  - hooks/pre-compact.js
  - hooks/gsd-statusline.js
  - package.json
  - scripts/build-hooks.js
autonomous: true

must_haves:
  truths:
    - "GSD launcher works on Windows (cmd, PowerShell, Git Bash), macOS, and Linux without bash dependency"
    - "Pre-compact hook runs on all platforms without bash dependency"
    - "Statusline renders correctly with proper Unicode/color fallbacks on all terminals"
    - "npm/bun creates platform-appropriate wrappers for the Node.js launcher"
  artifacts:
    - path: "bin/gsd.js"
      provides: "Cross-platform Node.js launcher replacing bash bin/gsd"
      exports: []
    - path: "hooks/pre-compact.js"
      provides: "Cross-platform Node.js pre-compact hook replacing pre-compact.sh"
      exports: []
  key_links:
    - from: "bin/gsd.js"
      to: "src/platform"
      via: "require for platform detection"
      pattern: "require.*platform"
    - from: "bin/gsd.js"
      to: "src/config/ConfigLoader"
      via: "require for config loading"
      pattern: "require.*ConfigLoader"
    - from: "package.json"
      to: "bin/gsd.js"
      via: "bin field pointing to .js launcher"
      pattern: '"gsd".*"bin/gsd.js"'
---

<objective>
Rewrite the bash launcher (bin/gsd) and bash hook (hooks/pre-compact.sh) in Node.js, and integrate the platform module into the statusline hook for terminal-aware rendering.

Purpose: Eliminates the two remaining bash dependencies that prevent GSD from running on Windows without Git Bash. After this plan, all GSD entry points are pure Node.js.

Output: bin/gsd.js (Node.js launcher), hooks/pre-compact.js (Node.js hook), updated statusline with platform-aware terminal detection, updated package.json bin field.
</objective>

<execution_context>
@C:\Users\Destiny\.claude/get-stuff-done/workflows/execute-plan.md
@C:\Users\Destiny\.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cross-platform-foundation/09-CONTEXT.md
@bin/gsd
@hooks/pre-compact.sh
@hooks/gsd-statusline.js
@src/platform/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite bin/gsd launcher in Node.js</name>
  <files>
    bin/gsd.js
    package.json
  </files>
  <action>
1. Create `bin/gsd.js` with `#!/usr/bin/env node` shebang, replicating all functionality from `bin/gsd`:

   a. **Path resolution:** Use `src/platform/paths` (pathe) for all path operations. Resolve GSD_HOME using `src/platform/paths.gsdHome()`. No cygpath needed -- Node.js handles Windows paths natively.

   b. **Config management (ensure_config equivalent):**
      - Check if GSD_HOME directory exists, create if not (with hooks/ subdirectory)
      - Check if config.json exists, create default if not (same JSON content as current bash version)
      - Migration: check for version field, add if missing (use ConfigLoader + JSON5 as current bash version does, but directly in Node.js without spawning node subprocess)

   c. **Startup banner:**
      - Use `src/platform/terminal` to detect color support
      - If color supported, use ANSI codes for green/blue/yellow. If not, plain text
      - Display "Get Stuff Done v{version}" with separator lines
      - Check for .planning/STATE.md and .planning/CONTINUE.md, display status

   d. **Launch Claude:** Use `child_process.spawn` with `{ stdio: 'inherit', shell: true }` to exec `claude` with passthrough args. On Windows, `claude` may need to resolve to `claude.cmd` -- `shell: true` handles this automatically.

   e. **Error handling:** Catch and display platform-appropriate error messages. If `claude` command not found, display helpful message with install instructions.

2. Update `package.json`:
   - Add a new bin entry: `"gsd": "bin/gsd.js"` (keep existing `"get-stuff-done": "bin/install.js"`)
   - Add `"src"` to the `files` array so the platform module is included in npm package

3. Do NOT delete `bin/gsd` (bash version) yet -- keep as reference. It can be removed in a future cleanup.

Important: The launcher must work when run via `npx @chude/get-stuff-done gsd` or when installed globally. Test that `node bin/gsd.js` works from project root.
  </action>
  <verify>
    - `node bin/gsd.js --help` (or similar) shows output without bash errors
    - `node -c bin/gsd.js` passes syntax check
    - package.json has `"gsd": "bin/gsd.js"` in bin field
    - package.json has `"src"` in files array
  </verify>
  <done>
    bin/gsd.js exists as a Node.js replacement for bin/gsd. It resolves paths cross-platform, creates default config if missing, displays startup banner with terminal-aware coloring, and launches claude with passthrough args. package.json bin field updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite pre-compact.sh in Node.js and update statusline terminal detection</name>
  <files>
    hooks/pre-compact.js
    hooks/gsd-statusline.js
    scripts/build-hooks.js
  </files>
  <action>
1. Create `hooks/pre-compact.js` with `#!/usr/bin/env node` shebang, replicating all functionality from `hooks/pre-compact.sh`:

   a. **Read stdin:** Read JSON input from stdin (same pattern as gsd-statusline.js uses)
   b. **Parse trigger:** Extract trigger type from input JSON (auto or manual)
   c. **Log event:** Append `{timestamp} | COMPACTION | trigger={trigger}` to `{PLANNING_DIR}/events.log`
   d. **Save continuation context:** Read STATE.md (first 50 lines), write CONTINUE.md with compaction timestamp, trigger, resume instructions, and last known state
   e. **Save metrics snapshot:** If metrics.json exists, copy to metrics.{timestamp}.json
   f. **Exit codes:** Exit 0 to allow compaction, Exit 2 to block (same as bash version)
   g. **Use `src/platform/paths`** for all path operations (pathe join, resolve)
   h. **Error handling:** Catch errors, log to stderr, exit 2 (block compaction on error)

   Use `fs.promises` for async file operations. Keep the same GSD_PLANNING_DIR env var support.

2. Update `hooks/gsd-statusline.js`:
   - Import and use `src/platform/terminal` for the `supportsUnicode()` function instead of the inline implementation (lines 48-55 of current file). The platform module has expanded terminal detection covering Windows Terminal, ConEmu, VS Code, iTerm2, Alacritty, Kitty, Hyper.
   - Keep rest of statusline logic unchanged.

3. Update `scripts/build-hooks.js`:
   - Add `hooks/pre-compact.js` as a build target alongside existing hooks (gsd-statusline.js, gsd-check-update.js)
   - Ensure the esbuild config bundles the new hook correctly (it needs src/platform/ bundled in since hooks are distributed as standalone files)
   - Check that the entry points array includes the new hook

4. Do NOT delete `hooks/pre-compact.sh` yet -- keep as reference.

Important: The pre-compact hook reads from stdin. Follow the exact same stdin reading pattern used in gsd-statusline.js (process.stdin events). The hook command in settings.json uses `node path/to/pre-compact.js` -- no shell dependency.
  </action>
  <verify>
    - `echo '{"trigger":"manual"}' | node hooks/pre-compact.js` creates CONTINUE.md in .planning/ (or GSD_PLANNING_DIR) and appends to events.log
    - `node -c hooks/pre-compact.js` passes syntax check
    - `node -c hooks/gsd-statusline.js` passes syntax check
    - `bun run build:hooks` succeeds and produces output in hooks/dist/ that includes pre-compact
  </verify>
  <done>
    hooks/pre-compact.js replaces pre-compact.sh with pure Node.js. gsd-statusline.js uses platform module for terminal detection. Build script produces bundled hooks including pre-compact.js.
  </done>
</task>

</tasks>

<verification>
- bin/gsd.js runs without bash: `node bin/gsd.js` on Windows (non-Git-Bash terminal)
- hooks/pre-compact.js processes stdin JSON and creates expected files
- gsd-statusline.js still renders correctly
- `bun run build:hooks` succeeds
- No remaining bash dependencies in bin/ or hooks/ for runtime execution
</verification>

<success_criteria>
All GSD entry points (launcher, hooks) are pure Node.js with no bash dependency. The launcher creates config, displays banner, and launches claude on all platforms. The pre-compact hook reads stdin, logs events, and creates continuation context. Statusline uses platform-aware terminal detection.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cross-platform-foundation/09-02-SUMMARY.md`
</output>
