---
phase: 40.6
plan: 02
status: complete
completed_at: 2026-06-22T23:20:00+01:00
requirements: ["UPGRADE-11"]
---

# Phase 40.6 Plan 02 Summary

## One-Liner

Inspected `@opengsd/gsd-core@1.5.0` in an isolated temp workspace, verified package
integrity/tag correlation, compared it to the current legacy package layout, and
updated the authority manifest with evidence-backed bin/path mappings.

## Files Modified

- `.planning/upstream-authority.json`
- `.planning/upstream-authority.schema.json`
- `.planning/phases/40.6-upstream-authority-migration/40.6-MIGRATION-SPIKE.md`
- `.planning/phases/40.6-upstream-authority-migration/40.6-02-SUMMARY.md`

## Commands Run

```powershell
npm view @opengsd/gsd-core@1.5.0 dist.integrity dist.shasum dist.tarball license scripts gitHead maintainers --json
gh release view v1.5.0 -R open-gsd/gsd-core --json tagName,publishedAt,targetCommitish,url
npm pack @opengsd/gsd-core@1.5.0 --pack-destination <scratch> --json
npm install --prefix <scratch>/install-root @opengsd/gsd-core@1.5.0 --ignore-scripts --no-audit --no-fund
gh api repos/open-gsd/gsd-core/git/ref/tags/v1.5.0
```

## Scratch Status

Scratch path:

`C:\Users\Destiny\AppData\Local\Temp\gsd-40.6-opengsd-spike-20260622-225152`

The scratch path is outside the repository, so no project-local ignore rule is needed.
Raw command outputs were left there as non-repo evidence for this session.

## Key Findings

- npm `dist.integrity` and pack integrity match.
- npm `dist.shasum` and pack shasum match.
- npm `gitHead` matches the GitHub `v1.5.0` tag commit.
- Open GSD package root is `node_modules/@opengsd/gsd-core`.
- Open GSD workflows live at `gsd-core/workflows`, replacing legacy
  `get-shit-done/workflows`.
- Open GSD `gsd-tools` lives at `gsd-core/bin/gsd-tools.cjs`.
- Open GSD core has no `gsd-sdk` bin.
- Root-level `agents`, `commands`, `hooks`, and `bin/install.js` still exist.

## Manifest Changes

`.planning/upstream-authority.json` now has:

- `active.sourceRoot = "."`
- `active.bin.gsd-core = "bin/install.js"`
- `active.bin.gsd-tools = "gsd-core/bin/gsd-tools.cjs"`
- `active.bin.gsd_run = "gsd-core/bin/gsd_run"`
- `active.paths.workflows = "gsd-core/workflows"`
- `active.paths.commands = "commands/gsd"`
- `active.paths.agents = "agents"`
- `active.paths.hooksRuntime = "hooks/dist"`
- `active.paths.hooksSource = "hooks"`

The schema was updated with the `active.paths` contract in the same wave.

## Plan 03 Notes

- Start with tests for `scripts/lib/upstream-source.js`.
- Migrate `compose.js` path constants to manifest/helper-backed paths.
- Migrate `check-overrides.js`, `check-boundary.js`, and `preview-update.js`.
- Use argv-based npm querying in preview-update.
- Do not rely on `.planning/` from any package-shipped runtime path.
- Regenerate or explicitly defer `dist/**`; Plan 04 cannot close without package
  artifact verification.

## Deviations

None. No global install was modified and no lifecycle scripts were run during the
Open GSD install.

