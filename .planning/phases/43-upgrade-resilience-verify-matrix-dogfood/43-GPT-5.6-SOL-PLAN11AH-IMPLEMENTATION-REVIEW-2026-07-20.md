# GPT-5.6 Sol Plan 11AH Implementation Review

## Review identity

- Reviewer: `gpt-5.6-sol`
- Reasoning effort: `xhigh`
- Review subject: `30a51363`
- Review mode: read-only implementation critique
- Result: `REJECT`
- Elapsed time: approximately 20 minutes

## Evidence observed by the reviewer

- Prior Fable review digest matched the recorded SHA-256.
- The reviewed worktree was clean.
- Focused tests reported 208 passing tests and 1,402 expectations across eight files.
- Static authority verification passed.
- Contract-derived topology reported five workflows, required attempt 1, 39 Tier A
  jobs, 21 Tier B subjects, 18 standalone artifacts, and three paired bundles.
- Both trusted performance pins were ancestors of the reviewed head.
- The reviewer did not run the full suite, strict coverage, repository compatibility,
  full lint, real Hyperfine, hosted workflows, or public mutations.

## Closure blockers reported

1. The pure hosted-evidence join accepts parsed Tier B receipts, parsed binding
   manifests, and caller-supplied digest strings without receiving and
   revalidating the paired raw receipt and comparison bytes.
2. The shipped `verify-hosted-ci collect` path still independently selects the
   latest attempt for each workflow even though Plan 11AH requires one exact-head,
   first-attempt cycle and Plan 11AJ owns the first live collector.
3. Automatic-token verification checks step bindings and sensitive expressions,
   but does not reject token-shaped keys inherited from workflow or job scopes,
   or reusable-workflow secret forwarding.
4. Fork pull-request jobs can execute unconditional `actions/cache` steps and
   therefore publish caches despite the accepted no-publication invariant.
5. Windows flake evidence passes `${{ github.sha }}` to `flake-triage`; on a pull
   request this records the synthetic merge SHA rather than the exact head SHA.

## Later-plan follow-up reported

The explicit issue-proposal mutation workflow validates only artifact shape in
preview mode. Before live mutation is authorized, record-level preview validation,
duplicate and empty identity rejection, provenance checks, and bot-owned marker
verification should be required, especially before stale-flake closure.

## Local lead disposition

- Findings 1 through 5 require local reproduction and correction before Fable
  implementation review.
- The issue-mutation finding remains non-live and owner-gated. It belongs in Plan
  11AJ acceptance criteria rather than expanding the current workflow slice.
- The review also exposed incomplete immutable-action-pin test coverage. All
  repository workflow files must be covered, and existing floating tags must be
  corrected before Plan 11AH can close.
- No hosted or public action is authorized by this review.
