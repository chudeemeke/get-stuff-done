---
phase: 43
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
status: pending
requirements: ["UPGRADE-01"]
files_modified:
  - bin/install.js
  - tests/installer-safety.test.js
  - tests/version-provenance.test.js
  - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-02-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "installer has preflight, transaction snapshot, rollback, and commit helpers"
    - "rollback restores GSD-owned files and never removes unrelated user files"
    - "installed VERSION/package provenance is unambiguous after upgrade"
  artifacts:
    - "bin/install.js"
    - "tests/installer-safety.test.js"
    - "tests/version-provenance.test.js"
  key_links:
    - "UPGRADE-01"
    - "D-20"
---

<objective>
Harden installer failure handling and VERSION/package-version provenance so
upgrade safety is recoverable and package identity is clear.
</objective>

<context>
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-CONTEXT.md
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-RESEARCH.md
@bin/install.js
@overrides/bin/install.js.REASON.md
@scripts/cousin-smoke.js
</context>

<tasks>

<task id="02-01" type="auto" tdd="true">
  <name>Write installer transaction tests first</name>
  <files>tests/installer-safety.test.js</files>
  <read_first>
    - tests/installer-safety.test.js
    - bin/install.js
    - overrides/bin/install.js.REASON.md
  </read_first>
  <action>
    Extend installer tests before implementation:
    - `preflightInstallTarget(targetDir, distDir)` errors when `dist/.overlay-manifest.json` or `dist/.install-meta.json` is missing.
    - `createInstallTransaction(targetDir, distDir)` snapshots `gsd-file-manifest.json`, `.overlay-manifest.json`, `.install-meta.json`, and `settings.json`.
    - rollback restores previous `settings.json`.
    - rollback removes newly copied overlay files listed in `dist/.overlay-manifest.json`.
    - rollback never removes `CLAUDE.md`, `rules/user.md`, `projects/keep.md`, or unrelated files not listed in GSD manifests.
    - `install()` reports `rollback: "applied"` in an injected failure path.
  </action>
  <acceptance_criteria>
    - `bun test tests/installer-safety.test.js` fails before implementation and exits 0 after implementation.
    - Test file contains `preflightInstallTarget`, `createInstallTransaction`, `rollback`, `CLAUDE.md`, and `rules/user.md`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/installer-safety.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="02-02" type="auto">
  <name>Implement installer transaction helpers</name>
  <files>bin/install.js</files>
  <read_first>
    - tests/installer-safety.test.js
    - bin/install.js
    - scripts/verify-upgrade.js
  </read_first>
  <action>
    Refactor installer failure handling with exportable helpers:
    - `preflightInstallTarget(targetDir, distDir)` validates target safety, upstream installer existence, `dist/.overlay-manifest.json`, and `dist/.install-meta.json`.
    - `createInstallTransaction(targetDir, distDir)` snapshots existing GSD-owned files, metadata, and `settings.json`.
    - `rollbackInstallTransaction(transaction)` restores snapshots and removes newly copied overlay files from the current `dist/.overlay-manifest.json`.
    - `commitInstallTransaction(transaction)` removes transaction scratch space after success.
    - `install(distDir, targetDir, userArgs, options = {})` supports injected `spawnImpl`, `copyOverlayFilesImpl`, `writeInstallMetaImpl`, `patchStatusLineImpl`, and `exitImpl`.
    - on upstream, overlay, metadata, or statusline failure, attempt rollback and print `Rollback applied` or `Rollback failed`.
    Preserve existing uninstall, v2 cleanup, statusline, and manifest-driven deletion behavior.
  </action>
  <acceptance_criteria>
    - `bun test tests/installer-safety.test.js` exits 0.
    - `node -e "const i=require('./bin/install.js'); for (const k of ['preflightInstallTarget','createInstallTransaction','rollbackInstallTransaction']) if (typeof i[k] !== 'function') process.exit(1)"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/installer-safety.test.js</automated>
    <automated>node -e "const i=require('./bin/install.js'); for (const k of ['preflightInstallTarget','createInstallTransaction','rollbackInstallTransaction']) if (typeof i[k] !== 'function') process.exit(1)"</automated>
  </verify>
  <done>false</done>
</task>

<task id="02-03" type="auto" tdd="true">
  <name>Write VERSION/package provenance tests first</name>
  <files>tests/version-provenance.test.js</files>
  <read_first>
    - tests/launcher-provenance.test.js
    - scripts/cousin-smoke.js
    - bin/install.js
    - package.json
  </read_first>
  <action>
    Create `tests/version-provenance.test.js` covering D-20 clarity:
    - install metadata records fork package name/version separately from upstream package/version.
    - launcher provenance reads fork version from `package.json`.
    - launcher provenance reads upstream package/version from `dist/.install-meta.json` when installed.
    - no code path reports upstream `@opengsd/gsd-core` as the fork package.
  </action>
  <acceptance_criteria>
    - `bun test tests/version-provenance.test.js` fails before implementation and exits 0 after implementation.
    - Test file contains `forkPackage`, `upstreamPackage`, `forkVersion`, and `upstreamVersion`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/version-provenance.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="02-04" type="auto">
  <name>Implement VERSION/package provenance clarity</name>
  <files>bin/install.js; scripts/cousin-smoke.js</files>
  <read_first>
    - tests/version-provenance.test.js
    - bin/install.js
    - scripts/cousin-smoke.js
    - scripts/lib/upstream-source.js
  </read_first>
  <action>
    Ensure installed and runtime provenance surfaces distinguish:
    - `forkPackage: "@chude/get-stuff-done"`.
    - `forkVersion` from root `package.json`.
    - `upstreamPackage: "@opengsd/gsd-core"`.
    - `upstreamVersion` from `dist/.install-meta.json` when present.
    Preserve `gsd --version --json` as non-interactive and ensure the provenance fields are stable for cousin/upgrade smoke tests.
  </action>
  <acceptance_criteria>
    - `bun test tests/version-provenance.test.js tests/launcher-provenance.test.js` exits 0.
    - `rg -n "forkPackage|upstreamPackage|forkVersion|upstreamVersion" bin/install.js scripts/cousin-smoke.js tests/version-provenance.test.js` finds matches.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/version-provenance.test.js tests/launcher-provenance.test.js</automated>
  </verify>
  <done>false</done>
</task>

</tasks>

<threat_model>
Installer failure can leave partial user configuration, and unclear package
identity can hide upgrade faults. Mitigate with transactional rollback and
separate fork/upstream provenance fields under tests.
</threat_model>

<verification>
Run:
- `bun test tests/installer-safety.test.js tests/version-provenance.test.js tests/launcher-provenance.test.js`
- `node -e "const i=require('./bin/install.js'); for (const k of ['preflightInstallTarget','createInstallTransaction','rollbackInstallTransaction']) if (typeof i[k] !== 'function') process.exit(1)"`
</verification>
