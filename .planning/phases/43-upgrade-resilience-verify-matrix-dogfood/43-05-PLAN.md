---
phase: 43
plan: 05
type: execute
wave: 5
depends_on: ["04"]
status: pending
requirements: ["UPGRADE-08"]
files_modified:
  - package.json
  - bun.lock
  - scripts/lib/semantic-js.js
  - scripts/check-overrides.js
  - overrides/**/*.REASON.md
  - tests/check-overrides.test.js
  - tests/check-overrides-integration.test.js
  - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-05-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "comment-only JavaScript upstream changes do not mark overrides stale"
    - "whitespace-only JavaScript upstream changes do not mark overrides stale"
    - "semantic JavaScript upstream changes still fail the blocking gate"
    - "JS REASON files record Semantic SHA-256 in addition to byte SHA-256"
  artifacts:
    - "scripts/lib/semantic-js.js"
    - "scripts/check-overrides.js"
    - "overrides/**/*.REASON.md"
  key_links:
    - "UPGRADE-08"
    - "D-09"
    - "D-10"
    - "D-11"
---

<objective>
Add JavaScript semantic override staleness so comments and whitespace do not
produce false positives while real behavior changes remain blocking.
</objective>

<context>
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-CONTEXT.md
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-RESEARCH.md
@scripts/check-overrides.js
@tests/check-overrides.test.js
@tests/check-overrides-integration.test.js
</context>

<tasks>

<task id="05-01" type="auto" tdd="true">
  <name>Write semantic override tests first</name>
  <files>tests/check-overrides.test.js; tests/check-overrides-integration.test.js</files>
  <read_first>
    - tests/check-overrides.test.js
    - tests/check-overrides-integration.test.js
    - scripts/check-overrides.js
  </read_first>
  <action>
    Add failing tests before implementation:
    - `.js` REASON file with matching `Semantic SHA-256` and mismatched byte SHA returns status `fresh-semantic` for only added line comments.
    - `.js` REASON file with matching `Semantic SHA-256` and mismatched byte SHA returns status `fresh-semantic` for whitespace-only changes.
    - semantic JavaScript AST/content changes return `stale`.
    - `.js` upstream with mismatched byte SHA and missing `Semantic SHA-256` remains `stale`.
    - non-JS overrides keep byte-hash behavior.
    - parse failures fall back to byte-hash behavior and return `stale` when hashes differ.
  </action>
  <acceptance_criteria>
    - `bun test tests/check-overrides.test.js tests/check-overrides-integration.test.js` fails before implementation and exits 0 after implementation.
    - Tests contain `fresh-semantic`, `Semantic SHA-256`, `comment`, `whitespace`, and `parse failure`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/check-overrides.test.js tests/check-overrides-integration.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="05-02" type="auto">
  <name>Implement semantic JavaScript hash helper</name>
  <files>scripts/lib/semantic-js.js; package.json; bun.lock</files>
  <read_first>
    - tests/check-overrides.test.js
    - package.json
  </read_first>
  <action>
    Add `acorn@8.17.0` as a devDependency. Create `scripts/lib/semantic-js.js` with:
    - `parseJavaScriptForSemanticHash(source, filePath)` using Acorn with `ecmaVersion: "latest"` and source type fallback from `module` to `script`.
    - `normalizeJavaScriptAst(ast)` removing location/range/comment-only data and preserving semantic literals, identifiers, imports, exports, and control flow.
    - `semanticHashJavaScript(source, filePath)` returning a SHA-256 hash of canonical JSON.
    - explicit parse failure result so policy code can fall back to byte-hash behavior.
    Keep the helper pure and independent from override reporting.
  </action>
  <acceptance_criteria>
    - `bun install --frozen-lockfile --ignore-scripts` exits 0 after lockfile update.
    - `rg -n "acorn|semanticHashJavaScript|normalizeJavaScriptAst|parseJavaScriptForSemanticHash" package.json scripts/lib/semantic-js.js tests/check-overrides.test.js` finds matches.
    - `bun test tests/check-overrides.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun install --frozen-lockfile --ignore-scripts</automated>
    <automated>bun test tests/check-overrides.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="05-03" type="auto">
  <name>Integrate semantic comparison into override gate</name>
  <files>scripts/check-overrides.js; overrides/**/*.REASON.md</files>
  <read_first>
    - scripts/check-overrides.js
    - scripts/lib/semantic-js.js
    - tests/check-overrides.test.js
    - overrides/hooks/gsd-check-update.js.REASON.md
    - overrides/hooks/gsd-statusline.js.REASON.md
  </read_first>
  <action>
    Update `scripts/check-overrides.js`:
    - add `extractSemanticHashFromReason(reasonPath)` for `- Semantic SHA-256: <64-char hex>`.
    - compute current upstream semantic hash for `.js` overrides.
    - return status `fresh-semantic` only when the recorded semantic hash equals the current upstream semantic hash.
    - return `stale` when byte hash differs and `Semantic SHA-256` is missing.
    - preserve byte-hash behavior for non-JS overrides and parse failures.
    For every current `.js` override REASON file, add `- Semantic SHA-256: <hash>` immediately below the existing `- SHA-256:` line, generated from active 1.5.0 upstream source.
  </action>
  <acceptance_criteria>
    - `node scripts/check-overrides.js` exits 0.
    - `rg -n "fresh-semantic|OK \\(semantic\\)|Semantic SHA-256|semanticHashJavaScript" scripts/check-overrides.js tests/check-overrides.test.js overrides/**/*.REASON.md` finds matches.
    - non-JS REASON files do not contain `Semantic SHA-256`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-overrides.js</automated>
    <automated>bun test tests/check-overrides.test.js tests/check-overrides-integration.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="05-04" type="auto">
  <name>Record semantic staleness summary</name>
  <files>.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-05-SUMMARY.md</files>
  <read_first>
    - scripts/check-overrides.js
    - scripts/lib/semantic-js.js
    - overrides/hooks/gsd-check-update.js.REASON.md
  </read_first>
  <action>
    Create `43-05-SUMMARY.md` listing JS-only scope, `acorn@8.17.0`, semantic hash evidence, byte-hash fallback behavior, parse-failure behavior, markdown deferral to v1.3.0, and tests run.
  </action>
  <acceptance_criteria>
    - Summary contains `.js`, `acorn@8.17.0`, `Semantic SHA-256`, `byte-hash`, `parse failure`, `v1.3.0`, and `UPGRADE-08`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "\\.js|acorn@8.17.0|Semantic SHA-256|byte-hash|parse failure|v1.3.0|UPGRADE-08" .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-05-SUMMARY.md</automated>
  </verify>
  <done>false</done>
</task>

</tasks>

<threat_model>
Semantic comparison can weaken a blocking gate if implemented too broadly.
Mitigate by limiting semantic pass to parsed `.js` files with recorded semantic
hashes and preserving byte-hash behavior for non-JS and parse failures.
</threat_model>

<verification>
Run:
- `node scripts/check-overrides.js`
- `rg -n "Semantic SHA-256" overrides/**/*.REASON.md`
- `bun test tests/check-overrides.test.js tests/check-overrides-integration.test.js`
</verification>
