---
phase: 40.6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/decisions/003-UPSTREAM-AUTHORITY-MIGRATION.md
  - .planning/upstream-authority.json
  - .planning/upstream-authority.schema.json
  - .planning/phases/40.6-upstream-authority-migration/40.6-01-SUMMARY.md
autonomous: false
requirements: ["UPGRADE-11"]
estimated_minutes: 90
gap_closure: false
must_haves:
  truths:
    - "ADR records old authority, new authority, decision rationale, non-goals, risks, and rollback/reconsideration criteria"
    - "Machine-readable authority manifest exists and pins @opengsd/gsd-core@1.5.0"
    - "Manifest records legacy package/repo as deprecated evidence, not active authority"
    - "Manifest scope is explicit: .planning manifest is maintainer/build-time only unless a package-included generated artifact is added later"
    - "ADR records package integrity/provenance evidence fields required before layout trust"
    - "No production code migration happens in this wave"
  artifacts:
    - path: "docs/decisions/003-UPSTREAM-AUTHORITY-MIGRATION.md"
      provides: "Human-readable authority decision"
      min_lines: 60
    - path: ".planning/upstream-authority.json"
      provides: "Machine-readable upstream authority contract for migration tooling"
      min_lines: 20
    - path: ".planning/upstream-authority.schema.json"
      provides: "Schema contract for authority manifest evolution"
      min_lines: 20
---

<objective>
Create the upstream authority decision and package/source contract before touching
implementation code.
</objective>

<context>
@.planning/phases/40.6-upstream-authority-migration/40.6-CONTEXT.md
@.planning/phases/40.6-upstream-authority-migration/40.6-RESEARCH.md
@.planning/phases/40.6-upstream-authority-migration/40.6-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task id="01-01" type="auto">
  <name>Read authority evidence and define ADR structure</name>
  <files>docs/decisions/003-UPSTREAM-AUTHORITY-MIGRATION.md</files>
  <read_first>
    - .planning/phases/40.6-upstream-authority-migration/40.6-CONTEXT.md
    - .planning/phases/40.6-upstream-authority-migration/40.6-RESEARCH.md
  </read_first>
  <action>
    Confirm the ADR should answer:
    1. What is the active upstream authority?
    2. What is the legacy authority and why is it no longer sufficient?
    3. What stays invariant in the fork architecture?
    4. What is explicitly out of scope?
    5. What evidence would make us reconsider?
  </action>
</task>

<task id="01-02" type="auto">
  <name>Create upstream authority ADR</name>
  <files>docs/decisions/003-UPSTREAM-AUTHORITY-MIGRATION.md</files>
  <action>
    Write an ADR with these sections:
    - Status: Accepted for Phase 40.6 implementation.
    - Context.
    - Decision.
    - Consequences.
    - Non-goals.
    - Evidence snapshot with command names and URLs.
    - Reconsideration criteria.

    Required decision text:
    - Active package: `@opengsd/gsd-core`.
    - Initial pin: `1.5.0`.
    - Active repository: `open-gsd/gsd-core`.
    - Legacy package: `get-shit-done-cc`.
    - Legacy repository: `gsd-build/get-shit-done`.
    - Do not use `latest`, `next`, canary, or dev tags.
    - Do not treat `@opengsd/get-shit-done-redux` as the target package because npm marks it deprecated in favor of core.
    - `.planning/upstream-authority.json` is maintainer/build-time state. Runtime or package-shipped code must not assume `.planning/` exists.
    - Plan 02 must capture `dist.integrity`, `dist.shasum`, `dist.tarball`, `license`, package scripts, and release/tag correlation before trusting package layout.
  </action>
</task>

<task id="01-03" type="auto">
  <name>Create machine-readable authority manifest</name>
  <files>.planning/upstream-authority.json</files>
  <action>
    Create JSON with this minimum shape:

    ```json
    {
      "schemaVersion": 1,
      "contractScope": "maintainer-build-time",
      "active": {
        "packageName": "@opengsd/gsd-core",
        "version": "1.5.0",
        "repository": "https://github.com/open-gsd/gsd-core",
        "npmUrl": "https://www.npmjs.com/package/@opengsd/gsd-core",
        "sourceRoot": null,
        "bin": {}
      },
      "legacy": {
        "packageName": "get-shit-done-cc",
        "repository": "https://github.com/gsd-build/get-shit-done",
        "npmUrl": "https://www.npmjs.com/package/get-shit-done-cc",
        "status": "deprecated-authority"
      },
      "rules": {
        "exactVersionPinRequired": true,
        "allowLatestTag": false,
        "allowPrerelease": false,
        "globalInstallMutationAllowed": false,
        "runtimeMayReadPlanningManifest": false
      }
    }
    ```

    Keep `sourceRoot` nullable until Plan 02 verifies the package layout. Plan 02 may
    fill it in with evidence.
  </action>
</task>

<task id="01-03b" type="auto">
  <name>Create authority manifest schema</name>
  <files>.planning/upstream-authority.schema.json</files>
  <action>
    Create a small JSON Schema for `.planning/upstream-authority.json` covering:
    - `schemaVersion`
    - `contractScope`
    - `active.packageName`
    - `active.version`
    - `active.repository`
    - `active.sourceRoot`
    - `active.bin`
    - `legacy`
    - `rules`

    The schema must reject `latest`, `next`, canary, and dev versions for
    `active.version`.
  </action>
</task>

<task id="01-04" type="auto">
  <name>Validate ADR and manifest</name>
  <files>.planning/phases/40.6-upstream-authority-migration/40.6-01-SUMMARY.md</files>
  <action>
    Run lightweight checks:
    - JSON parse for `.planning/upstream-authority.json`.
    - JSON parse for `.planning/upstream-authority.schema.json`.
    - Grep ADR for all active and legacy package/repo names.
    - Grep ADR for `dist.integrity`, `dist.shasum`, `dist.tarball`, `license`, `scripts`, and `.planning`.
    - Confirm no production code files changed in this wave.

    Write `40.6-01-SUMMARY.md` with:
    - Files created.
    - Evidence checked.
    - Any unresolved questions for Plan 02.
  </action>
</task>

</tasks>

<success>
Plan 01 succeeds when the authority decision and manifest exist, parse, and explicitly
block legacy/latest-based migration shortcuts.
</success>
