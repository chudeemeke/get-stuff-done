# Phase 40.5 Research: Upstream Bump & Phase 41 Decision Re-verification

**Researched:** 2026-04-28
**Status:** COMPLETE
**Confidence:** HIGH (all findings verified against upstream tarball + GitHub PR API + fork filesystem)

<user_constraints>
## User Constraints (from CONTEXT.md)

CONTEXT.md is fully locked. Methodology decisions D-01..D-23 + amendments A-01..A-03 are not subject to revision in this research. This section reproduces them verbatim so the planner can verify compliance.

### Locked Decisions (verbatim)

#### Re-verification Methodology (Criterion #4)
- **D-01:** Verification depth for the 20 D-XX/A-XX items NOT explicitly named in success criterion #4: per-decision matrix, semantic check (cited symbol/API/line behavior re-verified, not just file existence). The 2 explicitly named (D-09 vs upstream #2499; 999.1 vs upstream #2487) get the same treatment.
- **D-02:** Matrix ordering: by upstream-touch surface. Decisions citing upstream files ranked first; fork-only file citations last.
- **D-03:** Amendments captured as A-07+ in Phase 41 CONTEXT.md's existing Amendments Log (chronological log; original D-XX content amended in-place).
- **D-04:** Verification owner: interactive Claude in plan/execute (NOT subagent). User wants to be in the loop on verification thinking. Tasks must be sized for interactive execution with user-attention budget per row. No batch verifier-agent step.
- **D-05:** Drift signal threshold: cited symbol/API/line behavior changed. Whitespace/comment-only delta = unchanged. Behavior delta = drifted. Mechanical (file moved/renamed but behavior preserved) = drifted with auto-amendment proposal.
- **D-06:** Amendment sign-off gate: user reviews each amendment before commit.

#### Override Drift + #1859 Cleanup (Criterion #2 + #3)
- **D-07:** Default response on SHA-256 mismatch: human review every drift. AST-diff is Phase 43 territory.
- **D-08:** Default action on overlay/upstream collision: compare semantics, then decide. Equivalent → remove overlay, use upstream. Meaningfully divergent → move to overrides/ with REASON.md + SHA snapshot.
- **D-09 (this is methodology, not the Phase 41 D-09):** Handling for PR #1859 (review.md per-CLI model selection): audit + remove if equivalent. Diff fork's `overlay/review.md` (and config.cjs deltas) against upstream's merged version; remove if equivalent, otherwise move to `overrides/` with REASON.md citing the divergence.

#### Verification Artifact Shape (Criterion #5)
- **D-10:** `40.5-VERIFICATION.md` uses standard `gsd-verifier` output — no custom shape.
- **D-11:** `40.5-DECISION-VERIFY.md` (separate evidence file): 22-row matrix. Columns: ID | Cited target | Upstream commit/SHA | Status | Evidence | Amendment ref. Status enum: unchanged | drifted | amended.
- **D-12:** `40.5-DRIFT-LOG.md` (separate evidence file): per-file SHA mismatch + semantic compare + refresh-or-amend decision.
- **D-13:** `40.5-COLLISIONS.md` (separate evidence file): per-overlay collision + keep/move/remove decision (includes #1859 audit).
- **D-14:** Refreshed override snapshots: committed inline; no separate aggregator file. Each override's REASON.md gets `Snapshot:` field updated atomically with the verification commit.
- **D-15:** `40.5-README.md`: short index of phase-dir contents and why each exists.

#### Planning Sequence
- **D-16:** Wave structure: sequential, criterion-by-criterion.
  - Wave 1: criterion #1 (upstream pin bump + `bun install` + Linux + macOS test pass)
  - Wave 2: criterion #2 (override drift detection → DRIFT-LOG.md)
  - Wave 3: criterion #3 (compose collision audit → COLLISIONS.md, including #1859)
  - Wave 4: criterion #4 (22-decision matrix → DECISION-VERIFY.md, with interactive review per D-04)
  - Wave 5: criterion #5 (VERIFICATION.md synthesis + README + final commit)
- **D-17:** Failure path on MATERIAL drift mid-execution: halt the wave, capture amendment as A-07+ via interactive review, resume.

#### Backlog Item Disposition
- **D-18:** 999.1 absorbed into Phase 40.5 scope. Disposition: (a) upstream #2487 addresses gap → close 999.1; (b) doesn't → 999.1 stays backlog with existing 3 promotion paths intact.
- **D-19:** 999.2 attempt Option C in-backlog first (rename 4 keys under existing `features.*` namespace).
- **D-20:** 999.3 stays backlog gated by Phase 40.5 verification per existing promotion criteria. After bump, grep upstream's `workflows/plan-phase.md`. If bug still present: file upstream issue + PR; record in 40.5 VERIFICATION.md. If already fixed: close as NOT-A-GAP.

#### Phase 41 Plan-Phase Trigger Gates
- **D-21:** Gates for `/gsd:plan-phase 41` (all required):
  1. Phase 40.5 VERIFICATION.md PASS
  2. Zero MATERIAL drift in the 22-decision matrix
  3. 999.2 Option C disposition recorded
  4. Phase 41 RESEARCH.md + VALIDATION.md sanity check

#### REASON.md Format
- **D-22:** Any new override created during 40.5 writes its REASON.md following existing convention (Title, Why, Upstream snapshot Version+SHA-256, What's different, Review trigger).

#### Wave Time Estimates (planner sizing guidance)
- **D-23:** Per-wave estimates (guidance, not commitment): Wave 1 30-60 min; Wave 2 30-60 min (~15 min/override); Wave 3 30-90 min; Wave 4 90-180 min (10 min/upstream-cited row, 3 min/fork-only row); Wave 5 30-60 min. Total 3.5-7 hours, 2-3 sessions.

### Claude's Discretion
None within methodology. Discretion limited to interactive judgment calls during execution per D-04 (e.g. when a drift is whitespace-only vs behavior).

### Deferred Ideas (OUT OF SCOPE)
- Phase 43 UPGRADE-05 dogfood bump (this is preparatory currency, not the dogfood execution)
- Phase 43 UPGRADE-04 AST-diff drift detection
- 999.2 config schema drift (handled in-backlog via Option C — see D-19)
- `/gsd:plan-phase 41` (fires after 40.5 PASS — see D-21 gates)

### Amendments to CONTEXT.md (verbatim)
- **A-01 (2026-04-28):** Dropped initial proposal "promote 999.1 → Phase 40.6 and 999.2 → Phase 40.7." Replaced with D-18, D-19, D-20.
- **A-02 (2026-04-28):** Dropped "next session runs `/gsd:resume-work → /gsd:discuss-phase 40.5`." Corrected to `/gsd:resume-work → /gsd:plan-phase 40.5`.
- **A-03 (2026-04-28):** Dropped drift toward custom VERIFICATION.md sections. Framework's `gsd-verifier` agent owns the template. Supplementary evidence files (D-11..D-15) sit alongside, never in place of it.
</user_constraints>

<phase_requirements>
## Phase Requirements

| ID | Description | Research Support |
|----|-------------|------------------|
| UPGRADE-10 | Pre-Phase-41 upstream currency bump — bump 1.34.2 → 1.38.2 (or latest stable), refresh override SHA-256 snapshots, audit compose collisions, re-verify all Phase 41 CONTEXT.md decisions against fresh upstream state. | This research file. Sections 1-7 below provide the data the planner needs to size each wave. |
</phase_requirements>

## Project Constraints (from CLAUDE.md)

No project-level CLAUDE.md exists at repo root. No `.claude/skills/` or `.agents/skills/` directories. Global user-level CLAUDE.md applies (Chude WoW): hexagonal architecture, SOLID, 95%+ coverage at each metric, TDD, no AI attribution in commits, no emojis. Author all commits as `Chude <chude@emeke.org>`.

## Validation Architecture

> Nyquist Dimension 8 — what "verified" means for Phase 40.5 end-to-end.

### Test Framework
| Property | Value |
|----------|-------|
| Framework | `bun test` (bun:test) |
| Config file | None — defaults |
| Quick run command | `bun test` |
| Full suite command | `bun test` (project's full suite is the same; per `package.json`) |

### Phase Requirements → Validation Map
| Criterion | Behavior to verify | Verification primitive | Automated check command | Evidence file |
|-----------|----|-----|---|----|
| #1 (pin bump) | `package.json` pins 1.38.2; `bun install` succeeds; Linux + macOS tests pass | (a) string match `"get-shit-done-cc": "1.38.2"`; (b) `bun install --frozen-lockfile` exit 0; (c) `bun test` exit 0 on Linux + macOS | `bun install --frozen-lockfile && bun test` | `40.5-VERIFICATION.md` Wave 1 section + CI run links |
| #2 (override drift) | Every override REASON.md SHA matches `node_modules/get-shit-done-cc/<rel>` SHA OR has documented drift refresh | `node scripts/check-overrides.js` exit code; SHA-256 compare per file | `node scripts/check-overrides.js` | `40.5-DRIFT-LOG.md` (per D-12) + refreshed REASON.md per D-14 |
| #3 (compose collisions) | No new overlay/upstream path collisions; #1859 review.md handled per D-09 | `bun run compose` runs without COMP-08 collision errors; manual diff of overlay vs upstream tarball; #1859 specific check | `bun run compose` + tar/diff inspection | `40.5-COLLISIONS.md` (per D-13) |
| #4 (22-decision re-verify) | Each D-XX/A-XX cited symbol/API/line behavior re-verified semantically against fresh upstream | Interactive Claude review per D-04; semantic check per D-05; user sign-off per D-06 | None automated — interactive | `40.5-DECISION-VERIFY.md` (per D-11) + Phase 41 CONTEXT.md amendments A-07+ |
| #5 (synthesis) | VERIFICATION.md cites all 4 supplementary files; README.md indexes phase-dir; backlog dispositions recorded | `gsd-verifier` agent runs final pass; manual checklist | `gsd-verifier` (framework agent) | `40.5-VERIFICATION.md` (standard shape per D-10) + `40.5-README.md` (per D-15) |

### Sampling Rate
- **Per task commit:** `bun test` (fast — fork suite, ~30-60s)
- **Per wave merge:** `bun install --frozen-lockfile && bun run compose && bun test` (full pipeline)
- **Phase gate:** All 5 waves green, all 4 D-21 gates satisfied, before Phase 41 planning may proceed.

### Wave 0 Gaps
None. The fork already has:
- `scripts/check-overrides.js` (SHA verification + REASON.md enforcement, 345 lines)
- `scripts/compose.js` (built-in COMP-08 collision detection)
- `bun test` (full test suite)
- `tests/check-overrides.test.js` (unit tests for the verification script)

40.5 is verification-driven, not net-new-test-creation. No new test infrastructure required by 40.5 itself; Phase 41 will add `tests/check-overrides-integration.test.js` per its D-18, but that's not 40.5 scope.

### Verified output evidence files (per D-10..D-15)
1. `40.5-VERIFICATION.md` — standard `gsd-verifier` output (D-10)
2. `40.5-DECISION-VERIFY.md` — 22-row matrix (D-11)
3. `40.5-DRIFT-LOG.md` — per-file SHA + semantic compare (D-12)
4. `40.5-COLLISIONS.md` — per-overlay collision decision (D-13)
5. `40.5-README.md` — phase-dir index (D-15)
6. Refreshed `overrides/hooks/gsd-check-update.js.REASON.md` — inline SHA snapshot update (D-14)
7. Refreshed `overrides/hooks/gsd-statusline.js.REASON.md` — inline SHA snapshot update (D-14)
8. Phase 41 CONTEXT.md Amendments Log entries A-07+ (if any drifted) — original D-XX content amended in-place (D-03, D-06)

---

## 1. Upstream Changelog (1.34.2 → 1.38.2)

**Verified via:** Bare clone of `gsd-build/get-shit-done` (HIGH confidence — direct git operations on official upstream).

**Note:** The npm metadata reports the upstream repository URL has migrated from `TACHES/get-shit-done` (memory-recorded) to `gsd-build/get-shit-done` (current). Both are TACHES-organisation-owned; npm `repository.url` was updated. No action required for the bump — the package name `get-shit-done-cc` is unchanged.

### Version landscape
| Version | Released | Status for 40.5 |
|---|---|---|
| 1.34.2 | 2026-04-06 | Current pin |
| 1.38.0 | 2026-04-18 | Skipped |
| 1.38.1 | 2026-04-19 | Skipped |
| 1.38.2 | 2026-04-21 | **Target per CONTEXT.md** |
| 1.38.3 | 2026-04-21 | Available |
| 1.38.4 | 2026-04-25 | Available — first version containing #2487 + #2499 |
| 1.38.5 | 2026-04-25 | **Latest stable (`dist-tags.latest`)** |

### Diff size by candidate target
| Range | Commits | Files changed | Insertions | Deletions |
|---|---|---|---|---|
| 1.34.2..1.38.2 | 248 | 437 | 53,973 | 2,628 |
| 1.34.2..1.38.4 | 389 | 672 | 94,454 | 9,739 |
| 1.34.2..1.38.5 | 394 | 674 | 94,499 | 9,774 |
| 1.38.2..1.38.5 | 146 | 415 | 42,593 | 9,213 |

> **Reconciliation with ROADMAP.md baseline:** ROADMAP.md says "312 commits / 566 files at 2026-04-21 poll". Tag-anchored count is 248 commits / 437 files. The 312/566 figure was likely measured against `main` HEAD on 2026-04-21 (same day v1.38.2 was tagged), which had additional commits between v1.38.2 tag and HEAD-of-main at poll time. Both are correct; the tag-anchored figure is the one that lands in `bun install`.

### File-touch surface (top dirs, 1.34.2..1.38.2)
```
   76 sdk/src               (TypeScript SDK — fork doesn't ship sdk/, no impact)
   72 get-shit-done/workflows  (workflow markdown — composed verbatim by fork)
   34 commands/gsd          (slash commands — composed verbatim)
   28 get-shit-done/references
   19 get-shit-done/bin     (CLI internals — composed verbatim)
   13 .github/workflows     (upstream's own CI — fork doesn't import these)
    6 get-shit-done/templates
```

### Predicted Wave 3 collision candidates (preflight)

**Definitive collision check** — cross-checked every fork `overlay/*` path against the v1.38.2 npm tarball (`package/...`). Result:

| Overlay file | Upstream v1.38.2 path | Collision? |
|---|---|---|
| `overlay/agents/general-purpose.md` | (none) | NO |
| `overlay/agents/gsd-oversight-execution.md` | (none) | NO |
| `overlay/agents/gsd-oversight-planning.md` | (none) | NO |
| `overlay/agents/gsd-oversight-sync.md` | (none) | NO |
| `overlay/agents/gsd-oversight-verification.md` | (none) | NO |
| `overlay/bin/sync-tools.cjs` | (none) | NO |
| `overlay/bin/validate-configs.js` | (none) | NO |
| `overlay/branding.json` | (none) | NO (fork-additive metadata) |
| `overlay/commands/gsd/upstream.md` | (none) | NO (fork-only command) |
| `overlay/features.json` | (none) | NO (fork-additive metadata) |
| `overlay/hooks/pre-compact.js` | (none) | NO |
| `overlay/hooks/pre-compact.sh` | (none) | NO |
| `overlay/lib/sync.cjs` | (none) | NO |
| `overlay/memory/gsd-executor.md` | (none — upstream ships `agents/gsd-executor.md`, different path) | NO |
| `overlay/memory/gsd-plan-checker.md` | (none — upstream ships `agents/gsd-plan-checker.md`, different path) | NO |
| `overlay/teams/execute-phase-team.md` | (none) | NO |
| `overlay/teams/plan-phase-team.md` | (none) | NO |
| `overlay/teams/upstream-sync-team.md` | (none) | NO |
| `overlay/teams/verify-work-team.md` | (none) | NO |
| `overlay/workflows/set-profile.md` | (none) | NO |
| `overlay/workflows/upstream-sync.md` | (none) | NO |

**Wave 3 prediction:** ZERO new path-level collisions in 1.34.2 → 1.38.2. The only files that could drift are the two existing overrides under `overrides/hooks/` (covered in Wave 2 by SHA check, not by Wave 3's collision audit). Wave 3 will resolve fast unless `bun run compose` surfaces a runtime collision the static path-cross-check missed (low likelihood — `compose.js` COMP-08 detector runs the same check).

**Note for the planner:** `overlay/get-shit-done` is a symlink to the project root (visible in `ls -la overlay/`). Drop it from collision logic — it's a build artifact, not an overlay file.

---

## 2. Upstream PR Status

### #2487 — `feat(roadmap): surface wave dependencies and cross-cutting constraints`
- **State:** MERGED (2026-04-20T22:19:21Z), merge commit `bd27d4fa`
- **Branch:** `feat/issue-2447` → `main`
- **Closes:** #2447
- **In which version?** **Tagged in v1.38.4 (2026-04-25)**, NOT in v1.38.2.
- **What it does (verified from PR body + diff):**
  - Adds `roadmap.annotate-dependencies <phase>` SDK command that post-processes the ROADMAP plan list to insert wave dependency notes (`**Wave 2** *(blocked on Wave 1 completion)*`) and a `**Cross-cutting constraints:**` subsection listing `must_haves.truths` entries that appear in 2+ plans.
  - Operation is idempotent and purely derived from existing PLAN frontmatter.
  - Adds step 13d to `plan-phase.md` to call this after planning completes.
- **Files touched:** `get-shit-done/bin/gsd-tools.cjs` (+4/-1), `get-shit-done/bin/lib/roadmap.cjs` (+163/-0), `get-shit-done/workflows/plan-phase.md` (+18/-4), `sdk/src/query/index.ts` (+4/-1), `sdk/src/query/roadmap.ts` (+47/-0), `tests/enh-2447-roadmap-wave-deps.test.cjs` (+205/-0).
- **Does it close the 999.1 hypothesis (plan-checker wave file-collision detection)?** **NO.** The 999.1 hypothesis is about plans in the SAME wave editing the SAME file (creating merge conflicts on parallel execution). #2487 is orthogonal: it surfaces dependencies BETWEEN waves and shared `must_haves.truths` across plans, but does not introduce file-collision detection inside a wave. Verification of the actual `gsd-plan-checker` agent in v1.38.2 confirms only data-transformation conflict detection (lines 486, 497 of `agents/gsd-plan-checker.md`) and a `files_modified` reference at line 761 — no wave-level write collision check. **999.1 hypothesis remains unaddressed by upstream.**

### #2499 — `feat(plan-phase): chunked mode + filesystem fallback for Windows stdio hang`
- **State:** MERGED (2026-04-21T12:40:39Z), merge commit `b2534e8a`
- **Branch:** `feat/issue-2310` → `main`
- **In which version?** **Tagged in v1.38.4 (2026-04-25)**, NOT in v1.38.2.
- **What it does (verified from PR diff):** Adds chunked plan-phase mode + filesystem fallback to mitigate a Windows-specific stdio hang during long agent invocations.
- **Files touched:** `agents/gsd-planner.md` (+4/-0), `docs/CONFIGURATION.md` (+2/-0), `docs/INVENTORY-MANIFEST.json` (+1/-0), `docs/INVENTORY.md` (+3/-2), `get-shit-done/bin/lib/config-schema.cjs` (+1/-0), `get-shit-done/references/planner-chunked.md` (+49/-0), `get-shit-done/workflows/plan-phase.md` (+171/-1), `tests/enh-2310-chunked-plan-phase.test.cjs` (+236/-0).
- **Does it reduce Phase 41's REL-01/REL-02 subprocess-migration surface (D-09)?** **PARTIALLY, but the surfaces are different.** Phase 41 D-09 targets `tests/**` subprocess timeouts (every `execSync`/`exec`/`spawnSync`/`spawn` in fork test files). #2499 targets the upstream `plan-phase.md` workflow's invocation of the gsd-planner agent under Windows — an upstream behavior, not a fork test concern. The fork's flake surface (per `tests/sync.test.cjs`, `tests/installer-safety.test.js`, `tests/hooks.test.js`) is **fork-only code** and unaffected by #2499. The skin-fork triangulation per D-09 (categorize each call site as fork-only/upstream-code/CI-plumbing) was always the right discipline; #2499 doesn't change which call sites need fork-side timeouts. Phase 41 D-09 stays valid.

### #1859 — `feat(review): add per-CLI model selection via config`
- **State:** MERGED (2026-04-10T14:44:15Z), merge commit `7e221718`
- **In which version?** **Tagged in v1.35.0**, present in v1.38.2 (and v1.38.3, v1.38.4, v1.38.5).
- **Fork status:** The fork has NO `overlay/workflows/review.md` and NO `overrides/workflows/review.md`. The fork composes upstream's `review.md` AS-IS into `dist/get-shit-done/workflows/review.md`. There is no fork-side divergence to reconcile.
- **Wave 3 #1859 audit prediction:** Resolves in <15 minutes. The audit task is "confirm fork has no `review.md` overlay/override that conflicts with upstream's #1859 changes." Answer: confirmed by directory listing. No action required.

### #2126 (referenced in research input)
- **State:** CLOSED, NOT MERGED. Title: `Revert "ci: fix release pipeline" (#1956)`. Not relevant to Windows stdio context — title is misleading; this was a CI-pipeline revert PR. **Skip.**

### Implication for the bump target choice (NEEDS-USER-DECISION — see §8)
CONTEXT.md mandates "1.38.2 (or latest stable at execution time)" — letting the planner choose. **The data argues for 1.38.4 or 1.38.5**, not 1.38.2:
- Both #2487 and #2499 land in v1.38.4. Bumping to 1.38.2 means re-verifying Phase 41 against an upstream state that's missing the very PRs CONTEXT.md success criterion #4 names as targets to compare against.
- Bumping to 1.38.4 (146 more commits, 415 more files vs 1.38.2) means the verification compares Phase 41 decisions against the post-#2487/#2499 upstream — which is what success criterion #4 actually intends.
- Latest stable (1.38.5) adds 5 more commits over 1.38.4 (mostly tests, minor fixes). Choosing 1.38.5 follows the "or latest stable at execution time" clause cleanly.

This is presented as a NEEDS-USER-DECISION item in §8 because it affects Wave 1 success criterion strictly — the user should decide before planning starts.

---

## 3. Phase 41 CONTEXT.md Citation Map

> 22 rows total: 18 decisions D-01..D-18 + 4 amendments A-01..A-06 (note: Phase 41 has 6 amendments per CONTEXT.md text, but A-01..A-04 each amend a single D-XX, and A-05/A-06 each amend several. The matrix per D-11 should treat each A-XX as one row, not duplicate per amended D-XX).
>
> **Wave 4 sizing per D-23:** 10 min/upstream-cited row + 3 min/fork-only row.

### Citation map (every D-XX/A-XX from Phase 41 CONTEXT.md)

| ID | What it cites | Citation kind | D-02 priority | D-23 estimate |
|----|---------------|---------------|---------------|---------------|
| D-01 | `.github/workflows/ci.yml:85-100` (fork CI), `scripts/check-overrides.js`, `scripts/check-boundary.js` | fork-only | tail | 3 min |
| D-02 | `.changelog-conflict-check.sh` (fork shell script, doesn't exist yet — Phase 41 creates it), Phase 42 DOCS-06 reference | fork-only + future-phase | tail | 3 min |
| D-03 | `tests/fixtures/changelog-conflict/*` (fork fixtures, doesn't exist yet) | fork-only | tail | 3 min |
| D-04 | `MAINTENANCE.md` (fork doc, doesn't exist yet), `.changelog-conflict-check.sh`, `.planning/audits/suppressions.json` | fork-only | tail | 3 min |
| D-05 | `.planning/audits/suppressions.json` (fork artifact), AJV strict-schema (Phase 39 precedent) | fork-only | tail | 3 min |
| D-06 | `SECURITY.md` at repo root (fork-only) | fork-only | tail | 3 min |
| D-07 | `gitleaks-action@v2`, `osv-scanner-action@v2`, `bun.lock`, `.gitleaks.toml`, `.planning/audits/low-sev-findings.jsonl` (fork artifact) | fork-only + external action | tail | 3 min (fork-side); SEPARATE — verify the named GHA actions still exist + recommended versions |
| D-08 | `step-security/harden-runner@v2`, `MAINTENANCE.md` (fork) | fork-only + external action | tail | 3 min (fork) + verify harden-runner@v2 still recommended |
| **D-09** | **upstream `tests/helpers/test-timeouts.js` — but actually a fork file (verified)**, modern Node ≥18 timeout APIs (`execSync`/`AbortSignal.timeout`/spawn signal), `tests/sync.test.cjs` (fork test), `tests/hooks.test.js`, `tests/installer-safety.test.js`, `tests/installer-exports.test.js` | fork-only + Node API stability | tail | 3 min (fork-only) + **EXPLICIT verification per success criterion #4: re-check D-09 vs upstream #2499** — see §2 above; result is "stays valid, surfaces are different" |
| D-10 | GitHub Issues API as flake collector, GHA Script step, structured labels | fork-only (uses GitHub but not citing upstream code) | tail | 3 min |
| D-11 | "2 working days hard time budget" (process decision, no file citation) | process-only | tail | 3 min |
| D-12 | bun:test `test.skip.if`, GHA job summary, MAINTENANCE.md Escape-Hatch table | external API + fork doc | tail | 3 min (verify bun:test API still has `test.skip.if`) |
| D-13 | `scripts/bench.js` (fork, doesn't exist yet — Phase 41 creates it), `scripts/compose.js` (fork), `bun install --ignore-scripts` (bun feature), `.planning/perf/test-timing.json` (fork artifact) | fork-only | tail | 3 min |
| D-14 | hyperfine (`--warmup`, `--runs`, `--export-json`), `perf-baseline.json`, GHA timeout 30 min | external tool + fork artifact | tail | 3 min (verify hyperfine flags stable; hyperfine is at v1.18.0, stable; mostly fork) |
| D-15 | `perf-baseline.json` JSON shape, `test-timing.json` JSON shape — both fork artifacts | fork-only | tail | 3 min |
| D-16 | `scripts/bench.js`, `scripts/bench-test-timing.js`, `config/perf-baseline.schema.json`, `config/test-timing.schema.json` — all fork-only | fork-only | tail | 3 min |
| D-17 | `workflow_dispatch` workflow `10x-validation.yml` (fork doc, doesn't exist yet), GHA primitive | fork-only | tail | 3 min |
| D-18 | `tests/check-overrides.test.js` (fork), `tests/check-overrides-integration.test.js` (fork, doesn't exist yet), `node scripts/check-overrides.js` | fork-only | tail | 3 min |
| A-01 → D-02 | Same as D-02 + `markdownlint-cli2@0.22.0` (Phase 42 DOCS-06 dep) | fork-only + external lib | tail | 3 min |
| A-02 → D-04 | Same as D-04 (clarifies "scope-partial, quality-full" framing) | fork-only | tail | 3 min |
| A-03 → D-07 | Same as D-07 (reverses scanner-level pre-filtering) | fork-only + external action | tail | 3 min |
| A-04 → D-09 | Same as D-09 (softens REL-02 wording, adds triangulation requirement) | fork-only + Node API | tail | 3 min + **EXPLICIT verification per success criterion #4** (see §2 — same conclusion) |
| A-05 → D-10 | Same as D-10 (adds dedup key, label taxonomy, auto-close, scope cap) | fork-only + GitHub Issues | tail | 3 min |
| A-06 → D-13/D-15/D-16 | Same as D-13/D-15/D-16 (splits perf-baseline.json from test-timing.json) | fork-only | tail | 3 min |

### Summary by D-02 priority

| Priority bucket | Row count | Per-row time | Subtotal |
|---|---|---|---|
| **Upstream-cited (D-02 head)** | **0** | 10 min | **0 min** |
| **Fork-only (D-02 tail)** | 22 | 3 min | 66 min |
| **Explicit named verification targets** (D-09 vs #2499; 999.1 vs #2487) | 2 | ~15 min each (already done in §2) | 30 min (already largely amortised) |
| **External-action/library refresh** (gitleaks, osv-scanner, harden-runner, hyperfine, bun:test API) | 5 | ~5 min each | 25 min |
| **Total Wave 4 estimate (revised)** | | | **~120 min (2 hours)** |

**Comparison with D-23's estimate of 90-180 min for Wave 4:** Falls within the lower half of the range. The reason: ALL 22 rows are fork-only or external. The 10-min/upstream-cited estimate was a pessimistic ceiling for a row category that doesn't exist in this Phase 41 CONTEXT.md. Drift volume (per D-17, halt-and-amend on MATERIAL drift) is the only thing that could push Wave 4 past 2 hours — and given the citation profile, drift will be near-zero.

> **Planner note:** The interactive-Claude-per-row constraint (D-04) still applies even though each row is fast. The user wants to be in the loop. Don't bundle multiple rows per task; one task per D-XX/A-XX with a manifest of what was checked, OR a small group (3-4 rows) per task with explicit per-row evidence in the task output. The latter is more interactive-friendly without sacrificing per-row evidence.

---

## 4. Override Drift Detection (Wave 2 inputs)

### Current overrides on disk
| Override | Companion REASON.md | Recorded version | Recorded SHA-256 |
|---|---|---|---|
| `overrides/hooks/gsd-check-update.js` | `overrides/hooks/gsd-check-update.js.REASON.md` | 1.34.2 | `61fcb20c5ce5e07346b99c0bc8e6bf9de58d1d088cf3e34d4de53898aee07c2b` |
| `overrides/hooks/gsd-statusline.js` | `overrides/hooks/gsd-statusline.js.REASON.md` | 1.34.2 | `2e155c727e12190dfbddd13a7f18f1e9d85a4f62a66cec2d4ab2b489d17e31c5` |

**Sanity check (current state):** Both recorded SHAs match the currently-installed `node_modules/get-shit-done-cc/hooks/<file>.js` SHAs at v1.34.2. The override system is in a healthy pre-bump state.

### Upstream paths each override shadows (verified against v1.38.2 tarball layout)
- `overrides/hooks/gsd-check-update.js` → `node_modules/get-shit-done-cc/hooks/gsd-check-update.js`
- `overrides/hooks/gsd-statusline.js` → `node_modules/get-shit-done-cc/hooks/gsd-statusline.js`

The fork's `scripts/check-overrides.js` resolves this mechanically: `overrides/<rel>` shadows `node_modules/get-shit-done-cc/<rel>`. Confirmed by reading the script (lines 168-188).

### Drift expectation post-bump (1.34.2 → 1.38.2 OR 1.38.5)

Upstream churn on the two overridden files between 1.34.2 and 1.38.2:

| File | Lines added | Lines deleted | Net change | Commits touching |
|---|---|---|---|---|
| `hooks/gsd-check-update.js` | +13 | -88 | -75 | 2 (`#2224` complete stale-hooks fix; `#2141` add bash hooks to MANAGED_HOOKS staleness check) |
| `hooks/gsd-statusline.js` | +149 | -15 | +134 | 3 (`#2219` scale context meter to CLAUDE_CODE_AUTO_COMPACT_WINDOW; `#2162` contextual warning for dev installs with stale hooks; `#1990` surface GSD milestone/phase/status when no active todo) |

Both files WILL drift on bump. The drift is meaningful (both behavior + line-count). Per D-07, **human review every drift, no auto-classify**. Per D-14, refresh `Snapshot:` in REASON.md atomically with the verification commit.

### Procedure for the planner (do NOT execute now — task spec for Wave 2)

Per Wave 2 in D-16, the per-override task should be:

1. After Wave 1 lands (`bun install` against the new pin), the new `node_modules/get-shit-done-cc/hooks/gsd-check-update.js` is on disk with the post-bump content.
2. Run `node scripts/check-overrides.js`. It will exit 1 with `stale` status and report `recordedHash` (from REASON.md) vs `currentHash` (just-installed file).
3. For each stale entry, the interactive verification per D-07:
   - Diff the new upstream file against the OLD one (1.34.2): `git -C node_modules/get-shit-done-cc log --oneline -p -- hooks/gsd-check-update.js` is NOT possible because `node_modules` is not git. Instead, use the bare-clone approach: `git -C /path/to/upstream-bare-clone diff v1.34.2..v1.38.2 -- hooks/gsd-check-update.js` (or use `npm view get-shit-done-cc@1.34.2` and `@1.38.2` with `npm pack` to extract both tarballs and `diff -u`).
   - Read both diffs in interactive mode with the user. For each upstream change:
     - Is it a behavior change to a function/concern the override re-implements? → DRIFTED with semantic conflict; document in DRIFT-LOG.md what part of the upstream improvement is sacrificed by keeping the override.
     - Is it a behavior change to a concern the override doesn't touch? → DRIFTED but harmless; refresh SHA snapshot.
     - Is it whitespace/comment only? → Not a real drift in D-05's sense; refresh SHA.
4. Refresh `Snapshot: Version` and `Snapshot: SHA-256` in `<override>.REASON.md` per D-14, atomically with the verification commit.
5. If the override's reason for existing has been overtaken by upstream (e.g. upstream now ships `detectConfigDir` and the fork plans to expand beyond Claude Code), surface that as a candidate for **removing the override** — but per D-08, that's a Wave 3 collision-style decision, not Wave 2. Wave 2 just records the drift; Wave 3 decides "keep override / fold upstream improvements / remove override entirely".

### Overlay → override promotion candidates

**None identified.** Per the §1 collision check, zero overlay files collide with v1.38.2 upstream. The current 2 overrides are the entire override surface and remain the entire override surface post-bump. (If the user picks 1.38.4 or 1.38.5 as the bump target per §2, the analysis reruns — the 415-file 1.38.2..1.38.5 diff is much larger and could surface new collision candidates. The overlay file list itself is small enough — 21 unique paths — that a manual rerun of §1's table against the chosen target tarball takes 5 minutes.)

---

## 5. #1859 Audit Specifics (Wave 3 inputs)

### Upstream merge facts
- PR #1859 — `feat(review): add per-CLI model selection via config`
- Merge commit `7e2217186a45c082a83ca3292aff93043934a39d`
- Merged 2026-04-10T14:44:15Z
- First tagged in **v1.35.0**; present in v1.38.2 and all subsequent versions.

### Files touched upstream by #1859 (verified via `gh pr view 1859 --json files`)
The fork ships none of these as overlay or override files. Verified by:
- `find overlay -name 'review*.md'` → empty
- `find overrides -name 'review*.md'` → empty
- `git log --oneline --all -- '**/review.md'` → fork has only `commit cd0edb7 / d908bfd` and `c41a9f5` which add fork-side `.claude/commands/gsd/review-backlog.md` and `.claude/commands/gsd/review.md` (slash command entry-points, NOT the upstream `workflows/review.md` workflow content).
- Composed `dist/get-shit-done/workflows/review.md` is upstream's content as-is.

### Wave 3 audit scope for #1859

**The audit is a confirmation, not a reconciliation.** The task is one paragraph in COLLISIONS.md:

> "PR #1859 (review per-CLI model selection) merged upstream 2026-04-10, present from v1.35.0 through current target. The fork has no `overlay/workflows/review.md`, no `overrides/workflows/review.md`, and composes upstream's `review.md` as-is into `dist/`. No fork divergence exists to reconcile against #1859. Action: NONE. Verified 2026-04-XX by:
> - `find overlay -name 'review*.md'` → empty
> - `find overrides -name 'review*.md'` → empty
> - `diff dist/get-shit-done/workflows/review.md node_modules/get-shit-done-cc/get-shit-done/workflows/review.md` → no diff (composition is verbatim)."

**Estimated time:** <15 min, well below D-23's "30 min if upstream's review.md matches our overlay semantically."

### Other Wave 3 collision audit scope

Per success criterion #3, the audit is for **any new** collision the bump introduces. Per §1 above, the static path-cross-check shows zero new collisions. Wave 3's primary task therefore degenerates to:
1. Run `bun run compose` against fresh-bumped `node_modules/`. Confirm exit 0 (no COMP-08 collision errors).
2. Document the #1859 audit per above.
3. Document zero new collisions per the §1 table.

**Estimated Wave 3 time (revised):** 30-45 min, lower half of D-23's 30-90 min range.

If the user picks 1.38.4 or 1.38.5 as the bump target, redo §1's table against that tarball — the larger 1.38.2..1.38.5 diff (415 files, vs 437 in 1.34.2..1.38.2 — almost identical surface) **could** surface a new collision but probably won't (overlay paths are mostly fork-additive concerns: oversight agents, sync utilities, teams, profiles).

---

## 6. Backlog Item Disposition (999.1, 999.3)

### 999.1 — Investigate plan-checker wave collision detection

**Hypothesis (from CONTEXT.md):** `gsd-plan-checker` does not detect plans in the same execution wave modifying the same files (would create guaranteed merge conflicts on parallel execution).

**Upstream check #1: Did PR #2487 close the gap?**

NO. Per §2 above, #2487 surfaces wave-dependency annotations and cross-cutting `must_haves.truths` constraints — orthogonal to wave-level file-write collision. The 999.1 hypothesis remains live.

**Upstream check #2: Does upstream's gsd-plan-checker.md @v1.38.2 (or v1.38.5) have wave-level file collision detection at all?**

Verified by reading `agents/gsd-plan-checker.md` @v1.38.2:
- Line 444: cross-references Wave 0 task to test file (passing test → blocking link, but that's about Wave 0 task structure, not wave collisions).
- Line 486: "For each shared data path, check if one plan's transformation conflicts with another's" — this is data-transformation conflict detection (semantic), not file-write collision detection.
- Line 497: "Severity: WARNING for potential conflicts. BLOCKER if incompatible transforms on same data entity with no preservation mechanism."
- Line 761: `grep "files_modified:" "$PHASE_DIR"/$PHASE-01-PLAN.md` — uses `files_modified` to read plan frontmatter, but doesn't check wave-overlap.

**Conclusion:** Upstream tracks data-transformation conflicts but NOT wave-level write collisions to the same file path. The 999.1 hypothesis is at least partially valid as a real upstream gap. Per D-18 disposition tree:

- (a) `#2487` addresses gap → **NOT MET**
- (b) upstream doesn't address it → **MET** → **999.1 stays backlog with its existing 3 promotion paths (Path A upstream PR / Path B PROCESS-08 / Path C decimal-insert into Phase 42) intact.**

The 999.1 CONTEXT.md correctly captured the over-strict-rule caveat ("Two plans adding DIFFERENT new functions to the same file are mergeable without conflict"). That refinement work still belongs to the 2-hour investigation when 999.1 is taken up.

**Wave 4 / Wave 5 task for 999.1:** Record the disposition above in `40.5-DECISION-VERIFY.md` (or in `40.5-VERIFICATION.md` directly per D-10 — the planner picks). Required output per CONTEXT.md success criterion #4.

### 999.3 — UI-gate regex word-boundary fix

**Hypothesis:** Upstream's `workflows/plan-phase.md` UI-gate regex matches substrings naively (`view` inside `re-review`, `form` inside `formatting`, etc.). File upstream PR with `\b...\b` word-boundaries fix only if bug still present after bump.

**Upstream check at v1.38.2:**

Located the regex at line 457 of `get-shit-done/workflows/plan-phase.md`:

```bash
echo "$PHASE_SECTION" | grep -iE "UI|interface|frontend|component|layout|page|screen|view|form|dashboard|widget" > /dev/null 2>&1
```

**Bug status: STILL PRESENT.** No word-boundary anchors. The pattern `view` matches inside `re-review`, `interview`, `overview`. The pattern `form` matches inside `formatting`, `format`, `transform`, `inform`, `perform`, `formal`. The pattern `page` matches inside `paginate`, `pageant`. False-positive surface is significant for any phase whose description happens to contain these common English words.

**Fix the planner can write upstream:** Convert each alternative to `\b<word>\b`:

```bash
echo "$PHASE_SECTION" | grep -iE "\b(UI|interface|frontend|component|layout|page|screen|view|form|dashboard|widget)\b" > /dev/null 2>&1
```

(Note: `UI` is a 2-letter token; `\bUI\b` correctly matches `UI`, `UI/UX`, `UI:` and rejects `BUILD`, `GUI` if those should be excluded. Verify with the user that this scope is desired before filing — the original regex may have intentionally been generous.)

**Per D-20 disposition:**
- Bug still present → **file upstream issue + PR with `\b...\b` word boundaries**; record filing in `40.5-VERIFICATION.md`.
- Per `feedback_verify_upstream_scope.md` discipline (3 prior "upstream bug" labels turned out to be fork-specific): the verification step in this RESEARCH.md is the verification — bug confirmed in upstream tarball v1.38.2, line 457, no fork involvement.

**Wave 5 task for 999.3:** Draft + file the upstream issue + PR (or capture as a follow-up commit). Estimated 1-2h per CONTEXT.md backlog estimate.

---

## 7. Wave Time Estimate Refinement

Updated estimates with concrete counts from this research, vs D-23 ranges:

| Wave | D-23 estimate | Refined estimate | Reasoning |
|------|----|----|----|
| **Wave 1** (pin bump + install + Linux/macOS test) | 30-60 min | **30-45 min interactive** + CI runtime (~15 min) | Mechanical bump; 2 file changes (`package.json` pin + `bun.lock`). CI runtime varies. Lower half of range. |
| **Wave 2** (override drift → DRIFT-LOG.md) | 30-60 min | **45-90 min** (15-30 min/override × 2 overrides + writeup) | D-23 estimated ~15 min/override. Both overrides have meaningful drift (gsd-check-update.js -75 lines net, gsd-statusline.js +134 lines net). Each will need 30-min semantic review per D-07 to evaluate whether upstream improvements should be folded in vs override retained. **Upper half of range.** |
| **Wave 3** (compose collisions + #1859 → COLLISIONS.md) | 30-90 min | **30-45 min** | Zero new path-level collisions predicted (§1 table). #1859 audit resolves to "no fork divergence" in <15 min (§5). `bun run compose` is the runtime confirmation. Lower half. |
| **Wave 4** (22-decision matrix → DECISION-VERIFY.md) | 90-180 min | **~120 min (2 hours)** | All 22 rows are fork-only (none upstream-cited per D-02 priority). 22 × 3 min = 66 min for fork rows + ~30 min for the 2 named verification targets (D-09 vs #2499; 999.1 vs #2487 — already done in this RESEARCH.md, just need to be recorded) + ~25 min for external-action refresh checks (gitleaks, osv-scanner, harden-runner, hyperfine, bun:test API stability). **Lower half of range.** |
| **Wave 5** (VERIFICATION.md synthesis + README + final commit) | 30-60 min | **30-60 min** + 999.3 upstream PR drafting (separate task, 1-2h) | Synthesis is fast given the supplementary files. 999.3 upstream PR is a Wave 5 deliverable per D-20 but its work is independent of synthesis. **Per D-23 range.** |

### Refined total
- Without 999.3 upstream PR: **~4 hours focused interactive work** (well within D-23's 3.5-7h range, lower half).
- With 999.3 upstream PR drafting: **~5-6 hours** (still within range).
- Likely spans **2 sessions** (not 3, given lower-half estimates) unless drift-amendment volume in Wave 4 surprises us. Per CONTEXT.md, Wave 4 surfacing >5 material drifts triggers a second session for that wave alone — but since all 22 rows are fork-only and have no upstream surface to drift against, this scenario is implausible.

### Key risks affecting these estimates

1. **Wave 2 risk:** If the user wants to fold upstream's `hooks/gsd-check-update.js` improvements (`isNewer`, `detectConfigDir`, stale hook detection, shared cache — see UPGRADE-07 in REQUIREMENTS.md) **into the override now** instead of deferring to Phase 43 UPGRADE-07, Wave 2 becomes a meaningful refactor (1-2h additional). **Phase 43 explicitly owns UPGRADE-07**, so the right answer is "refresh SHA snapshot only, defer the merge to Phase 43." But the planner must hold this line during execution (per `honor-directives.md`).

2. **Wave 4 risk:** If the user picks 1.38.4 or 1.38.5 instead of 1.38.2 (per the NEEDS-USER-DECISION in §8), the 22-decision matrix verifies against a slightly larger upstream surface, but the citation profile (all fork-only) doesn't change — Wave 4 estimate stays at ~120 min.

3. **Bun lockfile risk:** The fork uses `bun.lock` (not `bun.lockb` or `package-lock.json`). After `package.json` pin change, `bun install --frozen-lockfile` will fail until `bun install` regenerates the lockfile. Wave 1 must run `bun install` (no `--frozen-lockfile`) once to regenerate, commit both `package.json` and `bun.lock` together. Standard pattern for npm pin bumps; planner should not surprise-budget for this.

---

## 8. Open Questions / NEEDS-USER-DECISION

### Q1 — Bump target: 1.38.2, 1.38.4, or 1.38.5?

**The data argues for 1.38.4 or 1.38.5, NOT 1.38.2.**

CONTEXT.md says "Bump upstream pin from `get-shit-done-cc@1.34.2` to `1.38.2`" but also success criterion #1 says "(or latest stable at execution time)." Success criterion #4 names #2487 and #2499 as the comparison targets.

| Target | Pros | Cons |
|---|---|---|
| **1.38.2** | CONTEXT.md says so; lower diff size (248/437 vs 394/674 for 1.38.5) | Doesn't contain #2487 or #2499 — the very PRs success criterion #4 names. Verifying D-09 vs #2499 against an upstream that doesn't have #2499 is incoherent. |
| **1.38.4** | First version containing #2487 + #2499. Tagged 2026-04-25, 3 days old at 40.5 execution time. | Not the latest — letting it be skipped over by 1.38.5 means a dogfood gap closer to Phase 43. |
| **1.38.5** (latest) | Honors "or latest stable at execution time"; minimal additional churn over 1.38.4 (5 commits, 2 files). | None substantive. |

**Recommended target:** 1.38.5. Reason: matches "latest stable at execution time", contains the named verification PRs, and the additional churn over 1.38.4 is trivial (5 commits, mostly tests + minor fixes).

If the user wants strict literal CONTEXT.md compliance, target 1.38.2 — but then re-verifying D-09 vs #2499 becomes a hypothetical (verify against upstream's `main` HEAD or against the merged PR diff itself, not against the bumped state). This adds verification awkwardness.

**Action requested:** User picks 1.38.2 / 1.38.4 / 1.38.5. The planner sizes Wave 1 against that choice and Wave 4 verifies against that choice's actual upstream tree.

### Q2 — Should Phase 41 D-09 / A-04 surface `tests/helpers/test-timeouts.js` as upstream-cited?

The 41-CONTEXT.md text reads: "`tests/helpers/test-timeouts.js` — exports `SUBPROCESS_TIMEOUT=15000` and `HEAVY_SUBPROCESS_TIMEOUT=30000`."

Verification: this file does NOT exist in upstream (`tar -tzf get-shit-done-cc-1.38.2.tgz | grep test-timeouts` is empty). It's a fork-only file. The 41-CONTEXT.md correctly classified it as a fork-only canonical reference under "Subprocess pattern foundation." No correction needed; this RESEARCH.md just confirms that classification.

### Q3 — Wave 2 fold-or-defer for upstream `hooks/gsd-check-update.js` improvements

UPGRADE-07 (Phase 43, NOT 40.5) explicitly scopes: "Upstream hook improvements merged into `overrides/hooks/gsd-check-update.js` (isNewer, detectConfigDir, stale hook detection, shared cache) with atomic coupling to `gsd-statusline.js`; fork-specific behavior preserved."

**Phase 40.5 should NOT do this work.** Wave 2 only refreshes the SHA snapshot per D-14; the actual merge is Phase 43 territory. Per `honor-directives.md`, the planner should not silently expand Wave 2's scope to fold upstream improvements. **No user decision needed; flagging for planner discipline only.**

### Q4 — Should the bump runbook surface `bun.lock` regeneration as Wave 1 task or auto-step?

`bun install --frozen-lockfile` after a `package.json` pin change fails until `bun.lock` is regenerated. The Wave 1 task list should include "run `bun install` (without `--frozen-lockfile`) to regenerate the lockfile, then commit both `package.json` and `bun.lock` atomically." Not a user decision — flagging for planner explicit task.

### Q5 — `overlay/get-shit-done` symlink

Listed in `overlay/` directory listing as a symlink to the project root (`overlay/get-shit-done -> /c/.../get-stuff-done/get-stuff-done`). This is a build / development convenience symlink, not an overlay file. Confirmed it's not picked up by collision detection (compose.js excludes the directory; check-overrides.js targets `overrides/`, not `overlay/`).

**Action:** None for 40.5. If it confuses Wave 3 manual inspection, the planner can document "ignore overlay/get-shit-done — symlink, not an overlay" in COLLISIONS.md.

---

## RESEARCH COMPLETE

### Key findings summary

1. **Bump target should likely be 1.38.5 (or 1.38.4)**, not 1.38.2 — the named verification PRs (#2487, #2499) are NOT in 1.38.2; they first land in 1.38.4. NEEDS-USER-DECISION (§8 Q1).
2. **Zero new path-level overlay collisions** predicted in 1.34.2 → 1.38.2 (and likely also through 1.38.5). Wave 3 will resolve fast.
3. **#1859 audit is a non-event for the fork** — no fork-side `review.md` divergence exists. <15 min Wave 3 task.
4. **Both existing overrides (`gsd-check-update.js`, `gsd-statusline.js`) WILL drift on bump** — meaningful behavior changes upstream. Wave 2 needs human review per D-07. SHA refresh per D-14.
5. **All 22 D-XX/A-XX rows in Phase 41 CONTEXT.md are fork-only** (no upstream-file citations). D-02's "upstream-cited rows first" priority head is empty. Wave 4 sizing collapses to ~2 hours.
6. **999.1 hypothesis remains live** (PR #2487 doesn't address wave-level file-write collisions). Per D-18(b), 999.1 stays backlog.
7. **999.3 bug confirmed still present** in upstream `workflows/plan-phase.md` line 457. Per D-20, file upstream issue + PR (1-2h Wave 5 task).
8. **D-09 vs #2499 conclusion:** different surfaces. Phase 41's fork-test subprocess timeout work stays valid; #2499's upstream plan-phase chunked-mode fix is orthogonal.
9. **Refined total estimate:** ~4 hours focused interactive work (lower half of D-23's 3.5-7h range), likely 2 sessions, +1-2h for the optional 999.3 upstream PR.

### Confidence breakdown

| Area | Level | Reason |
|------|-------|--------|
| Upstream changelog (commit/file counts) | HIGH | Direct git operations on bare clone of `gsd-build/get-shit-done` |
| PR #2487/#2499/#1859 status + version inclusion | HIGH | `gh pr view` API + `git tag --contains` cross-check |
| Override drift expectation | HIGH | `git diff --shortstat v1.34.2..v1.38.2` per file + commit list |
| Overlay collision matrix (zero new collisions) | HIGH | Static path cross-check overlay/* vs npm tarball; further validated by `compose.js` COMP-08 detector |
| #1859 fork-side scope (no fork divergence) | HIGH | Filesystem search + dist diff; multiple confirmations |
| 999.1 hypothesis disposition | HIGH | Read upstream `gsd-plan-checker.md` directly + #2487 PR body |
| 999.3 bug confirmation | HIGH | Direct grep of upstream tarball file at line 457 |
| Phase 41 citation map (fork-only majority) | HIGH | Read 41-CONTEXT.md exhaustively + cross-check each cited file in upstream tarball |
| Time estimate refinement | MEDIUM | Estimates are bounded but interactive verification time is operator-dependent |
| Bump-target recommendation (1.38.5 over 1.38.2) | HIGH (data) / NEEDS-USER (final call) | Data clear; user owns the call |

### File created

`.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-RESEARCH.md`

### Ready for planning

Research complete. Planner can now produce sequential, criterion-by-criterion PLAN.md files per D-16, sized per the refined estimates in §7. Surface Q1 (bump target choice) to the user before Wave 1 task generation.

---

*Sources:*
- `gh pr view` against `gsd-build/get-shit-done` for PR #2487, #2499, #1859, #2126 status
- `git clone --bare https://github.com/gsd-build/get-shit-done.git`; `git diff --shortstat v1.34.2..v1.38.{2,4,5}`; `git tag --contains <merge-commit>`; `git ls-tree`, `git show v1.38.2:<path>`
- `npm view get-shit-done-cc versions --json`; `npm view get-shit-done-cc@1.38.2`
- `curl -sL https://registry.npmjs.org/get-shit-done-cc/-/get-shit-done-cc-1.38.2.tgz | tar -tzf` (definitive tarball layout)
- Direct read of `.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md`, `40.5-CONTEXT.md`, `REQUIREMENTS.md`, `STATE.md`, `ROADMAP.md`, `package.json`, `scripts/check-overrides.js`, `overrides/hooks/*.REASON.md`
- `find overlay -type f`, `find overrides -type f`, `sha256sum` cross-check
