---
phase: 10-claude-code-adoption
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - get-stuff-done/workflows/discuss-phase.md
  - get-stuff-done/workflows/verify-work.md
  - get-stuff-done/workflows/execute-phase.md
  - get-stuff-done/workflows/verify-phase.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All 4 workflow files in project source match installed versions (content parity)"
    - "AskUserQuestion integration present in all 4 source workflows (discuss-phase: 9, verify-work: 9, execute-phase: 9, verify-phase: 2)"
    - "Project source workflows use ~/.claude/ portable paths (no hardcoded absolute paths)"
    - "npm publish from project source would ship workflows WITH AskUserQuestion integration"
  artifacts:
    - path: "get-stuff-done/workflows/discuss-phase.md"
      provides: "Workflow with AskUserQuestion integration (9 calls)"
      contains: "AskUserQuestion"
    - path: "get-stuff-done/workflows/verify-work.md"
      provides: "Workflow with AskUserQuestion integration (9 calls)"
      contains: "AskUserQuestion"
    - path: "get-stuff-done/workflows/execute-phase.md"
      provides: "Workflow with AskUserQuestion integration (9 calls)"
      contains: "AskUserQuestion"
    - path: "get-stuff-done/workflows/verify-phase.md"
      provides: "Workflow with AskUserQuestion integration (2 calls)"
      contains: "AskUserQuestion"
  key_links:
    - from: "get-stuff-done/workflows/*.md (project source)"
      to: "C:\\Users\\Destiny\\.claude\\get-stuff-done\\workflows\\*.md (installed)"
      via: "Content parity -- source is what gets published and installed"
      pattern: "AskUserQuestion"
---

<objective>
Sync 4 workflow files from installed location to project source to close the AskUserQuestion parity gap.

Purpose: Phase 10 Plan 04 added AskUserQuestion integration to 4 workflows in the installed location (C:\Users\Destiny\.claude\get-stuff-done\workflows\) but these changes were never synced to publishable project source. Without this sync, npm publish would ship workflows without AskUserQuestion integration — violating Phase 10 Success Criterion 5: "All Phase 10 artifacts exist in project source."

Output: 4 workflow files updated in get-stuff-done/workflows/ with full AskUserQuestion integration matching installed versions
</objective>

<execution_context>
@C:\Users\Destiny\.claude/get-stuff-done/workflows/execute-plan.md
@C:\Users\Destiny\.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-claude-code-adoption/10-VERIFICATION.md
@.planning/phases/10-claude-code-adoption/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync 4 workflow files from installed to project source</name>
  <files>
    get-stuff-done/workflows/discuss-phase.md
    get-stuff-done/workflows/verify-work.md
    get-stuff-done/workflows/execute-phase.md
    get-stuff-done/workflows/verify-phase.md
  </files>
  <action>
    Copy all 4 workflow files from installed location to project source:

    SOURCE (installed, authoritative for content): C:\Users\Destiny\.claude\get-stuff-done\workflows\
    TARGET (project source, needs updating): get-stuff-done/workflows/ in project root

    For each of the 4 workflow files:
    1. Read the file from C:\Users\Destiny\.claude\get-stuff-done\workflows\{name}.md
    2. Write to get-stuff-done/workflows/{name}.md in project source, REPLACING the existing content

    Files to sync:
    - discuss-phase.md (installed: 481 lines with 9 AskUserQuestion calls; source: 433 lines with 4 calls)
    - verify-work.md (installed: 633 lines with 9 AskUserQuestion calls; source: 596 lines with 1 call)
    - execute-phase.md (installed: 629 lines with 9 AskUserQuestion calls; source: 596 lines with 0 calls)
    - verify-phase.md (installed: 646 lines with 2 AskUserQuestion calls; source: 628 lines with 0 calls)

    PATH NORMALIZATION CHECK: The installed workflow files do NOT contain hardcoded Windows paths (verified — 0 matches for C:\Users\Destiny). No path normalization is needed. Copy content as-is.

    However, if any file DOES contain hardcoded absolute paths to C:\Users\Destiny\.claude\, replace them with ~/.claude/ equivalents. The installer's copyWithPathReplacement function handles the reverse conversion during install.

    Do NOT modify the installed files. Only write to project source.
  </action>
  <verify>
    Run these checks:
    1. Line count parity: wc -l for each source file should match installed
       - discuss-phase.md: expect ~481 lines
       - verify-work.md: expect ~633 lines
       - execute-phase.md: expect ~629 lines
       - verify-phase.md: expect ~646 lines
    2. AskUserQuestion count parity:
       - discuss-phase.md: grep -c "AskUserQuestion" (expect 9)
       - verify-work.md: grep -c "AskUserQuestion" (expect 9)
       - execute-phase.md: grep -c "AskUserQuestion" (expect 9)
       - verify-phase.md: grep -c "AskUserQuestion" (expect 2)
    3. No hardcoded Windows paths: grep -r "C:\\Users\\Destiny" get-stuff-done/workflows/ (expect 0 matches)
    4. Diff check: diff each source file against installed (expect 0 differences)
  </verify>
  <done>
    All 4 workflow files in project source match installed versions exactly. AskUserQuestion integration present in all workflows (29 total calls). No hardcoded absolute paths. Source-to-installed parity achieved for workflows.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify agent content drift is installer behavior (no fix needed)</name>
  <files>
    (no files modified)
  </files>
  <action>
    The VERIFICATION.md flags 3 agents (gsd-executor, gsd-verifier, gsd-planner) as having "content drift" between source and installed. Investigation reveals this is NOT a real gap — two expected installer transformations cause the differences:

    1. **Path replacement:** Source uses portable `~/.claude/` paths. The installer's copyWithPathReplacement function converts these to platform-specific absolute paths (`C:\Users\Destiny\.claude/`) DURING INSTALL.
    2. **Line ending normalization:** Source uses LF line endings. The installer (or Windows git/filesystem) converts to CRLF in the installed location.

    Both transformations are EXPECTED behavior. The source versions are MORE CORRECT than the installed versions because they use portable paths and Unix line endings.

    ACTION: No code changes needed. Verify the finding by running:
    1. diff between source and installed for each agent (use --strip-trailing-cr to normalize line endings)
    2. Confirm remaining differences are ONLY path-related (~/.claude/ vs C:\Users\Destiny\.claude/)
    3. Confirm no semantic content differences exist (logic, instructions, sections)

    If any non-path, non-line-ending differences are found, those WOULD be real drift and should be addressed by copying from installed to source with path normalization. Report findings in the summary.
  </action>
  <verify>
    For each of the 3 flagged agents:
    1. Run diff --strip-trailing-cr between source and installed
    2. Inspect ALL differing lines after line ending normalization
    3. Confirm remaining differences are ONLY path references (~/.claude/ vs C:\Users\Destiny\.claude/)
    4. If semantic content differences found (not path or line endings), escalate (do not auto-fix)
  </verify>
  <done>
    Agent content drift confirmed as installer behavior (path replacement + line ending normalization) — not a real gap. Source versions are authoritative with portable ~/.claude/ paths and LF line endings. No action required.
  </done>
</task>

</tasks>

<verification>
Comprehensive source-to-installed parity check:
1. All 4 workflow files in source match installed (content + line count)
2. AskUserQuestion call counts match: discuss-phase(9), verify-work(9), execute-phase(9), verify-phase(2)
3. No hardcoded absolute paths in source workflows
4. Agent content drift explained as installer behavior (no fix needed)
5. Project source is now fully authoritative — npm publish ships all Phase 10 artifacts
</verification>

<success_criteria>
- 4 workflow files in get-stuff-done/workflows/ with AskUserQuestion integration
- Source-to-installed diff shows 0 differences for all 4 workflows
- Zero hardcoded absolute paths in source workflows
- Agent drift documented as non-issue (installer path replacement)
- Phase 10 Success Criterion 5 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/10-claude-code-adoption/10-07-SUMMARY.md`
</output>
