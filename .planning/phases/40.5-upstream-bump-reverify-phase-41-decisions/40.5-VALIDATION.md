---
phase: 40.5
slug: upstream-bump-reverify-phase-41-decisions
status: draft
nyquist_compliant: false
wave_0_complete: true
created: 2026-04-28
---

# Phase 40.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Phase is verification-driven (re-verifying Phase 41 CONTEXT.md decisions against a fresh upstream tree); not net-new-feature. Bump target resolved to `get-shit-done-cc@1.38.5` per CONTEXT.md A-04.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `bun test` (bun:test) |
| **Config file** | None — bun defaults |
| **Quick run command** | `bun test` |
| **Full suite command** | `bun install --frozen-lockfile && bun run compose && bun test` |
| **Estimated runtime** | ~30-60 seconds (quick); ~90-120 seconds (full pipeline) |

---

## Sampling Rate

- **After every task commit:** Run `bun test`
- **After every wave merge:** Run `bun install --frozen-lockfile && bun run compose && bun test`
- **Before `/gsd:verify-work`:** Full suite must be green; all 4 D-21 gates must be satisfied
- **Max feedback latency:** 60 seconds (quick); 120 seconds (full pipeline)

---

## Per-Criterion Verification Map

| Criterion | Plan/Wave | Behavior to verify | Verification primitive | Automated command | Evidence file | Status |
|-----------|-----------|--------------------|------------------------|-------------------|---------------|--------|
| #1 — pin bump | Wave 1 | `package.json` pins `get-shit-done-cc@1.38.5`; `bun install --frozen-lockfile` succeeds; `bun test` green on Linux + macOS | (a) string match `"get-shit-done-cc": "1.38.5"`; (b) `bun install --frozen-lockfile` exit 0; (c) `bun test` exit 0 on both OS | `bun install --frozen-lockfile && bun test` | `40.5-VERIFICATION.md` Wave 1 section + CI run links | ⬜ pending |
| #2 — override drift | Wave 2 | Every override REASON.md `Snapshot: SHA-256` matches the upstream `node_modules/get-shit-done-cc/<rel>` SHA on v1.38.5, OR has explicit drift refresh recorded | `node scripts/check-overrides.js` exit code; per-file SHA-256 compare against upstream tarball | `node scripts/check-overrides.js` | `40.5-DRIFT-LOG.md` (D-12) + refreshed REASON.md per D-14 (inline) | ⬜ pending |
| #3 — compose collisions | Wave 3 | `bun run compose` runs without COMP-08 collision errors; #1859 review.md audited per D-09 (audit + remove if equivalent / move to overrides/) | `bun run compose` exit code; manual diff of overlay paths vs upstream v1.38.5 tarball; #1859 specific audit (overlay/review.md does NOT exist per RESEARCH.md §5 — confirm) | `bun run compose` + `tar -tzf get-shit-done-cc-1.38.5.tgz \| grep <path>` | `40.5-COLLISIONS.md` (D-13) | ⬜ pending |
| #4 — 22-decision re-verify | Wave 4 | Each D-01..D-18 + A-01..A-06 cited symbol/API/line behavior in Phase 41 CONTEXT.md re-verified semantically against fresh v1.38.5 tree | Interactive Claude review per D-04; semantic check per D-05; user sign-off per D-06 (each amendment); upstream-cited rows first per D-02 (RESEARCH.md §3 confirms all 22 are fork-only — priority head empty, all rows priority tail) | None automated — interactive | `40.5-DECISION-VERIFY.md` (D-11) + Phase 41 CONTEXT.md A-07+ amendments (D-03) | ⬜ pending |
| #5 — synthesis + 999.x disposition | Wave 5 | VERIFICATION.md cites all 4 supplementary files (DECISION-VERIFY, DRIFT-LOG, COLLISIONS, README); 999.1 disposition recorded per D-18; 999.3 disposition recorded per D-20 (file upstream PR if bug present in v1.38.5 — confirmed present per RESEARCH.md §6) | `gsd-verifier` agent runs final pass; manual checklist of D-21 plan-phase-41 trigger gates; backlog disposition recording | `gsd-verifier` (framework agent) + manual D-21 gate review | `40.5-VERIFICATION.md` (standard shape per D-10) + `40.5-README.md` (D-15) | ⬜ pending |

---

## Wave 0 Requirements

Per RESEARCH.md §Validation Architecture: **None.** The fork already has all needed infrastructure:

- [x] `scripts/check-overrides.js` — SHA verification + REASON.md enforcement (345 lines)
- [x] `scripts/compose.js` — built-in COMP-08 collision detection
- [x] `tests/check-overrides.test.js` — unit tests for verification script
- [x] `bun test` — full test suite

**No new test infrastructure required by 40.5 itself.** Phase 41 will add `tests/check-overrides-integration.test.js` per its D-18, but that's Phase 41 scope, not 40.5.

---

## Manual-Only Verifications

| Behavior | Criterion | Why Manual | Test Instructions |
|----------|-----------|------------|-------------------|
| 22-decision matrix re-verification (Wave 4) | #4 | Per D-04 — user wants interactive Claude in plan/execute, not subagent verifier batch. Semantic check per D-05 cannot be regex-automated; human eyes required. | Read each D-XX/A-XX from `41-CONTEXT.md`; cite the upstream artifact (file:line / function); compare semantics against v1.38.5 tree; status = unchanged / drifted / amended; record in `40.5-DECISION-VERIFY.md`; user signs off each A-07+ amendment per D-06. |
| #1859 review.md audit (Wave 3) | #3 | RESEARCH.md §5 confirms no `overlay/workflows/review.md` and no `overrides/workflows/review.md` exist on the fork side. Audit is a confirmation step (manual file existence check + composed-output diff), not a reconciliation. | `find overlay overrides -name 'review.md' 2>/dev/null` returns nothing; `bun run compose` + diff resulting `dist/.claude/.../review.md` against upstream's merged 2026-04-10 review.md — record in `40.5-COLLISIONS.md`. |
| 999.3 UI-gate regex PR (Wave 5, optional) | #5 | Filing an upstream issue + PR is a GitHub workflow, not an automated check. Bug confirmed still present at `workflows/plan-phase.md:457` in v1.38.2 per RESEARCH.md §6; re-confirm at v1.38.5. | If bug present: `gh issue create` + `gh pr create` with `\b...\b` word boundaries; record commit + PR # in `40.5-VERIFICATION.md`. If upstream already fixed at v1.38.5: close 999.3 NOT-A-GAP with the fixing commit SHA. |
| Phase 41 CONTEXT.md amendment integration | #4 | Per D-03 + D-06, A-07+ amendments edit Phase 41 CONTEXT.md original D-XX content in-place (chronological log records the deltas). User reviews each amendment before commit. | For every drift found: append A-07+ to Phase 41 CONTEXT.md Amendments Log; amend the original D-XX paragraph; user signs off; commit. |

---

## Validation Sign-Off

- [ ] All 5 criteria have either an automated command or a manual-verification entry above
- [ ] Sampling continuity: every wave runs `bun test` post-merge (no 3 consecutive tasks without sampling)
- [ ] Wave 0 covers all MISSING references (Wave 0 is empty per the fork's existing infra — confirmed in §Wave 0 Requirements)
- [ ] No watch-mode flags (`bun test --watch` not used in CI/sampling — full runs only)
- [ ] Feedback latency < 120s for full pipeline
- [ ] `nyquist_compliant: true` flipped after planner produces PLAN.md files and gsd-plan-checker confirms Dimension 8 coverage

**Approval:** pending — flips on plan-checker `## VERIFICATION PASSED` for Phase 40.5 plans.
