---
phase: 40.5
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - overrides/hooks/gsd-check-update.js.REASON.md
  - overrides/hooks/gsd-statusline.js.REASON.md
  - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md
autonomous: false
requirements: ["UPGRADE-10"]
estimated_minutes: 75
gap_closure: false
must_haves:
  truths:
    - "node scripts/check-overrides.js exits 0 OR every reported drift has documented disposition in 40.5-DRIFT-LOG.md"
    - "Both override REASON.md files have refreshed Snapshot fields (Version: 1.38.5, SHA-256 matching current node_modules tree)"
    - "Each drift was reviewed semantically per D-07 (human review every drift, no auto-classify)"
    - "Wave 2 explicitly does NOT fold upstream gsd-check-update.js improvements into the override (that is Phase 43 UPGRADE-07 territory per RESEARCH.md §8 Q3)"
    - "40.5-DRIFT-LOG.md cites criterion #2 evidence per D-12"
    - "REASON.md format remains the existing convention per D-22 (Title, Why, Upstream snapshot Version+SHA-256, What's different, Review trigger — no new sections)"
  artifacts:
    - path: ".planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md"
      provides: "Per-file SHA mismatch + semantic compare + refresh-or-amend decision (D-12)"
      contains: "overrides/hooks/gsd-check-update.js"
      min_lines: 30
    - path: "overrides/hooks/gsd-check-update.js.REASON.md"
      provides: "Refreshed Snapshot Version=1.38.5 + new SHA-256 of upstream file (D-14)"
      contains: "Snapshot:"
    - path: "overrides/hooks/gsd-statusline.js.REASON.md"
      provides: "Refreshed Snapshot Version=1.38.5 + new SHA-256 of upstream file (D-14)"
      contains: "Snapshot:"
  key_links:
    - from: "overrides/hooks/gsd-check-update.js.REASON.md"
      to: "node_modules/get-shit-done-cc/hooks/gsd-check-update.js"
      via: "SHA-256 snapshot field"
      pattern: "SHA-256:\\s*[0-9a-f]{64}"
    - from: "overrides/hooks/gsd-statusline.js.REASON.md"
      to: "node_modules/get-shit-done-cc/hooks/gsd-statusline.js"
      via: "SHA-256 snapshot field"
      pattern: "SHA-256:\\s*[0-9a-f]{64}"
    - from: "40.5-DRIFT-LOG.md"
      to: "40.5-VERIFICATION.md (Wave 5)"
      via: "Wave 5 cites this file as criterion #2 evidence"
      pattern: "40\\.5-DRIFT-LOG\\.md"
---

<objective>
Wave 2 of Phase 40.5: Detect override drift introduced by the 1.34.2 → 1.38.5 bump.
Per RESEARCH.md §4, both existing overrides (`overrides/hooks/gsd-check-update.js` and
`overrides/hooks/gsd-statusline.js`) WILL drift — gsd-check-update.js (-75 lines net,
2 commits including #2224 stale-hooks fix and #2141 bash-hooks MANAGED_HOOKS extension);
gsd-statusline.js (+134 lines net, 3 commits including #2219 context-meter scaling, #2162
dev-install warnings, #1990 GSD milestone surfacing).

Per D-07: human review every drift (no AST-diff — that's Phase 43 UPGRADE-04). Per D-14:
refresh `Snapshot:` field in REASON.md inline. Per D-22: keep REASON.md format unchanged
(Title, Why, Upstream snapshot Version+SHA-256, What's different, Review trigger).
**Per RESEARCH.md §8 Q3 + Wave 2 risk #1: do NOT fold upstream improvements** (isNewer,
detectConfigDir, stale-hook detection, shared cache) into the override during Wave 2 —
that is Phase 43 UPGRADE-07 territory. Wave 2 only refreshes the SHA snapshot.

Satisfies success criterion #2 of Phase 40.5: every override's SHA-256 snapshot in
its REASON.md matches the current upstream source; any drift is either resolved by
refreshing the snapshot (benign upstream change) or explicitly documented (override
intent vs upstream change conflict).

Purpose: Confirm both overrides are still warranted after the bump, refresh SHA snapshots
inline, and record the drift evidence for Wave 5 VERIFICATION.md citation.

Output: 40.5-DRIFT-LOG.md (per D-12), refreshed REASON.md Snapshot fields on both
overrides (per D-14, atomic with the verification commit). Tasks are interactive
(autonomous: false) because per D-07 the user wants to be in the loop on each drift's
semantic decision (refresh vs document conflict).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CONTEXT.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-RESEARCH.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VALIDATION.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-01-SUMMARY.md
@scripts/check-overrides.js
@overrides/hooks/gsd-check-update.js.REASON.md
@overrides/hooks/gsd-statusline.js.REASON.md
</context>

<tasks>

<task id="02-01" type="auto">
  <name>Task 02-01: Run check-overrides.js to capture baseline drift report</name>
  <files>.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md</files>
  <read_first>
    - scripts/check-overrides.js (full script — confirm output shape; note hashFileContent ~line 75 and walkDir ~line 48)
    - overrides/hooks/gsd-check-update.js.REASON.md (current Snapshot fields)
    - overrides/hooks/gsd-statusline.js.REASON.md (current Snapshot fields)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CONTEXT.md (D-07, D-12, D-14, D-22)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-RESEARCH.md §4 (drift expectation table; procedure subsection)
  </read_first>
  <action>
    Run `node scripts/check-overrides.js` from the repo root with the post-bump tree on disk. Per RESEARCH.md §4 expectations: BOTH overrides will report `stale` because v1.38.5 has meaningful behavior changes to both files vs the recorded 1.34.2 SHA.

    Capture the full stdout + stderr. Expected exit code is 1 (drift detected). If exit code is 0 (no drift), STOP — that contradicts RESEARCH.md §4 and may indicate either (a) bun install didn't update node_modules properly, or (b) overrides were silently fixed earlier. Surface to user.

    Initialize `.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md` per D-12 with the following minimum structure:

    ```markdown
    # Phase 40.5 Drift Log (Criterion #2 Evidence)

    **Wave:** 2
    **Generated:** <YYYY-MM-DD HH:MM>
    **check-overrides.js exit code:** <0 or 1>
    **Upstream version under test:** get-shit-done-cc@1.38.5

    ## check-overrides.js raw report

    ```
    <verbatim stdout from `node scripts/check-overrides.js` — quoted block>
    ```

    ## Per-override semantic review

    (One section per drifted override — populated in Tasks 02-02 and 02-03)

    ## Disposition summary

    | Override | Pre-SHA | Post-SHA | Disposition | Snapshot refreshed? |
    |---|---|---|---|---|
    | overrides/hooks/gsd-check-update.js | <recorded> | <current> | <refresh / document conflict / remove> | <yes/no/n/a> |
    | overrides/hooks/gsd-statusline.js   | <recorded> | <current> | <refresh / document conflict / remove> | <yes/no/n/a> |
    ```

    Cited inline by 40.5-VERIFICATION.md (Wave 5) as criterion #2 evidence per D-12.

    Do NOT make any decisions about disposition in this task — that's Tasks 02-02 and 02-03 with the user in the loop per D-07.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md` returns success
    - `wc -l .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md` returns >= 20 (skeleton + raw report present)
    - File contains the literal string `## check-overrides.js raw report`
    - File contains the literal string `1.38.5` (upstream version under test)
    - File contains both override paths: `grep -c 'overrides/hooks/gsd-check-update.js' 40.5-DRIFT-LOG.md` returns >= 1; same for gsd-statusline.js
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-overrides.js; echo "EXIT=$?"; test -f .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md &amp;&amp; grep -q '1.38.5' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md</automated>
  </verify>
  <done>
    40.5-DRIFT-LOG.md exists with header + raw check-overrides.js output + skeleton sections for per-override review and disposition summary. Both override paths referenced. The actual semantic review and disposition decisions happen in Tasks 02-02 + 02-03.
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="02-02" type="checkpoint:human-verify" gate="blocking">
  <name>Task 02-02: Semantic review of gsd-check-update.js drift + Snapshot refresh (interactive)</name>
  <what-built>
    40.5-DRIFT-LOG.md skeleton from Task 02-01 with check-overrides.js raw report. Both REASON.md files at their pre-bump (1.34.2) Snapshot state.
  </what-built>
  <how-to-verify>
    Interactive review per D-07 (human review every drift). Steps:

    1. **Compute current upstream SHA:**
       ```bash
       sha256sum node_modules/get-shit-done-cc/hooks/gsd-check-update.js
       # Record this hex as <NEW-SHA>
       ```
    2. **Confirm pre-bump SHA in REASON.md** (cross-reference with RESEARCH.md §4 — should be `61fcb20c5ce5e07346b99c0bc8e6bf9de58d1d088cf3e34d4de53898aee07c2b`):
       ```bash
       grep -E 'SHA-256:' overrides/hooks/gsd-check-update.js.REASON.md
       ```
    3. **Generate semantic diff** between v1.34.2 and v1.38.5 of upstream's hooks/gsd-check-update.js. Two possible methods:
       - (preferred) Use a bare clone of `gsd-build/get-shit-done`: `git -C <bare-clone-dir> diff v1.34.2..v1.38.5 -- hooks/gsd-check-update.js | tee /tmp/check-update-upstream-diff.txt`
       - (fallback) Download both tarballs: `npm pack get-shit-done-cc@1.34.2 && npm pack get-shit-done-cc@1.38.5`, extract, diff: `diff -u <old>/package/hooks/gsd-check-update.js <new>/package/hooks/gsd-check-update.js`
    4. **Read the diff WITH THE USER.** Per RESEARCH.md §4 the changes are: PR #2224 (complete stale-hooks fix), PR #2141 (add bash hooks to MANAGED_HOOKS staleness check). Per D-05 classify each upstream change:
       - Whitespace/comment-only near a citation? → `unchanged` in D-05's sense (refresh SHA, no behavior conflict)
       - Behavior change to a function/concern the override re-implements? → DRIFTED with semantic conflict (document in DRIFT-LOG.md what part of the upstream improvement is sacrificed by keeping the override)
       - Behavior change to a concern the override does NOT touch? → DRIFTED but harmless (refresh SHA snapshot, document the upstream improvement noted but not folded)
    5. **CONSTRAINT — DO NOT FOLD UPSTREAM IMPROVEMENTS** per RESEARCH.md §8 Q3: even if the user asks "should we just take upstream's improvements?", the answer is "Phase 43 UPGRADE-07 owns that work, not 40.5". Per `honor-directives.md` apply this discipline. Wave 2 only refreshes the SHA snapshot.
    6. **Update REASON.md:** Per D-22, edit `overrides/hooks/gsd-check-update.js.REASON.md` to update the `Snapshot:` block:
       - `Version: 1.38.5` (was `1.34.2`)
       - `SHA-256: <NEW-SHA>` (computed in step 1)
       - DO NOT add new sections; DO NOT remove existing sections (Title, Why, Upstream snapshot, What's different, Review trigger).
       - The "What's different" section is the BEHAVIOR DELTA between override and upstream — if the user determines that some upstream improvement now duplicates fork-side override logic OR conflicts with override intent, NOTE that observation in this section without removing existing content. Note for Phase 43 UPGRADE-07.
    7. **Append per-override review section to 40.5-DRIFT-LOG.md** under `## Per-override semantic review`:
       ```markdown
       ### overrides/hooks/gsd-check-update.js
       - Pre-bump SHA (1.34.2): <recorded>
       - Post-bump SHA (1.38.5): <NEW-SHA>
       - Upstream commits affecting file (1.34.2..1.38.5): #2224, #2141
       - Semantic delta: <prose summary of what the user + Claude reviewed>
       - Disposition per D-07: refresh (or document conflict)
       - Snapshot refreshed in REASON.md: yes
       - Note for Phase 43 UPGRADE-07: <observations about which upstream improvements should fold during UPGRADE-07>
       ```

    **Pass:** REASON.md Snapshot updated to 1.38.5 + new SHA; DRIFT-LOG.md per-override review section populated; user has confirmed disposition.
    **Halt:** if upstream change is a hard conflict that suggests removing the override entirely (per D-08 Wave 3 territory), surface to user — Wave 3 will handle removal decisions, not Wave 2.
  </how-to-verify>
  <resume-signal>
    Type "drift refreshed" once REASON.md Snapshot field is updated and DRIFT-LOG.md review section is populated. If conflict requires removal: type "remove candidate" and provide rationale; planner will revise Wave 3 plan to handle removal.
  </resume-signal>
  <files></files>
  <action>Interactive checkpoint task — execution detail is in the &lt;how-to-verify&gt; block above. The action is to walk the user through the verification steps interactively per D-04 and capture results.</action>
  <verify>
    <automated>manual</automated>
  </verify>
  <done>Resume-signal phrase received from user (see &lt;resume-signal&gt; above).</done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="02-03" type="checkpoint:human-verify" gate="blocking">
  <name>Task 02-03: Semantic review of gsd-statusline.js drift + Snapshot refresh (interactive)</name>
  <what-built>
    Task 02-02 complete: gsd-check-update.js Snapshot refreshed; DRIFT-LOG.md has its review section.
  </what-built>
  <how-to-verify>
    Identical procedure to Task 02-02 but for `overrides/hooks/gsd-statusline.js`. Per RESEARCH.md §4: +134 lines net, 3 commits — #2219 (scale context meter to CLAUDE_CODE_AUTO_COMPACT_WINDOW), #2162 (contextual warning for dev installs with stale hooks), #1990 (surface GSD milestone/phase/status when no active todo).

    Pre-bump recorded SHA per RESEARCH.md §4: `2e155c727e12190dfbddd13a7f18f1e9d85a4f62a66cec2d4ab2b489d17e31c5`

    1. Compute new SHA: `sha256sum node_modules/get-shit-done-cc/hooks/gsd-statusline.js`
    2. Generate semantic diff: `git -C <bare-clone> diff v1.34.2..v1.38.5 -- hooks/gsd-statusline.js`
    3. Read with user. Classify per D-05.
    4. **CONSTRAINT — DO NOT FOLD** UPGRADE-07-eligible improvements (this file is atomically coupled to gsd-check-update.js per UPGRADE-07).
    5. Update `overrides/hooks/gsd-statusline.js.REASON.md` Snapshot block per D-22.
    6. Append per-override review section to 40.5-DRIFT-LOG.md under `## Per-override semantic review`:
       ```markdown
       ### overrides/hooks/gsd-statusline.js
       - Pre-bump SHA (1.34.2): 2e155c727e12190dfbddd13a7f18f1e9d85a4f62a66cec2d4ab2b489d17e31c5
       - Post-bump SHA (1.38.5): <NEW-SHA>
       - Upstream commits affecting file (1.34.2..1.38.5): #2219, #2162, #1990
       - Semantic delta: <user-confirmed prose>
       - Disposition per D-07: refresh
       - Snapshot refreshed in REASON.md: yes
       - Note for Phase 43 UPGRADE-07: <observations>
       ```

    **Pass:** REASON.md Snapshot updated; DRIFT-LOG.md review section populated.
  </how-to-verify>
  <resume-signal>
    Type "drift refreshed" once REASON.md Snapshot is updated and DRIFT-LOG.md review section is populated.
  </resume-signal>
  <files></files>
  <action>Interactive checkpoint task — execution detail is in the &lt;how-to-verify&gt; block above. The action is to walk the user through the verification steps interactively per D-04 and capture results.</action>
  <verify>
    <automated>manual</automated>
  </verify>
  <done>Resume-signal phrase received from user (see &lt;resume-signal&gt; above).</done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="02-04" type="auto">
  <name>Task 02-04: Re-run check-overrides.js to confirm zero drift, populate disposition summary, atomic commit</name>
  <files>
    .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md
    overrides/hooks/gsd-check-update.js.REASON.md
    overrides/hooks/gsd-statusline.js.REASON.md
  </files>
  <read_first>
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md (post-Task-02-03 state)
    - overrides/hooks/gsd-check-update.js.REASON.md (post-Task-02-02 state — Snapshot refreshed)
    - overrides/hooks/gsd-statusline.js.REASON.md (post-Task-02-03 state — Snapshot refreshed)
  </read_first>
  <action>
    1. Re-run `node scripts/check-overrides.js`. Expected exit code: 0 (no drift — both Snapshots now match current upstream SHAs).

       If exit code is non-zero, that means the SHA refresh in Tasks 02-02/02-03 was incorrect (mismatch between recorded SHA and computed file hash). HALT and surface — re-compute and fix REASON.md Snapshot fields, then re-run.

    2. Append the `## Disposition summary` table to 40.5-DRIFT-LOG.md with both rows fully populated:

       ```markdown
       ## Disposition summary

       | Override | Pre-SHA (1.34.2) | Post-SHA (1.38.5) | Disposition | Snapshot refreshed? |
       |---|---|---|---|---|
       | overrides/hooks/gsd-check-update.js | 61fcb20c5ce5e07346b99c0bc8e6bf9de58d1d088cf3e34d4de53898aee07c2b | <NEW-SHA-from-02-02> | refresh (per D-07; UPGRADE-07 fold deferred to Phase 43) | yes |
       | overrides/hooks/gsd-statusline.js | 2e155c727e12190dfbddd13a7f18f1e9d85a4f62a66cec2d4ab2b489d17e31c5 | <NEW-SHA-from-02-03> | refresh (per D-07; UPGRADE-07 fold deferred to Phase 43) | yes |

       ## check-overrides.js post-refresh exit
       - Exit code: 0 (no drift)
       - Confirmed against: `node scripts/check-overrides.js`
       ```

    3. Atomic commit per D-14 (refreshed snapshots committed inline with verification work):

       ```bash
       git add overrides/hooks/gsd-check-update.js.REASON.md \
               overrides/hooks/gsd-statusline.js.REASON.md \
               .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md
       git commit -m "$(cat <<'EOF'
docs(40.5): refresh override Snapshot fields for v1.38.5 and record drift log

Phase 40.5 Wave 2 — override drift detection (UPGRADE-10, criterion #2).

Both existing overrides drifted on the 1.34.2 -> 1.38.5 bump (expected
per RESEARCH.md §4):
- overrides/hooks/gsd-check-update.js: -75 lines net (#2224 stale-hooks
  fix; #2141 bash-hooks MANAGED_HOOKS extension)
- overrides/hooks/gsd-statusline.js: +134 lines net (#2219 context-meter
  scaling; #2162 dev-install warnings; #1990 GSD milestone surfacing)

Per D-07 every drift was reviewed semantically with human-in-the-loop.
Disposition for both: refresh Snapshot (no semantic conflict with override
intent at this bump).

Per RESEARCH.md §8 Q3 + Wave 2 risk: upstream improvements eligible for
UPGRADE-07 fold (isNewer, detectConfigDir, stale-hook detection, shared
cache) are explicitly NOT folded in Wave 2 — that work is Phase 43.

Evidence: 40.5-DRIFT-LOG.md (cited by Wave 5 VERIFICATION.md per D-12).

Refs: UPGRADE-10
EOF
)"
       ```

    No `--no-verify`. No AI attribution.
  </action>
  <acceptance_criteria>
    - `node scripts/check-overrides.js; echo $?` returns exit code 0
    - `grep -E 'Version:\s*1\.38\.5' overrides/hooks/gsd-check-update.js.REASON.md` matches
    - `grep -E 'Version:\s*1\.38\.5' overrides/hooks/gsd-statusline.js.REASON.md` matches
    - `grep -cE 'SHA-256:\s*[0-9a-f]{64}' overrides/hooks/gsd-check-update.js.REASON.md` returns >= 1
    - `grep -cE 'SHA-256:\s*[0-9a-f]{64}' overrides/hooks/gsd-statusline.js.REASON.md` returns >= 1
    - `grep -c 'Disposition summary' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md` returns 1
    - `grep -c '^## ' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md` returns >= 4 (raw report, per-override review, disposition summary, post-refresh exit)
    - `git log -1 --format='%s' | grep -E '^docs\(40\.5\): refresh override Snapshot fields'` matches
    - `git log -1 --stat` shows exactly 3 files: both REASON.md + 40.5-DRIFT-LOG.md
    - `git log -1 --format='%B' | grep -ci 'co-authored-by\|generated with claude'` returns 0
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-overrides.js &amp;&amp; grep -E 'Version:\s*1\.38\.5' overrides/hooks/gsd-check-update.js.REASON.md &amp;&amp; grep -E 'Version:\s*1\.38\.5' overrides/hooks/gsd-statusline.js.REASON.md</automated>
  </verify>
  <done>
    Override drift fully resolved: check-overrides.js exits 0; both REASON.md Snapshot fields refreshed to 1.38.5 + new SHA; 40.5-DRIFT-LOG.md has full disposition summary. Atomic commit landed. Wave 3 can proceed against the refreshed override state.
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

</tasks>

<verification>
Wave 2 success criteria (bound to Phase 40.5 success criterion #2):

1. `node scripts/check-overrides.js` exits 0 (no drift remaining)
2. Both REASON.md files have `Version: 1.38.5` and a 64-char SHA-256 hex
3. 40.5-DRIFT-LOG.md exists with raw report + per-override review (2 sections) + disposition summary
4. Wave 2 commit is atomic (3 files, single commit, conventional message, no AI attribution)
5. NO upstream improvements were folded into the override (defer to Phase 43 UPGRADE-07 — verifiable by `git diff HEAD~ HEAD -- overrides/hooks/*.js` returning empty for the .js files; only .REASON.md files changed)

Verification commands:

```bash
node scripts/check-overrides.js; echo "EXIT=$?"
grep -E 'Version:\s*1\.38\.5' overrides/hooks/*.REASON.md
git log -1 --stat | grep -c 'REASON.md\|DRIFT-LOG.md'  # expect 3
git diff HEAD~ HEAD -- 'overrides/hooks/*.js'  # expect empty (no .js fold)
```
</verification>

<success_criteria>
- [ ] check-overrides.js exits 0 post-refresh
- [ ] Both REASON.md Snapshot blocks reflect Version: 1.38.5 + new SHA-256
- [ ] 40.5-DRIFT-LOG.md exists with all 4 expected sections (raw report, per-override review, disposition summary, post-refresh exit)
- [ ] Per-override review sections cite the specific upstream PR numbers (#2224, #2141, #2219, #2162, #1990)
- [ ] Wave 2 commit landed atomically (3 files)
- [ ] No fold of upstream improvements into override .js files (Phase 43 UPGRADE-07 boundary held)
- [ ] Wave 3 (40.5-03-PLAN.md) can proceed
</success_criteria>

<output>
After completion, create `.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-02-SUMMARY.md` with:

- Wave 2 commit SHA
- check-overrides.js post-refresh exit code (must be 0)
- Both Pre/Post SHA pairs from the disposition table
- Confirmation that no .js files were modified (Phase 43 boundary held)
- Any UPGRADE-07-eligible observations recorded in REASON.md "What's different" sections (forward signal for Phase 43 planner)
</output>