---
phase: 40.5-upstream-bump-reverify-phase-41-decisions
plan: 01.5
subsystem: prevention-stack
tags: [ci, hooks, gitleaks, branch-protection, husky, ssot]
provides:
  - Wave 1.5a — bunfig dist/** exclusion + hooks SSOT manifest + path-validation meta-test
  - Wave 1.5b — per-line meta-test:skip escape marker for runtime-created paths
  - Wave 1.5c — eslint dist/ exclusion
  - Wave 1.5d — categorical primitives (.gitattributes CRLF policy + debt-baseline.json + amendment template)
  - Wave 1.6-01 — Workflow Lint CI job (actionlint via Docker)
  - Wave 1.6-02 — GitHub Actions upgraded to Node 24-supporting versions (checkout@v6, cache@v5)
  - Wave 1.6-03 — "no test files in dist/" CI guard
  - Wave 1.6-04 — Ratchet gate consumer with per-OS thresholds from debt-baseline.json
  - Wave 1.7-A — Secret-scanning gate stack (gitleaks CI + .gitleaks.toml + .gitignore patterns + native scanning + push protection + dependabot) + public-flip
  - Wave 1.7-01 — Reproducible branch protection setup script + applied live
  - Wave 1.8-01 — Pre-push hook for local-CI parity (husky)
affects: [wave-2, wave-3, wave-4, wave-5, ci-pipeline, repo-visibility]
tech-stack:
  added: [husky, actionlint, gitleaks]
  patterns: [SSOT-manifest, ratchet-gate, defense-in-depth-secrets, structural-prevention]
key-files:
  created:
    - hooks/index.js
    - scripts/check-debt-ratchet.cjs
    - scripts/setup-branch-protection.sh
    - .gitleaks.toml
    - .husky/pre-push
    - tests/test-config-hygiene.test.js
    - tests/test-path-validation.test.js
    - docs/decisions/002-MANIFEST-DRIVEN-KNOWLEDGE.md
    - .planning/audits/debt-baseline.json
  modified:
    - bunfig.toml
    - eslint.config.js
    - .gitattributes
    - .gitignore
    - .github/workflows/ci.yml
    - tests/hooks.test.js
    - scripts/build.js
    - scripts/check-parity.js
    - package.json
    - bun.lock
key-decisions:
  - A-06 — Binding-gate redefinition (4 chronic failures = pass per A-10 outcome)
  - A-07 — Wave 1.5 absorption (1.5a/b/c/d + 1.6 + 1.7 + 1.8 absorbed into Phase 40.5)
  - A-08 — Out-of-scope boundaries (layers E/F/G are cross-project; not in 40.5)
  - A-09 — Sequencing (1.5 → 1.6 → 1.7 → 1.8 end-to-end before Wave 2)
  - A-10 — Post-push gate outcome + scope split
  - A-11 — Wave 1.7-A secret-scanning gate stack + public-flip prep
duration: ~6-8h cumulative (spread across multiple sessions 2026-04-30 .. 2026-05-06)
completed: 2026-05-06
---

# Phase 40.5 Waves 1.5/1.6/1.7/1.8 SUMMARY (Retroactive, written 2026-05-14)

**The four prevention-stack sub-waves absorbed into Phase 40.5 per A-07. Each delivered STRUCTURAL prevention (meta-test, CI gate, hook installer) so the bug class cannot recur. Final state: PR #3 CI matches A-06 redefined gate (4 chronic failures, no net new regressions); branch protection live on `main` with 5 required status checks; repo public; secrets defense-in-depth active.**

## Performance
- **Duration:** ~6-8h cumulative across multiple sessions (2026-04-30 .. 2026-05-06)
- **Sub-waves:** 4 (1.5, 1.6, 1.7-A+1.7, 1.8)
- **Tasks:** ~14 atomic commits
- **Files created:** ~10
- **Files modified:** ~10

## Sub-wave outcomes

### Wave 1.5a — Test-config hygiene + SSOT manifest

Categorical fix for ~538 of the 586 PR #3 failures: upstream packaging ships `.test.ts` files; compose copies them to `dist/sdk/`; bun-test was discovering them and failing on Bun's TypeScript syntax differences.

- `42e2ebb` — `fix(test-config): exclude dist/** from bun-test discovery` — bunfig.toml `[test].exclude` includes `**/dist/**`.
- `2b1d32b` — `test(test-config): add hygiene meta-test + filter upstream tests in compose` — meta-test asserts the bunfig invariant + zero test files in dist/ at test time.
- `39f1af2` — `fix(hooks): correct stale overlay/hooks paths after v3.0.0 architecture` — hooks tests pointed at deleted v2.x paths.
- `a36c95c` — `test(test-config): add path-validation meta-test for hardcoded test paths` — catches future path-drift.
- `63896dc` — `feat(hooks): introduce SSOT manifest at hooks/index.js` — single source of truth for hook list; consumers (scripts, tests) read from it.
- `9c77ef5` — `test(hooks): add manifest invariant tests for hooks/index.js`.
- `cbd549c` — `refactor(scripts/build): consume hooks manifest`.
- `cb5e914` — `refactor(scripts/check-parity): consume hooks manifest`.
- `63e1d44` — `refactor(tests/hooks): consume hooks manifest`.
- `c15cd65` — `docs(adr): record decision 002 - manifest-driven knowledge` (ADR-002).

### Wave 1.5b — Runtime-symlink escape marker

- `aa50059` — `feat(meta-test): add per-line meta-test:skip escape marker for runtime-created paths` — A-10 follow-up; allows a specific test path that's runtime-symlinked to opt out of hygiene checks without disabling the entire check.

### Wave 1.5c — Lint dist/ exclusion

- `e14da5d` — `fix(eslint): exclude dist/ from lint discovery + extend hygiene meta-test` — eslint was tripping on minified dist/ output.

### Wave 1.5d — Categorical primitives

- `724aea3` — `feat(prevention): categorical primitives for Wave 1.5d (CRLF policy + debt baseline + amendment template)` — `.gitattributes` CRLF policy locked in, `.planning/audits/debt-baseline.json` initialized for ratchet gate, amendment template added to phase doc workflows.

### Wave 1.6 — CI workflow hardening

- `8942512` — `feat(ci): add Workflow Lint job (actionlint via Docker)` — Task 1.6-01.
- `1fd7781` — `feat(ci): upgrade actions to Node 24-supporting versions` — checkout@v6 + cache@v5. Task 1.6-02.
- `d4b4238` — `feat(ci): guard against test files in dist/` — Task 1.6-03 (CI-side companion to the bunfig fix).
- `3567636` — `feat(ci): wire ratchet gate consumer with per-OS threshold support` — Task 1.6-04. **Boundary & Override Check** job became BLOCKING — carries the ratchet gate from `scripts/check-debt-ratchet.cjs` consuming `.planning/audits/debt-baseline.json`. Per-OS thresholds because Windows and POSIX file-system behavior differ for certain boundary checks.

### Wave 1.7-A — Secret-scanning gate stack + public-flip prep

Pre-Wave-1.7 audit per A-11 surfaced zero active forcing functions for secret leakage. Wave 1.7-A delivered three project-local gates plus the public-flip:

- `ac785d0` — `feat(security): Wave 1.7-A secret-scanning gate stack + public-flip prep` — bundle of:
  - G1: `.gitignore` proactive patterns (`*.env`, `*.pem`, `*.key`, `id_rsa`, `id_ed25519`, `credentials*`, `secrets.json`, `.planning/upstream-outreach-drafts.md`).
  - G3: `secret-scan` CI job installing gitleaks v8.21.2 binary directly (avoids `gitleaks-action` GITLEAKS_LICENSE org-repo requirement).
  - G5: GitHub-native secret scanning + push protection enabled post-flip via `gh api`.
  - Dependabot configured.
  - Repo flipped public.
- `d218926` — `fix(security): allowlist upstream test fixtures in gitleaks config` — `.gitleaks.toml` path-allowlist for upstream test fixtures (path allowlist over regex-fingerprint per D-26 / A-11 decision).

G2 (husky pre-commit) and G4 (prepublishOnly) deferred to aidev's inbox per A-11 scope-cut.

### Wave 1.7-01 — Branch protection live

- `c41409c` — `feat(security): Wave 1.7 — reproducible branch protection setup script` — `scripts/setup-branch-protection.sh` applies + verifies. Required status checks = A-06+A-10 binding-gate set + Secret Scan (5 jobs). `required_approving_review_count=1` with `enforce_admins=false` (Option A per decision: solo maintainer can self-merge via admin power; contributor PRs see review-required signal — OSS-ready posture).

### Wave 1.8-01 — Pre-push hook for local-CI parity

- `53312eb` — `feat(hooks): pre-push hook for local-CI parity — Wave 1.8 Task 1.8-01` — husky v9.1.7 added as devDep with `prepare` script. `.husky/pre-push` runs the CI sequence locally: `bun install --frozen-lockfile --ignore-scripts && bun run compose && bun run lint && bun test`. Bypass: `git push --no-verify` (git-native).

## Closure-loop demonstrated end-to-end

Mid-Wave-1.7-A, the audit surfaced that aidev's documented `quality-gate` Secrets gate was vapourware (declared "PASSED" while every documented gate silently skipped — A-11 finding). A cross-project issue was filed via `~/Projects/ai-dev-environment/docs/inbox/2026-05-05-get-stuff-done-quality-gate-skill-missing-secrets-gate.md`. Within 24h aidev shipped the 4-stage fix (release-time gate + pre-commit hook + global SSOT + project pre-commit) AND counter-notified this project's inbox per schema v1.2 `closure_notify_to`. Counter-notification was reviewed + Kept (defense-in-depth) + archived 2026-05-14 in this session (commit `4ff6945` on main). **First production use of cross-project-issues schema v1.2 closure-notify cascade.**

## CI Status at Wave 1.8 Completion

Per A-06 redefined gate: PR #3 CI on Linux + macOS reports the same 4 chronic failures as main's last red run (2026-04-06). Net regression count vs main = 0. **Gate PASSED on commit `c41409c`** (CI run 25403475329). Wave 1.8 (`53312eb`) is the latest commit; CI re-verification at push time pending.

## Why this SUMMARY is retroactive

Wave-by-wave SUMMARY files were not authored during execution because the four sub-waves flowed continuously into each other per A-09 sequencing ("end-to-end as ONE cohesive integration") and the absence of inter-wave breakpoints meant no natural moment to pause and write. Each wave's exit was recorded in the next wave's commit context and in CONTEXT.md amendments (A-07 through A-11). Backfilled 2026-05-14 as part of the Path A planning-rigor cleanup that surfaced A-12 (retarget amendment) and missing SUMMARYs.

## Next Phase Readiness

Wave 2 ready to proceed per A-09 sequencing. The remaining waves (2, 3, 4, 5) operate against the post-Wave-1.8 state: v1.39.1 upstream pin, full prevention stack active, branch protection enforcing required checks, repo public.
