---
phase: 40.6
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - package.json
  - bun.lock
  - scripts/lib/upstream-source.js
  - scripts/compose.js
  - scripts/check-overrides.js
  - scripts/check-boundary.js
  - scripts/preview-update.js
  - overlay/branding.json
  - overrides/hooks/gsd-check-update.js.REASON.md
  - tests/upstream-source.test.js
  - tests/compose.test.js
  - tests/check-overrides.test.js
  - tests/check-boundary.test.js
  - tests/preview-update.test.js
  - tests/preview-update-coverage.test.js
  - .planning/phases/40.6-upstream-authority-migration/40.6-03-SUMMARY.md
autonomous: false
requirements: ["UPGRADE-11"]
estimated_minutes: 240
gap_closure: false
must_haves:
  truths:
    - "Active upstream dependency is @opengsd/gsd-core@1.5.0"
    - "Active tooling uses a centralized upstream identity helper/manifest"
    - "Helper is explicitly maintainer/build-time only unless a package-included generated artifact is created and tested"
    - "No active script hardcodes get-shit-done-cc as the authority"
    - "Tests cover the helper and at least one migrated caller"
    - "preview-update npm calls use argv-based process execution, not shell command strings"
    - "dist regeneration decision is explicit"
    - "Historical references are not mass-edited"
---

<objective>
Migrate active code from legacy upstream package assumptions to Open GSD authority,
using TDD and a centralized upstream identity abstraction.
</objective>

<context>
@.planning/phases/40.6-upstream-authority-migration/40.6-CONTEXT.md
@.planning/phases/40.6-upstream-authority-migration/40.6-RESEARCH.md
@.planning/phases/40.6-upstream-authority-migration/40.6-VALIDATION.md
@.planning/phases/40.6-upstream-authority-migration/40.6-MIGRATION-SPIKE.md
@.planning/upstream-authority.json
</context>

<tasks>

<task id="03-01" type="auto">
  <name>Add failing tests for upstream identity abstraction</name>
  <files>tests; scripts/lib/upstream-source.js</files>
  <action>
    Add tests before implementation. Minimum coverage:
    - helper exposes active package name/version/repository.
    - helper resolves node_modules package dir for scoped package names.
    - helper resolves source root from manifest/spike data.
    - helper rejects `latest`, `next`, canary, and dev tags if a future caller tries to use them.
    - helper rejects path traversal or absolute paths in manifest-controlled `sourceRoot`.
    - helper reports clear errors for missing package dir, missing source root, and missing expected bin entries.

    Run the targeted test and confirm RED.
  </action>
</task>

<task id="03-02" type="auto">
  <name>Implement upstream identity helper</name>
  <files>scripts/lib/upstream-source.js</files>
  <action>
    Implement a small helper that centralizes:
    - active package name.
    - active version.
    - package directory resolution.
    - source root resolution.
    - bin map access.
    - legacy metadata if needed for migration reports.

    Read `.planning/upstream-authority.json` for maintainer/build tooling only. The
    helper must clearly fail or be unreachable in published package runtime paths unless
    Plan 03 creates and tests a package-included generated authority artifact.
  </action>
</task>

<task id="03-03" type="auto">
  <name>Migrate package dependency and lockfile</name>
  <files>package.json; bun.lock</files>
  <action>
    Replace active devDependency:
    - remove `get-shit-done-cc`.
    - add `@opengsd/gsd-core` pinned to `1.5.0`.

    Run `bun install` or the repo's established lockfile update command. Do not use a
    semver range. Do not use `latest`.
  </action>
</task>

<task id="03-04" type="auto">
  <name>Migrate active scripts to helper</name>
  <files>scripts/compose.js; scripts/check-overrides.js; scripts/check-boundary.js; scripts/preview-update.js</files>
  <action>
    Update active scripts to use the helper/manifest instead of scattered hardcodes:
    - compose default upstream/source dir.
    - override check upstream source path.
    - boundary check upstream file set.
    - preview-update current/latest query and instructions.

    `preview-update.js` must query `@opengsd/gsd-core`, not `get-shit-done-cc`, and
    still present exact-pin review guidance. Use argument-array process execution
    (`execFileSync`/`spawnSync` style) for npm queries; do not build a shell command
    string with a scoped package name.
  </action>
</task>

<task id="03-05" type="auto">
  <name>Migrate branding and override reason references</name>
  <files>overlay/branding.json; overrides/hooks/gsd-check-update.js.REASON.md</files>
  <action>
    Update active branding rules and REASON.md references to reflect Open GSD authority.
    Preserve the invariant from earlier overlay work: do not replace bare internal
    `get-shit-done` path identifiers unless the package-layout spike proves a new safe
    rule is needed.
  </action>
</task>

<task id="03-05b" type="auto">
  <name>Decide and execute dist regeneration boundary</name>
  <files>dist; .planning/phases/40.6-upstream-authority-migration/40.6-03-SUMMARY.md</files>
  <action>
    Decide whether this wave regenerates `dist/**`.

    Preferred: run `bun run compose` after source migration and include any required
    tracked `dist/**` changes in the implementation wave, because the package publishes
    `dist`.

    If dist regeneration is deferred, record the reason and make Plan 04 treat dist
    regeneration as a blocking verification step before Phase 40.6 can complete.
  </action>
</task>

<task id="03-06" type="auto">
  <name>Run focused tests and record summary</name>
  <files>.planning/phases/40.6-upstream-authority-migration/40.6-03-SUMMARY.md</files>
  <action>
    Run focused tests for changed scripts first:
    - `bun test tests/upstream-source.test.js`
    - `bun test tests/compose.test.js`
    - `bun test tests/check-overrides.test.js`
    - `bun test tests/check-boundary.test.js`
    - `bun test tests/preview-update.test.js tests/preview-update-coverage.test.js`

    Then run the fastest broader test subset that exercises compose/override/update
    paths. Record:
    - RED test evidence.
    - GREEN test evidence.
    - any failures deferred to Plan 04.
  </action>
</task>

</tasks>

<success>
Plan 03 succeeds when active code no longer depends on legacy upstream identity and
focused tests prove the new helper path.
</success>
