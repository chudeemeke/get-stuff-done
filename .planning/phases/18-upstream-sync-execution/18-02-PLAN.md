---
phase: 18-upstream-sync-execution
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - .planning/sync/sync-manifest.json
  - .planning/phases/18-upstream-sync-execution/approach-comparison.md
  - get-stuff-done/bin/gsd-tools.js
  - "(all files modified by Batch 7-8 cherry-picks)"
autonomous: true

must_haves:
  truths:
    - "All 11 Batch 7 commits (v1.19.0..v1.19.1) are cherry-picked with conflicts resolved"
    - "All 17 Batch 8 commits (v1.19.1..v1.19.2) are cherry-picked with conflicts resolved"
    - "The .cjs rename is applied: get-stuff-done/bin/gsd-tools.js is renamed to get-stuff-done/bin/gsd-tools.cjs"
    - "All 48 reference sites in agents/, commands/, get-stuff-done/workflows/ are updated from gsd-tools.js to gsd-tools.cjs"
    - "Fork branding is correct after both batches"
    - "Full test suite passes after each batch"
  artifacts:
    - path: "get-stuff-done/bin/gsd-tools.cjs"
      provides: "Renamed gsd-tools with .cjs extension"
      min_lines: 1900
    - path: ".planning/sync/sync-manifest.json"
      provides: "Updated sync state with Batches 7-8"
      contains: "v1.19.2"
  key_links:
    - from: "agents/*.md"
      to: "get-stuff-done/bin/gsd-tools.cjs"
      via: "gsd-tools.cjs references (not .js)"
      pattern: "gsd-tools\\.cjs"
    - from: "commands/gsd/*.md"
      to: "get-stuff-done/bin/gsd-tools.cjs"
      via: "gsd-tools.cjs references"
      pattern: "gsd-tools\\.cjs"
---

<objective>
Cherry-pick Batches 7 and 8 (v1.19.0..v1.19.2, 28 commits total). Batch 8 contains the critical gsd-tools.js to .cjs rename, requiring a comprehensive reference update across 48 sites.

Purpose: Batch 7 is cleanup after the v1.19.0 reverts (low risk). Batch 8 introduces the .cjs extension rename which is a structural change affecting every agent, command, and workflow file that references gsd-tools. Getting the reference update right is essential -- any missed reference will cause "Cannot find module" errors.

Output: 28 commits applied, gsd-tools.cjs rename complete, all 48 references updated, tests passing
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-upstream-sync-execution/18-RESEARCH.md
@.planning/phases/18-upstream-sync-execution/18-CONTEXT.md
@.planning/sync/branding-map.json
@.planning/phases/18-upstream-sync-execution/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick Batch 7 (v1.19.0..v1.19.1, 11 commits)</name>
  <files>(all files modified by cherry-picks), .planning/sync/sync-manifest.json, .planning/phases/18-upstream-sync-execution/approach-comparison.md</files>
  <action>
Cherry-pick all 11 commits from v1.19.0..v1.19.1 using the same `--no-commit` review workflow established in Plan 01.

**Batch 7 overview (v1.19.0..v1.19.1):**
- Post-revert cleanup and documentation
- Phase archiving milestone
- Wave diagram docs
- User Guide

Risk: MEDIUM -- mostly documentation and cleanup. Conflicts expected to be Mechanical (path branding) only.

**Workflow per commit:**
1. `git cherry-pick --no-commit <hash>`
2. If conflicts: classify per 4-step triage, resolve per priority rules
3. Check for protected file deletions: `git diff --staged --diff-filter=D`
4. If protected file staged for deletion: `git restore --staged <file>`
5. Commit: `git commit -m "cherry-pick(sync): <original message> (<short-hash>)"`

**After all 11 commits:**
1. Branding pass:
   ```bash
   grep -r "get-shit-done\|glittercowboy\|get-shit-done-cc" agents/ commands/ get-stuff-done/ hooks/ bin/ --include="*.js" --include="*.md" --include="*.json" | grep -v "node_modules\|\.planning\|\.upstream\|upstream-sync\|upstream\.md\|PENDING"
   ```
2. Fix any upstream branding, commit: `fix(sync): apply branding pass for Batch 7`
3. Run `bun test` -- all tests must pass before proceeding to Batch 8
4. Update sync manifest with 11 applied entries
5. Update approach comparison if any divergences found
6. Commit: `docs(18): update sync state after Batch 7`
  </action>
  <verify>
- `bun test` passes with 563+ tests, 0 failures
- No upstream branding in tracked files
- Sync manifest has 11 new entries in applied array
  </verify>
  <done>All 11 Batch 7 commits applied, branding clean, tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Cherry-pick Batch 8 (v1.19.1..v1.19.2, 17 commits) and complete .cjs rename</name>
  <files>get-stuff-done/bin/gsd-tools.js (renamed to .cjs), agents/*.md, commands/gsd/*.md, get-stuff-done/workflows/*.md, .planning/sync/sync-manifest.json</files>
  <action>
Cherry-pick all 17 commits from v1.19.1..v1.19.2. This batch contains commit `24b933e` which renames gsd-tools.js to gsd-tools.cjs.

**Batch 8 overview (v1.19.1..v1.19.2):**
- Phase archiving
- tmpfile for JSON output (important for Windows)
- Model overrides
- Phase padding normalization
- **CRITICAL: gsd-tools.js renamed to gsd-tools.cjs (commit 24b933e)**

**Special handling for the .cjs rename:**

The upstream commit renames `get-shit-done/bin/gsd-tools.js` to `get-shit-done/bin/gsd-tools.cjs`. The fork has `get-stuff-done/bin/gsd-tools.js`. The cherry-pick should trigger a rename of the fork's file. If git does not auto-detect the rename:

1. Manually rename: `git mv get-stuff-done/bin/gsd-tools.js get-stuff-done/bin/gsd-tools.cjs`
2. Verify the file content is preserved (fork's validation import must still be present)

**After the rename commit, update ALL reference sites:**

Search for remaining `.js` references:
```bash
grep -rn "gsd-tools\.js" agents/ commands/ get-stuff-done/workflows/ get-stuff-done/references/ get-stuff-done/templates/ --include="*.md" --include="*.js" --include="*.json"
```

Replace `gsd-tools.js` with `gsd-tools.cjs` in every hit. The branding-map.json `post_module_split_only` section documents this pattern but it should be applied NOW (at the rename point, not waiting for Batch 12).

Expected reference sites (48 total across):
- `agents/gsd-*.md` -- all agent files reference the gsd-tools path
- `commands/gsd/*.md` -- all command files
- `get-stuff-done/workflows/*.md` -- all workflow files
- `get-stuff-done/references/*.md` -- reference docs
- `get-stuff-done/templates/*.md` -- template files

After updating all references, commit: `fix(sync): update 48 gsd-tools.js references to .cjs`

**Standard batch workflow for remaining commits:**
1. Cherry-pick each commit with `--no-commit`
2. Resolve conflicts per triage model
3. Protect fork files from deletion
4. Commit with structured message

**After all 17 commits + rename:**
1. Branding pass (scan for upstream branding)
2. Reference consistency check:
   ```bash
   # Should return 0 hits (no stale .js references)
   grep -rn "gsd-tools\.js" agents/ commands/ get-stuff-done/ --include="*.md" | grep -v "\.planning\|\.upstream\|PENDING\|CHANGELOG\|approach-comparison"
   ```
3. Run `bun test` -- all tests must pass
4. Update sync manifest with 17 applied entries
5. Update approach comparison if divergences found
6. Commit: `docs(18): update sync state after Batch 8`
  </action>
  <verify>
- `ls get-stuff-done/bin/gsd-tools.cjs` succeeds (file exists with .cjs extension)
- `ls get-stuff-done/bin/gsd-tools.js 2>/dev/null` fails (old .js file gone)
- `grep -rn "gsd-tools\.js" agents/ commands/ get-stuff-done/ --include="*.md" | grep -v "\.planning\|\.upstream\|PENDING\|CHANGELOG\|approach-comparison" | wc -l` returns 0
- `grep "validateGitSHA\|validateBranchName" get-stuff-done/bin/gsd-tools.cjs` confirms fork validation import preserved
- `bun test` passes with 563+ tests, 0 failures
- Sync manifest has 17 new entries
  </verify>
  <done>All 17 Batch 8 commits applied, gsd-tools.js successfully renamed to gsd-tools.cjs, all 48 reference sites updated, fork validation import preserved, tests passing</done>
</task>

</tasks>

<verification>
- Batches 7-8 fully applied (28 commits)
- gsd-tools.cjs rename complete with no stale .js references
- Fork's validation import preserved in gsd-tools.cjs
- No upstream branding leaked through
- Test suite passes
- Sync manifest up to date
</verification>

<success_criteria>
- sync/v1.20.5 branch has 28 more cherry-pick commits
- get-stuff-done/bin/gsd-tools.cjs exists, get-stuff-done/bin/gsd-tools.js does not
- Zero stale gsd-tools.js references in agents/commands/workflows
- Fork validation import present in gsd-tools.cjs
- bun test: 563+ pass, 0 fail
- sync-manifest.json: 28 more applied entries (total ~58 with Batch 6)
</success_criteria>

<output>
After completion, create `.planning/phases/18-upstream-sync-execution/18-02-SUMMARY.md`
</output>
