---
phase: 41
plan: 06
type: execute
wave: 3
depends_on: ["01", "02", "03", "05"]
status: pending
requirements: ["REL-02", "REL-03"]
files_modified:
  - .github/workflows/ci.yml
  - scripts/flake-triage.js
  - tests/flake-triage.test.js
  - tests/check-boundary.test.js
  - tests/check-overrides.test.js
  - tests/compose.test.js
  - tests/preview-update-coverage.test.js
  - tests/validate-configs.test.js
  - tests/installer-safety.test.js
  - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-06-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "All remaining test subprocess call sites are migrated or explicitly justified"
    - "Windows test failures create or update deduplicated flake issues with D-10 labels"
    - "Any active REL-03 skip is locally visible through an in-test skip wrapper reason"
    - "Active REL-03 skips are listed in the Windows job summary"
    - "The interim flake collector scope cap is visible in CI output"
  artifacts:
    - path: "scripts/flake-triage.js"
      provides: "JUnit flake parser and REL-03 skip scanner"
      exports: ["main"]
    - path: ".github/workflows/ci.yml"
      provides: "Windows failure flake collector and REL-03 job-summary hook"
      contains: "flake-triage"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/flake-triage.js"
      via: "Windows failure issue creation"
      pattern: "flake-triage"
---

<objective>
Finish REL-02 subprocess hardening across the remaining test surface and make
Windows flake telemetry observable in CI.

Purpose: Plan 05 handles the highest-volume subprocess files. This plan closes
the remaining subprocess inventory and creates the D-10 interim flake collector
without mixing in the 10x validation closure workflow.
</objective>

<context>
@.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md
@.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-RESEARCH.md
@.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-VALIDATION.md
@.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-05-SUMMARY.md
@tests/helpers/subprocess-with-timeout.js
@.github/workflows/ci.yml
</context>

<tasks>

<task id="06-01" type="auto">
  <name>Migrate remaining subprocess test files</name>
  <files>tests/check-boundary.test.js; tests/check-overrides.test.js; tests/compose.test.js; tests/preview-update-coverage.test.js; tests/validate-configs.test.js; tests/installer-safety.test.js</files>
  <read_first>
    - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-05-SUMMARY.md
    - tests/check-boundary.test.js
    - tests/check-overrides.test.js
    - tests/compose.test.js
    - tests/preview-update-coverage.test.js
    - tests/validate-configs.test.js
    - tests/installer-safety.test.js
  </read_first>
  <action>
    Complete D-09 for remaining `tests/**` subprocess call sites in this plan's
    files. Use the Plan 05 helper. Preserve test semantics and assertion output.

    If a raw subprocess call remains anywhere under `tests/**`, it must meet one
    of these conditions:
    - it is in `tests/helpers/subprocess-with-timeout.js`;
    - it is a mock helper that does not spawn a real process;
    - it has a line-level comment explaining why the shared helper cannot express
      the behavior.

    Add those exceptions to `41-06-SUMMARY.md`; do not hide them.
  </action>
  <acceptance_criteria>
    - `bun test tests/check-boundary.test.js tests/check-overrides.test.js tests/compose.test.js tests/preview-update-coverage.test.js tests/validate-configs.test.js tests/installer-safety.test.js` exits 0.
    - `rg -n "execSync|spawnSync|exec\\(|spawn\\(" tests -g "*.js" -g "*.cjs"` shows no unclassified real subprocess call outside helper/mock files.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/check-boundary.test.js tests/check-overrides.test.js tests/compose.test.js tests/preview-update-coverage.test.js tests/validate-configs.test.js tests/installer-safety.test.js</automated>
    <automated>node -e "const fs=require('fs'); const path=require('path'); const re=/\\b(execSync|spawnSync|exec\\(|spawn\\()/; const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>{const p=path.join(d,e.name); return e.isDirectory()?walk(p):p}); const hits=walk('tests').filter(f=>/\\.(js|cjs)$/.test(f) && !f.includes('subprocess-with-timeout') && re.test(fs.readFileSync(f,'utf8'))); if(hits.length){ console.log(hits.join('\\n')); process.exit(1); }"</automated>
  </verify>
  <done>
    Done when the focused remaining-test set passes and every real raw
    subprocess call outside helper/mock files is either eliminated or listed with
    file, line, and reason in `41-06-SUMMARY.md`.
  </done>
</task>

<task id="06-02" type="auto" tdd="true">
  <name>Create flake triage parser and REL-03 scanner</name>
  <files>scripts/flake-triage.js; tests/flake-triage.test.js</files>
  <read_first>
    - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Write `tests/flake-triage.test.js` first. Cover:
    - parsing a JUnit XML failure into dedup key
      `{test-file-path}::{test-name}::{platform}`.
    - labels include `flake-report`, `flake-platform-windows`,
      `flake-file-<basename>`, and `rel-03-candidate` after at least three hits
      in fourteen days.
    - scanning source files for `REL-03-` skip reasons and producing a Markdown
      job-summary table.
    - validating every active REL-03 skip uses one of these shapes:
      `test.skip.if(isWindows, { reason: 'REL-03-N: <issue-url>, deadline YYYY-MM-DD' })`
      for bun:test, or a documented `node:test` equivalent with the same
      `REL-03-N`, issue URL, and deadline fields.

    Implement `scripts/flake-triage.js` with:
    - `--junit <file> --platform <platform> --run-url <url> --commit <sha> --output <json>`.
    - `--scan-rel03 --output-summary <file>`.
    - `--validate-rel03-wrappers` that exits non-zero if an active REL-03 skip is
      missing an issue URL, a `deadline YYYY-MM-DD`, or a supported in-test skip
      wrapper shape.
    - no GitHub API calls; workflow uses the output JSON for create/comment.
  </action>
  <acceptance_criteria>
    - `bun test tests/flake-triage.test.js` exits 0.
    - `node scripts/flake-triage.js --scan-rel03 --output-summary .planning/audits/rel03-summary-test.md` exits 0 and writes a Markdown section beginning `### Active REL-03 skips`.
    - `node scripts/flake-triage.js --validate-rel03-wrappers` exits 0 when there are no active REL-03 skips or when all active skips include issue URL and deadline.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/flake-triage.test.js</automated>
    <automated>node scripts/flake-triage.js --scan-rel03 --output-summary .planning/audits/rel03-summary-test.md</automated>
    <automated>node scripts/flake-triage.js --validate-rel03-wrappers</automated>
  </verify>
  <done>
    Done when flake triage tests pass and REL-03 scanning writes a Markdown
    summary beginning `### Active REL-03 skips`, and wrapper validation proves
    active REL-03 skips have local in-test visibility with issue URL and deadline.
  </done>
</task>

<task id="06-03" type="auto">
  <name>Wire Windows flake issue collector and REL-03 visibility</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - scripts/flake-triage.js
  </read_first>
  <action>
    Modify the existing `test` job in `.github/workflows/ci.yml`:
    - run tests with coverage and JUnit output, for example
      `bun test --coverage --reporter=junit --reporter-outfile test-results.xml`.
    - upload `test-results.xml` on failure.
    - on Windows failure only, run `scripts/flake-triage.js` to create
      `flake-events.json`.
    - use `actions/github-script` to find an open issue with the dedup title; if
      found, add a comment containing date, run URL, commit SHA, bun version, and
      node version; if not found, create one.
    - add labels from D-10 exactly.
    - always append the REL-03 summary to `$GITHUB_STEP_SUMMARY` for Windows runs.
    - print the scope cap line:
      `Interim flake collector scope cap: more than 5 flaky files/week requires Tailscale collector planning.`

    Do not add a new external flake service in Phase 41.
  </action>
  <acceptance_criteria>
    - `rg -n "test-results.xml|flake-events.json|flake-report|flake-platform-windows|rel-03-candidate|Active REL-03 skips|more than 5 flaky files/week" .github/workflows/ci.yml` finds all required strings.
    - `bash scripts/lint-workflows.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/lint-workflows.sh</automated>
    <automated>rg -n "test-results.xml|flake-events.json|flake-report|flake-platform-windows|rel-03-candidate|Active REL-03 skips|more than 5 flaky files/week" .github/workflows/ci.yml</automated>
  </verify>
  <done>
    Done when workflow lint exits 0 and `ci.yml` contains the JUnit output,
    flake event, D-10 labels, REL-03 summary, and scope-cap strings.
  </done>
</task>

<task id="06-04" type="auto">
  <name>Record plan summary</name>
  <files>.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-VALIDATION.md
  </read_first>
  <action>
    Create `41-06-SUMMARY.md` with:
    - remaining raw subprocess call audit result.
    - focused test results.
    - workflow lint result.
    - whether any REL-03 skip exists in source.
    - confirmation that D-11's 2-working-day unresolved-flake timebox is owned
      by Plan 07 before Phase 41 closure.
  </action>
  <acceptance_criteria>
    - `41-06-SUMMARY.md` exists and contains `Remaining raw subprocess calls`.
    - `41-06-SUMMARY.md` contains `D-11`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-06-SUMMARY.md'; const s=fs.readFileSync(p,'utf8'); if(!s.includes('Remaining raw subprocess calls') || !s.includes('D-11')) process.exit(1)"</automated>
  </verify>
  <done>
    Done when `41-06-SUMMARY.md` exists and contains both `Remaining raw
    subprocess calls` and `D-11`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test results to GitHub Issues | CI parses failure output and writes issues/comments using GITHUB_TOKEN. |
| Test source to REL-03 summary | Source code skip reasons become CI summary evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-06-01 | Denial of Service | flake issue creation | mitigate | Dedup by test-file/test-name/platform and comment on existing issues. |
| T-41-06-02 | Tampering | flake parser output | mitigate | Parser is locally tested and workflow creates issues only from structured JSON fields. |
| T-41-06-03 | Repudiation | residual subprocess calls | mitigate | Summary lists every classified exception with file, line, and reason. |
</threat_model>

<verification>
Run:
- `bun test tests/flake-triage.test.js`
- `bun test tests/check-boundary.test.js tests/check-overrides.test.js tests/compose.test.js tests/preview-update-coverage.test.js tests/validate-configs.test.js tests/installer-safety.test.js`
- `rg -n "execSync|spawnSync|exec\\(|spawn\\(" tests -g "*.js" -g "*.cjs"`
- `bash scripts/lint-workflows.sh`

Expected: focused tests exit 0. Raw subprocess grep either has no real call
sites outside helper/mock files or every remaining real call is listed with
reason in `41-06-SUMMARY.md`.
</verification>

<success_criteria>
Plan 06 succeeds when remaining subprocess calls are migrated or justified and
the Windows flake collector is deduplicated, tested, and visible in CI output.
</success_criteria>

<output>
After completion, create `.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-06-SUMMARY.md`.
</output>
