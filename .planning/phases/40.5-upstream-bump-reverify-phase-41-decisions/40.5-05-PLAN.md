---
phase: 40.5
plan: 05
type: execute
wave: 5
depends_on: ["04"]
files_modified:
  - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md
  - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-README.md
  - .planning/STATE.md
autonomous: false
requirements: ["UPGRADE-10"]
estimated_minutes: 90
gap_closure: false
must_haves:
  truths:
    - "40.5-VERIFICATION.md exists in standard gsd-verifier shape per D-10 (no custom shape)"
    - "VERIFICATION.md cites all 4 supplementary evidence files inline: 40.5-DECISION-VERIFY.md (criterion #4), 40.5-DRIFT-LOG.md (criterion #2), 40.5-COLLISIONS.md (criterion #3), 40.5-README.md (phase-dir index)"
    - "40.5-README.md exists per D-15 — short index of phase-dir contents and why each exists"
    - "999.1 disposition recorded: stays backlog with existing 3 promotion paths (Path A/B/C) intact per D-18(b) since #2487 doesn't close gap"
    - "999.3 disposition recorded: per D-20 + Wave 4 Target 3 — file upstream issue + PR if bug present, OR close NOT-A-GAP with fix commit SHA"
    - "STATE.md updated: Phase 40.5 status = COMPLETE (or partial-complete with explicit note); next-session command = `/gsd:plan-phase 41` (with D-21 gate check explicit)"
    - "All 4 D-21 plan-phase-41 trigger gates verified satisfied (or specifically called out as pending)"
    - "Final bun test + bun run compose green sweep recorded as evidence in VERIFICATION.md"
  artifacts:
    - path: ".planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md"
      provides: "Standard gsd-verifier output for Phase 40.5 (D-10)"
      min_lines: 50
    - path: ".planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-README.md"
      provides: "Phase-dir index per D-15"
      min_lines: 15
    - path: ".planning/STATE.md"
      provides: "Updated current position + Phase 40.5 COMPLETE marker; next-session command set"
      contains: "Phase 40.5"
  key_links:
    - from: "40.5-VERIFICATION.md"
      to: "40.5-DECISION-VERIFY.md, 40.5-DRIFT-LOG.md, 40.5-COLLISIONS.md, 40.5-README.md"
      via: "inline citation of all 4 supplementary files (D-11..D-15)"
      pattern: "40\\.5-(DECISION-VERIFY|DRIFT-LOG|COLLISIONS|README)\\.md"
    - from: "STATE.md"
      to: "/gsd:plan-phase 41 readiness"
      via: "D-21 trigger-gate check note"
      pattern: "plan-phase 41"
---

<objective>
Wave 5 of Phase 40.5: Synthesis. Produce 40.5-VERIFICATION.md (standard gsd-verifier
shape per D-10) citing the 4 supplementary evidence files. Produce 40.5-README.md
(phase-dir index per D-15). Record 999.1 and 999.3 dispositions. Update STATE.md to
reflect Phase 40.5 close. Verify all 4 D-21 gates for /gsd:plan-phase 41 readiness. Run
final green sweep (`bun test` + `bun run compose`).

Per D-10: VERIFICATION.md uses standard gsd-verifier output — no custom shape. The
gsd-verifier framework agent owns the template (per A-03 reconciliation: don't drift
into custom sections). Supplementary files (DECISION-VERIFY, DRIFT-LOG, COLLISIONS,
README) sit alongside, never in place of it.

Per D-15: README is a short index, self-documenting, zero framework dependency. Combines
with VERIFICATION.md forward-citations for two-layer orphan-prevention.

Per D-18(b) (confirmed by Wave 4 Target 2): 999.1 stays backlog. Record disposition.
Per D-20 + Wave 4 Target 3: 999.3 disposition is either (a) file upstream issue + PR
with `\b...\b` word boundaries if bug confirmed present at v1.38.5, or (b) close
NOT-A-GAP with fix commit SHA if upstream already fixed. The Wave 4 Task 04-06 output
determines which.

Per D-21 plan-phase-41 trigger gates (all 4 required):
1. Phase 40.5 VERIFICATION.md PASS
2. Zero MATERIAL drift in the 22-decision matrix (A-07+ amendments OK)
3. 999.2 Option C disposition recorded (resolved OR explicitly pending with reason)
4. Phase 41 RESEARCH.md + VALIDATION.md sanity check (still on disk, reflect post-bump reality)

Note: 999.1 disposition is automatic per D-18 (no separate gate); 999.3 disposition
is automatic per D-20 (no separate gate).

Satisfies success criterion #5 of Phase 40.5: findings recorded in VERIFICATION.md;
commits/files diff counted; override deltas listed; CONTEXT.md amendment summary;
upstream filing candidates noted.

Output: 40.5-VERIFICATION.md, 40.5-README.md, updated STATE.md, possible upstream PR
filing (999.3, autonomous: false). Phase 40.5 closed; user knows next command is
`/gsd:plan-phase 41`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CONTEXT.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-RESEARCH.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VALIDATION.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-04-SUMMARY.md
@.planning/STATE.md
</context>

<tasks>

<task id="05-01" type="auto">
  <name>Task 05-01: Generate 40.5-VERIFICATION.md (standard gsd-verifier shape)</name>
  <files>.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CONTEXT.md (D-10 — standard gsd-verifier shape; A-03 reconciliation — don't invent custom sections)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md (criterion #4 evidence to cite)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DRIFT-LOG.md (criterion #2 evidence to cite)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-COLLISIONS.md (criterion #3 evidence to cite)
  </read_first>
  <action>
    Two paths per D-10 — pick whichever the executor's environment supports:

    **Path A (preferred): Run the framework gsd-verifier agent.**

    If the framework's gsd-verifier agent is available in this Claude Code session, invoke it against Phase 40.5 to produce 40.5-VERIFICATION.md in its standard shape. The agent reads the phase context and produces the canonical output. After it runs:
    - Confirm 40.5-VERIFICATION.md exists at `.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md`.
    - Confirm it cites all 4 supplementary files (40.5-DECISION-VERIFY.md, 40.5-DRIFT-LOG.md, 40.5-COLLISIONS.md, 40.5-README.md) — if it doesn't, edit to add inline citations under the appropriate criterion sections.
    - Confirm it records all 5 success criteria with PASS/FAIL/PARTIAL status.

    **Path B (fallback if gsd-verifier agent is not invokable in this session):**

    Write 40.5-VERIFICATION.md by hand following the standard shape that other phase VERIFICATION.md files in the project use. Read one of `.planning/milestones/v1.1.0-phases/*/[!4]*-VERIFICATION.md` (any prior phase verification file) to establish the canonical shape — replicate that shape verbatim. Per A-03: do NOT invent new top-level sections; the framework owns the template.

    Required content regardless of path:

    1. Phase identifier + UPGRADE-10 traceability.
    2. Per-criterion PASS/FAIL/PARTIAL matrix:
       - Criterion #1 (pin bump 1.34.2 -> 1.38.5; bun install; Linux+macOS test pass) — PASS evidence: Wave 1 commit SHA + CI run URL.
       - Criterion #2 (override drift detection) — PASS evidence: cite 40.5-DRIFT-LOG.md inline (per D-12).
       - Criterion #3 (compose collision audit + #1859) — PASS evidence: cite 40.5-COLLISIONS.md inline (per D-13).
       - Criterion #4 (22-decision matrix re-verify) — PASS evidence: cite 40.5-DECISION-VERIFY.md inline (per D-11); summarize Targets 1, 2, 3 conclusions.
       - Criterion #5 (synthesis) — populated by this task itself.
    3. Diff counts from RESEARCH.md §1: 1.34.2..1.38.5 = 394 commits / 674 files.
    4. Override deltas summary (cite DRIFT-LOG.md): both overrides Snapshot-refreshed.
    5. CONTEXT.md amendment summary: cite count of A-07+ entries from Wave 4 (zero or specific list with brief description of each).
    6. Upstream filing candidates: 999.3 disposition (file PR or close NOT-A-GAP per Task 04-06 result).
    7. Backlog disposition summary: 999.1 stays backlog (per D-18(b) + Wave 4 Target 2); 999.3 disposition (per D-20 + Wave 4 Target 3); 999.2 noted as separately tracked Option C in-backlog (per D-19).
  </action>
  <acceptance_criteria>
    - test -f .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md returns success
    - File contains the literal string `UPGRADE-10`
    - File contains the literal string `1.38.5`
    - File cites all 4 supplementary files: `grep -cE '40\.5-(DECISION-VERIFY|DRIFT-LOG|COLLISIONS|README)\.md' 40.5-VERIFICATION.md` returns >= 4
    - File records all 5 criteria with status: `grep -cE '[Cc]riterion #[1-5]' 40.5-VERIFICATION.md` returns >= 5
    - File mentions 999.1 disposition (stays backlog): `grep -c '999\.1' 40.5-VERIFICATION.md` returns >= 1
    - File mentions 999.3 disposition: `grep -c '999\.3' 40.5-VERIFICATION.md` returns >= 1
    - File mentions diff count: `grep -cE '394 commits|674 files' 40.5-VERIFICATION.md` returns >= 1 (citing RESEARCH.md §1 numbers — also acceptable to cite ROADMAP.md baseline 312/566 with explanation per RESEARCH.md §1 Reconciliation)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md &amp;&amp; test "$(grep -cE '40\.5-(DECISION-VERIFY|DRIFT-LOG|COLLISIONS|README)\.md' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md)" -ge "4"</automated>
  </verify>
  <done>
    40.5-VERIFICATION.md exists in standard gsd-verifier shape with all 5 criterion statuses, all 4 supplementary file citations, diff counts, override deltas, amendment summary, and 999.x disposition summary.
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="05-02" type="auto">
  <name>Task 05-02: Generate 40.5-README.md (phase-dir index per D-15)</name>
  <files>.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-README.md</files>
  <read_first>
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/ directory listing (`ls -la` to enumerate all files in the phase dir)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CONTEXT.md (D-15 — short index, zero framework dependency)
  </read_first>
  <action>
    Create `.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-README.md`. Short index of phase-dir contents and why each exists. Per D-15: self-documenting; zero framework dependency.

    Required content (one line per file present in the phase dir):

    ```
    # Phase 40.5 — Upstream Bump & Phase 41 Decision Re-verification

    Pre-Phase-41 currency bump (UPGRADE-10). Bumps upstream pin 1.34.2 -> 1.38.5
    (per CONTEXT.md A-04), refreshes override SHA snapshots, audits compose collisions,
    re-verifies all 22 Phase 41 CONTEXT.md decisions against fresh upstream tree, and
    disposes of in-scope backlog items 999.1 and 999.3.

    ## Files in this directory

    | File | Why it exists |
    |---|---|
    | 40.5-CONTEXT.md | Locked context (23 decisions D-01..D-23 + 4 amendments A-01..A-04). The methodology document. |
    | 40.5-RESEARCH.md | Phase research output (~50KB). Upstream changelog data, PR status, citation map, drift expectations, refined wave time estimates, NEEDS-USER-DECISION on bump target (resolved by A-04). |
    | 40.5-VALIDATION.md | Nyquist Dimension 8. Sampling rate, per-criterion verification map. |
    | 40.5-01-PLAN.md ... 40.5-05-PLAN.md | The 5 wave plans, sequential per D-16. |
    | 40.5-01-SUMMARY.md ... 40.5-05-SUMMARY.md | Per-wave summaries created at wave close. |
    | 40.5-DRIFT-LOG.md | Wave 2 evidence: per-file SHA mismatch + semantic compare + refresh-or-amend decision (criterion #2 evidence per D-12). |
    | 40.5-COLLISIONS.md | Wave 3 evidence: per-overlay collision + #1859 audit + keep/move/remove decision (criterion #3 evidence per D-13). |
    | 40.5-DECISION-VERIFY.md | Wave 4 evidence: 22-row decision matrix (criterion #4 evidence per D-11). |
    | 40.5-VERIFICATION.md | Wave 5 standard gsd-verifier output (D-10) citing all of the above. |
    | 40.5-README.md | This file (D-15). |

    ## Closing

    Phase 40.5 closes when 40.5-VERIFICATION.md is PASS and STATE.md updated.
    Next: `/gsd:plan-phase 41` (after D-21 trigger-gate check).
    ```
  </action>
  <acceptance_criteria>
    - test -f .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-README.md returns success
    - wc -l 40.5-README.md returns >= 15
    - File contains a row for every file in the phase dir (cross-check with `ls .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/`)
    - File mentions next command: `grep -c 'plan-phase 41' 40.5-README.md` returns >= 1
    - File mentions UPGRADE-10
    - File mentions 1.38.5
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-README.md &amp;&amp; grep -q 'plan-phase 41' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-README.md &amp;&amp; grep -q '1.38.5' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-README.md</automated>
  </verify>
  <done>
    40.5-README.md exists. Phase-dir index complete. Forward-citation to /gsd:plan-phase 41 next-command set.
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="05-03" type="checkpoint:human-verify" gate="blocking">
  <name>Task 05-03: 999.3 disposition — file upstream PR (if bug present) OR close NOT-A-GAP (if fixed)</name>
  <what-built>
    Wave 4 Task 04-06 Target 3 verified the bug status at v1.38.5 line ~457 of `workflows/plan-phase.md`. Wave 5 Task 05-01 + 05-02 produced VERIFICATION.md and README.md.
  </what-built>
  <how-to-verify>
    Two paths depending on Wave 4 Task 04-06 Target 3 outcome.

    **Path A — bug still present at v1.38.5 (per RESEARCH.md §6 prediction):**

    1. Confirm Wave 4 Task 04-06 conclusion: bug confirmed at line N of `node_modules/get-shit-done-cc/get-shit-done/workflows/plan-phase.md` where N is whatever line number Task 04-06 found.
    2. Per `feedback_verify_upstream_scope.md` discipline (3 prior "upstream bug" labels turned out to be fork-specific): we have already verified — the regex IS in upstream's tarball file, no fork involvement.
    3. **DRAFT the upstream issue body** (markdown) and PR body. Issue body must include:
       - Symptom: regex `grep -iE "UI|interface|frontend|component|layout|page|screen|view|form|dashboard|widget"` matches inside common English words (`view` inside `re-review`, `interview`, `overview`; `form` inside `formatting`, `format`, `transform`, `inform`, `perform`, `formal`; `page` inside `paginate`).
       - Repro: a phase description containing one of the false-positive substrings triggers the UI gate incorrectly.
       - Root cause: missing word-boundary anchors in the regex alternation.
       - Proposed fix: `grep -iE "\b(UI|interface|frontend|component|layout|page|screen|view|form|dashboard|widget)\b"`. Note caveat: `\bUI\b` matches `UI`, `UI/UX`, `UI:`. If the original regex was intentionally generous, the user should review scope before merging.
       - Risks / verify: confirm in PR review whether the original regex's substring-matching was intentional. If yes, alternative fix would be a more conservative allowlist.
    4. **Show the user the drafted issue + PR bodies** before filing. User reviews and sign-offs.
    5. **File the issue and PR:**
       ```bash
       gh issue create --repo gsd-build/get-shit-done \
         --title "fix(plan-phase): UI-gate regex matches inside common English words (missing \\b boundaries)" \
         --body "$(cat /tmp/issue-body.md)" \
         --label bug
       # Capture issue number, then:
       gh pr create --repo gsd-build/get-shit-done \
         --title "fix(plan-phase): add word-boundary anchors to UI-gate regex" \
         --body "$(cat /tmp/pr-body.md)"
       # Capture PR number.
       ```
       Per global rule `git-commits.md`: no AI attribution in PR body. Per global rule `communication-style.md`: first-person singular voice ("I noticed...", not "We noticed...").
    6. Record issue number + PR number in 40.5-VERIFICATION.md (under criterion #5 / 999.3 disposition section). User sign-off required.

    **Path B — bug already fixed by upstream between v1.38.2 and v1.38.5:**

    1. Confirm Wave 4 Task 04-06 conclusion: regex now contains `\b...\b` word boundaries OR was restructured.
    2. Capture the fix commit SHA: `git -C <bare-clone> log -p --oneline -- get-shit-done/workflows/plan-phase.md | grep -B5 'word.boundar\|\\\\b'` (find the commit that added the boundaries) OR find via PR/commit search on gsd-build/get-shit-done.
    3. Record in 40.5-VERIFICATION.md under 999.3 disposition: NOT-A-GAP closed; cite fix commit SHA + PR (if known).
    4. Update PROJECT.md / ROADMAP.md backlog 999.3 entry to status `closed-not-a-gap` with the SHA.

    **Pass criterion for this task:** 999.3 disposition recorded in 40.5-VERIFICATION.md with either (Path A) issue + PR numbers OR (Path B) fix commit SHA. User has signed off. NO unilateral PR filing without user review of bodies.

    **Constraint per `honor-directives.md`:** if user wants to defer the PR filing entirely (e.g., postpone to next session), record the deferral with explicit owner (user) + concrete trigger (e.g., "by 2026-05-15") in 40.5-VERIFICATION.md per `no-hidden-debt.md`. Do NOT file silently OR forget silently.
  </how-to-verify>
  <resume-signal>
    Type "999.3 disposition recorded" once VERIFICATION.md is updated with the path taken (Path A issue/PR numbers, Path B SHA, or deferred with trigger). Type "halt: <reason>" if the verification surfaces something unexpected.
  </resume-signal>
  <files></files>
  <action>Interactive checkpoint task — execution detail is in the &lt;how-to-verify&gt; block above. The action is to walk the user through the verification steps interactively per D-04 and capture results.</action>
  <verify>
    <automated>manual</automated>
  </verify>
  <done>Resume-signal phrase received from user (see &lt;resume-signal&gt; above).</done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="05-04" type="auto">
  <name>Task 05-04: Final green sweep — bun test + bun run compose</name>
  <files></files>
  <read_first>
    - package.json (confirm test/compose scripts unchanged)
  </read_first>
  <action>
    Run the final smoke sweep before declaring Phase 40.5 done. Both commands must exit 0 (on Linux/macOS/WSL2; if executor is on Windows native, follow Wave 1 Task 01-03 protocol — local Windows result is informational, binding gate is CI on Linux+macOS).

    1. `bun install --frozen-lockfile` — must exit 0 (lockfile committed in Wave 1 should be consistent).
    2. `bun run compose` — must exit 0 (no new collisions; same as Wave 3 Task 03-01 result).
    3. `bun test` — must exit 0 on non-Windows platforms.

    Capture exit codes. Append a "Final green sweep" subsection to 40.5-VERIFICATION.md citing exit codes for each command + the platform the sweep ran on. Do NOT commit yet — the final commit happens in Task 05-05 (atomic with STATE.md update).

    If any command fails, HALT and surface to user — Phase 40.5 is not complete.
  </action>
  <acceptance_criteria>
    - bun install --frozen-lockfile exits 0
    - bun run compose exits 0
    - bun test exits 0 (on Linux/macOS/WSL2; Windows-only flake captured per Task 01-03 protocol)
    - 40.5-VERIFICATION.md has a "Final green sweep" subsection with exit codes captured
  </acceptance_criteria>
  <verify>
    <automated>bun install --frozen-lockfile &amp;&amp; bun run compose &amp;&amp; bun test &amp;&amp; grep -q 'Final green sweep' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md</automated>
  </verify>
  <done>
    All three commands green. VERIFICATION.md updated with final-sweep evidence. Ready for STATE.md update + final commit.
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

<task id="05-05" type="auto">
  <name>Task 05-05: Update STATE.md + D-21 gate check + final atomic commit</name>
  <files>
    .planning/STATE.md
    .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md
    .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-README.md
  </files>
  <read_first>
    - .planning/STATE.md (current state — confirm "Phase 40.5 not started" frontmatter)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md (post-Tasks-05-01..05-04 state — all sections populated, final-sweep recorded)
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-README.md (post-Task-05-02 state)
    - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-RESEARCH.md (D-21 gate #4 — confirm exists)
    - .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/ directory listing (confirm both 41-RESEARCH.md and 41-VALIDATION.md present per D-21 gate #4; if absent, flag in commit message)
  </read_first>
  <action>
    1. **D-21 plan-phase-41 trigger-gate check.** Append a "Plan-Phase-41 Readiness (D-21 Gates)" subsection to 40.5-VERIFICATION.md with checkboxes for all 4 gates:

       ```
       ## Plan-Phase-41 Readiness (D-21 Gates)

       - [x] Gate 1: Phase 40.5 VERIFICATION.md PASS (this file, all 5 criteria PASS)
       - [x] Gate 2: Zero MATERIAL drift in 22-decision matrix (per Wave 4 conclusion; A-07+ amendments OK)
       - [<x or check-with-user>] Gate 3: 999.2 Option C disposition recorded (separately tracked in `.planning/phases/999.2-resolve-config-schema-drift-and-fork-extension-namespace/CONTEXT.md`; per D-19 attempt Option C in-backlog first)
       - [x] Gate 4: Phase 41 RESEARCH.md + VALIDATION.md sanity check (both exist on disk; reflect post-bump reality given Wave 4 found <U/D/A> drift counts)

       All gates satisfied: <yes/no with reason>.

       Next command: `/gsd:plan-phase 41`
       ```

       For Gate 3, run `ls .planning/phases/999.2-resolve-config-schema-drift-and-fork-extension-namespace/CONTEXT.md` — if exists, gate satisfied (the disposition is the existing CONTEXT.md). If not, surface to user (this would be unexpected per current state).

       For Gate 4, run `ls .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-RESEARCH.md .planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-VALIDATION.md`. If both files exist, gate satisfied. (Note: 41-CONTEXT.md was potentially amended in Wave 4 — that's not a separate gate.)

    2. **Update STATE.md.** Edit the frontmatter and the "Current Position" section:

       Frontmatter:
       - `status:` change to `"Phase 40.5 COMPLETE 2026-04-XX. UPGRADE-10 satisfied. Upstream pin at 1.38.5. Phase 41 CONTEXT.md re-verified (status counts: <U> unchanged / <D> drifted / <A> amended; <count> A-07+ amendments). Ready for /gsd:plan-phase 41."`
       - `stopped_at:` updated similarly with the closure narrative
       - `last_updated:` ISO datetime now
       - `last_activity:` "<DATE> -- Phase 40.5 closed; UPGRADE-10 satisfied; pin at 1.38.5; ready for /gsd:plan-phase 41"
       - `progress.completed_phases:` increment by 1 (if was 0, now 1)
       - `progress.total_plans:` add 5 (Wave 1-5)
       - `progress.completed_plans:` add 5

       Body sections:
       - "Current Position": Phase = "Phase 41 (not started; D-21 gates satisfied per Phase 40.5 VERIFICATION.md)". Status: "Ready for /gsd:plan-phase 41."
       - "Upstream state": update pin from "Fork upstream pin at 1.34.2 (Phase 40.5 will bump to 1.38.2)" to "Fork upstream pin at 1.38.5 (bumped 2026-04-XX in Phase 40.5 Wave 1; latest stable per A-04)".
       - Append to "Roadmap Evolution": "Phase 40.5 closed <DATE>; UPGRADE-10 satisfied. Wave 4 found <U/D/A> drift counts; <count> A-07+ amendments to Phase 41 CONTEXT.md."
       - "Session Continuity" → Resume file: `.planning/phases/41-foundation-flip-gate-install-audit-surface-windows-slo/41-CONTEXT.md` (if amended) or unchanged.

    3. **Atomic commit:**

       ```bash
       git add .planning/STATE.md \
               .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md \
               .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-README.md
       git commit -m "$(cat <<'EOF'
docs(40.5): close Phase 40.5 — UPGRADE-10 satisfied; ready for /gsd:plan-phase 41

Phase 40.5 Wave 5 — synthesis (UPGRADE-10, criterion #5).

Pre-Phase-41 upstream currency bump complete. Upstream pin bumped
1.34.2 -> 1.38.5 (Wave 1). Both override SHA snapshots refreshed
inline (Wave 2). Compose-collision audit clean — zero new path-level
collisions; #1859 review.md non-event confirmed (Wave 3). All 22
Phase 41 CONTEXT.md decisions re-verified against fresh upstream;
status counts: <U> unchanged / <D> drifted / <A> amended (Wave 4).

Backlog dispositions:
- 999.1 (plan-checker wave collision): stays backlog. Upstream PR
  #2487 surfaces wave-dependency annotations and shared
  must_haves.truths but does NOT address wave-level file-write
  collision. Existing 3 promotion paths (A/B/C) intact per D-18(b).
- 999.3 (UI-gate regex): <Path A: filed upstream issue #X + PR #Y |
  Path B: closed NOT-A-GAP, fix commit SHA <hash>>.
- 999.2 (config schema drift): tracked separately at
  .planning/phases/999.2-*/CONTEXT.md per D-19 (Option C in-backlog).

D-21 plan-phase-41 trigger gates: all 4 satisfied.
- Gate 1: VERIFICATION.md PASS (this commit)
- Gate 2: Zero MATERIAL drift in 22-decision matrix
- Gate 3: 999.2 disposition recorded
- Gate 4: Phase 41 RESEARCH.md + VALIDATION.md present

Final green sweep: bun install --frozen-lockfile (exit 0) +
bun run compose (exit 0) + bun test (exit 0 on Linux/macOS/WSL2).

Evidence:
- 40.5-VERIFICATION.md (this file, standard gsd-verifier shape per D-10)
- 40.5-DECISION-VERIFY.md (Wave 4, criterion #4)
- 40.5-DRIFT-LOG.md (Wave 2, criterion #2)
- 40.5-COLLISIONS.md (Wave 3, criterion #3)
- 40.5-README.md (phase-dir index per D-15)

Refs: UPGRADE-10
EOF
)"
       ```

       No `--no-verify`. No AI attribution.

    4. Push the commit. The CI run is informational at this point (Wave 1 already established Linux+macOS green; Wave 5 only modified .planning/* docs which are not exercised by CI tests).
  </action>
  <acceptance_criteria>
    - grep -c 'Plan-Phase-41 Readiness' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md returns >= 1
    - grep -cE 'Gate [1-4]' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md returns >= 4
    - .planning/STATE.md frontmatter `status:` field references "Phase 40.5 COMPLETE"
    - .planning/STATE.md body references upstream pin at "1.38.5" (no remaining "1.34.2 (Phase 40.5 will bump to 1.38.2)" stale text)
    - git log -1 --format='%s' | grep -E '^docs\(40\.5\): close Phase 40\.5' matches
    - git log -1 --stat shows exactly 3 files: STATE.md + 40.5-VERIFICATION.md + 40.5-README.md
    - git log -1 --format='%B' | grep -ci 'co-authored-by\|generated with claude' returns 0
    - git status --porcelain returns empty
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Phase 40\.5 COMPLETE' .planning/STATE.md &amp;&amp; grep -q '1\.38\.5' .planning/STATE.md &amp;&amp; ! grep -q 'Phase 40\.5 will bump to 1\.38\.2' .planning/STATE.md &amp;&amp; test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>
    Phase 40.5 closed. STATE.md reflects completion. VERIFICATION.md has D-21 gate check. All 5 success criteria recorded as PASS. User can run /gsd:plan-phase 41 next.
  </done>
  <requirements>["UPGRADE-10"]</requirements>
</task>

</tasks>

<verification>
Wave 5 success criteria (bound to Phase 40.5 success criterion #5):

1. 40.5-VERIFICATION.md exists in standard gsd-verifier shape, cites all 4 supplementary files inline
2. 40.5-README.md exists per D-15
3. 999.1 disposition recorded: stays backlog (per D-18(b) + Wave 4 Target 2)
4. 999.3 disposition recorded: either filed upstream PR (Path A) OR closed NOT-A-GAP with SHA (Path B), with user sign-off
5. STATE.md updated: Phase 40.5 COMPLETE; pin at 1.38.5; next command /gsd:plan-phase 41
6. All 4 D-21 plan-phase-41 trigger gates verified satisfied
7. Final green sweep recorded: bun test + bun run compose + bun install --frozen-lockfile all exit 0
8. Wave 5 commit landed atomically (3 files)

Verification commands:

```bash
test -f .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md
test -f .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-README.md
grep -q 'Phase 40\.5 COMPLETE' .planning/STATE.md
grep -q '1\.38\.5' .planning/STATE.md
grep -cE 'Gate [1-4]' .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-VERIFICATION.md  # expect >= 4
git log --oneline -10  # confirm Wave 1..5 commit chain visible
```
</verification>

<success_criteria>
- [ ] 40.5-VERIFICATION.md exists, standard gsd-verifier shape, cites 4 supplementary files
- [ ] 40.5-README.md exists, phase-dir index complete
- [ ] 999.1 stays backlog (recorded in VERIFICATION.md + commit message)
- [ ] 999.3 disposition resolved (Path A: PR filed with user-signed-off bodies, OR Path B: NOT-A-GAP with SHA, OR explicit deferral with owner+trigger per no-hidden-debt.md)
- [ ] STATE.md updated: pin at 1.38.5, Phase 40.5 COMPLETE, next command /gsd:plan-phase 41
- [ ] D-21 trigger gates 1-4 all satisfied (recorded in VERIFICATION.md)
- [ ] Final green sweep recorded: 3 commands all exit 0
- [ ] Wave 5 commit landed (3 files: STATE.md + VERIFICATION.md + README.md)
- [ ] Phase 40.5 declared COMPLETE; user knows next step
</success_criteria>

<output>
After completion, create `.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-05-SUMMARY.md` with:

- Wave 5 commit SHA
- Final-sweep exit codes (3 commands)
- 999.1 disposition (one line: stays backlog per D-18(b))
- 999.3 disposition (one line: Path A PR # / Path B SHA / deferred to <date> with owner)
- D-21 gates result (4 lines, one per gate)
- Phase 40.5 close timestamp
- Total Phase 40.5 elapsed time (sum of Wave 1..5 SUMMARY estimates / actuals)
- Commit chain hashes (Wave 1..5) for the milestone log
</output>
