---
phase: 19-post-sync-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build.js
  - bin/install.js
  - tests/gsd-tools.test.js
autonomous: true
requirements:
  - SYNC-II-03

must_haves:
  truths:
    - "esbuild produces dist/gsd-tools.cjs (not .js) from the modular source"
    - "copy-mode install overwrites bin/gsd-tools.cjs with the self-contained bundle"
    - "workflows calling node gsd-tools.cjs hit the bundled file in copy-mode installs"
    - "bun test passes with all gsd-tools dist regression guards green"
  artifacts:
    - path: "scripts/build.js"
      provides: "esbuild pipeline producing dist/gsd-tools.cjs"
      contains: "gsd-tools.cjs"
    - path: "bin/install.js"
      provides: "Copy-mode bundle replacement targeting gsd-tools.cjs"
      contains: "gsd-tools.cjs"
    - path: "get-stuff-done/bin/dist/gsd-tools.cjs"
      provides: "Self-contained bundled gsd-tools for copy-mode install"
      min_lines: 100
  key_links:
    - from: "scripts/build.js"
      to: "get-stuff-done/bin/dist/gsd-tools.cjs"
      via: "esbuild.buildSync outfile"
      pattern: "gsd-tools\\.cjs"
    - from: "bin/install.js"
      to: "get-stuff-done/bin/dist/gsd-tools.cjs"
      via: "fs.copyFileSync to installed gsd-tools.cjs"
      pattern: "dist.*gsd-tools\\.cjs"
---

<objective>
Fix the esbuild bundling pipeline to produce dist/gsd-tools.cjs (instead of .js) and update install.js to overwrite the correct target, so copy-mode installs produce working gsd-tools that workflows can call via `node gsd-tools.cjs`.

Purpose: After Phase 18's module split, workflows call `node gsd-tools.cjs` but the bundle is named `gsd-tools.js`. Copy-mode installs get a broken gsd-tools.cjs (the unbundled router with unresolvable lib/ and src/ paths) instead of the self-contained bundle.

Output: Working esbuild pipeline producing correctly-named dist output, install.js targeting the right file, and all regression tests passing.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-post-sync-stabilization/19-RESEARCH.md

@scripts/build.js
@bin/install.js
@tests/gsd-tools.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename dist output to gsd-tools.cjs in build pipeline and install.js</name>
  <files>scripts/build.js, bin/install.js, tests/gsd-tools.test.js</files>
  <action>
Three targeted changes to rename the dist output from gsd-tools.js to gsd-tools.cjs:

1. **scripts/build.js** (line 87): Change the `dest` variable in `buildGsdTools()` from:
   `const dest = path.join(GSD_DIST_DIR, 'gsd-tools.js');`
   to:
   `const dest = path.join(GSD_DIST_DIR, 'gsd-tools.cjs');`

2. **bin/install.js** (lines 1503-1504): Change BOTH bundle path constants from:
   `const bundledToolsSrc = path.join(src, 'get-stuff-done', 'bin', 'dist', 'gsd-tools.js');`
   `const installedTools = path.join(skillDest, 'bin', 'gsd-tools.js');`
   to:
   `const bundledToolsSrc = path.join(src, 'get-stuff-done', 'bin', 'dist', 'gsd-tools.cjs');`
   `const installedTools = path.join(skillDest, 'bin', 'gsd-tools.cjs');`

   IMPORTANT: There are exactly TWO references on consecutive lines. Grep for `gsd-tools.js` in install.js to confirm no others exist. Do NOT change any other occurrences of gsd-tools in install.js (the source router file is correctly named gsd-tools.cjs already).

3. **tests/gsd-tools.test.js** (line 13): Change the DIST_TOOLS_PATH constant from:
   `const DIST_TOOLS_PATH = path.join(PROJECT_ROOT, 'get-stuff-done', 'bin', 'dist', 'gsd-tools.js');`
   to:
   `const DIST_TOOLS_PATH = path.join(PROJECT_ROOT, 'get-stuff-done', 'bin', 'dist', 'gsd-tools.cjs');`

After making the changes, delete the old dist file if it exists:
   `rm -f get-stuff-done/bin/dist/gsd-tools.js`

Then rebuild:
   `node scripts/build.js`

This should produce `get-stuff-done/bin/dist/gsd-tools.cjs` (expected ~189KB self-contained bundle).
  </action>
  <verify>
1. `node scripts/build.js` completes without error and reports "Bundling get-stuff-done/bin/gsd-tools.cjs... done"
2. `ls -la get-stuff-done/bin/dist/gsd-tools.cjs` exists and is > 100KB
3. `ls get-stuff-done/bin/dist/gsd-tools.js` does NOT exist (old file removed)
4. `node get-stuff-done/bin/dist/gsd-tools.cjs generate-slug "test text"` returns "test-text"
5. `grep -n "gsd-tools.js" bin/install.js` returns NO results (all references now .cjs)
6. `grep -n "gsd-tools.js" scripts/build.js` returns NO results
7. `grep -n "gsd-tools.js" tests/gsd-tools.test.js` returns NO results
  </verify>
  <done>
Build pipeline produces dist/gsd-tools.cjs, install.js targets gsd-tools.cjs for both source and destination, test constant points to .cjs, and the bundle executes correctly in isolation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify end-to-end copy-mode simulation</name>
  <files>tests/gsd-tools.test.js</files>
  <action>
Run the full test suite to confirm the rename did not break anything:

1. Run `bun test` and verify all tests pass. The gsd-tools dist regression tests (in the "Dist bundle regression guards" describe block) should all pass with the new .cjs path:
   - "bundled gsd-tools.cjs exists and is a non-trivial bundle"
   - "bundled gsd-tools.cjs contains expected exports"
   - "bundled gsd-tools.cjs can execute generate-slug"
   - "bundled gsd-tools.cjs handles frontmatter validate"

2. Simulate copy-mode install to verify the bundle works in isolation:
   ```bash
   mkdir -p /tmp/gsd-copy-test/bin
   cp get-stuff-done/bin/dist/gsd-tools.cjs /tmp/gsd-copy-test/bin/gsd-tools.cjs
   node /tmp/gsd-copy-test/bin/gsd-tools.cjs generate-slug "copy mode works"
   # Expected output: "copy-mode-works"
   node /tmp/gsd-copy-test/bin/gsd-tools.cjs --help 2>&1 | head -5
   # Should show usage info, NOT a MODULE_NOT_FOUND error
   rm -rf /tmp/gsd-copy-test
   ```

3. If any test failures occur, investigate and fix. The most likely failure mode is a test that hardcodes "gsd-tools.js" somewhere other than DIST_TOOLS_PATH -- grep for it.

Do NOT modify the test logic. Only the path constant should have changed.
  </action>
  <verify>
1. `bun test` reports all tests passing (649+ tests, 0 failures)
2. Copy-mode simulation: `node /tmp/gsd-copy-test/bin/gsd-tools.cjs generate-slug "test"` returns "test"
3. No MODULE_NOT_FOUND errors when running the bundle from an isolated directory
  </verify>
  <done>
Full test suite passes with the renamed dist output. Copy-mode install simulation confirms the bundle works in isolation without access to lib/ or src/ directories. SYNC-II-03 is complete.
  </done>
</task>

</tasks>

<verification>
- `node scripts/build.js` succeeds and produces dist/gsd-tools.cjs
- `bun test` passes all tests (649+)
- Bundle works in isolation: `node /tmp/gsd-copy-test/bin/gsd-tools.cjs generate-slug "test"` returns "test"
- No references to `gsd-tools.js` remain in build.js, install.js, or gsd-tools.test.js
</verification>

<success_criteria>
esbuild bundling pipeline produces dist/gsd-tools.cjs from the modular source. Copy-mode install (install.js) overwrites the correct target file (bin/gsd-tools.cjs) with the self-contained bundle. All 649+ tests pass. The bundle executes correctly when copied to an isolated directory with no access to lib/ or src/.
</success_criteria>

<output>
After completion, create `.planning/phases/19-post-sync-stabilization/19-01-SUMMARY.md`
</output>
