---
phase: 42
plan: 05
type: execute
wave: 3
depends_on: ["03"]
status: pending
requirements: ["DOCS-05", "DOCS-06"]
files_modified:
  - package.json
  - scripts/lint-docs.js
  - .markdownlint-cli2.yaml
  - lychee.toml
  - .github/workflows/ci.yml
  - tests/ci-workflow.test.js
  - tests/docs-gates.test.js
  - .planning/REQUIREMENTS.md
  - .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-05-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "markdownlint-cli2 runs in CI against tracked markdown"
    - "lychee-action runs in CI against tracked markdown"
    - "generated and dependency markdown copies are excluded"
    - "markdownlint-cli2 implementation uses the reconciled current npm version"
---

<objective>
Add markdown and link gates without hiding tracked documentation debt.
</objective>

<context>
@.planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-CONTEXT.md
@INSTALL.md
@.github/workflows/ci.yml
@tests/ci-workflow.test.js
@package.json
</context>

<tasks>

<task id="05-01" type="auto" tdd="true">
  <name>Write docs gate tests first</name>
  <files>tests/docs-gates.test.js; tests/ci-workflow.test.js</files>
  <read_first>
    - tests/ci-workflow.test.js
    - package.json
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Add tests before implementation:
    - `package.json` contains a docs lint script.
    - markdownlint config exists.
    - CI contains a docs gate job.
    - CI uses `lycheeverse/lychee-action@v2`.
    - CI runs markdownlint-cli2.
    - docs target excludes `node_modules`, `dist`, and `overlay/get-shit-done`.
    - tracked markdown is the source of truth; tests assert the docs lint path uses `git ls-files *.md` semantics, not a recursive glob.
  </action>
  <acceptance_criteria>
    - `bun test tests/docs-gates.test.js tests/ci-workflow.test.js` fails before implementation and exits 0 after implementation.
    - Test files contain `lycheeverse/lychee-action@v2` and `markdownlint-cli2`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/docs-gates.test.js tests/ci-workflow.test.js</automated>
  </verify>
</task>

<task id="05-02" type="auto">
  <name>Add markdownlint config and package command</name>
  <files>package.json; bun.lock; scripts/lint-docs.js; .markdownlint-cli2.yaml; .planning/REQUIREMENTS.md</files>
  <read_first>
    - package.json
    - .planning/REQUIREMENTS.md
    - .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-CONTEXT.md
  </read_first>
  <action>
    Add `markdownlint-cli2` dev dependency using current npm version `0.23.0` unless implementation proves incompatibility.

    Add `scripts/lint-docs.js`:
    - collect targets from `git ls-files "*.md"` so tracked markdown is authoritative.
    - filter out tracked generated/dependency copies only for `node_modules/**`, `dist/**`, and `overlay/get-shit-done/**`.
    - invoke `markdownlint-cli2` with the explicit tracked target list.
    - exit non-zero when git target discovery or markdownlint fails.

    Add package script:
    - `"lint:docs": "node scripts/lint-docs.js"`

    Add `.markdownlint-cli2.yaml` with explicit configuration. Prefer fixing tracked docs over disabling rules. Any disabled rule must include a short comment explaining why it is inappropriate for this repo.

    Preserve `.planning/REQUIREMENTS.md` DOCS-06 at the reconciled `markdownlint-cli2@0.23.0` version unless implementation finds and documents a concrete compatibility reason to pin differently.
  </action>
  <acceptance_criteria>
    - `package.json` contains `markdownlint-cli2`.
    - `package.json` contains `"lint:docs"`.
    - `scripts/lint-docs.js` contains `git ls-files` and does not contain a `"**/*.md"` markdown target.
    - `.planning/REQUIREMENTS.md` claims `markdownlint-cli2@0.23.0` unless `42-05-SUMMARY.md` documents a compatibility reason for a different pin.
    - `bun install --frozen-lockfile --ignore-scripts` exits 0.
    - `bun run lint:docs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun install --frozen-lockfile --ignore-scripts</automated>
    <automated>bun run lint:docs</automated>
  </verify>
</task>

<task id="05-03" type="auto">
  <name>Add docs CI gate with lychee</name>
  <files>.github/workflows/ci.yml; lychee.toml; tests/ci-workflow.test.js</files>
  <read_first>
    - .github/workflows/ci.yml
    - tests/ci-workflow.test.js
    - package.json
  </read_first>
  <action>
    Add a `docs-gates` job to `.github/workflows/ci.yml`:
    - checkout.
    - setup bun.
    - `bun install --frozen-lockfile --ignore-scripts`.
    - `bun run lint:docs`.
    - collect link-check targets from `git ls-files "*.md"` with the same generated/dependency exclusions.
    - run `lycheeverse/lychee-action@v2` against that explicit tracked markdown target list only; do not use a recursive `**/*.md` glob.

    Add `lychee.toml` with generated/dependency exclusions:
    - `node_modules/**`
    - `dist/**`
    - `overlay/get-shit-done/**`

    Extend `tests/ci-workflow.test.js` to assert docs job, markdownlint command, lychee action, `git ls-files` target discovery, and exclusions.
  </action>
  <acceptance_criteria>
    - `bun test tests/docs-gates.test.js tests/ci-workflow.test.js` exits 0.
    - `bash scripts/lint-workflows.sh` exits 0.
    - `rg -n "docs-gates|lint:docs|lycheeverse/lychee-action@v2" .github/workflows/ci.yml tests/ci-workflow.test.js` finds matches.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/docs-gates.test.js tests/ci-workflow.test.js</automated>
    <automated>bash scripts/lint-workflows.sh</automated>
  </verify>
</task>

<task id="05-04" type="auto">
  <name>Fix tracked markdown and links</name>
  <files>README.md; QUICKSTART.md; MAINTENANCE.md; INSTALL.md; docs/**/*.md; .planning/**/*.md</files>
  <read_first>
    - .markdownlint-cli2.yaml
    - lychee.toml
    - INSTALL.md
  </read_first>
  <action>
    Run `bun run lint:docs` and fix tracked markdown failures. Then run the lychee CI job or equivalent local command if available and fix broken links. Do not modify generated `dist`, dependency trees, or ignored upstream copies.
  </action>
  <acceptance_criteria>
    - `bun run lint:docs` exits 0.
    - Link checker job exits 0 in CI, or `42-05-SUMMARY.md` records a local limitation plus the successful CI run URL.
    - No broad ignore excludes all `.planning`, all `docs`, or all root markdown.
  </acceptance_criteria>
  <verify>
    <automated>bun run lint:docs</automated>
  </verify>
</task>

<task id="05-05" type="auto">
  <name>Record docs gate summary</name>
  <files>.planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-VALIDATION.md
  </read_first>
  <action>
    Create `42-05-SUMMARY.md` listing markdownlint version, lychee action version, excluded generated/dependency paths, docs commands run, and any requirement text updates.
  </action>
  <acceptance_criteria>
    - `42-05-SUMMARY.md` contains `markdownlint-cli2`, `lychee`, `node_modules`, `dist`, and `overlay/get-shit-done`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "markdownlint-cli2|lychee|node_modules|dist|overlay/get-shit-done" .planning/phases/42-budget-enforcement-process-hardening-cousin-test/42-05-SUMMARY.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
Docs gates can become a false signal if broad ignores hide tracked docs. Keep generated/dependency exclusions narrow and require tracked markdown to pass.
</threat_model>

<verification>
Run:
- `bun install --frozen-lockfile --ignore-scripts`
- `bun run lint:docs`
- `bun test tests/docs-gates.test.js tests/ci-workflow.test.js`
- `bash scripts/lint-workflows.sh`
</verification>
