---
phase: 10-claude-code-adoption
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - C:\Users\Destiny\.claude\get-stuff-done\teams\plan-phase-team.md
  - C:\Users\Destiny\.claude\get-stuff-done\teams\execute-phase-team.md
  - C:\Users\Destiny\.claude\get-stuff-done\teams\upstream-sync-team.md
  - C:\Users\Destiny\.claude\get-stuff-done\teams\verify-work-team.md
  - C:\Users\Destiny\.claude\agents\gsd-oversight-planning.md
  - C:\Users\Destiny\.claude\agents\gsd-oversight-execution.md
  - C:\Users\Destiny\.claude\agents\gsd-oversight-sync.md
  - C:\Users\Destiny\.claude\agents\gsd-oversight-verification.md
autonomous: true

must_haves:
  truths:
    - "Four team template files exist defining team shapes for plan-phase, execute-phase, upstream-sync, and verify-work"
    - "Four oversight agent definitions exist, specialized per workflow"
    - "Each team template has both native TeamCreate path and subagent fallback path"
    - "Oversight agents have flag-and-advise authority only (no blocking, no source code write access)"
    - "Oversight agents use the same memory protocol as core agents (.planning/memory/)"
  artifacts:
    - path: "C:\\Users\\Destiny\\.claude\\get-stuff-done\\teams\\plan-phase-team.md"
      provides: "Team template for plan-phase research parallelization"
      contains: "fallback"
    - path: "C:\\Users\\Destiny\\.claude\\get-stuff-done\\teams\\execute-phase-team.md"
      provides: "Team template for parallel plan execution with oversight"
      contains: "fallback"
    - path: "C:\\Users\\Destiny\\.claude\\get-stuff-done\\teams\\upstream-sync-team.md"
      provides: "Team template for parallel upstream analysis"
      contains: "fallback"
    - path: "C:\\Users\\Destiny\\.claude\\get-stuff-done\\teams\\verify-work-team.md"
      provides: "Team template for parallel verification"
      contains: "fallback"
    - path: "C:\\Users\\Destiny\\.claude\\agents\\gsd-oversight-planning.md"
      provides: "Oversight agent specialized for plan-phase quality"
      contains: "flag-and-advise"
    - path: "C:\\Users\\Destiny\\.claude\\agents\\gsd-oversight-execution.md"
      provides: "Oversight agent specialized for execution quality"
      contains: "flag-and-advise"
    - path: "C:\\Users\\Destiny\\.claude\\agents\\gsd-oversight-sync.md"
      provides: "Oversight agent specialized for upstream sync safety"
      contains: "flag-and-advise"
    - path: "C:\\Users\\Destiny\\.claude\\agents\\gsd-oversight-verification.md"
      provides: "Oversight agent specialized for verification completeness"
      contains: "flag-and-advise"
  key_links:
    - from: "team templates"
      to: "oversight agent definitions"
      via: "teammate references in team template files"
      pattern: "gsd-oversight"
    - from: "oversight agents"
      to: ".planning/memory/"
      via: "memory_protocol section referencing oversight agent memory files"
      pattern: "\\.planning/memory/gsd-oversight"
    - from: "team templates"
      to: "existing GSD agents"
      via: "agent references for teammates"
      pattern: "gsd-(executor|verifier|planner|phase-researcher|codebase-mapper)"
---

<objective>
Create team templates for 4 workflows and 4 specialized oversight agent definitions.

Purpose: Enable parallel agent orchestration in GSD workflows per user decision. Team templates define team shapes that workflows reference. Each workflow gets a specialized oversight agent that monitors quality without blocking execution. All templates include subagent fallback paths since agent teams are experimental.

Output: 4 team template files in `teams/` directory + 4 oversight agent definitions
</objective>

<execution_context>
@C:\Users\Destiny\.claude/get-stuff-done/workflows/execute-plan.md
@C:\Users\Destiny\.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-claude-code-adoption/10-CONTEXT.md
@.planning/phases/10-claude-code-adoption/10-RESEARCH.md
@.planning/phases/10-claude-code-adoption/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 team template files</name>
  <files>
    C:\Users\Destiny\.claude\get-stuff-done\teams\plan-phase-team.md
    C:\Users\Destiny\.claude\get-stuff-done\teams\execute-phase-team.md
    C:\Users\Destiny\.claude\get-stuff-done\teams\upstream-sync-team.md
    C:\Users\Destiny\.claude\get-stuff-done\teams\verify-work-team.md
  </files>
  <action>
Create the `teams/` directory at `C:\Users\Destiny\.claude\get-stuff-done\teams\` and create 4 team template files.

Each template follows this structure:
- YAML frontmatter: `name`, `description`, `workflow` (which workflow uses it), `experimental: true`, `fallback: sequential-subagents`
- **## Team Configuration** section with Lead, Teammates (with agent references and tasks), and Observer (oversight agent)
- **## Task Dependencies** section showing execution order
- **## Soft Cap** note: "This team has {N} members. Soft cap is 8 per user decision."
- **## Fallback (non-team mode)** section with exact subagent spawn sequence for when `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` is not enabled

**1. plan-phase-team.md:**
```yaml
---
name: plan-phase-research-team
description: Parallel research team for plan-phase workflow. Researcher and codebase mapper work simultaneously, feeding findings into planner.
workflow: plan-phase
experimental: true
fallback: sequential-subagents
---
```
- Lead: plan-phase orchestrator (delegate mode, model: inherit)
- Teammate 1: gsd-phase-researcher (domain research, model: inherit)
- Teammate 2: gsd-codebase-mapper (codebase analysis, model: sonnet)
- Observer: gsd-oversight-planning (research quality watcher, model: sonnet)
- Total: 4 members (well under soft cap of 8)
- Task dependencies: researcher and mapper run parallel, cross-reference depends on both, oversight reviews completeness last
- Fallback: Spawn gsd-phase-researcher via Task, spawn gsd-codebase-mapper via Task (parallel), wait for both, synthesize in orchestrator

**2. execute-phase-team.md:**
```yaml
---
name: execute-phase-team
description: Parallel execution team with conflict watcher. Multiple executors can run wave-parallel plans while oversight monitors for drift.
workflow: execute-phase
experimental: true
fallback: sequential-subagents
---
```
- Lead: execute-phase orchestrator (delegate mode, model: inherit)
- Teammates: gsd-executor (1 per parallel plan in current wave, model: per config profile)
- Observer: gsd-oversight-execution (requirement drift watcher, model: sonnet)
- Note: Team size varies by wave -- soft cap of 8 means max ~6 parallel executors + lead + oversight
- Task dependencies: All executors in a wave run parallel, oversight monitors continuously, next wave starts after current completes
- Fallback: Sequential Task tool spawns per wave (current GSD behavior), no oversight

**3. upstream-sync-team.md:**
```yaml
---
name: upstream-sync-team
description: Parallel upstream analysis team. Analyzer, conflict checker, and branding handler work simultaneously on commit analysis.
workflow: upstream-sync
experimental: true
fallback: sequential-subagents
---
```
- Lead: upstream-sync orchestrator (delegate mode, model: inherit)
- Teammate 1: upstream analyzer (analyzes upstream commits for integration, model: sonnet)
- Teammate 2: fork conflict checker (identifies fork-specific conflicts, model: sonnet)
- Teammate 3: branding handler (checks for branding/naming preservation, model: haiku)
- Observer: gsd-oversight-sync (security and fork integrity watcher, model: sonnet)
- Total: 5 members
- Task dependencies: All 3 analyzers run parallel per commit batch, oversight reviews after each batch
- Fallback: Sequential analysis (current sync workflow), each concern addressed in order
- Note: This is a HIGH-STAKES workflow -- oversight flag routing goes through lead per CONTEXT.md decision

**4. verify-work-team.md:**
```yaml
---
name: verify-work-team
description: Parallel verification team. Functional, cross-platform, and security verifiers work simultaneously on phase outputs.
workflow: verify-work
experimental: true
fallback: sequential-subagents
---
```
- Lead: verify-work orchestrator (delegate mode, model: inherit)
- Teammate 1: gsd-verifier (functional verification -- goal-backward analysis, model: inherit)
- Teammate 2: cross-platform verifier (platform-specific checks, model: sonnet)
- Teammate 3: security verifier (security-focused review, model: sonnet)
- Observer: gsd-oversight-verification (verification completeness watcher, model: sonnet)
- Total: 5 members
- Task dependencies: All 3 verifiers run parallel, oversight reviews combined results, final synthesis by lead
- Fallback: Spawn gsd-verifier via Task (current behavior), run additional verification checks sequentially in orchestrator

All team templates must include this note at the bottom:
```
## Prerequisites
- Agent teams require `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` in environment or settings.json
- Known limitations: no session resumption with in-process teammates, one team per session, no nested teams
- All workflows MUST work without teams (fallback path is the primary path until teams stabilize)
```
  </action>
  <verify>
Verify all 4 files exist and have correct structure:
```bash
ls C:/Users/Destiny/.claude/get-stuff-done/teams/
```
Should list: plan-phase-team.md, execute-phase-team.md, upstream-sync-team.md, verify-work-team.md

Verify each has fallback section:
```bash
grep -l "Fallback" C:/Users/Destiny/.claude/get-stuff-done/teams/*.md
```
Should list all 4 files.

Verify experimental flag documented:
```bash
grep -l "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS" C:/Users/Destiny/.claude/get-stuff-done/teams/*.md
```
Should list all 4 files.
  </verify>
  <done>Four team template files exist in `teams/` directory, each defining team shape, task dependencies, soft cap compliance, fallback path, and experimental prerequisites.</done>
</task>

<task type="auto">
  <name>Task 2: Create 4 specialized oversight agent definitions</name>
  <files>
    C:\Users\Destiny\.claude\agents\gsd-oversight-planning.md
    C:\Users\Destiny\.claude\agents\gsd-oversight-execution.md
    C:\Users\Destiny\.claude\agents\gsd-oversight-sync.md
    C:\Users\Destiny\.claude\agents\gsd-oversight-verification.md
  </files>
  <action>
Create 4 NEW agent definition files at `C:\Users\Destiny\.claude\agents\`. These are NEW files -- they do not exist yet.

All oversight agents share these characteristics per CONTEXT.md decisions:
- **Authority:** Flag and advise ONLY -- never block execution
- **Monitoring:** Active monitoring (watches every action, flags in real-time)
- **Memory:** Uses `.planning/memory/` with the same protocol as all other agents
- **Tools:** Read, Grep, Glob, Bash ONLY (no Write except for their own memory file, no Edit for source code)
- **Expertise accumulation:** Initial seeding from project knowledge, then auto-learning via memory

All oversight agents use this flag format in their system prompt:
```markdown
## Flag Format

**FLAG: [SEVERITY] [Category]**
- Requirement: [which requirement or plan task is at risk]
- Memory: [which memory entry prompted this check, if any]
- Finding: [what you observed, with file:line references]
- Suggested fix: [actionable recommendation]
- Severity: CRITICAL (blocks goal) | WARNING (degrades quality) | INFO (notable)
```

All oversight agents include a `<memory_protocol>` section (same pattern as core agents) and an `<effort_calibration>` section.

**1. gsd-oversight-planning.md:**
```yaml
---
name: gsd-oversight-planning
description: Watches plan creation for requirement coverage gaps, scope creep, dependency errors, and CONTEXT.md decision violations. Flags without blocking.
tools: Read, Write, Grep, Glob, Bash
color: red
---
```
- Role: Monitor plan-phase quality -- requirement coverage, dependency correctness, scope sizing, context fidelity
- Watch for: Missing requirements in plan tasks, circular dependencies, tasks exceeding context budget, plans contradicting CONTEXT.md locked decisions, deferred ideas appearing in tasks
- Memory file: `.planning/memory/gsd-oversight-planning.md`
- Effort: base HIGH, upscale to MAXIMUM for requirement coverage analysis
- Flag routing: Direct to planner (plan-phase is not high-stakes per CONTEXT.md)
- Note: Write tool included ONLY for memory file updates -- oversight agents do NOT write source code or plan files

**2. gsd-oversight-execution.md:**
```yaml
---
name: gsd-oversight-execution
description: Watches plan execution for requirement drift, missed deviations, security gaps, and quality regressions. Flags without blocking.
tools: Read, Write, Grep, Glob, Bash
color: red
---
```
- Role: Monitor execution quality -- requirement drift, missing deviations, security, quality
- Watch for: Executor implementing differently from plan spec, fixes without deviation documentation, missing input validation, exposed secrets, tests removed, error handling stripped, TODOs left, cross-plan file conflicts
- Memory file: `.planning/memory/gsd-oversight-execution.md`
- Effort: base HIGH, upscale to MAXIMUM for requirement drift detection
- Flag routing: Direct to executor (routine); through lead for CRITICAL flags

**3. gsd-oversight-sync.md:**
```yaml
---
name: gsd-oversight-sync
description: Watches upstream sync for fork integrity violations, branding losses, security risks in cherry-picked code, and protected path modifications. Flags without blocking.
tools: Read, Write, Grep, Glob, Bash
color: red
---
```
- Role: Monitor upstream sync safety -- fork identity, security, protected paths
- Watch for: Cherry-picks modifying protected paths (package.json, branding files), upstream code with shell injection patterns, branding/naming regressions (get-shit-done leaking into fork), merge conflicts left unresolved, config validation bypassed
- Memory file: `.planning/memory/gsd-oversight-sync.md`
- Effort: base HIGH, upscale to MAXIMUM for security analysis of cherry-picked code
- Flag routing: Through lead (upstream-sync is HIGH-STAKES per CONTEXT.md decision)
- Note: This workflow routes ALL flags through lead, not direct to executor

**4. gsd-oversight-verification.md:**
```yaml
---
name: gsd-oversight-verification
description: Watches verification process for incomplete coverage, false passes, and untested wiring. Flags without blocking.
tools: Read, Write, Grep, Glob, Bash
color: red
---
```
- Role: Monitor verification completeness -- coverage gaps, false passes, missing wiring checks
- Watch for: Truths marked "pass" without evidence (grep/test output), wiring checks skipped, artifacts verified for existence but not content, gap analysis that misses real failures, verification report that contradicts codebase state
- Memory file: `.planning/memory/gsd-oversight-verification.md`
- Effort: base HIGH, upscale to MAXIMUM for false-pass detection
- Flag routing: Direct to verifier (routine verification)

Each agent definition must include:
1. YAML frontmatter (name, description, tools, color)
2. `<role>` section with the role description, authority statement ("Flag and advise ONLY"), and self-contained nature ("You cannot spawn sub-agents")
3. `<monitoring_protocol>` section with what to watch for and flag format
4. `<memory_protocol>` section (same pattern as core agents, referencing `.planning/memory/gsd-oversight-{workflow}.md`)
5. `<effort_calibration>` section with agent-specific triggers
6. `<expertise_accumulation>` section explaining how to learn from valid flags, false positives, and new patterns
  </action>
  <verify>
Verify all 4 oversight agent files exist:
```bash
ls C:/Users/Destiny/.claude/agents/gsd-oversight-*.md
```
Should list: gsd-oversight-planning.md, gsd-oversight-execution.md, gsd-oversight-sync.md, gsd-oversight-verification.md

Verify flag-and-advise authority:
```bash
grep -l "flag-and-advise\|Flag and advise" C:/Users/Destiny/.claude/agents/gsd-oversight-*.md
```
Should list all 4 files.

Verify memory protocol:
```bash
grep -l "memory_protocol" C:/Users/Destiny/.claude/agents/gsd-oversight-*.md
```
Should list all 4 files.

Verify no Edit tool (oversight agents must not modify source code):
```bash
grep "tools:.*Edit" C:/Users/Destiny/.claude/agents/gsd-oversight-*.md
```
Should return nothing (no matches).
  </verify>
  <done>Four specialized oversight agent definitions created with flag-and-advise authority, memory protocol integration, role-specific monitoring protocols, and expertise accumulation. No Edit tool granted. Write tool included only for memory file updates.</done>
</task>

</tasks>

<verification>
1. `teams/` directory exists at `C:\Users\Destiny\.claude\get-stuff-done\teams\` with 4 template files
2. Each team template has Lead, Teammates, Observer, task dependencies, soft cap note, and fallback section
3. 4 oversight agent definitions exist at `C:\Users\Destiny\.claude\agents\gsd-oversight-*.md`
4. Oversight agents have flag-and-advise authority only (no blocking)
5. Oversight agents have Read, Write, Grep, Glob, Bash tools (Write for memory only, no Edit)
6. All oversight agents include memory_protocol, effort_calibration, and expertise_accumulation sections
7. High-stakes workflow (upstream-sync) routes flags through lead; others route directly
8. All team templates include experimental prerequisites and subagent fallback
</verification>

<success_criteria>
- 4 team templates define team shapes for plan-phase, execute-phase, upstream-sync, and verify-work
- 4 oversight agents specialize in planning quality, execution drift, sync safety, and verification completeness
- Teams respect soft cap of 8 per user decision
- All templates have working subagent fallback paths (teams are experimental)
- Oversight agents accumulate expertise through memory system established in Plan 01
</success_criteria>

<output>
After completion, create `.planning/phases/10-claude-code-adoption/10-03-SUMMARY.md`
</output>
