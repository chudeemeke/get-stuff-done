---
phase: 43
plan: 11
type: execute
wave: 37
depends_on: ["43-11AB"]
status: blocked
blocker: 43-11-BLOCKER.md
requirements: ["UPGRADE-07", "UPGRADE-08", "UPGRADE-09", "SHIP-03A"]
files_modified:
  - overrides/**/*.REASON.md
  - overlay/hooks/*.REASON.md
  - hooks/index.js
  - config/production-source-contract.json
  - CHANGELOG.md
  - dist/**
  - .planning/vetted-upstream-versions.json
  - .planning/evidence/phase43-coverage.json
  - .planning/evidence/phase43-compat.json
  - .planning/evidence/phase43-sbom.json
  - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-11-BLOCKER-STATE.json
  - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-11-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "override snapshots are refreshed only after authority pin moved"
    - "worker classification is re-evaluated against the bumped target"
    - "Phase 43 gates run against the bumped target"
    - "post-bump mutations regenerate and revalidate both SHIP-08 children before summary"
    - "temporary reports are removed or explicitly committed as evidence"
    - "the bounded Plan 11 summary cannot claim final product closure before Plan 11Q"
  artifacts:
    - "overrides/**/*.REASON.md"
    - "CHANGELOG.md"
    - "dist/bom.json"
  key_links:
    - "upstream pin -> override REASON snapshots -> blocking staleness gate"
    - "override delta -> CHANGELOG Override Churn -> dogfood evidence"
    - "bun run dist -> dist/bom.json -> npm tarball SHIP-03A evidence"
    - "Phase 43 machine evidence -> 43-11-SUMMARY.md -> closeout validator"
---

<objective>
Refresh override evidence after the bump, reclassify the worker if applicable,
and run Phase 43 gates against the bumped target.
</objective>

<context>
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-10-SUMMARY.md
@scripts/check-overrides.js
@scripts/generate-override-churn.js
@scripts/run-compat-matrix.js
@scripts/generate-sbom.js
@CHANGELOG.md
</context>

<tasks>

<task id="11-01" type="auto">
  <name>Refresh override snapshots after bump</name>
  <files>overrides/**/*.REASON.md; overlay/hooks/*.REASON.md; hooks/index.js; config/production-source-contract.json</files>
  <read_first>
    - scripts/check-overrides.js
    - overrides/hooks/gsd-check-update.js.REASON.md
    - overrides/hooks/gsd-statusline.js.REASON.md
    - overrides/hooks/gsd-check-update-worker.js.REASON.md
    - hooks/index.js
  </read_first>
  <action>
    After dependency bump and `bun install`, run `node scripts/check-overrides.js`. Review every stale/orphaned override. Update REASON `Upstream snapshot` version/hash values only after reviewing new target source. Update `Semantic SHA-256` values for `.js` overrides. Remove Plan 06/07 temporary forward-port caveats where the active snapshot now genuinely matches the new target.
    Check overlay and upstream worker existence conditionally through filesystem
    predicates; an absent overlay path is expected and must not be a mandatory
    read. If `overlay/hooks/gsd-check-update-worker.js` exists and the target
    upstream package contains `hooks/gsd-check-update-worker.js`, move it to
    `overrides/hooks/gsd-check-update-worker.js`, register kind `override`, and
    add target-version snapshot REASON. If target still lacks a counterpart,
    keep kind `overlay` and document why. When the worker is already an override,
    inspect the existing override/REASON pair and refresh it in place.
    If worker classification changes a tracked executable path, update the live
    production source contract in the same task so the path remains classified
    exactly once, assigned to exactly one group, and governed by the correct
    SHIP-08A or SHIP-08B contract. Do not run scoped coverage against a stale
    path classification.
  </action>
  <acceptance_criteria>
    - `node scripts/check-overrides.js` exits 0.
    - `.js` override REASON files contain `Semantic SHA-256`.
    - `rg -n "Version: 1.6.1" overrides -g "*.REASON.md"` finds updated snapshots for touched overrides if target is `1.6.1`.
    - `rg -n "gsd-check-update-worker.js.*kind.*override|gsd-check-update-worker.js.*kind.*overlay|kind.*override.*gsd-check-update-worker.js|kind.*overlay.*gsd-check-update-worker.js" hooks/index.js` proves worker classification was reviewed after bump.
    - source-contract reconciliation passes with no missing, duplicate, or ungrouped executable path.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-overrides.js</automated>
    <automated>bun run test:coverage:four-metric -- --validate-source-contract</automated>
  </verify>
  <done>false</done>
</task>

<task id="11-02" type="auto">
  <name>Generate bump churn and SBOM</name>
  <files>CHANGELOG.md; dist/**; .planning/evidence/phase43-sbom.json</files>
  <read_first>
    - scripts/generate-override-churn.js
    - scripts/generate-sbom.js
    - CHANGELOG.md
  </read_first>
  <action>
    Run `node scripts/generate-override-churn.js --write` to update the CHANGELOG
    override-churn section under Unreleased. Regenerate `dist/**` mechanically
    only through `bun run dist`; do not inspect or hand-edit hundreds of generated
    files as planning context. Review the generator inputs, command result,
    package manifest, and bounded parity/digest evidence instead. Run `node
    scripts/validate-phase43-evidence.js capture-sbom --output
    .planning/evidence/phase43-sbom.json`; it must pack into marker-owned temp
    storage, compare the tarball SBOM bytes with `dist/bom.json`, record digests,
    and clean up. Do not edit published release sections.
  </action>
  <acceptance_criteria>
    - `CHANGELOG.md` contains `### Override Churn`.
    - `bash .changelog-conflict-check.sh --self-test` exits 0.
    - `bun run dist` exits 0.
    - the Node filesystem assertion for `dist/bom.json` exits 0.
    - schema-valid SHIP-03A evidence proves tarball inclusion and an exact SBOM digest match.
  </acceptance_criteria>
  <verify>
    <automated>bash .changelog-conflict-check.sh --self-test</automated>
    <automated>bun run dist</automated>
    <automated>node -e "const fs=require('node:fs'); if(!fs.existsSync('dist/bom.json')) process.exit(1)"</automated>
    <automated>node scripts/validate-phase43-evidence.js capture-sbom --output .planning/evidence/phase43-sbom.json</automated>
  </verify>
  <done>false</done>
</task>

<task id="11-03" type="auto">
  <name>Regenerate and validate post-bump production assurance</name>
  <files>.planning/vetted-upstream-versions.json; .planning/evidence/phase43-coverage.json; .planning/evidence/phase43-compat.json; .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-11-BLOCKER-STATE.json</files>
  <read_first>
    - package.json
    - scripts/verify-upgrade.js
    - scripts/run-compat-matrix.js
    - scripts/generate-sbom.js
    - .github/workflows/upgrade-verifier.yml
    - .github/workflows/compat-matrix.yml
    - config/production-source-contract.json
    - .planning/evidence/phase43-coverage.json
    - .planning/evidence/phase43-compat.json
  </read_first>
  <action>
    Run `bun run phase43:preflight` and stop if its structured check of Node, Bun, exact Jest/c8 pins, Git Bash, `gh pr checks --json`, or Verdaccio local/CI classification fails. Verdaccio availability is also classified by `verify-upgrade.js`'s existing health preflight, while the GitHub workflow's service container remains authoritative when a local registry is unavailable. Then run:
    - `bun run compose`.
    - `bun run build`.
    - `bun run sbom`.
    - `bun run finalize-dist`.
    - `bun run verify-upgrade --from 1.5.0 --to <target> --json --report upgrade-report.json` when Verdaccio is available locally; if unavailable, record CI upgrade-verifier as authoritative and do not fake success.
    - `node scripts/run-compat-matrix.js --manifest .planning/vetted-upstream-versions.json --require-all --json --report .planning/evidence/phase43-compat.json`, atomically replacing the prior durable bytes only on exact three-row success.
    - `node scripts/vetted-upstream-versions.js --apply-matrix-evidence .planning/evidence/phase43-compat.json` followed by `--validate`.
    - `bun run test:coverage:four-metric -- --report .planning/evidence/phase43-coverage.json` after every source/snapshot mutation is complete.
    - `node scripts/validate-phase43-evidence.js refresh-production-assurance` to validate the new SHIP-08A/SHIP-08B artifacts, atomically rebind the resolved blocker state's governed digests, and roll back its own state write on any post-write failure.
    - `node scripts/validate-phase43-evidence.js --require-production-assurance` and `--require-plan11-unblocked` against the real refreshed artifacts.
    Stop without creating `43-11-SUMMARY.md` if any required local gate is red. Root `upgrade-report.json` is removed after its durable digest is captured; the compatibility and coverage files under `.planning/evidence/` are durable and must not be deleted. Recursive runner temp cleanup must use the marker-owned helper.
  </action>
  <acceptance_criteria>
    - `bun run dist` exits 0.
    - durable `.planning/evidence/phase43-compat.json` contains exactly three passing rows and its digest matches the refreshed manifest/blocker state.
    - regenerated SHIP-08A evidence covers the live post-mutation source set at >=95% in all four metrics.
    - `--require-production-assurance` and `--require-plan11-unblocked` both exit zero after transactional blocker-state refresh.
    - temporary `upgrade-report.json` and all marker-owned runner roots are removed; durable evidence remains tracked.
    - Node, Bun, Jest/c8, Git Bash, and `gh` versions plus Verdaccio local/CI classification are recorded.
  </acceptance_criteria>
  <verify>
    <automated>bun run dist</automated>
    <automated>node scripts/run-compat-matrix.js --manifest .planning/vetted-upstream-versions.json --require-all --json --report .planning/evidence/phase43-compat.json</automated>
    <automated>node scripts/vetted-upstream-versions.js --apply-matrix-evidence .planning/evidence/phase43-compat.json</automated>
    <automated>bun run test:coverage:four-metric -- --report .planning/evidence/phase43-coverage.json</automated>
    <automated>node scripts/validate-phase43-evidence.js refresh-production-assurance</automated>
    <automated>node scripts/validate-phase43-evidence.js --require-production-assurance</automated>
    <automated>node scripts/validate-phase43-evidence.js --require-plan11-unblocked</automated>
  </verify>
  <done>false</done>
</task>

<task id="11-04" type="auto">
  <name>Record bounded post-bump gate summary</name>
  <files>.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-11-SUMMARY.md</files>
  <read_first>
    - CHANGELOG.md
    - dist/bom.json
    - .planning/vetted-upstream-versions.json
  </read_first>
  <action>
    Create `43-11-SUMMARY.md` only after every required local gate exits zero and
    the refreshed production-assurance validator proves current governed
    digests. List snapshot updates, worker classification decision, churn
    output, SBOM output, SHIP-08A metrics, SHIP-08B snapshot/N=3 result, local
    gate results, CI-deferred Verdaccio evidence if applicable, tool preflight,
    and report cleanup status. State explicitly that final hosted execution,
    Fable whole-project review, and product closure remain Plan 11Q work.
  </action>
  <acceptance_criteria>
    - Summary contains `snapshot`, `worker classification`, `Override Churn`, `dist/bom.json`, `compat-matrix`, `upgrade-report`, `UPGRADE-05`, `SHIP-03A`, `SHIP-08A`, `SHIP-08B`, and `Plan 11Q`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "snapshot|worker classification|Override Churn|dist/bom.json|compat-matrix|upgrade-report|UPGRADE-05|SHIP-03A|SHIP-08A|SHIP-08B|Plan 11Q" .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-11-SUMMARY.md</automated>
  </verify>
  <done>false</done>
</task>

</tasks>

<threat_model>
Post-bump cleanup can hide unresolved override drift. Mitigate by running the
blocking override gate after the pin moves, recording worker classification, and
requiring temporary evidence files to be removed or explicitly committed.
Post-unblock source or snapshot changes can stale earlier assurance, so Plan 11
regenerates both child artifacts, transactionally refreshes blocker bindings,
and revalidates real evidence before any summary exists.
</threat_model>

<verification>
Run:
- `node scripts/check-overrides.js`
- `bun run test:coverage:four-metric -- --validate-source-contract`
- `bun run dist`
- `node scripts/run-compat-matrix.js --manifest .planning/vetted-upstream-versions.json --require-all --json --report .planning/evidence/phase43-compat.json`
- `bun run test:coverage:four-metric -- --report .planning/evidence/phase43-coverage.json`
- `node scripts/validate-phase43-evidence.js refresh-production-assurance`
- `node scripts/validate-phase43-evidence.js --require-production-assurance`
- `bash .changelog-conflict-check.sh --self-test`
</verification>
