---
phase: 43
plan: 08
type: execute
wave: 8
depends_on: ["43-07"]
status: pending
requirements: ["UPGRADE-09"]
files_modified:
  - scripts/generate-override-churn.js
  - tests/override-churn.test.js
  - CHANGELOG.md
  - package.json
  - .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-08-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "Override Churn section is generated deterministically"
    - "churn classifies changed, carried, orphaned, added, and removed overrides"
    - "CHANGELOG insertion touches only override-churn markers under Unreleased"
  artifacts:
    - "scripts/generate-override-churn.js"
    - "tests/override-churn.test.js"
    - "CHANGELOG.md"
  key_links:
    - "UPGRADE-09"
    - "D-12"
---

<objective>
Automate override-churn reporting so upstream bump release notes are generated
from reviewed upstream and override metadata rather than hand-curated drift.
</objective>

<context>
@.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-CONTEXT.md
@scripts/check-overrides.js
@CHANGELOG.md
@.changelog-conflict-check.sh
</context>

<tasks>

<task id="08-01" type="auto" tdd="true">
  <name>Write override churn tests first</name>
  <files>tests/override-churn.test.js</files>
  <read_first>
    - scripts/check-overrides.js
    - tests/check-overrides.test.js
    - CHANGELOG.md
  </read_first>
  <action>
    Create `tests/override-churn.test.js` before implementation. Cover:
    - changed override becomes `changed`.
    - unchanged override becomes `carried`.
    - override with no upstream counterpart becomes `orphaned`.
    - newly added override path becomes `added`.
    - removed override path becomes `removed`.
    - output order is deterministic by path.
    - CHANGELOG insertion replaces only content between `<!-- override-churn:start -->` and `<!-- override-churn:end -->`.
  </action>
  <acceptance_criteria>
    - `bun test tests/override-churn.test.js` fails before implementation and exits 0 after implementation.
    - Test file contains `changed`, `carried`, `orphaned`, `added`, `removed`, and `override-churn:start`.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/override-churn.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="08-02" type="auto">
  <name>Implement override churn generator</name>
  <files>scripts/generate-override-churn.js; package.json; CHANGELOG.md</files>
  <read_first>
    - tests/override-churn.test.js
    - scripts/check-overrides.js
    - CHANGELOG.md
  </read_first>
  <action>
    Implement `scripts/generate-override-churn.js`:
    - CLI options: `--from-upstream-dir <path>`, `--to-upstream-dir <path>`, `--overrides-dir <path>`, `--changelog <path>`, `--write`, `--json`.
    - classify each override as `changed`, `carried`, `orphaned`, `added`, or `removed`.
    - emit deterministic JSON and markdown.
    - update only the section between `<!-- override-churn:start -->` and `<!-- override-churn:end -->`.
    - keep generated content under Unreleased.
    Add package script `"override:churn": "node scripts/generate-override-churn.js"`.
  </action>
  <acceptance_criteria>
    - `node scripts/generate-override-churn.js --help` exits 0.
    - `bun test tests/override-churn.test.js` exits 0.
    - `CHANGELOG.md` contains `<!-- override-churn:start -->` and `<!-- override-churn:end -->`.
    - `package.json` contains `"override:churn": "node scripts/generate-override-churn.js"`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/generate-override-churn.js --help</automated>
    <automated>bun test tests/override-churn.test.js</automated>
  </verify>
  <done>false</done>
</task>

<task id="08-03" type="auto">
  <name>Add deterministic churn check</name>
  <files>tests/override-churn.test.js; package.json</files>
  <read_first>
    - scripts/generate-override-churn.js
    - tests/override-churn.test.js
    - package.json
  </read_first>
  <action>
    Add a deterministic local check that runs `node scripts/generate-override-churn.js --json` against fixtures and fails on malformed output without requiring a real bump delta on every PR.
  </action>
  <acceptance_criteria>
    - `bun test tests/override-churn.test.js` exits 0.
    - `node scripts/generate-override-churn.js --json` exits 0 or exits with documented fixture-required usage when no upstream dirs are supplied.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/override-churn.test.js</automated>
    <automated>node scripts/generate-override-churn.js --help</automated>
  </verify>
  <done>false</done>
</task>

<task id="08-04" type="auto">
  <name>Record churn summary</name>
  <files>.planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-08-SUMMARY.md</files>
  <read_first>
    - scripts/generate-override-churn.js
    - CHANGELOG.md
  </read_first>
  <action>
    Create `43-08-SUMMARY.md` listing churn classifications, CHANGELOG marker behavior, package script, tests run, and any deferred changelog follow-up.
  </action>
  <acceptance_criteria>
    - Summary contains `Override Churn`, `changed`, `carried`, `orphaned`, `added`, `removed`, and `UPGRADE-09`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Override Churn|changed|carried|orphaned|added|removed|UPGRADE-09" .planning/phases/43-upgrade-resilience-verify-matrix-dogfood/43-08-SUMMARY.md</automated>
  </verify>
  <done>false</done>
</task>

</tasks>

<threat_model>
Hand-written churn notes drift. Mitigate by deriving churn from file evidence,
keeping output deterministic, and limiting CHANGELOG writes to explicit markers.
</threat_model>

<verification>
Run:
- `node scripts/generate-override-churn.js --help`
- `bun test tests/override-churn.test.js`
- `bash .changelog-conflict-check.sh --self-test`
</verification>
