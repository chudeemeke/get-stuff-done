---
phase: 02-statusline-redesign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/gsd-statusline.js
  - src/config/ConfigLoader.js
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Progress bar displays green when context usage is below 37.5% (50% of 75)"
    - "Progress bar displays yellow when context usage is 37.5-56.25% (50-75% of 75)"
    - "Progress bar displays red when context usage exceeds 56.25% (75% of 75)"
    - "Progress bar blinks when context usage exceeds 65.625% (87.5% of 75)"
  artifacts:
    - path: "hooks/gsd-statusline.js"
      provides: "Statusline with correct threshold default"
      contains: "autocompactThreshold = 75"
    - path: "src/config/ConfigLoader.js"
      provides: "Config loader with correct threshold defaults"
      contains: "autocompact_threshold: 75"
  key_links:
    - from: "hooks/gsd-statusline.js"
      to: "config/default-config.json"
      via: "matching default value"
      pattern: "autocompactThreshold = 75"
---

<objective>
Fix hardcoded autocompact_threshold defaults that cause incorrect progress bar color stages.

Purpose: UAT found the progress bar showing yellow at 28% context usage when it should be green. Root cause: hardcoded defaults are 50 instead of 75 (the value in default-config.json), making the green zone 0-25% instead of 0-37.5%.

Output: Corrected threshold defaults aligning code with default-config.json, enabling proper color stage transitions.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-statusline-redesign/02-UAT.md
@config/default-config.json

Key facts:
- default-config.json has autocompact_threshold: 75
- hooks/gsd-statusline.js line 10 has autocompactThreshold = 50 (wrong)
- hooks/gsd-statusline.js line 15 has getConfigValue fallback = 50 (wrong)
- src/config/ConfigLoader.js line 29 has autocompact_threshold: 50 (wrong)
- src/config/ConfigLoader.js line 71 has autocompact_threshold: 50 (wrong)

With threshold = 75:
- Green zone: 0-37.5% (50% of 75)
- Yellow zone: 37.5-56.25% (75% of 75)
- Red (no blink): 56.25-65.625% (87.5% of 75)
- Red (blink): 65.625%+ (87.5% of 75)

User is on Git Bash in Windows Terminal (supports ANSI blink). Once threshold is fixed, blink should trigger at correct percentage.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hardcoded defaults in hooks/gsd-statusline.js</name>
  <files>hooks/gsd-statusline.js</files>
  <action>
    Change line 10 from:
    `let autocompactThreshold = 50;  // Default`
    to:
    `let autocompactThreshold = 75;  // Default (matches default-config.json)`

    Change line 15 from:
    `autocompactThreshold = getConfigValue(config, 'context_management.autocompact_threshold', 50);`
    to:
    `autocompactThreshold = getConfigValue(config, 'context_management.autocompact_threshold', 75);`

    Both values must be 75 to match config/default-config.json.
  </action>
  <verify>
    grep -n "autocompactThreshold = 75" hooks/gsd-statusline.js
    grep -n "autocompact_threshold', 75" hooks/gsd-statusline.js
    Both should return matches.
  </verify>
  <done>
    hooks/gsd-statusline.js has autocompactThreshold defaulting to 75 in both the variable declaration and the getConfigValue fallback.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix hardcoded defaults in src/config/ConfigLoader.js</name>
  <files>src/config/ConfigLoader.js</files>
  <action>
    Change line 29 in getDefaults() from:
    `autocompact_threshold: 50,`
    to:
    `autocompact_threshold: 75,`

    Change line 71 in createDefaultConfig() from:
    `autocompact_threshold: 50,`
    to:
    `autocompact_threshold: 75,`

    Both values must be 75 to match config/default-config.json.
  </action>
  <verify>
    grep -n "autocompact_threshold: 75" src/config/ConfigLoader.js
    Should return two matches (lines ~29 and ~71).
  </verify>
  <done>
    src/config/ConfigLoader.js has autocompact_threshold defaulting to 75 in both getDefaults() and createDefaultConfig().
  </done>
</task>

</tasks>

<verification>
1. All four hardcoded values changed from 50 to 75
2. Values match config/default-config.json (autocompact_threshold: 75)
3. No other code changes - surgical fix only

Manual verification (for UAT re-test):
- At 28% context usage, progress bar should now be GREEN (below 37.5%)
- At 40% context usage, progress bar should be YELLOW (between 37.5-56.25%)
- At 60% context usage, progress bar should be RED without blink (between 56.25-65.625%)
- At 70% context usage, progress bar should be RED with blink (above 65.625%)
</verification>

<success_criteria>
1. grep "autocompactThreshold = 75" hooks/gsd-statusline.js returns 1 match
2. grep "autocompact_threshold', 75" hooks/gsd-statusline.js returns 1 match
3. grep "autocompact_threshold: 75" src/config/ConfigLoader.js returns 2 matches
4. No other files modified
5. Total: 4 values changed from 50 to 75
</success_criteria>

<output>
After completion, create `.planning/phases/02-statusline-redesign/02-03-SUMMARY.md`
</output>
