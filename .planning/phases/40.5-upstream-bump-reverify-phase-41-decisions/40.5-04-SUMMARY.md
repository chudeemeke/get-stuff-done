---
phase: 40.5-upstream-bump-reverify-phase-41-decisions
plan: 04
subsystem: verification
tags: [decision-matrix, phase-41-reverify, upstream-v1.39.1, named-targets, criterion-4]
provides:
  - 24-row decision matrix verified against v1.39.1 (D-01..D-18 + A-01..A-06)
  - 3 named-target verifications complete (D-09 vs #2499, 999.1 vs #2487, 999.3 regex at v1.39.1)
  - 6 external-action/library refresh checks with explicit version-refresh commands cited
  - D-21 plan-phase-41 trigger gate #2 satisfied (zero MATERIAL drift)
affects: [wave-5, phase-41]
tech-stack:
  added: []
  patterns: [version-refresh-primitive, fork-only-presence-check, triangulation-closure]
key-files:
  created:
    - .planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-DECISION-VERIFY.md
  modified: []
key-decisions:
  - D-04 — interactive Claude in plan/execute (no subagent verifier batch); honored across all 24 rows
  - D-05 — drift threshold = cited symbol/API/line behavior changed (whitespace/comment-only deltas do not count); zero behaviors changed
  - D-17 — material drift halts wave (no halt fired; zero material drift)
  - D-21 gate #2 — satisfied; Phase 41 plan-phase eligible to proceed after Phase 40.5 close
duration: ~60min
completed: 2026-05-17
---

# Phase 40.5 Wave 4: Phase 41 CONTEXT.md Decision Re-verification

**Verified all 24 Phase 41 design decisions (18 D-XX + 6 A-XX) against the v1.39.1 upstream tree. Zero material drift. Three named targets (D-09 vs #2499, 999.1 vs #2487, 999.3 regex at v1.39.1) verified per ROADMAP.md criterion #4. Phase 41 plan-phase remains eligible to proceed unmodified (D-21 gate #2 met).**

## Performance

- **Duration:** ~60 min (D-23 estimate was ~120 min; actual was faster because the D-02 priority head — "upstream-cited rows first" — was empty per RESEARCH.md §3, leaving only fork-only rows + 6 external-action refreshes)
- **Tasks:** 7 sub-tasks (04-01 scaffold → 04-02..04-06 row verification chunks → 04-07 atomic commit)
- **Files created:** 1 (`40.5-DECISION-VERIFY.md` — 24-row matrix + 3 named-target sections + Wave 4 conclusion)
- **Files modified:** 0 (Phase 41 CONTEXT.md unchanged — zero A-07+ amendments needed)

## Status counts

| Status | Count | Notes |
|---|---|---|
| unchanged | 24 | All rows verified against v1.39.1 cited symbol/API/line behavior |
| drifted | 0 | No upstream change invalidated a Phase 41 decision |
| amended | 0 | No A-07+ entries authored; Phase 41 CONTEXT.md unmodified |

**Total:** 24/24 rows non-pending. D-21 gate #2 satisfied by direct verification (no amendment integration needed).

## Named-target conclusions (criterion #4 explicit)

### Target 1: Phase 41 D-09 vs upstream PR #2499 (Windows stdio chunked-mode) — UNCHANGED

Surfaces are different. D-09 targets fork `tests/**` subprocess timeouts via `tests/helpers/test-timeouts.js` and a forthcoming `runWithTimeout` helper wrapping `spawnSync({timeout, killSignal})`. #2499 targets upstream `plan-phase.md` workflow's planner-agent invocation under Windows (chunked-mode split + filesystem fallback for inter-step state). Triangulation closure (codex P2 #2): (1) basename collision check on all 5 fork test files returns empty; (2) `scripts/compose.js:438-441` filter excludes `*.test.*` from dist regardless of name; (3) fork test files contain zero references to upstream planner surfaces (`plan-phase`, `Task(`, `gsd-sdk query`, `chunked`, `CHUNKED_MODE`). **D-09 still required in Phase 41.**

### Target 2: 999.1 hypothesis vs upstream PR #2487 (wave-dependencies) — 999.1 STAYS BACKLOG

#2487's Dimension 9 "Cross-Plan Data Contracts" in `node_modules/get-shit-done-cc/agents/gsd-plan-checker.md` (978 lines) detects SEMANTIC data-transformation conflicts ("Plan A strips data Plan B needs in original form"). 999.1 is MECHANICAL same-wave file-write collision detection (overlapping `files_modified:` arrays). Different problem classes; different detection inputs; different failure modes. Grep of upstream plan-checker for `wave.{0,30}collision|files_modified.*overlap|same.*wave.*file|wave.*write.*conflict` returns 0 matches. **999.1 stays backlog with its 3 promotion paths (Path A upstream PR / Path B PROCESS-08 / Path C decimal-insert Phase 42) intact per D-18(b).**

### Target 3: 999.3 vs upstream `workflows/plan-phase.md` UI-gate regex at v1.39.1 — BUG STILL PRESENT

Line **472** in `node_modules/get-shit-done-cc/get-shit-done/workflows/plan-phase.md` (shifted from line 457 at v1.38.2 due to +15 line drift in v1.38.2..v1.39.1; file now 1620 lines). Pattern unchanged: `grep -iE "UI|interface|frontend|component|layout|page|screen|view|form|dashboard|widget"` — no `\b` word boundaries. False-positive substring matches on "perform", "preview", "format", "transform", "interview", "information", "platform", etc. Recommended fix: `grep -iwE` (`-w` adds word boundaries) or explicit `\b...\b` anchors. **Wave 5 disposition per D-20: file upstream PR against `gsd-build/get-shit-done`** (repo confirmed from `node_modules/get-shit-done-cc/package.json` `repository.url`).

## External-action refresh summary

All 6 external-action/library refresh table rows cite an actual version-refresh command + output. Zero rows fell back to `unchanged-presumed-no-tooling`.

| Action / Library | Latest verified | Command used | Status |
|---|---|---|---|
| gitleaks-action | v2.3.9 (2025-04-17) | `gh release list -R gitleaks/gitleaks-action --limit 5` | major v2 unchanged |
| osv-scanner-action | v2.3.8 (2026-05-11) | `gh release list -R google/osv-scanner-action --limit 5` | major v2 unchanged |
| harden-runner | v2.19.3 (2026-05-14) | `gh release list -R step-security/harden-runner --limit 5` | major v2 unchanged |
| hyperfine | v1.20.0 (2025-11-18) | `gh release list -R sharkdp/hyperfine --limit 5` | 2 minor bumps from cited v1.18.0; flags stable |
| bun:test test.skip.if API | bun v1.3.14 (2026-05-13) | `bun --version` (local 1.3.5) + `gh release list -R oven-sh/bun --limit 5` | API stable since v1.0 |
| markdownlint-cli2 | 0.22.1 | `npm view markdownlint-cli2 version` | patch bump; non-breaking |

**Codex P2 #1 (SHA-pinning GHA actions instead of floating `@v2`):** captured for Phase 44 backlog hardening; NOT material Wave-4 drift (cited `@v2` floating-major is deliberate per Phase 41 design — auto-picks security patches).

## Forward signals for Wave 5

- **999.3 upstream PR:** target repo `gsd-build/get-shit-done`. Fix shape: `grep -iwE` or `\b...\b` anchors in line 472 of `get-shit-done/workflows/plan-phase.md`. PR body should reference the false-positive substrings table from DECISION-VERIFY.md Target 3.
- **Phase 41 minor advisory (NOT Wave 5 work; for Phase 41 execution):** D-14 wording can be tightened from "hyperfine v1.18.0" to "hyperfine (latest at CI time, ≥ v1.18.0)" since flags are stable across minors. Not blocking.
- **Codex P2 #1 (SHA-pinning):** captured for Phase 44; not Phase 41 scope.

## Task Commits

| Task | Description | Commit / Status |
|---|---|---|
| 04-01 | Scaffold DECISION-VERIFY.md with 24-row matrix shell | (folded into Wave 4 commit) |
| 04-02 | Verify D-01..D-06 + A-01..A-02 (CI structure + security surface) | (background session) |
| 04-03 | Verify D-07..D-09 + A-03..A-04 + Target 1 (D-09 vs #2499) | (background session) |
| 04-04 | Verify D-10..D-12 + A-05 (flake collector + escape hatch) | (background-session research; foreground apply) |
| 04-05 | Verify D-13..D-18 + A-06 (perf baseline + flip gate) | (foreground session) |
| 04-06 | Targets 2 & 3 (999.1 vs #2487; 999.3 regex at v1.39.1) | (foreground session) |
| 04-07 | Wave 4 conclusion + atomic commit | this commit |

## Methodology notes

- Per D-04, verification owner was interactive Claude (no subagent verifier batch). User reviewed verification thinking per row, not just verdicts.
- Per D-05, drift threshold was "cited symbol/API/line behavior changed" — whitespace/comment-only deltas near a citation were treated as unchanged. Minor version bumps within the same major (hyperfine v1.18.0 → v1.20.0; markdownlint-cli2 0.22.0 → 0.22.1; bun v1.3.x latest) were classified non-material.
- Per D-17, material drift would have halted the wave for amendment review. No halt fired.
- Version-refresh primitive (defined in 40.5-04-PLAN.md objective) was used per external row: `gh release list` (primary), `curl + jq` (fallback unused — gh authenticated), `npm view` (markdownlint-cli2), `bun --version` cross-check (test.skip.if).
- Codex (gpt-5.5, high reasoning) cross-AI review applied to Target 1 named-target reasoning during the background session (P0/P1: none; P2 #2 triangulation strengthening applied; P2 #1 SHA-pinning captured for Phase 44).

## Wave 5 readiness

Wave 5 is unblocked. Per D-21, plan-phase-41 trigger gates require BOTH (1) successful upstream bump verified (Waves 1-3) AND (2) zero MATERIAL drift in 24-decision matrix (this wave). Both met. Wave 5 (final wave of Phase 40.5) covers: 999.3 upstream PR filing + Phase 40.5 closure artifacts.
