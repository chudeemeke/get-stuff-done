---
phase: 18-upstream-sync-execution
plan: 05
type: execute
wave: 5
depends_on: ["18-04"]
files_modified:
  - .planning/phases/18-upstream-sync-execution/approach-comparison.md
  - package.json
  - README.md
autonomous: false

must_haves:
  truths:
    - "Approach comparison document covers every area where fork and upstream solved the same problem differently"
    - "Test suite passes at 95%+ coverage on all 4 metrics (statements, branches, functions, lines)"
    - "Cross-platform CI passes on all 3 platforms (macOS, Linux, Windows)"
    - "Package name is @chude/get-stuff-done, all URLs point to chudeemeke/get-stuff-done"
    - "sync/v1.20.5 branch is merged to main"
    - "No upstream branding exists anywhere in the merged codebase"
  artifacts:
    - path: ".planning/phases/18-upstream-sync-execution/approach-comparison.md"
      provides: "Complete upstream approach comparison document"
      contains: "Resolution"
    - path: "package.json"
      provides: "Fork identity preserved through sync"
      contains: "@chude/get-stuff-done"
  key_links:
    - from: "approach-comparison.md"
      to: "all sync batches"
      via: "divergence documentation"
      pattern: "Fork approach.*Upstream approach"
    - from: ".github/workflows/ci.yml"
      to: "test suite"
      via: "cross-platform CI matrix"
      pattern: "macos.*ubuntu.*windows"
---

<objective>
Complete the approach comparison document, run final validation (coverage, cross-platform CI, smoke tests), and merge sync/v1.20.5 to main.

Purpose: This plan delivers the final three success criteria: the approach comparison document (SYNC-II-06), the 95%+ coverage requirement (SYNC-II-05), and the merge to main (completing SYNC-II-01). It also performs the comprehensive identity audit (SYNC-II-04) and a manual smoke test checkpoint before merge.

Output: Approach comparison document complete, all 5 success criteria met, sync branch merged to main
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-upstream-sync-execution/18-RESEARCH.md
@.planning/phases/18-upstream-sync-execution/18-CONTEXT.md
@.planning/phases/18-upstream-sync-execution/18-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete approach comparison and final validation</name>
  <files>.planning/phases/18-upstream-sync-execution/approach-comparison.md</files>
  <action>
**Step 1: Complete the approach comparison document.**

Review all divergences discovered during Batches 6-12 (already captured incrementally in approach-comparison.md) and ensure these areas are documented:

**Required entries (at minimum):**

1. **gsd-verifier Write tool** -- Fork preserved the fix independently. Upstream reverted it then re-fixed later. Document both approaches and the fork's proactive fix.

2. **Statusline architecture** -- Fork: 172 lines, 4-stage context window indicator with theme system, config loader, platform detection. Upstream: 108 lines, simplified reader. Resolution: fork wins, with bridge file write added for context-monitor compatibility.

3. **Update system** -- Fork: `execFileSync(npmCmd, ['view', '@chude/get-stuff-done', 'version'])` for Windows portability. Upstream: `execSync('npm view get-shit-done-cc version')`. Resolution: fork wins (more portable).

4. **Install mode detection** -- Fork: smart `--link` flag, Node.js version warning, runtime-aware install. Upstream: `--all` flag, `getConfigDirFromHome()`. Resolution: merge (adopt upstream's new runtimes, keep fork's mode detection).

5. **Module architecture** -- Fork started with upstream's monolith, now adopted upstream's 11-module split. Document the fork's validation import as the key customization preserved in the modular architecture.

6. **Test infrastructure** -- Fork: bun test, 563+ tests, 95%+ coverage. Upstream: node --test, separate test files. Document coexistence strategy.

7. **Build system** -- Fork: esbuild bundling via scripts/build.js. Upstream: scripts/build-hooks.js. Document how fork's build pipeline must adapt to modular structure (Phase 19 concern, note here).

8. **Any additional divergences** discovered during cherry-pick resolution that are in the batch notes.

**Step 2: Comprehensive identity audit.**

Scan the ENTIRE codebase for upstream identity leaks:

```bash
# Full branding scan (all file types)
grep -r "get-shit-done\|glittercowboy\|get-shit-done-cc" . --include="*.js" --include="*.cjs" --include="*.md" --include="*.json" --include="*.yml" --include="*.yaml" | grep -v "node_modules\|\.git/\|\.planning/sync/\|\.upstream/\|approach-comparison\|CHANGELOG"
```

Fix any remaining upstream branding. Pay special attention to:
- `package.json`: name, repository, homepage, bugs, author
- `README.md`: title, badges, URLs, install commands
- `bin/install.js`: package name in output strings
- All agent/command/workflow files: path references
- New upstream files adopted during sync: may still have upstream URLs

```bash
# Verify fork identity fields in package.json
node -e "const p = require('./package.json'); console.log('name:', p.name); console.log('repo:', p.repository?.url); console.log('homepage:', p.homepage); console.log('bugs:', p.bugs?.url); console.log('author:', p.author);"
```

All must reference `@chude/get-stuff-done` and `chudeemeke/get-stuff-done`.

**Step 3: Coverage enforcement.**

```bash
bun test --coverage
```

Verify all 4 metrics are at 95%+:
- Statements >= 95%
- Branches >= 95%
- Functions >= 95%
- Lines >= 95%

If any metric is below 95%, this is a blocker. Possible causes:
- New upstream code in lib/*.cjs not covered by fork tests (fork tests target the old monolith)
- The fix depends on Phase 19 (esbuild migration) or additional test writing
- If coverage is close (90-94%), document the gap and note that it needs closure
- If coverage is far below 95%, identify which new modules lack coverage and create targeted tests

**Step 4: Push sync branch and trigger CI.**

```bash
git push -u origin sync/v1.20.5
```

Monitor CI for all 3 platforms (macOS, Linux, Windows). If CI fails on any platform:
- Diagnose the failure (likely path separator issues on Windows or missing dependency on Linux)
- Fix and push again
- Do NOT proceed to merge until CI is green on all 3 platforms

**Step 5: Commit final documentation.**

```bash
git commit -m "docs(18): complete approach comparison and identity audit"
```
  </action>
  <verify>
- Approach comparison document has entries for all 7+ areas listed above
- `grep -r "get-shit-done\|glittercowboy\|get-shit-done-cc" . --include="*.js" --include="*.cjs" --include="*.md" --include="*.json" | grep -v "node_modules\|\.git/\|\.planning/sync/\|\.upstream/\|approach-comparison\|CHANGELOG" | wc -l` returns 0
- `bun test --coverage` shows 95%+ on all 4 metrics
- CI passes on all 3 platforms (or CI is triggered and pending)
  </verify>
  <done>Approach comparison complete, identity audit clean, coverage at 95%+, CI triggered</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete upstream sync of 106 commits (v1.18.0 through upstream/main HEAD) on the sync/v1.20.5 branch, including:
- 30 commits from Batch 6 (v1.19.0 features + "Revert 12 PRs" resolution)
- 28 commits from Batches 7-8 (cleanup + gsd-tools.cjs rename)
- 49 commits from Batches 9-11 (auto-advance, requirements tracking, subagent discovery)
- 11 commits from Batch 12 (module split into 11 CJS domain modules)
- Approach comparison document covering all fork-upstream divergences
- Fork identity preserved (package name, URLs, branding)
  </what-built>
  <how-to-verify>
1. **Smoke test the gsd-tools CLI:**
   ```bash
   node get-stuff-done/bin/gsd-tools.cjs --help
   node get-stuff-done/bin/gsd-tools.cjs current-timestamp
   node get-stuff-done/bin/gsd-tools.cjs init plan-phase 18
   ```
   All should work without errors.

2. **Verify module structure:**
   ```bash
   ls -la get-stuff-done/bin/lib/
   ```
   Should show 11 .cjs files.

3. **Check fork identity:**
   ```bash
   node -e "const p = require('./package.json'); console.log(p.name, p.version);"
   ```
   Should show `@chude/get-stuff-done 2.2.x`.

4. **Review approach comparison:**
   Open `.planning/phases/18-upstream-sync-execution/approach-comparison.md` and verify it covers the key divergences.

5. **Check CI status:**
   ```bash
   gh run list --branch sync/v1.20.5 --limit 3
   ```
   All 3 platforms should be green (or review failures).

6. **Review test results:**
   ```bash
   bun test
   ```
   Should show 563+ passing tests.
  </how-to-verify>
  <resume-signal>Type "approved" to merge sync/v1.20.5 to main, or describe issues to fix first</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Merge sync branch to main</name>
  <files>(git merge operation)</files>
  <action>
After user approval from the checkpoint:

1. Ensure on sync/v1.20.5 branch:
   ```bash
   git checkout sync/v1.20.5
   ```

2. Merge to main with --no-ff for clear merge point:
   ```bash
   git checkout main
   git merge --no-ff sync/v1.20.5 -m "merge(18): upstream sync v1.18.0 through v1.20.5+ (106 commits, modular gsd-tools)"
   ```

3. Update sync manifest:
   - Set `status` to `complete`
   - Set `completedAt` to current ISO timestamp
   - Set `mergedAt` to current ISO timestamp
   - Set `mergeMethod` to `no-ff`
   - Set `mergedInto` to `main`
   - Record merge SHA

4. Push main:
   ```bash
   git push origin main
   ```

5. Final verification on main:
   ```bash
   bun test
   node get-stuff-done/bin/gsd-tools.cjs current-timestamp
   ```

6. Commit manifest update: `docs(18): mark sync complete and merged to main`
  </action>
  <verify>
- `git branch --show-current` returns `main`
- `git log --oneline -1` shows the merge commit
- `bun test` passes on main
- `.planning/sync/sync-manifest.json` has `status: "complete"` and `mergedInto: "main"`
  </verify>
  <done>sync/v1.20.5 merged to main, sync manifest marked complete, tests passing on main branch</done>
</task>

</tasks>

<verification>
- All 5 success criteria verified:
  1. All 185 upstream commits integrated (79 from Phase 8 + 106 from Phase 18)
  2. gsd-tools structured as 11 CJS domain modules under bin/lib/
  3. Fork identity preserved (package name, URLs, branding)
  4. Test suite passes at 95%+ coverage on all 3 CI platforms
  5. Approach comparison document exists with comprehensive coverage
- sync/v1.20.5 merged to main
- Sync manifest complete
</verification>

<success_criteria>
- All 5 phase success criteria are TRUE
- sync/v1.20.5 branch merged to main
- CI green on all 3 platforms
- bun test passes on main branch
- Sync manifest status is "complete"
- No upstream branding anywhere in codebase
</success_criteria>

<output>
After completion, create `.planning/phases/18-upstream-sync-execution/18-05-SUMMARY.md`
</output>
