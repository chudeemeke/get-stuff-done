---
phase: 20
slug: sync-safety-transparency
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-02-23
---

# Phase 20 — Validation Strategy

> Generated by `gsd-phase-researcher` during `/gsd:plan-phase 20`.
> Updated by `gsd-plan-checker` after plan approval.
> Governs feedback sampling during `/gsd:execute-phase 20`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (Suite A)** | bun:test for `*.test.js` files |
| **Framework (Suite B)** | node:test for `*.test.cjs` files (upstream format) |
| **Config file** | `bunfig.toml` — includes `*.test.js`, excludes `*.test.cjs` |
| **Quick run command** | `node --test tests/sync.test.cjs` |
| **Full suite command** | `node --test tests/*.test.cjs && bun test` |
| **Coverage command** | `bun test --coverage` |
| **Estimated runtime** | ~30-60s (full suite), ~10-20s (sync tests only) |
| **CI pipeline** | `.github/workflows/release.yml` — cross-platform matrix (macOS, Linux, Windows) |

**New test file:** `tests/sync.test.cjs` (node:test + node:assert, upstream format)

**Coverage note:** `sync.test.cjs` runs under node --test, not bun:test. Coverage for sync.cjs tracked via bun:test only if a parallel `sync.test.js` exists. Strategy: write `.test.cjs` for integration/CLI tests, add `.test.js` if coverage gaps emerge.

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test tests/sync.test.cjs`
- **After every task commit (any file):** Run `bun test`
- **After every plan wave:** Run `bun test --coverage`
- **Before `/gsd:verify-work`:** Full suite must be green (`node --test tests/*.test.cjs && bun test --coverage`)
- **Maximum acceptable task feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 20-01-01 | 01 | 0 | SYNC-01, SYNC-02, SYNC-04 | scaffold | `node --test tests/sync.test.cjs` | ❌ W0 | ⬜ pending |
| 20-01-02 | 01 | 1 | SYNC-01, SYNC-02, SYNC-04 | unit+integration | `node --test tests/sync.test.cjs` | ❌ W0 | ⬜ pending |
| 20-02-01 | 02 | 2 | SYNC-01, SYNC-02, SYNC-04 | manual | N/A (workflow markdown) | N/A | ⬜ pending |
| 20-02-02 | 02 | 2 | SYNC-04 | manual | N/A (upstream.md command) | N/A | ⬜ pending |

*Status values: ⬜ pending / ✅ green / ❌ red / ⚠ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [ ] `tests/sync.test.cjs` — scaffold with stubbed tests for SYNC-01, SYNC-02, SYNC-04
- [ ] `tests/helpers.cjs` extension — `createTempGitProject()` helper (git init + empty commit for checkpoint tests)
- [ ] `get-stuff-done/bin/lib/sync.cjs` — stub module exporting all 4 commands + 6 internal helpers
- [ ] `get-stuff-done/bin/gsd-tools.cjs` — router registration for `sync-preview` and `sync-checkpoint`

*Wave 0 validation:* `node --test tests/sync.test.cjs` exits with known stub failures (not crashes). `bun test` passes (no regressions).

---

## Manual-Only Verifications

> Behaviors that genuinely cannot be automated, with justification.
> These are surfaced during `/gsd:verify-work` UAT.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| MV-01: Dry-run gate stops before Stage 4 | SYNC-04 | Workflow markdown is not executable code; conditional bash blocks run inline by orchestrator | Run `/gsd:upstream --dry-run` against test branch; confirm no cherry-picks, no file changes, dry-run summary printed |
| MV-02: Colorized terminal output on all 3 platforms | SYNC-01 | ANSI color rendering requires TTY; CI runs non-interactive | Run `node gsd-tools.cjs sync-preview HEAD~5..HEAD` in real terminal on each platform; confirm colored output |
| MV-03: Rollback offer appears inline during conflict | SYNC-02 | Requires real cherry-pick conflict during real sync run | Review Stage 4 conflict-handling block; optionally trigger known-conflict scenario |
| MV-04: Checkpoint cleanup at Stage 7 completion | SYNC-02 | Requires full sync run to reach Stage 7 | After test sync run, confirm `git tag -l sync-checkpoint-*` returns empty |

---

## Validation Sign-Off

Updated by `gsd-plan-checker` when plans are approved:

- [ ] All tasks have automated verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/gsd:execute-phase 20`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | 4 | `node --test tests/sync.test.cjs` | — | — | scaffold |
| 1 | 1 | `node --test tests/sync.test.cjs && bun test --coverage` | — | — | ⬜ pending |
| 2 | 2 | `node --test tests/sync.test.cjs && bun test` | — | — | ⬜ pending |

**Phase validation complete:** pending
