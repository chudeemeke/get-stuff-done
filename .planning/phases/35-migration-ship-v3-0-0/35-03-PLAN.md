---
phase: 35-migration-ship-v3-0-0
plan: 03
type: execute
wave: 3
depends_on:
  - 35-01
  - 35-02
files_modified: []
autonomous: false
requirements:
  - MIG-04
  - MIG-05

must_haves:
  truths:
    - "npm registry has @chude/get-stuff-done@3.0.0"
    - "bunx @chude/get-stuff-done@3.0.0 --help prints v3.0 help"
    - "Git tag v3.0.0 exists"
  artifacts: []
  key_links:
    - from: "package.json"
      to: "npm registry"
      via: "aidev publish"
      pattern: "@chude/get-stuff-done"
---

<objective>
Validate the package with npm pack, release v3.0.0 via aidev release major, and publish to npm via aidev publish.

Purpose: MIG-05 requires shipping as v3.0.0. MIG-04 requires preserving the package name. This plan runs the full release cycle with pre-publish validation.

Output: v3.0.0 published to npm, git tag created, package installable.
</objective>

<execution_context>
@C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-migration-ship-v3-0-0/35-CONTEXT.md
@.planning/phases/35-migration-ship-v3-0-0/35-01-SUMMARY.md
@.planning/phases/35-migration-ship-v3-0-0/35-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-publish validation via npm pack</name>
  <files></files>
  <read_first>
    - package.json (verify files array, bin field, prepublishOnly, version)
    - .planning/phases/35-migration-ship-v3-0-0/35-01-SUMMARY.md (confirm Plan 01 changes landed)
  </read_first>
  <action>
    Run pre-publish validation to confirm the package contents are correct before release (per D-08, validation is CI + prepublishOnly, but npm pack dry-run catches file-array issues early):

    1. Run composition to ensure dist/ is fresh:
    ```bash
    bun run compose
    ```

    2. Run `npm pack --dry-run` and verify the tarball contents include:
    - `dist/` directory with composed output
    - `bin/install.js` and `bin/gsd.js`
    - `overlay/branding.json` and `overlay/features.json`
    - `package.json`, `README.md`, `LICENSE`, `UPGRADING.md`
    - NO `overlay/src/`, `scripts/`, `tests/`, `.planning/`, `src/`

    3. Verify the individual dist/src/ imports resolve (launcher dependency check):
    ```bash
    node -e "require('./dist/src/platform/paths'); require('./dist/src/platform/terminal'); require('./dist/src/config/ConfigLoader'); console.log('All dist/src imports resolve')"
    ```

    4. Run the full test suite to confirm nothing is broken:
    ```bash
    bun test
    ```

    5. Verify package name is preserved (MIG-04):
    ```bash
    node -e "const p=require('./package.json'); console.log(p.name); process.exit(p.name==='@chude/get-stuff-done'?0:1)"
    ```

    If any of these checks fail, stop and diagnose before proceeding to release.
  </action>
  <verify>
    <automated>cd "C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done" && bun run compose && npm pack --dry-run 2>&1 | head -30 && node -e "require('./dist/src/platform/paths'); require('./dist/src/config/ConfigLoader'); console.log('imports OK')" && bun test</automated>
  </verify>
  <acceptance_criteria>
    - `bun run compose` exits 0 (composition succeeds)
    - `npm pack --dry-run` output includes dist/, bin/, overlay/branding.json, overlay/features.json
    - `npm pack --dry-run` output does NOT include overlay/src/, scripts/, tests/, .planning/
    - `node -e "require('./dist/src/platform/paths')"` succeeds (no MODULE_NOT_FOUND)
    - `bun test` exits 0 (all tests pass)
    - package.json name is `@chude/get-stuff-done` (MIG-04)
  </acceptance_criteria>
  <done>Package contents validated, all imports resolve, tests pass, ready for release</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Release v3.0.0 and publish to npm</name>
  <files></files>
  <action>
    User must run the release and publish commands (they require interactive input for OTP).

    1. Release (bumps version, commits, tags, pushes):
    ```
    aidev release major
    ```
    This will: run tests, bump 2.4.0 to 3.0.0 in package.json, commit, tag v3.0.0, push to origin.

    2. Publish to npm (requires OTP):
    ```
    aidev publish
    ```
    This will: run prepublishOnly (bun run compose), then npm publish. Prompts for npm OTP.

    3. Verify the publish:
    ```
    npm view @chude/get-stuff-done version
    ```
    Should show `3.0.0`.

    4. Verify installability (optional smoke test):
    ```
    bunx @chude/get-stuff-done@3.0.0 --help
    ```
  </action>
  <verify>
    <automated>npm view @chude/get-stuff-done version 2>/dev/null | grep -c "3.0.0"</automated>
  </verify>
  <done>v3.0.0 published to npm, git tag v3.0.0 exists, package installable via bunx</done>
</task>

</tasks>

<verification>
1. `npm view @chude/get-stuff-done version` returns `3.0.0`
2. `git tag --list v3.0.0` returns `v3.0.0`
3. `npm view @chude/get-stuff-done dist-tags.latest` returns `3.0.0`
</verification>

<success_criteria>
- v3.0.0 published to npm registry (MIG-05)
- Package name remains @chude/get-stuff-done (MIG-04)
- Git tag v3.0.0 exists
- bunx @chude/get-stuff-done@3.0.0 installs successfully
</success_criteria>

<output>
After completion, create `.planning/phases/35-migration-ship-v3-0-0/35-03-SUMMARY.md`
</output>
