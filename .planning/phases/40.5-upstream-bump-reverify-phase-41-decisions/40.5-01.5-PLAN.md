---
phase: 40.5
plan: 01.5
type: execute
wave: 1.5
depends_on: ["40.5-01"]
files_modified:
  - bunfig.toml
  - tests/test-config-hygiene.test.js
  - tests/hooks.test.js
  - tests/test-path-validation.test.js
  - .github/workflows/ci.yml
  - scripts/check-no-test-files-in-dist.js
  - scripts/setup-branch-protection.sh
  - .git/hooks/pre-push (installer + content)
  - scripts/install-git-hooks.sh
  - package.json (postinstall hook installer wiring)
autonomous: false
requirements: ["UPGRADE-10"]
estimated_minutes: 240
gap_closure: false
must_haves:
  truths:
    - "bunfig.toml excludes dist/** from test discovery"
    - "Meta-test asserts bunfig exclusion patterns AND zero test files in dist/ at test time"
    - "tests/hooks.test.js HOOKS constant resolves to existing files"
    - "Meta-test asserts every hardcoded test path constant resolves to an existing file"
    - "actionlint passes on .github/workflows/ci.yml"
    - "actions/checkout and actions/cache upgraded to versions supporting Node 24"
    - "CI step 'no test files in dist/' guards future drift"
    - "Branch protection rules applied to main on chudeemeke/get-stuff-done"
    - "scripts/setup-branch-protection.sh is reproducible (works as-is on any clone)"
    - "Pre-push hook runs `bun install && bun run compose && bun test` locally"
    - "Hook installer runs automatically on clone via package.json postinstall"
    - "PR #3 CI on Linux + macOS shows ONLY the 4 chronic main failures (net regressions = 0)"
  artifacts:
    - path: "bunfig.toml"
      provides: "Test discovery scoped to fork-owned files"
      contains: "dist/"
    - path: "tests/test-config-hygiene.test.js"
      provides: "Meta-test asserting test-config invariants"
      contains: "describe"
    - path: "tests/test-path-validation.test.js"
      provides: "Meta-test asserting hardcoded test paths exist"
      contains: "describe"
    - path: "scripts/check-no-test-files-in-dist.js"
      provides: "CI guard script for test-discovery hygiene"
      contains: "process.exit"
    - path: "scripts/setup-branch-protection.sh"
      provides: "Reproducible branch protection setup via gh API"
      contains: "gh api"
    - path: ".husky/pre-push"
      provides: "Local-CI parity hook"
      contains: "compose"
    - path: "scripts/install-git-hooks.sh"
      provides: "Hook installer for fresh clones"
      contains: "pre-push"
  key_links:
    - from: "bunfig.toml"
      to: "dist/"
      via: "test exclude pattern"
      pattern: 'exclude.*dist'
    - from: "tests/hooks.test.js"
      to: "overrides/hooks/"
      via: "HOOKS constant resolution"
      pattern: "overrides.*hooks"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-no-test-files-in-dist.js"
      via: "CI step invocation"
      pattern: "check-no-test-files-in-dist"
---

<objective>
Wave 1.5 of Phase 40.5: Establish the **CI prevention foundation** — structural fixes
to test-config hygiene + CI workflow hardening + branch protection + local-CI parity
hook. After this wave, CI on PR #3 should report ONLY the 4 chronic main failures
(net regressions vs main = 0), satisfying the redefined binding gate per A-06.

This is NOT a quick fix wave. Each fix delivers the structural prevention
(meta-test, CI gate, hook installer) — not just the patch — so the bug class
cannot recur. Per A-07 amendment, four sub-waves execute end-to-end before
returning to Wave 2.

The four sub-waves (executed in this single plan because they're tightly coupled):

1. **Wave 1.5a — Test-config hygiene fix + meta-tests** (Layer A)
2. **Wave 1.6 — CI workflow hardening** (Layer B)
3. **Wave 1.7 — Branch protection setup** (Layer C)
4. **Wave 1.8 — Pre-push hook for local-CI parity** (Layer D)

Out of scope (per A-08): layers E (aidev release gating), F (aidev scaffold
conventions), G (global ci-discipline rule). These are cross-project tooling.
</objective>

<execution_context>
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CI-DIAGNOSIS.md
@.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-CONTEXT.md
</execution_context>

<context>
@bunfig.toml
@tests/hooks.test.js
@.github/workflows/ci.yml
@package.json
</context>

<tasks>

<task id="1.5a-01" type="auto">
  <name>Task 1.5a-01: Add dist/** exclusion to bunfig.toml</name>
  <files>bunfig.toml</files>
  <read_first>
    - bunfig.toml (current state)
  </read_first>
  <action>
    Add `"dist/**"` to the `[test]` exclude array. Resulting config:
    ```toml
    [test]
    include = ["**/*.test.js"]
    exclude = ["**/*.test.cjs", "**/node_modules/**", "**/dist/**"]
    timeout = 15000
    ```
    This is the categorical fix for ~538 of the 586 PR #3 failures: upstream 1.38.5
    ships 77 .test.ts files; compose copies them to dist/sdk/; bun-test was
    discovering them. With dist/** excluded, they're no longer discovered.
  </action>
  <acceptance_criteria>
    - `grep '"dist' bunfig.toml` returns a match
    - `bun test` (after compose) does not run any tests from dist/
    - No syntactic break in bunfig.toml
  </acceptance_criteria>
  <verify>
    <automated>grep -E '"\*\*/dist/\*\*"' bunfig.toml</automated>
  </verify>
  <done>bunfig.toml updated with dist/** exclusion. Atomic commit.</done>
</task>

<task id="1.5a-02" type="auto">
  <name>Task 1.5a-02: Add test-config hygiene meta-test</name>
  <files>tests/test-config-hygiene.test.js</files>
  <read_first>
    - tests/ (existing test patterns to match style)
    - bunfig.toml (canonical exclusion patterns)
  </read_first>
  <action>
    Create `tests/test-config-hygiene.test.js` with meta-tests that assert:
    1. bunfig.toml exists and has `[test]` section
    2. exclude array includes `**/node_modules/**` and `**/dist/**`
    3. At test time, zero `.test.js`, `.test.ts`, `.test.cjs` files exist in dist/ (catches future drift if upstream packaging changes again or compose accidentally copies tests)
    4. Test discovery glob is bounded (no naked `**/*.test.*` without exclusions)

    These meta-tests fail loudly and explain WHY when the test config drifts. They
    are the structural prevention for the upstream-test-bundling failure mode.
  </action>
  <acceptance_criteria>
    - File created at tests/test-config-hygiene.test.js
    - All assertions pass against current bunfig.toml + dist/ state
    - Tests have descriptive names (a future Claude/dev can understand intent)
    - Failure messages are actionable (point at the file to fix)
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/test-config-hygiene.test.js</automated>
  </verify>
  <done>Meta-test file landed and passing. Atomic commit.</done>
</task>

<task id="1.5a-03" type="auto">
  <name>Task 1.5a-03: Fix tests/hooks.test.js HOOKS path constants</name>
  <files>tests/hooks.test.js</files>
  <read_first>
    - tests/hooks.test.js lines 95-115 (HOOKS constant block)
    - overrides/hooks/ (actual location of fork's overridden hooks)
    - overlay/hooks/ (location of fork-only hooks)
  </read_first>
  <action>
    Update HOOKS constant in tests/hooks.test.js so:
    - `checkUpdate` points to `overrides/hooks/gsd-check-update.js` (currently wrong: overlay/)
    - `statusline` points to `overrides/hooks/gsd-statusline.js` (currently wrong: overlay/)
    - `preCompact` stays at `overlay/hooks/pre-compact.js` (correct already)

    Update line-6 doc comment to reflect that fork hooks live in overrides/ for
    upstream-overrides and overlay/ for fork-only additions.

    Also update the `describe()` block names that say "overlay/hooks/gsd-statusline.js"
    to "overrides/hooks/gsd-statusline.js" to keep test names accurate.

    This fixes 42 of the 586 PR #3 failures.
  </action>
  <acceptance_criteria>
    - HOOKS.checkUpdate path resolves to an existing file (overrides/hooks/gsd-check-update.js)
    - HOOKS.statusline path resolves to an existing file (overrides/hooks/gsd-statusline.js)
    - HOOKS.preCompact unchanged (still overlay/hooks/pre-compact.js)
    - describe() block names match the files being tested
    - bun test tests/hooks.test.js shows hooks-related failures dropping to 0 (or to chronic-only count)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p=require('path');['overrides/hooks/gsd-check-update.js','overrides/hooks/gsd-statusline.js','overlay/hooks/pre-compact.js'].forEach(f=>{if(!fs.existsSync(p.join(process.cwd(),f)))process.exit(1)})"</automated>
  </verify>
  <done>tests/hooks.test.js HOOKS constants point at correct directories. Atomic commit.</done>
</task>

<task id="1.5a-04" type="auto">
  <name>Task 1.5a-04: Add path-validation meta-test</name>
  <files>tests/test-path-validation.test.js</files>
  <read_first>
    - tests/hooks.test.js (HOOKS pattern as canonical example)
  </read_first>
  <action>
    Create `tests/test-path-validation.test.js` that:
    1. Reads test files matching `tests/**/*.test.{js,cjs}`
    2. Extracts hardcoded path constants (lines that match `path.join(.*, 'overlay'|'overrides'|'hooks'|'commands'|...)`)
    3. Asserts each resolved path exists on disk

    This is the structural prevention for the stale-paths failure mode. Whenever
    a fork-side file moves, this meta-test fails loudly with the specific test
    file + line + missing path.

    Bound the regex to a known-paths whitelist (overlay/, overrides/, hooks/,
    commands/, agents/, scripts/) to avoid false positives on temp dirs etc.
  </action>
  <acceptance_criteria>
    - File created at tests/test-path-validation.test.js
    - Test passes against current state (post Task 1.5a-03)
    - Failure messages identify exactly which file:line has the broken path
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/test-path-validation.test.js</automated>
  </verify>
  <done>Path-validation meta-test landed and passing. Atomic commit.</done>
</task>

<task id="1.6-01" type="auto">
  <name>Task 1.6-01: Add actionlint to repo (lint CI workflow files)</name>
  <files>scripts/actionlint.sh, .github/workflows/ci.yml (potentially)</files>
  <read_first>
    - .github/workflows/ci.yml (full file)
  </read_first>
  <action>
    Add a script `scripts/lint-workflows.sh` that runs actionlint against
    .github/workflows/*.yml. Use the official actionlint Docker image (no
    install needed) for portability.

    Wire into CI as a new "Workflow Lint" job. This catches malformed CI
    configs at PR time, before they're merged.

    Run locally first to surface any existing issues. If existing workflow
    has actionable issues (deprecation warnings, syntax errors), fix them
    in this commit. The CI logs from PR #3 explicitly mention deprecated
    Node 20 actions — verify whether actionlint catches that and address.
  </action>
  <acceptance_criteria>
    - scripts/lint-workflows.sh exists and is executable
    - Running it locally returns exit 0 (no actionable errors)
    - .github/workflows/ci.yml passes actionlint
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/lint-workflows.sh</automated>
  </verify>
  <done>actionlint integrated. Atomic commit.</done>
</task>

<task id="1.6-02" type="auto">
  <name>Task 1.6-02: Upgrade actions to Node 24-supporting versions</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - PR #3 CI logs (re: Node 20 deprecation warnings)
  </read_first>
  <action>
    Upgrade `actions/checkout@v4` and `actions/cache@v4` to whatever current
    versions support Node 24 (likely already v4, but verify against GitHub
    Actions deprecation docs). Also verify `oven-sh/setup-bun@v2` is current.

    GitHub deprecated Node 20 on 2025-09-19; runners forced to Node 24 on
    2026-06-02. Get ahead of this proactively.
  </action>
  <acceptance_criteria>
    - All `uses:` references in ci.yml are at current major versions
    - actionlint still passes
    - No deprecation warnings expected in next CI run
  </acceptance_criteria>
  <verify>
    <automated>grep -E "actions/(checkout|cache)@v" .github/workflows/ci.yml | grep -v "@v4"</automated>
  </verify>
  <done>Actions updated. Atomic commit.</done>
</task>

<task id="1.6-03" type="auto">
  <name>Task 1.6-03: Add "no test files in dist/" CI guard</name>
  <files>scripts/check-no-test-files-in-dist.js, .github/workflows/ci.yml</files>
  <read_first>
    - bunfig.toml (current exclusions)
    - scripts/compose.js (compose pipeline)
  </read_first>
  <action>
    Create `scripts/check-no-test-files-in-dist.js` that:
    1. After compose runs, scans dist/ for any *.test.js, *.test.ts, *.test.cjs files
    2. If any found: print full list + exit 1 with actionable message
    3. If clean: print "OK: dist/ contains no test files" + exit 0

    Wire into CI workflow as a step BETWEEN `bun run compose` and `bun test`.
    This is the categorical CI gate matching the meta-test from Task 1.5a-02.

    Note: meta-test runs ALWAYS (every test invocation). CI guard runs on
    every PR. Defense-in-depth — if test discovery somehow includes dist/
    in CI but not locally, this script catches it.
  </action>
  <acceptance_criteria>
    - Script exists and is executable
    - Wired into ci.yml after compose step in test job
    - Running locally on current state passes (since dist/ is gitignored and stale)
    - Running locally after fresh compose passes (no test files in dist/ post Task 1.5a-01 fix)
  </acceptance_criteria>
  <verify>
    <automated>bun run compose && node scripts/check-no-test-files-in-dist.js</automated>
  </verify>
  <done>Guard script + CI step landed. Atomic commit.</done>
</task>

<task id="1.7-01" type="auto">
  <name>Task 1.7-01: Reproducible branch protection setup script</name>
  <files>scripts/setup-branch-protection.sh, scripts/setup-branch-protection.json</files>
  <read_first>
    - GitHub branch protection API docs (gh api /repos/{owner}/{repo}/branches/{branch}/protection)
  </read_first>
  <action>
    Create `scripts/setup-branch-protection.sh` that uses `gh api` to apply
    branch protection rules to main:

    Required rules:
    - Pull request required (no direct push to main)
    - At least 1 approval required
    - Required status checks: Test (ubuntu-latest), Test (macos-latest), Lint, Source Parity Check, Workflow Lint (newly added)
    - Strict status checks (require branch up-to-date with main before merge)
    - Linear history (no merge commits — keeps git history clean)
    - Conversation resolution required
    - No force pushes to main
    - No deletions of main

    Use a JSON config file `scripts/setup-branch-protection.json` for the rules
    spec, so they're version-controlled and reviewable. Script reads JSON,
    invokes `gh api PATCH`.

    Script must be idempotent — running it twice should be a no-op.
    Script must verify gh is authenticated before running.

    Apply to chudeemeke/get-stuff-done immediately after creation.
  </action>
  <acceptance_criteria>
    - Script + JSON config exist and are version-controlled
    - Script runs successfully against the live repo (chudeemeke/get-stuff-done)
    - `gh api repos/chudeemeke/get-stuff-done/branches/main/protection` confirms rules applied
    - Script handles failure modes gracefully (gh not auth'd, repo not found, network error)
  </acceptance_criteria>
  <verify>
    <automated>gh api repos/chudeemeke/get-stuff-done/branches/main/protection 2>&1 | grep -q "required_status_checks"</automated>
  </verify>
  <done>Branch protection live + reproducible script committed. Atomic commit.</done>
</task>

<task id="1.8-01" type="auto">
  <name>Task 1.8-01: Add pre-push hook for local-CI parity</name>
  <files>.husky/pre-push, package.json (devDependencies + prepare script)</files>
  <read_first>
    - package.json (current scripts + dependencies)
  </read_first>
  <action>
    Use `husky` (industry-standard, well-maintained) for git hook management:
    1. Add `husky` to devDependencies
    2. Add `"prepare": "husky"` to package.json scripts (auto-installs hooks on `bun install`)
    3. Create `.husky/pre-push` that runs the same sequence as CI:
       ```bash
       bun install --frozen-lockfile --ignore-scripts
       bun run compose
       bun run lint
       bun test
       ```
    4. Allow opt-out for emergency pushes via `git push --no-verify`
    5. Print clear messaging: "Running CI parity check (pre-push)... use --no-verify to skip"

    This catches CI failures locally before they hit CI minutes. It's the
    Layer D in the prevention stack — local-CI parity.
  </action>
  <acceptance_criteria>
    - .husky/pre-push exists and is executable
    - package.json has husky in devDependencies + prepare script
    - Running `bun install` triggers husky install
    - Test push to a throwaway branch fires the hook
    - --no-verify opt-out works
  </acceptance_criteria>
  <verify>
    <automated>test -x .husky/pre-push && grep -q '"husky"' package.json && grep -q '"prepare":' package.json</automated>
  </verify>
  <done>Pre-push hook installed and wired via prepare script. Atomic commit.</done>
</task>

<task id="1.5-99" type="checkpoint:human-verify" gate="blocking">
  <name>Task 1.5-99: Push and verify CI matches A-06 redefined gate</name>
  <what-built>
    - bunfig.toml dist/** exclusion
    - 2 meta-tests (test-config hygiene, test path validation)
    - tests/hooks.test.js HOOKS path correction
    - actionlint integration
    - Action version updates (Node 24 ready)
    - "no test files in dist/" CI guard
    - Branch protection setup script (applied live)
    - Pre-push hook installer (husky)
  </what-built>
  <how-to-verify>
    1. Push all Wave 1.5/1.6/1.7/1.8 commits to PR #3 branch.
    2. Watch CI: `gh run watch <id> --repo chudeemeke/get-stuff-done`.
    3. Per A-06 redefined gate: CI Linux + macOS should report ONLY the 4 chronic failures (Phase 29 PROTO-01/02, INST-04 uninstall, removeGsdFiles traversal). Net new regressions = 0.
    4. If MORE than 4 failures: Wave 1.5-1.8 didn't fully address the bug pattern. HALT and investigate.
    5. If exactly 4 chronic failures (matching main's 2026-04-06 failure set): Wave 1.5 PASSES the gate. Proceed to Wave 2.
    6. Verify branch protection live: `gh api repos/chudeemeke/get-stuff-done/branches/main/protection`.

    Pass: 4 chronic failures, branch protection live.
    Fail: 5+ failures OR new failures not in main's 4-failure set.
  </how-to-verify>
  <resume-signal>
    Type "1.5 verified" to advance to Wave 2.
    If gate failed: type "halt" + paste failure list.
  </resume-signal>
  <action>Push commits, monitor CI, verify A-06 gate, await user confirmation.</action>
  <verify>
    <automated>manual</automated>
  </verify>
  <done>User confirms 1.5 gate met. PR #3 ready for merge after Wave 5.</done>
</task>

</tasks>

<verification>
Wave 1.5/1.6/1.7/1.8 success criteria (bound to A-06 redefined gate):

1. bunfig.toml excludes dist/**; meta-test asserts this invariant
2. tests/hooks.test.js HOOKS paths resolve to existing files; meta-test asserts this invariant
3. actionlint passes on .github/workflows/ci.yml
4. CI guard "no test files in dist/" runs in CI between compose and test
5. Branch protection rules applied to main on chudeemeke/get-stuff-done
6. Pre-push hook installs via husky on `bun install`
7. PR #3 CI on Linux + macOS reports the 4 chronic failures and no others (net regressions vs main = 0)

Verification commands:
```bash
# Local
grep "dist" bunfig.toml
bun test tests/test-config-hygiene.test.js
bun test tests/test-path-validation.test.js
bun run compose && node scripts/check-no-test-files-in-dist.js
gh api repos/chudeemeke/get-stuff-done/branches/main/protection | jq .required_status_checks

# CI on PR #3 post-push
gh pr checks 3 --repo chudeemeke/get-stuff-done
```
</verification>

<success_criteria>
- [ ] bunfig.toml excludes dist/**
- [ ] tests/test-config-hygiene.test.js exists and passes
- [ ] tests/hooks.test.js HOOKS paths point at correct directories
- [ ] tests/test-path-validation.test.js exists and passes
- [ ] actionlint integrated and ci.yml passes
- [ ] Actions upgraded to Node 24-ready versions
- [ ] scripts/check-no-test-files-in-dist.js exists, wired into CI
- [ ] scripts/setup-branch-protection.sh exists, branch protection live
- [ ] .husky/pre-push installed via husky prepare script
- [ ] PR #3 CI matches A-06 redefined gate (4 chronic failures, no new regressions)
- [ ] User confirms gate met via "1.5 verified" resume signal
</success_criteria>

<output>
After completion, create `.planning/phases/40.5-upstream-bump-reverify-phase-41-decisions/40.5-01.5-SUMMARY.md` with:

- Each commit SHA + one-line description
- Pre/post failure counts (PR #3 CI)
- Branch protection JSON (sanitized)
- Pre-push hook contents
- Any anomalies observed
- Confirmation that A-06 gate met
- Hand-off back to Wave 2
</output>
