---
phase: 30-composition-pipeline-branding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/compose.js
  - tests/compose.test.js
autonomous: true
requirements: [COMP-09]
gap_closure: true

must_haves:
  truths:
    - "--diff reports CREDITS.md as added when it does not exist in dist/"
    - "--diff reports .install-meta.json as added/modified/unchanged correctly"
    - "--diff reports CREDITS.md as removed when branding config disables upstream credit and dist/ has it"
  artifacts:
    - path: "scripts/compose.js"
      provides: "computeDelta() includes additive outputs"
      contains: "CREDITS.md"
    - path: "tests/compose.test.js"
      provides: "Tests for additive output delta tracking"
      contains: "CREDITS.md.*delta|delta.*CREDITS"
  key_links:
    - from: "scripts/compose.js:computeDelta"
      to: "scripts/compose.js:generateCredits"
      via: "computeDelta generates expected CREDITS.md content for comparison"
      pattern: "generateCredits"
    - from: "scripts/compose.js:computeDelta"
      to: "scripts/compose.js:merge"
      via: "computeDelta mirrors the same .install-meta.json structure merge() writes"
      pattern: "install-meta\\.json"
---

<objective>
Close the UAT gap: computeDelta() does not track CREDITS.md or .install-meta.json because they are generated additively in merge() outside the manifest.

Purpose: --diff must show complete filename-level delta including all files that compose would produce, not just manifest entries.
Output: Updated computeDelta() that accounts for both additive outputs, with test coverage.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/compose.js
@tests/compose.test.js

<interfaces>
From scripts/compose.js (relevant exports and functions):
```javascript
// computeDelta() is internal (not exported), called by compose() when opts.diff is true
function computeDelta(state, distDir) { ... }

// generateCredits() is exported, returns string|null
function generateCredits(preserveUpstreamCredit) { ... }

// Pipeline state shape (from resolve()):
// state.manifest: Array<{relPath, sourcePath, content, brandedContent, action, stage}>
// state.branding: {substitutions: [...], preserveUpstreamCredit: boolean}
// state.meta: {upstreamVersion, overlayVersion, upstreamDir, overlayDir, brandingRulesApplied}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD -- add failing tests for additive output delta tracking</name>
  <files>tests/compose.test.js</files>
  <action>
Add a new describe block "computeDelta additive outputs" in the compose() end-to-end section of tests/compose.test.js. Write these failing tests:

1. "--diff delta includes CREDITS.md when preserveUpstreamCredit is true": Compose to a temp distDir, then call compose with diff:true. Assert that the delta array contains an entry with relPath "CREDITS.md" and status "unchanged" (since compose just wrote it).

2. "--diff delta includes .install-meta.json": Same setup. Assert delta contains an entry with relPath ".install-meta.json" and status "modified" (composed_at timestamp will always differ).

3. "--diff detects CREDITS.md as added when missing from dist/": Compose to distDir, manually delete dist/CREDITS.md, then run compose with diff:true. Assert delta contains relPath "CREDITS.md" with status "added".

4. "--diff detects CREDITS.md as removed when credit disabled": Create a mock setup where branding has preserveUpstreamCredit:false. Compose to distDir WITH credit enabled first (so CREDITS.md exists in dist/), then compose with diff:true using a config that has preserveUpstreamCredit:false. Assert delta contains relPath "CREDITS.md" with status "removed".

5. "--diff detects .install-meta.json as added when missing from dist/": Compose to distDir, manually delete dist/.install-meta.json, then run compose with diff:true. Assert delta contains relPath ".install-meta.json" with status "added".

Use the existing makeTempDir/rmDir/createMockUpstream/createMockOverlay helpers. For test 4, create a second overlay dir with preserveUpstreamCredit:false by writing a modified branding.json.

Run: `bun test tests/compose.test.js` -- expect the new tests to FAIL (RED phase).
  </action>
  <verify>
    <automated>cd "C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done" && bun test tests/compose.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>5 new tests exist and fail because computeDelta() does not track additive outputs</done>
</task>

<task type="auto">
  <name>Task 2: Fix computeDelta() to include CREDITS.md and .install-meta.json</name>
  <files>scripts/compose.js</files>
  <action>
Modify computeDelta() in scripts/compose.js to account for the two additive outputs that merge() generates outside the manifest. The fix has three parts:

**Part A -- Add CREDITS.md to the comparison set:**

After the manifest entry loop (after line 701), add logic to handle CREDITS.md:
- Call generateCredits(state.branding.preserveUpstreamCredit) to get the expected content (or null if credit not preserved)
- If expected content is not null:
  - Check if "CREDITS.md" exists in currentFiles
  - If not in currentFiles: push {relPath: "CREDITS.md", status: "added"}
  - If in currentFiles: compare content via Buffer.compare; push "modified" or "unchanged"
  - Add "CREDITS.md" to wouldWrite set (so removed-detection does not flag it)
- If expected content is null:
  - Do NOT add to wouldWrite (so if it exists in dist/, the removed-detection loop will flag it as "removed")

**Part B -- Add .install-meta.json to the comparison set:**

After the CREDITS.md block, add logic for .install-meta.json:
- Build the expected meta object using the same structure as merge() (upstream_version, overlay_version, composed_at, features_disabled, overrides_applied, branding_rules_applied)
- NOTE: composed_at will always differ (new timestamp vs old), so .install-meta.json will typically show as "modified" when dist/ exists. This is correct behavior -- it reflects that a new composition would update the timestamp.
- Check if ".install-meta.json" exists in currentFiles
- If not in currentFiles: push {relPath: ".install-meta.json", status: "added"}
- If in currentFiles: compare content; push "modified" or "unchanged"
- Add ".install-meta.json" to wouldWrite set

**Part C -- Remove the special-case exclusion on line 706:**

Change line 706 from:
```javascript
if (!wouldWrite.has(existingFile) && existingFile !== '.install-meta.json') {
```
to:
```javascript
if (!wouldWrite.has(existingFile)) {
```

This exclusion was a workaround for .install-meta.json not being in wouldWrite. Now that it is tracked properly, the exclusion is unnecessary and would hide legitimate removals.

Do NOT restructure the pipeline, change the manifest pattern, or modify merge(). This is a targeted fix to computeDelta() only.

Run: `bun test tests/compose.test.js` -- expect all tests to PASS (GREEN phase).
  </action>
  <verify>
    <automated>cd "C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/get-stuff-done" && bun test tests/compose.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>All compose tests pass including the 5 new additive output delta tests. computeDelta() now tracks CREDITS.md and .install-meta.json.</done>
</task>

</tasks>

<verification>
1. `bun test tests/compose.test.js` -- all tests pass (0 failures)
2. `bun run compose --diff` -- output now includes CREDITS.md and .install-meta.json in the delta counts
3. Manual check: delete dist/CREDITS.md, run `bun run compose --diff --verbose` -- CREDITS.md appears as [added]
</verification>

<success_criteria>
- computeDelta() reports CREDITS.md status (added/modified/unchanged/removed) based on expected content from generateCredits()
- computeDelta() reports .install-meta.json status (added/modified/unchanged) based on expected meta content
- The special-case .install-meta.json exclusion in the removed-detection loop is removed
- All existing compose tests still pass (no regressions)
- 5 new tests validate the additive output delta tracking
</success_criteria>

<output>
After completion, create `.planning/phases/30-composition-pipeline-branding/30-03-SUMMARY.md`
</output>
